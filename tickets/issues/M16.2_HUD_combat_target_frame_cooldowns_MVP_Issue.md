# Issue: M16.2 — HUD combat + target frame + cooldowns (MVP)

**Status:** Closed

---

## Rapport final (M16.2)

### 1) FICHIERS

**Créés :**
- `engine/client/CombatHud.h`
- `engine/client/CombatHud.cpp`
- `tickets/issues/M16.2_HUD_combat_target_frame_cooldowns_MVP_Issue.md`

**Modifiés :**
- `engine/client/UIModel.h`
- `engine/client/UIModel.cpp`
- `CMakeLists.txt`

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64\pkg\game\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

Détail environnement :
- `cmake` n’est pas disponible dans l’environnement de travail courant, donc la configuration / compilation locale n’a pas pu être lancée ici.
- Les diagnostics IDE sur les fichiers modifiés ne remontent pas d’erreur.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Le scope demandé par `M16.2` a été implémenté au minimum via :
  - extension de `UIModel` pour exposer cible active et mini combat log
  - ajout de `CombatHudPresenter` pour produire un panneau HUD responsive avec barres joueur/cible, mini combat log et cooldowns MVP
  - intégration build minimale dans `engine_core`
- La validation complète DoD reste incomplète car :
  - la compilation n’a pas pu être exécutée localement sans `cmake`
  - l’exécution n’a pas pu être validée
  - la barre mana est présente côté HUD MVP mais reste alimentée en mode `N/A` tant qu’aucune donnée mana n’existe dans le protocole réseau actuel

---

## Ticket content (M16.2) — référence

# M16.2 — HUD combat + target frame + cooldowns (MVP)

## Objectif
HUD jouable: HP/mana, cible, feedback combat minimal.

## Dépendances
- M16.1

## Livrables
- HUD widgets
- Target frame
- Damage float text (option)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Layout responsive; update à 60fps.

## Étapes d’implémentation
1. Créer panneau HUD.
2. Afficher barres stats et cible.
3. Afficher derniers hits (combat log mini).

## Critères de validation (DoD)
- [ ] En combat, HUD montre HP cible/joueur et updates.

## Notes / pièges à éviter
- Reste minimal; polish plus tard.
