# M06.2 — SSAO: generate pass (depth+normal)

**Status:** Closed

## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/render/vk/VkSsaoRaw.h`
- `engine/render/vk/VkSsaoRaw.cpp`
- `engine/render/vk/VkSsaoPipeline.h`
- `engine/render/vk/VkSsaoPipeline.cpp`
- `game/data/shaders/ssao.vert`
- `game/data/shaders/ssao.frag`

**Modifiés :**
- `engine/core/Engine.h` (membres `m_vkSsaoRaw`, `m_ssaoPipeline`, `m_fgSsaoRawId`; includes)
- `engine/core/Engine.cpp` (init SSAO raw + pipeline après SceneColorHDR; frame graph SSAO_Raw + pass SSAO_Generate; registry; Recreate; Shutdown)
- `CMakeLists.txt` (VkSsaoRaw.cpp, VkSsaoPipeline.cpp)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset <preset>`
- `cmake --build --preset <preset>`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Structure repo respectée ; code moteur sous `/engine`, contenu sous `/game/data` ; chemins relatifs via `paths.content` ; pass SSAO_Generate → SSAO_Raw (R16F) ; shader SSAO avec reconstruction posVS (invProj), TBN, kernel, range check smoothstep, occlusion 0..1.

---

## Ticket (original)

### Objectif
Calcul SSAO raw à partir depth+normal en view-space.

### Dépendances
- M06.1
- M03.1

### Livrables
- Pass SSAO_Generate -> SSAO_Raw (R8 ou R16F)
- Shader SSAO

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Reconstruct posVS via inverse projection; range check smoothstep.

### Étapes d'implémentation
1. Créer SSAO_Raw texture.
2. Shader: TBN, sample kernel, project to screen, compare depth.
3. Écrire occlusion 0..1.

### Critères de validation (DoD)
- [ ] AO visible (coins plus sombres) sans halos extrêmes.

### Notes / pièges à éviter
- Commencer avec radius ~1m, bias ~0.02.
