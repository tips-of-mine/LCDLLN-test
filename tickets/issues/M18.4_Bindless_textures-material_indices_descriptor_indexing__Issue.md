# Issue: M18.4 — Bindless textures/material indices (descriptor indexing)

**Status:** Closed

---

# M18.4 — Bindless textures/material indices (descriptor indexing)

## Objectif
Mettre en place bindless textures pour réduire bindings materials.

## Dépendances
- M03.3

## Livrables
- Descriptor indexing enable
- Global texture array
- Material uses indices

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- VK_EXT_descriptor_indexing + robust access.

## Étapes d’implémentation
1. Activer features descriptor indexing.
2. Créer descriptor set global textures[].
3. Modifier shaders materials pour indexer textures.
4. Migrer matériaux vers indices.

## Critères de validation (DoD)
- [ ] Rendu identique, mais bindings par draw fortement réduits.

## Notes / pièges à éviter
- Gérer lifetime textures (streaming) avec slots libres/RC.

---

## Rapport final

### 1) FICHIERS
- Créés :
  - `tickets/issues/M18.4_Bindless_textures-material_indices_descriptor_indexing_.md`
- Modifiés :
  - `CMakeLists.txt`
  - `engine/Engine.cpp`
  - `engine/render/DeferredPipeline.h`
  - `engine/render/DeferredPipeline.cpp`
  - `engine/render/GeometryPass.h`
  - `engine/render/GeometryPass.cpp`
  - `engine/render/InstanceBatch.h`
  - `engine/render/Material.h`
  - `engine/render/Material.cpp`
  - `engine/render/vk/VkDeviceContext.h`
  - `engine/render/vk/VkDeviceContext.cpp`
  - `game/data/shaders/gbuffer_geometry.vert`
  - `game/data/shaders/gbuffer_geometry.frag`
- Supprimés :
  - Aucun

### 2) COMMANDES WINDOWS À EXÉCUTER
- `glslangValidator -V -S vert -o game/data/shaders/gbuffer_geometry.vert.spv game/data/shaders/gbuffer_geometry.vert`
- `glslangValidator -V -S frag -o game/data/shaders/gbuffer_geometry.frag.spv game/data/shaders/gbuffer_geometry.frag`
- `cmake --preset vs2022-x64`
- `cmake --build --preset vs2022-x64-debug`
- `.\build\vs2022-x64\pkg\game\lcdlln.exe`

### 3) RÉSULTAT
- Compilation : NON TESTÉ
- Exécution   : NON TESTÉ

### 4) VALIDATION DoD
- Tous les points de `DEFINITION_OF_DONE.md` sont-ils respectés ?
  - NON
- Pourquoi :
  - `cmake` n'est pas disponible dans l'environnement courant, donc la configuration/compilation/exécution Windows n'ont pas pu être vérifiées ici.
  - Le critère ticket "Rendu identique, mais bindings par draw fortement réduits" n'a pas pu être validé sans exécution locale.
  - `BUILD_CHECK.md` n'a pas pu être relu ici car le chemin résolu par l'environnement n'était pas lisible.
