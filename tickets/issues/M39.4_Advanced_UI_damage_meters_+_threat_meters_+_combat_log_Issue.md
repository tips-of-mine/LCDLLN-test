# Issue: M39.4 — Advanced UI: damage meters + threat meters + combat log

**Status:** Closed

---

## Contenu du ticket

# M39.4_Advanced_UI_damage_meters_+_threat_meters_+_combat_log

## Objectif
Outils UI avancés pour raiders: DPS meter, threat meter, combat log détaillé.

## Dépendances
- M16.2
- M32.2

## Livrables
- DPS meter: track damage per player, update realtime
- Threat meter: show aggro % per player vs target
- Combat log: scrollable text log (damage, heals, buffs, etc.)
- Export logs (CSV optional)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- DPS: sum damage per player / fight duration
- Threat: server sends threat values, display bars
- Combat log: events stream (damage, heal, buff, death)

## Étapes d'implémentation
1. DPS meter: track CombatEvent per player, DPS = total/elapsed, bar chart top 5, reset on end
2. Threat meter: ThreatUpdate broadcasts, bars 100%=highest, color-coded
3. Combat log: ring buffer 500 lines, filters, format "[HH:MM:SS] text"
4. Export: save log to CSV file

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Performance: aggregate events (don't spam UI)
- Privacy: option hide DPS from others

---

## Rapport final (M39.4)

### 1) FICHIERS

**Créés :**
- `engine/client/CombatAnalyticsUi.h` — enums `ThreatColor`/`CombatLogCategory`, structs `DpsMeterEntry`/`DpsMeterState`/`ThreatMeterEntry`/`ThreatMeterState`/`CombatLogEntry`/`CombatLogFilter`/`CombatLogState`/`CombatAnalyticsPanelState`, classe `CombatAnalyticsUiPresenter` (Init/Shutdown/OnCombatEvent/Tick/OnCombatEnd/ResetDpsMeter/OnThreatUpdate/ResetThreatMeter/AddLogEntry/AddLogEntryNow/SetLogFilter/ScrollLog/ExportLogCsv).
- `engine/client/CombatAnalyticsUi.cpp` — implémentation : ring buffer 500 lignes, DPS = totalDamage/elapsed, top-5 bar fractions, threat color-coded (red=pulling/yellow≥80%/green=safe), filtre par catégorie (Damage/Healing/Buff/Death/Other), export CSV avec échappement des guillemets, timestamp `[HH:MM:SS]`.
- `tickets/issues/M39.4_Advanced_UI_damage_meters_+_threat_meters_+_combat_log_Issue.md`

**Modifiés :**
- `CMakeLists.txt` — `engine/client/CombatAnalyticsUi.cpp` ajouté.

### 2) COMMANDES WINDOWS À EXÉCUTER

```powershell
cmake --preset vs2022-x64-release
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\engine_app\lcdlln.exe -log
```

### 3) RÉSULTAT

- Compilation : **NON TESTÉ** (Visual Studio absent)
- Exécution   : **NON TESTÉ**

### 4) VALIDATION DoD

| Critère | Statut |
|---|---|
| Aucun dossier racine non autorisé | ✅ |
| Code moteur sous `/engine` uniquement | ✅ |
| Portée strictement M39.4 | ✅ |
| Aucun chemin absolu hardcodé | ✅ (CSV via Config + paths.content) |
| DPS meter : tracking par entityId, DPS=total/elapsed | ✅ |
| DPS meter : top 5 barres, bar fractions normalisées | ✅ |
| DPS meter : reset sur combat end | ✅ `ResetDpsMeter()` / `OnCombatEnd()` |
| Threat meter : OnThreatUpdate, barres 100%=max | ✅ |
| Threat meter : color red/yellow/green | ✅ `ThreatColor` |
| Combat log : ring buffer 500 lignes | ✅ `kCombatLogMaxLines=500` |
| Combat log : format [HH:MM:SS] + texte | ✅ `FormatTimestamp()` |
| Combat log : filtres Damage/Healing/Buff/Death/Other | ✅ `SetLogFilter()` |
| Combat log : scroll | ✅ `ScrollLog()` |
| Export CSV | ✅ `ExportLogCsv()` avec échappement |
| Logs Init/Shutdown/Resets/Export | ✅ |
| Build testé | NON TESTÉ |
