# Issue: M17.1 — FX mapping: events->fx JSON definitions

**Status:** Closed

---

## Rapport final (M17.1)

### 1) FICHIERS

**Créés :**
- `engine/client/FXManager.h`
- `engine/client/FXManager.cpp`
- `game/data/fx/effects.json`
- `game/data/fx/event_mappings.json`
- `tickets/issues/M17.1_FX_mapping_events-_fx_JSON_definitions_Issue.md`

**Modifiés :**
- `CMakeLists.txt`

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64\pkg\game\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

Détail environnement :
- `cmake` n’est pas disponible dans l’environnement de travail courant, donc la configuration / compilation locale n’a pas pu être lancée ici.
- Les diagnostics IDE sur les fichiers modifiés ne remontent pas d’erreur.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Le scope demandé par `M17.1` a été implémenté au minimum via :
  - schéma JSON data-driven sous `game/data/fx`
  - `FXManager` client-side uniquement
  - table de mapping `CombatEvent -> fxId`
  - spawn requests distinctes pour particle / decal / sound
  - test logique branché sur `CombatEvent` uniquement
- La validation complète DoD reste incomplète car :
  - la compilation n’a pas pu être exécutée localement sans `cmake`
  - l’exécution n’a pas pu être validée localement

---

## Ticket content (M17.1) — référence

# M17.1 — FX mapping: events->fx JSON definitions

## Objectif
Mapper events réseau (CombatEvent, etc.) vers FX data-driven (particles/decals/sounds).

## Dépendances
- M13.3
- M16.1

## Livrables
- fx/*.json schema
- FX manager
- Event->FX mapping table

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- FX id strings; paramètres scale/duration; deterministic enough.

## Étapes d’implémentation
1. Définir schema fx.json.
2. Créer FXManager: spawn particle, decal, sound.
3. Créer mapping par type event.
4. Tester sur CombatEvent.

## Critères de validation (DoD)
- [ ] Un hit déclenche FX configuré par JSON.

## Notes / pièges à éviter
- Ne pas exécuter FX côté serveur.
