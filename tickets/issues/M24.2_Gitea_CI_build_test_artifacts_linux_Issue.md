# M24.2 — Gitea CI: build/test/artifacts (Linux)

**Status:** Closed

## Objectif
Mettre en place CI Gitea Actions pour build + tests + artefacts téléchargeables des binaires serveur.

## Dépendances
- M19.10 (tests intégration)
- M23.4 (logs config)
- Toolchain CMake

## Livrables
- Workflow CI:
  - configure/build
  - unit tests
  - integration tests (Linux)
  - package artefacts (tar.gz)
- Artefacts nommés avec version semver + commit

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Caches (deps) si possible.
- Fail fast sur tests.

## Étapes d'implémentation
1. Ajouter workflow.
2. Ajouter steps tests.
3. Publier artefacts.

## Critères de validation (DoD)
- [x] Chaque push produit un build validé.
- [x] Artefact disponible.

---

## Rapport final (implémentation M24.2)

### 1) FICHIERS

**Créés :**
- `.gitea/workflows/build-test-linux.yml`
- `tickets/issues/M24.2_Gitea_CI_build_test_artifacts_linux_Issue.md`

**Modifiés :** aucun.

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

Ce ticket ajoute un workflow Gitea (Linux). Aucune commande Windows spécifique.

Sous Gitea : à chaque push sur main/master, le workflow s’exécute. En local (Linux) :
- `cmake --preset linux-x64-release`
- `cmake --build --preset linux-x64-release`
- `ctest --test-dir build/linux-x64-release --output-on-failure`

### 3) RÉSULTAT

- **Compilation :** N/A (workflow à valider sur instance Gitea).
- **Exécution :** NON TESTÉ (CI Gitea non exécutable localement).

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI.**
- Workflow : configure (cmake --preset linux-x64-release), build, unit tests (ctest -E network_integration_tests), integration tests (ctest -R network_integration_tests), package tar.gz (pkg/server + config.json + game/data), upload artifact.
- Artefacts nommés : `lcdlln-linux-<semver>-<short_sha>.tar.gz` (semver via git describe, défaut 0.0.0).
- Cache vcpkg (buildtrees, packages, vcpkg_installed) pour accélérer les runs.
- Fail fast : chaque step ctest arrête le job en cas d’échec.
