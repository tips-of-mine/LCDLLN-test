# Points de réapparition de la zone Feyhin (validation v12, wire v13).
# Format : zone_id type x y z [faction] [neutral_radius_m]
#   - type ∈ {graveyard, inn}
#   - faction (cimetières uniquement) : id de la faction propriétaire ; '-' ou absent = neutre partout.
#   - neutral_radius_m : rayon (m) dans lequel le cimetière est NEUTRE (toutes factions) ;
#     au-delà, il n'est éligible que pour les joueurs de sa faction. Absent = défaut serveur
#     (globals.graveyard_default_faction_neutral_radius_m).
# Le serveur choisit le cimetière ÉLIGIBLE (cf. rayon neutre de faction) le PLUS PROCHE du
# lieu de mort ; s'il n'y en a aucun, repli sur l'auberge / le point d'entrée en monde.
# Cimetière neutre ('-') par défaut : comportement identique à avant (tout le monde y respawn).
# NB : la colonne zone_id (1re) est l'id NUMÉRIQUE de la zone côté serveur (filtre
# ServerApp : point.zoneId == client->zoneId). Feyhin = zone_id 0 (cf. game/data/zones/zone_0/),
# soit world.active_zone "feyhin" ↔ zoneId 0. Conserver 0 ici tant que ce mapping tient.
0 graveyard 120.0 1.5 120.0 -
0 inn 88.0 1.5 100.0
