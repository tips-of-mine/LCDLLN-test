# M05.1 — IBL: BRDF LUT (compute) generation

**Status:** Closed

## Ticket (original)

### Objectif
Générer BRDF LUT 2D (R16G16) pour split-sum specular GGX.

### Dépendances
- M03.2

### Livrables
- Texture BRDF_LUT (256x256 RG16F)
- Compute shader intégration GGX

### Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Génération au boot (runtime) acceptable; offline plus tard.

### Étapes d'implémentation
1. Créer image BRDF_LUT + view + sampler.
2. Compute dispatch 256x256, écrire RG.
3. Barrières: GENERAL->SHADER_READ_ONLY.
4. Smoke test: LUT non nulle.

### Critères de validation (DoD)
- [ ] BRDF LUT générée une fois, réutilisée dans lighting.

### Notes / pièges à éviter
- Limiter samples (ex 1024) pour temps raisonnable; cache disque plus tard.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M05/M05.1_ISSUE.md` (issue de suivi)
- `engine/render/vk/VkBrdfLut.h` — Déclaration BRDF LUT (image 256x256 RG16F, view, sampler, compute pipeline).
- `engine/render/vk/VkBrdfLut.cpp` — Création image storage+sampled, view, sampler, pipeline compute, descriptor; Generate() avec barrières GENERAL→SHADER_READ_ONLY.
- `game/data/shaders/brdf_lut.comp` — Compute shader GGX (Hammersley, importance sample GGX, GeometrySmith), 1024 samples, sortie (scale, bias) en RG.

**Modifiés :**
- `CMakeLists.txt` — Ajout de `engine/render/vk/VkBrdfLut.cpp`.
- `engine/core/Engine.h` — Include VkBrdfLut.h, membre `m_vkBrdfLut`.
- `engine/core/Engine.cpp` — Après init shadow/lighting : chargement `shaders/brdf_lut.comp`, Init + Generate de `m_vkBrdfLut`, `SetBrdfLutView` sur le lighting pipeline; shutdown `m_vkBrdfLut`.
- `engine/render/vk/VkLightingPipeline.h` — `SetBrdfLutView(VkImageView, VkSampler)`.
- `engine/render/vk/VkLightingPipeline.cpp` — Binding 9 pour BRDF LUT (COMBINED_IMAGE_SAMPLER), pool +1, SetGBufferViews écrit binding 9 (placeholder), implémentation SetBrdfLutView.
- `game/data/shaders/lighting.frag` — Binding 9 `uBrdfLut`, échantillonnage (NdotV, roughness), terme IBL specular `iblSpec = brdf.x*F0 + brdf.y`, réutilisé dans Lo.

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win-x64`
- `cmake --build --preset win-x64`
- `.\build\engine_app.exe` (ou chemin selon preset)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (environnement sans cmake).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Structure du repo respectée (code sous /engine, contenu sous /game/data).
- BRDF LUT 256x256 RG16F générée une fois au boot via compute (dispatch 32x32, workgroup 8x8).
- Barrières : UNDEFINED→GENERAL (compute write), GENERAL→SHADER_READ_ONLY_OPTIMAL.
- LUT réutilisée dans lighting (binding 9, sample + terme IBL specular).
- Samples limités à 1024 dans le compute.
- **OUI** sous réserve de compilation/exécution validées en local.
