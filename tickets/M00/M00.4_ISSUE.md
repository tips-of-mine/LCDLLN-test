# M00.4 — Platform: window, input, filesystem abstraction

**Status:** Open

## Ticket (original)

### Objectif
Abstraction plateforme : création fenêtre, input (clavier/souris), chemins, lecture fichiers, time monotone (si besoin).

### Dépendances
- M00.1

### Livrables
- `/engine/platform/Window.*` (GLFW recommandé) + events resize/close
- `/engine/platform/Input.*` (WASD, mouse delta, key states)
- `/engine/platform/FileSystem.*` (read file, exists, list, path join)

### Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Fenêtre: création, resize callback, fullscreen toggle (option).
- Input: état touches, mouse delta, capture/release curseur.
- FS: lecture binaire/texte, chemins relatifs à `paths.content`.

### Étapes d'implémentation
1. Intégrer GLFW (ou Win32 si Windows-only).
2. Implémenter Window::PollEvents + callbacks vers engine.
3. Implémenter Input: update par frame + mouse look.
4. Implémenter FileSystem: ReadAllBytes/ReadAllText.
5. Smoke test: fenêtre + caméra libre.

### Critères de validation (DoD)
- [ ] Fenêtre s'ouvre, se ferme proprement, resize détecté.
- [ ] WASD + souris fonctionne dans loop.
- [ ] Lecture fichier config/asset via FS OK.

### Notes / pièges à éviter
- Isoler GLFW dans platform pour pouvoir swap plus tard.
- Ne pas bloquer sur I/O dans le thread render.

---

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M00/M00.4_ISSUE.md` (issue de suivi)
- `engine/platform/Window.h` — Create, Destroy, PollEvents, ShouldClose, GetWidth/GetHeight, GetNativeHandle, SetResizeCallback, SetCloseCallback
- `engine/platform/Window.cpp` — GLFW (init dans Create), framebuffer resize + close callbacks, pas d’exposition de GLFW dans le header
- `engine/platform/Input.h` — Update(window), GetKey/GetKeyWASD, GetMouseDelta, SetCursorCaptured, IsCursorCaptured
- `engine/platform/Input.cpp` — États clavier GLFW, position souris → delta, cursor capture (GLFW_CURSOR_DISABLED/NORMAL)
- `engine/platform/FileSystem.h` — SetContentRoot, GetContentRoot, PathJoin, ReadAllBytes, ReadAllText, Exists, List
- `engine/platform/FileSystem.cpp` — Résolution path (content root + relatif), ReadAllBytes/ReadAllText (ifstream), Exists (GetFileAttributes/stat), List (FindFirstFile/readdir)

**Modifiés :**
- `vcpkg.json` — Dépendance `glfw3`
- `CMakeLists.txt` — find_package(glfw3), lib engine_platform (Window, Input, FileSystem), link glfw, engine_app → engine_platform
- `main.cpp` — FileSystem::SetContentRoot(Config paths.content), test FS (Exists, List, ReadAllText), création fenêtre 1280x720, Input::Update, boucle PollEvents + resize log + FPS/WASD/mouse log, Destroy en sortie

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win-x64`
- `cmake --build --preset win-x64`
- `.\build\engine_app.exe`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake/GLFW non disponibles dans l’environnement).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Build & exécution : commandes listées ; à valider en local (GLFW via vcpkg).
- Structure : code sous `/engine` uniquement (platform sous engine/platform).
- Portée : uniquement Window, Input, FileSystem ; pas de refactor hors scope.
- Chemins : FileSystem utilise content root (paths.content) ; pas de chemin absolu hardcodé.
- Qualité : GLFW isolé dans .cpp ; fonctions documentées.

**Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI, sous réserve de compilation et exécution validées en local.
