# Issue: M12.3 — Volumes gameplay: triggers/spawns/transitions placement

**Status:** Closed

---

## Rapport final (M12.3)

### 1) FICHIERS

**Créés :**
- `tickets/issues/M12.3_Volumes_gameplay_triggers-spawns-transitions_placement_Issue.md`

**Modifiés :**
- `engine/editor/EditorMode.h`
- `engine/editor/EditorMode.cpp`
- `engine/platform/FileSystem.h`
- `engine/platform/FileSystem.cpp`
- `engine/platform/Input.h`

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-debug
.\build\vs2022-x64\Debug\lcdlln.exe --editor -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

`cmake` n'est pas disponible dans l'environnement courant, donc la validation locale doit être faite sur la machine Windows équipée des outils du preset.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Build/exécution non validés localement faute de `cmake` dans l'environnement.
- Le ticket est couvert côté shell éditeur pour :
  - entités volumes `trigger` / `spawnArea` / `zoneTransition`
  - champs d'inspector MVP
  - création/édition/sélection/export layout via `Ctrl+S`
- Limite restante :
  - l'étape `Rendu debug volumes (lines)` n'est pas implémentée en rendu scène Vulkan ; la visualisation MVP est fournie dans le shell éditeur (titre de fenêtre/logs/inspector), et l'export layout est écrit sous `paths.content/editor/volumes.layout.json`.

## Ticket content (M12.3) — référence

# M12.3 — Volumes gameplay: triggers/spawns/transitions placement

## Objectif
Permettre de placer volumes (box/sphere) pour triggers, spawns, transitions de zone.

## Dépendances
- M12.1

## Livrables
- Volume entities
- Inspector fields
- Visualization gizmos

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Types: trigger, spawnArea, zoneTransition.

## Étapes d’implémentation
1. Définir types volumes + propriétés.
2. UI création/édition.
3. Rendu debug volumes (lines).
4. Export dans layout.

## Critères de validation (DoD)
- [ ] Un trigger et une transition sont visibles et exportés.

## Notes / pièges à éviter
- Les scripts d'actions peuvent être string IDs au début.
