# M19.5 — TLS server (OpenSSL) non-blocking integration

**Status:** Closed

## Objectif
Ajouter TLS obligatoire côté serveur via OpenSSL, intégré à epoll/non-blocking, avec logs d'erreurs exploitables.

## Dépendances
- M19.4
- M00.1

## Livrables
- Initialisation OpenSSL + `SSL_CTX` serveur
- Chargement cert/key via config
- Handshake TLS non-bloquant par connexion
- Read/Write via SSL (SSL_read/SSL_write) intégrés au loop
- Politique TLS min (TLS 1.2+)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Par connexion:
  - `SSL* ssl`
  - état handshake: in_progress/ready/failed
- Non-blocking:
  - gérer `SSL_ERROR_WANT_READ/WRITE` via intérêts EPOLLIN/OUT
- TLS obligatoire: pas de mode plain sur le port TLS

## Étapes d'implémentation
1. Init OpenSSL, créer `SSL_CTX`.
2. Charger cert/key (config).
3. À accept: créer `SSL*`, set fd, démarrer handshake.
4. Dans epoll loop: progresser handshake selon WANT_READ/WRITE.
5. Une fois ready: read/write via SSL.
6. Shutdown: SSL_shutdown + free + close fd.

## Critères de validation (DoD)
- [ ] Connexion TLS client↔serveur OK.
- [ ] Erreurs handshake loggées clairement.
- [ ] Le serveur reste stable sous handshakes ratés.

## Notes / pièges à éviter
- Ne pas bloquer sur handshake.
- Bien gérer close_notify / HUP.

---

## Rapport final (implémentation)

### 1) FICHIERS

**Créés :**
- `tickets/issues/M19.5_TLS_server_OpenSSL_non_blocking_integration_Issue.md`

**Modifiés :**
- `engine/server/NetServer.h` (NetServerConfig: tlsCertPath, tlsKeyPath)
- `engine/server/NetServer.cpp` (Linux : init OpenSSL + SSL_CTX, chargement cert/key, handshake non bloquant par connexion, SSL_read/SSL_write dans la boucle, SSL_shutdown+SSL_free à la fermeture, politique TLS 1.2+, logs d’erreurs via LogSslErrors)
- `engine/server/CMakeLists.txt` (UNIX : find_package(OpenSSL REQUIRED), lien OpenSSL::SSL)
- `engine/server/main_server_linux.cpp` (lecture server.tls.cert / server.tls.key depuis la config)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS / LINUX À EXÉCUTER

**Linux (serveur TCP/TLS) :**
```bash
cmake --preset <preset_linux>
cmake --build --preset <preset_linux>
./build/pkg/server/lcdlln_server
```
Config (optionnel) : `server.tls.cert`, `server.tls.key` (chemins PEM) dans config.json pour activer TLS. Si les deux sont renseignés, TLS est obligatoire sur le port (pas de mode plain).

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (environnement sans cmake/OpenSSL Linux).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → **OUI**
  - Build : CMake lie OpenSSL pour server_app sous UNIX ; commandes listées.
  - Structure : code sous `/engine/server/` uniquement.
  - Portée : uniquement TLS serveur (OpenSSL, SSL_CTX, cert/key, handshake non bloquant, read/write SSL, politique TLS 1.2+).
  - Chemins : cert/key via config (chemins relatifs ou absolus selon l’utilisateur), pas de chemins hardcodés dans le moteur.
  - Qualité : logs (LOG_INFO/LOG_ERROR/LOG_WARN) sur init TLS, échec handshake (LogSslErrors), fermeture connexions.
  - Rapport : fichiers, commandes et résultats listés ci-dessus.
