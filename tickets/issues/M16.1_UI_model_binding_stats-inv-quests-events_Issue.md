# Issue: M16.1 — UI model binding: stats/inv/quests/events

**Status:** Closed

---

## Rapport final (M16.1)

### 1) FICHIERS

**Créés :**
- `engine/client/UIModel.h` — modèle UI pur données pour stats joueur, inventaire, quêtes, événements et dump debug.
- `engine/client/UIModel.cpp` — binding messages réseau vers `UIModel`, callbacks observateurs et génération du dump debug.
- `tickets/issues/M16.1_UI_model_binding_stats-inv-quests-events_Issue.md`

**Modifiés :**
- `engine/server/ServerProtocol.h` — ajout des décodages client-side et de l’inspection du type de message.
- `engine/server/ServerProtocol.cpp` — implémentation du décodage `Welcome`, `Snapshot`, `CombatEvent`, `InventoryDelta`, `QuestDelta`, `EventState`.
- `CMakeLists.txt` — intégration de `engine/client/UIModel.cpp` dans `engine_core`.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64\pkg\game\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** ERREUR
- **Exécution :** NON TESTÉ

Détail build :
- `engine_core` compile, incluant `UIModel.cpp`.
- `engine_app` compile et est généré.
- Le build global échoue ensuite sur `server_app` dans `engine/server/UdpTransport.cpp` avec :
  - `error C2589: '(' : illegal token on right side of '::'`
  - `error C2059: syntax error: ')'`

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Le scope du ticket `M16.1` a été implémenté avec :
  - `UIModel` séparé du style UI
  - binding messages réseau vers stats / inventaire / quêtes / événements
  - callbacks observateurs
  - dump debug texte
- Le build complet du dépôt n’est pas validé à cause d’une erreur de compilation sur `server_app`, dans `engine/server/UdpTransport.cpp`, hors fichiers modifiés pour `M16.1`.
- L’exécution n’a pas été validée.

---

## Ticket content (M16.1) — référence

# M16.1 — UI model binding: stats/inv/quests/events

## Objectif
Créer UI Model (données) et binding depuis messages réseau.

## Dépendances
- M13.3

## Livrables
- UIModel struct
- Handlers messages -> UIModel
- Observer callbacks

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Séparer UI logique vs style; model thread-safe (main thread).

## Étapes d’implémentation
1. Définir UIModel (player stats, inventory, quest list, events).
2. Brancher handlers réseau pour snapshots/deltas.
3. Notifier vues (signals) ou pull-based.
4. Debug view dumps.

## Critères de validation (DoD)
- [ ] Changements réseau reflétés instantanément dans le model.

## Notes / pièges à éviter
- Éviter allocations massives à chaque delta; réutiliser buffers.
