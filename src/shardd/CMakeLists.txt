# src/shardd/CMakeLists.txt — shard server (Linux + Windows sandbox).

# Les cibles shard_app (UNIX) et shard_sandbox_app (WIN32) sont déclarées
# dans le CMakeLists racine. Ce fichier est un placeholder.
