# M06.1 — SSAO: kernel + noise generation

**Status:** Closed

## Ticket (original)

### Objectif
Générer kernel SSAO (16-32 samples) + noise 4x4 pour rotation TBN.

### Dépendances
- M03.1

### Livrables
- UBO kernel samples
- Texture noise 4x4 (RG16F ou RG8) tiled

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Kernel biased vers centre; noise XY aléatoire.

### Étapes d'implémentation
1. Générer kernel CPU au boot.
2. Générer noise texture 4x4.
3. Uploader UBO + texture.

### Critères de validation (DoD)
- [ ] Kernel/noise valides et constants entre frames.

### Notes / pièges à éviter
- Clamper radius/bias via config.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/render/vk/VkSsaoKernelNoise.h`
- `engine/render/vk/VkSsaoKernelNoise.cpp`

**Modifiés :**
- `engine/core/Engine.h` — include `VkSsaoKernelNoise.h`, membre `m_ssaoKernelNoise`
- `engine/core/Engine.cpp` — init au boot (Config `ssao.radius`, `ssao.bias`, clamp dans Init), shutdown
- `CMakeLists.txt` — `VkSsaoKernelNoise.cpp`
- `config.json` — `ssao.radius`, `ssao.bias`

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset windows-default`
- `cmake --build --preset windows-default`
- `.\build\engine_app.exe`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- **Livrables :** UBO kernel (32 samples vec3 + radius + bias, std140, seed fixe), texture bruit 4x4 RG8 (tiled, REPEAT), upload au boot. Radius/bias lus depuis config et clamés (ClampRadius 0.01–2, ClampBias 0.0001–0.1).
- **Critère :** Kernel/noise valides et constants entre frames → génération avec seeds fixes (kKernelSeed, kNoiseSeed), pas de régénération par frame.
