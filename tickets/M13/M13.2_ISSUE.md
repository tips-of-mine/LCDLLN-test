# M13.2 — Spatial partition: cells 64m + interest set

**Status:** Open

## Objectif
Partitionner zone en cells 64m et calculer interest set (rayon).

## Dépendances
- M13.1
- M09.1

## Livrables
- CellGrid per zone
- ClientInterestSet computation

## Structure & chemins (verrouillé)
- Code moteur : /engine/...
- Contenu : /game/data/...
- Outils : /tools/...

## Spécification technique
- Rayon intérêt base: 3 cells (7x7).

## Étapes d'implémentation
1. Fonction pos->cell coord.
2. Maintenir mapping cell->entityIds.
3. Par client: calcul set cells autour.
4. Diff cells entrant/sortant.

## Critères de validation (DoD)
- [x] Interest set se met à jour quand joueur traverse cells.

## Notes / pièges à éviter
- Optimiser avec bitsets plus tard.

---

**Status:** Closed

---

## Rapport final M13.2

### 1) FICHIERS

**Créés :**
- `engine/world/CellGrid.h` — kCellSize (64), kCellsPerZone, CellCoord, WorldToCellCoord, GetCellsInRadius, class CellGrid (Insert, Remove, UpdatePosition, GetEntityIdsInCell).
- `engine/world/CellGrid.cpp` — Implémentation WorldToCellCoord, GetCellsInRadius, CellGrid (mapping entity->cell, cell->entityIds).
- `engine/world/InterestSet.h` — ClientInterestSet (Update(worldX, worldZ, radiusCells), CurrentCells, CellsEntered, CellsLeft).
- `engine/world/InterestSet.cpp` — Update: calcul des cellules dans le rayon (7x7), diff entrant/sortant.
- `tickets/M13/M13.2_ISSUE.md` — Issue de suivi (ouverte puis fermée).

**Modifiés :**
- `CMakeLists.txt` — Ajout engine/world/CellGrid.cpp et engine/world/InterestSet.cpp au target engine_core.

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe` (utilisation de CellGrid/ClientInterestSet depuis le code moteur ou serveur selon intégration future).

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI.
- Code sous /engine, pas de nouveau dossier racine. Portée limitée au ticket : CellGrid par zone, ClientInterestSet, rayon 3 cells (7x7), diff cells entrant/sortant. Interest set se met à jour quand le joueur traverse les cells (Update avec nouvelle position). Fonctions documentées.
