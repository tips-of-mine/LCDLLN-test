# Issue: M14.4 — Persistence: character state + inventory + position

**Status:** Closed

---

## Rapport final (M14.4)

### 1) FICHIERS

**Créés :**
- `engine/server/CharacterPersistence.h` — `PersistedCharacterState` et `CharacterPersistenceStore` pour le save/load des personnages.
- `engine/server/CharacterPersistence.cpp` — validation du fichier de schéma, chargement/sauvegarde des états personnages sous forme de fichiers `.ini` résolus via `paths.content`.
- `game/data/persistence/db/migrations/0001_characters_inventory.sql` — schéma SQL minimal `characters` + `inventory_items`.
- `tickets/issues/M14.4_Persistence_character_state_inventory_position_Issue.md`

**Modifiés :**
- `engine/server/ServerApp.h` — intégration du store de persistance, clé personnage persistée par client, autosave timer.
- `engine/server/ServerApp.cpp` — init/shutdown du store, load à la connexion, save autosave toutes les 30s, save sur transition de zone, pickup et arrêt serveur.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\engine_app\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (`cmake` indisponible dans l'environnement)
- **Exécution :** NON TESTÉ (`cmake` indisponible dans l'environnement)

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Build/exécution non validés localement car `cmake` n'est pas disponible dans l'environnement.
- Le ticket `M14.4` est couvert avec :
  - schéma SQL initial `characters` + `inventory_items`
  - pipeline save/load côté serveur
  - autosave timer (30 s par défaut)
  - restauration zone/position/stats/inventaire à la connexion
- Limite explicite : le runtime persiste les états via fichiers `.ini` sous `paths.content`, tout en livrant le schéma SQL/migration demandé ; aucune connexion DB runtime existante n'était présente dans le dépôt au moment de l'implémentation.

---

## Ticket content (M14.4) — référence

# M14.4 — Persistence: character state + inventory + position

## Objectif
Persister état joueur (zone+pos, stats, inventory) et recharger à la connexion.

## Dépendances
- M14.3

## Livrables
- DB schema (SQLite/PG)
- Save/load pipeline
- Autosave timer

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Save toutes 30s + on logout + events importants.

## Étapes d’implémentation
1. Créer tables characters + inventory_items.
2. Load à login -> spawn player.
3. Autosave timer.
4. Test déco/reco.

## Critères de validation (DoD)
- [ ] Après déco/reco, position et inventaire sont identiques.

## Notes / pièges à éviter
- Prévoir migrations DB dès le début (version schema).
