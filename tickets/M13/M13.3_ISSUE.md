# M13.3 — Replication: spawn/despawn + state updates by interest

**Status:** Closed

## Ticket (contenu)

# M13.3 — Replication: spawn/despawn + state updates by interest

## Objectif
Envoyer spawn/despawn et updates uniquement pour entités dans interest set.

## Dépendances
- M13.2

## Livrables
- Messages Spawn/Despawn
- Snapshot entity states

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- EntityId 64-bit; archetypeId; transform+state minimal.

## Étapes d'implémentation
1. Définir messages et sérialisation.
2. Sur entrée cell: envoyer spawns.
3. Sur sortie: envoyer despawns.
4. Snapshot: positions/rotations/vel des entités pertinentes.

## Critères de validation (DoD)
- [ ] Deux joueurs se voient bouger; entités hors range disparaissent.

## Notes / pièges à éviter
- Commencer sans delta compression; ajouter plus tard.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- engine/network/Replication.h
- engine/network/Replication.cpp

**Modifiés :**
- engine/network/Protocol.h (MsgType Spawn, Despawn, ClientInput)
- CMakeLists.txt (Replication.cpp dans engine_network; server_app lie engine_core)
- main_server.cpp (boucle réplication: ClientInput, CellGrid, InterestSet, BuildReplicationForClient, envoi Spawn/Despawn/Snapshot)
- main_client.cpp (envoi ClientInput position, réception/parse Spawn/Despawn/Snapshot, suivi entités répliquées)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- cmake --preset win64
- cmake --build --preset win64
- .\build\server_app.exe
- .\build\client_app.exe [host]
- (second client: .\build\client_app.exe 127.0.0.1)

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Code sous /engine; messages Spawn/Despawn + snapshot entity states; entrée cell → spawns, sortie → despawns; pas de delta compression. Deux clients en envoyant leur position reçoivent Spawn/Snapshot/Despawn par interest set; entités hors range disparaissent (Despawn).
