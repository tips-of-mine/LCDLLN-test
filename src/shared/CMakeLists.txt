# src/shared/CMakeLists.txt — sources d'engine_core (lib statique partagée).
# La cible engine_core est déclarée dans le CMakeLists racine.

# Tests partagés (formulas, util, metric, security, etc.) sont déclarés
# dans le CMakeLists racine via lcdlln_add_simple_test() — pas de duplication ici.
