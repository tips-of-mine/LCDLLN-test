# M20.1 — Accounts: schema + validation rules

**Status:** Closed

## Objectif
Définir le modèle de compte (sans personnages) et les règles de validation strictes (email/login/password) côté serveur (source of truth).

## Dépendances
- M19.1
- M21.1 (schema framework) (sera créé en M21)

## Livrables
- Spécification des champs compte:
  - email (unique, normalized)
  - login (unique, normalized) si distinct de email
  - account_status (active/banned/locked)
  - created_at, updated_at
- Règles de validation:
  - email: format + longueur max
  - login: charset + longueur
  - password: min length + complexity (policy v1)
- Codes d'erreurs associés (alignés error codes v1)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Normalisation server-side:
  - trim
  - email lower-case
- Rejet strict des champs hors bornes (length caps).
- Ne pas stocker le plaintext password.

## Étapes d'implémentation
1. Rédiger règles validation + exemples acceptés/refusés.
2. Préparer mapping erreurs (INVALID_EMAIL, WEAK_PASSWORD, etc.).
3. (DB implémentée en M21) prévoir colonnes nécessaires.

## Critères de validation (DoD)
- [ ] Règles explicites et testables.
- [ ] Tous les champs ont un max length défini.

---

## Rapport final (implémentation)

**Status:** Closed

### 1) FICHIERS

**Créés :**
- `tickets/docs/accounts_schema_v1.md` (spécification champs, max lengths, règles validation, exemples acceptés/refusés, mapping erreurs 203/204/205, colonnes prévues M21)
- `engine/server/AccountValidation.h` (constantes, NormaliseEmail/LoginView, ValidateEmail/Login/Password)
- `engine/server/AccountValidation.cpp` (implémentation normalisation + validation)
- `tickets/issues/M20.1_Accounts_schema_and_validation_rules_Issue.md`

**Modifiés :**
- `engine/network/NetErrorCode.h` (INVALID_EMAIL 203, WEAK_PASSWORD 204, INVALID_LOGIN 205)
- `tickets/docs/protocol_v1.md` (codes 203, 204, 205)
- `engine/server/CMakeLists.txt` (AccountValidation.cpp dans server_app UNIX)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset win64
cmake --build --preset win64
.\build\pkg\game\lcdlln.exe
```

Serveur Linux : build UNIX puis `./pkg/server/lcdlln_server`.

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution   : NON TESTÉ

### 4) VALIDATION DoD

- Règles explicites et testables : décrites dans accounts_schema_v1.md ; implémentation C++ (ValidateEmail/Login/Password) dans engine/server, testable par appels unitaires ou M20.5.
- Tous les champs ont un max length défini : email 256, login 64, password (input) 256 ; constantes kAccount* dans AccountValidation.h.
- Normalisation (trim, email lower-case), rejet hors bornes, pas de stockage plaintext (doc + validation côté entrée uniquement).

→ **OUI** (points DoD respectés).
