# M02.2 — Frame Graph: compilation dépendances + ordre d'exécution

**Status:** Closed

## Ticket (contenu)

# M02.2 — Frame Graph: compilation dépendances + ordre d'exécution

## Objectif
Calculer dépendances read/write entre passes et exécuter en ordre topologique.

## Dépendances
- M02.1

## Livrables
- Graphe de dépendances (writer→reader)
- Topological sort + détection cycles

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Une ressource écrite puis lue crée une dépendance.
- Support multi-writers interdit en MVP (assert), ou définir règles claires.

## Étapes d'implémentation
1. Construire edges passA->passB via lastWriter.
2. Effectuer tri topo (Kahn/DFS).
3. Assertions cycles et multi-writer (MVP).
4. Exécuter dans ordre compilé.

## Critères de validation (DoD)
- [ ] Graph compile pour 3+ passes, ordre correct.
- [ ] Cycle détecté et log fatal.

## Notes / pièges à éviter
- Plus tard: permettre multi-writer via subresources/attachments.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- tickets/M02/M02.2_ISSUE.md

**Modifiés :**
- engine/render/FrameGraph.h (compile(); Pass.reads/Pass.writes; m_compiledOrder, m_compiled; doc)
- engine/render/FrameGraph.cpp (addPass stocke reads/writes; compile(): lastWriter, edges writer→reader, Kahn, cycle→LOG_FATAL, multi-writer→LOG_FATAL; execute() utilise m_compiledOrder)
- engine/Engine.cpp (3e pass "PostRead" read-only pour valider 3+ passes et ordre topo)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset vs2022-x64`
- `cmake --build --preset vs2022-x64-debug`
- `.\build\vs2022-x64\Debug\engine_app.exe`

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Graphe de dépendances : lastWriter par ressource, arêtes writer→reader (pass A écrit R, pass B lit R ⇒ A avant B).
- Tri topologique : algorithme de Kahn ; si ordre compilé a moins de n passes → cycle → LOG_FATAL.
- Multi-writer : si une ressource a plusieurs writers → LOG_FATAL (MVP).
- Exécution dans l’ordre compilé (m_compiledOrder) ; compile() appelé au premier execute() si besoin.
- Graph 3+ passes : Engine a 3 passes (Clear, CopyPresent, PostRead) ; ordre correct (Clear puis passes qui lisent SceneColor).
