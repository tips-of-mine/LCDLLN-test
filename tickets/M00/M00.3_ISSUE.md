# M00.3 — Job System: thread pool, queues, job groups

**Status:** Open

## Ticket (original)

### Objectif
Mettre en place un système de jobs pour streaming/asset build/render prep : thread pool, work stealing simple, groupes + wait.

### Dépendances
- M00.1
- M00.2

### Livrables
- `/engine/core/jobs/JobSystem.*`
- `JobHandle`/`JobGroup` (Wait/IsDone)
- Queues thread-safe (MPSC) + worker threads
- Tests: 100k jobs + nested jobs

### Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- API: `Jobs::Init(n)`, `Jobs::Enqueue(fn)`, `Jobs::Wait(handle/group)`.
- Support des jobs courts; éviter allocations par job (pool/arena).
- Possibilité d'annuler via token (option MVP) ou versioning au-dessus.

### Étapes d'implémentation
1. Créer thread pool (n = cores-1) + stop flag.
2. Implémenter queue thread-safe (mutex/condvar MVP).
3. Définir JobHandle (counter) et JobGroup (atomic count).
4. Ajouter `Wait` qui pump aussi des jobs (avoid deadlock).
5. Écrire tests (1k, 100k, nested, multi-producer).

### Critères de validation (DoD)
- [ ] 100k jobs exécutés < quelques secondes (selon machine) sans deadlock.
- [ ] Wait fonctionne même si appelé depuis worker.
- [ ] Shutdown propre (join threads).

### Notes / pièges à éviter
- MVP mutex ok; optimise lock-free plus tard.
- Éviter jobs trop gros (préférer découpage).

---

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M00/M00.3_ISSUE.md` (issue de suivi)
- `engine/core/jobs/JobSystem.h` — JobHandle (counter index), JobGroup (atomic m_pending), Jobs::Init/Shutdown/Enqueue/EnqueueToGroup/Wait(handle|group)/IsDone
- `engine/core/jobs/JobSystem.cpp` — Thread pool (n = cores-1), queue mutex/condvar, pool de slots (128k) + pool de compteurs (128k), TryRunOneJob pour Wait (pump jobs, évite deadlock si Wait depuis worker), WorkerLoop + join au Shutdown

**Modifiés :**
- `CMakeLists.txt` — Ajout de `engine/core/jobs/JobSystem.cpp` au target engine_core
- `main.cpp` — Jobs::Init(0), test 100k jobs (JobGroup + Wait), test nested (Enqueue job qui Enqueue enfant et Wait(handle) depuis worker), Jobs::Shutdown avant Memory::Shutdown

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win-x64`
- `cmake --build --preset win-x64`
- `.\build\engine_app.exe`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l’environnement).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Build & exécution : commandes listées ; compilation/exécution à valider en local.
- Structure : code sous `/engine` uniquement (jobs sous engine/core/jobs).
- Portée : uniquement JobSystem, JobHandle, JobGroup, queue thread-safe, workers, tests 100k + nested.
- Qualité : pas d’alloc par job (pool de slots + compteurs), Wait pump des jobs (pas de deadlock si appelé depuis worker), Shutdown join des threads.

**Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI, sous réserve de compilation et exécution validées en local.
