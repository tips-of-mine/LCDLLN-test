# M14.4 — Persistence: character state + inventory + position

**Status:** Closed

## Objectif
Persister état joueur (zone+pos, stats, inventory) et recharger à la connexion.

## Dépendances
- M14.3

## Livrables
- DB schema (SQLite/PG)
- Save/load pipeline
- Autosave timer

## Spécification technique
- Save toutes 30s + on logout + events importants.

## Étapes d'implémentation
1. Créer tables characters + inventory_items.
2. Load à login -> spawn player.
3. Autosave timer.
4. Test déco/reco.

## Critères de validation (DoD)
- [x] Après déco/reco, position et inventaire sont identiques.

## Notes
- Prévoir migrations DB dès le début (version schema).

---

## Rapport final

### 1) FICHIERS

**Créés :**
- engine/persistence/CharacterDb.h
- engine/persistence/CharacterDb.cpp

**Modifiés :**
- engine/network/Protocol.h (MsgType::Logout = 11)
- engine/network/ServerCore.h (ServerClient.characterId)
- CMakeLists.txt (find_package(SQLite3), engine_persistence, server_app lié engine_persistence)
- main_server.cpp (DB open/init, Connect avec character_id, create/load character, ConnectAck 29 octets avec zone+position, envoi inventaire initial, Logout save+remove client, autosave 30s)
- main_client.cpp (Connect 9 octets avec character_id, argv[2] pour reconnect, ConnectAck 29 octets → characterId, zone, position, affichage characterId pour reco)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

- cmake --preset win64
- cmake --build --preset win64
- .\build\server_app.exe game/data
- .\build\client_app.exe 127.0.0.1
- Reconnect: .\build\client_app.exe 127.0.0.1 <characterId>

### 3) RÉSULTAT

- Compilation : NON TESTÉ (CMake/SQLite non validés dans l'environnement)
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Schema SQLite : schema_version, characters (character_id, zone_id, pos_x/y/z, hp, max_hp), inventory_items (character_id, item_id, count). Migrations via version. Save toutes les 30s + on Logout. Load à login et spawn (position/zone/inventaire) ; ConnectAck étendu avec zone+position ; inventaire initial envoyé après ConnectAck.
