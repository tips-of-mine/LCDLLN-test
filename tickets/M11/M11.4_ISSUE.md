# M11.4 — Probes: global zone probes (MVP) + export

**Status:** Closed

## Objectif
Générer/placer probes IBL (au moins 1 globale zone) et exporter probes.bin.

## Dépendances
- M11.2
- M05.4

## Livrables
- probes.bin
- zone atmosphere.json

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- MVP: 1 probe globale + extents; AAA: grid plus tard.

## Étapes d'implémentation
1. Définir format probes.bin (positions, radius, params).
2. Écrire au moins une probe (centre zone).
3. Hook runtime pour charger probes.

## Critères de validation (DoD)
- [x] Lighting peut utiliser probe globale (ou fallback sky).

## Notes / pièges à éviter
- Le prefilter/irradiance peuvent rester globaux au départ.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M11/M11.4_ISSUE.md` (issue de suivi)
- `engine/world/ProbesFormat.h` (format probes.bin : magic, version, ZoneProbe position/radius/intensity ; ZoneAtmosphere ; ReadProbesBin, ReadZoneAtmosphere)
- `engine/world/ProbesFormat.cpp` (implémentation des lecteurs)

**Modifiés :**
- `CMakeLists.txt` (ajout `engine/world/ProbesFormat.cpp` à engine_core)
- `engine/core/Engine.h` (include ProbesFormat.h ; membres `m_zoneProbes`, `m_zoneAtmosphere`)
- `engine/core/Engine.cpp` (chargement `base/probes.bin` et `base/zone_atmosphere.json` lors du chargement zone depuis zone.build_path)
- `tools/zone_builder.cpp` (M11.4 : écriture d’une probe globale au centre zone dans `probes.bin`, écriture de `zone_atmosphere.json` à la racine de la zone)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe` (avec config `zone.build_path` optionnel)
- `.\build\zone_builder.exe <layout.json> <scene.gltf> <outputDir>` → produit probes.bin et zone_atmosphere.json à la racine de outputDir

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans le PATH).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Format probes.bin défini (positions, radius, params/intensity) ; au moins une probe écrite (centre zone) ; runtime charge probes et zone_atmosphere ; lighting utilise déjà le sky global (fallback) et les données probe sont disponibles pour usage futur (prefilter/irradiance restent globaux au départ).
