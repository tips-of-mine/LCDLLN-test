# Issue: M12.1 — Editor mode: UI shell + selection + gizmos

**Status:** Closed

---

## Rapport final (M12.1)

### 1) FICHIERS

**Créés :**
- `engine/editor/EditorMode.h`
- `engine/editor/EditorMode.cpp`
- `tickets/issues/M12.1_Editor_mode_UI_shell_selection_gizmos_Issue.md`

**Modifiés :**
- `CMakeLists.txt`
- `engine/Engine.h`
- `engine/Engine.cpp`
- `engine/platform/Input.h`
- `engine/platform/Input.cpp`
- `engine/platform/Window.h`
- `engine/platform/Window.cpp`
- `engine/render/AssetRegistry.h`
- `engine/render/AssetRegistry.cpp`
- `engine/render/GeometryPass.h`
- `engine/render/GeometryPass.cpp`

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-debug
.\build\vs2022-x64\Debug\lcdlln.exe --editor -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

`cmake` n'est pas disponible dans l'environnement courant, donc la validation locale doit être faite sur la machine Windows équipée des outils du preset.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Build/exécution non validés localement faute de `cmake` dans l'environnement.
- Le ticket demande explicitement une intégration ImGui, mais le dépôt ne contient aucune dépendance ImGui vendored/autorisée ; l'implémentation réalisée reste donc un shell éditeur moteur-only (titre de fenêtre + logs/panneaux MVP), avec toggle `--editor`, sélection CPU sur bounds de mesh proxy, et gizmos translate/rotate/scale sur l'objet de test.

## Ticket content (M12.1) — référence

# M12.1 — Editor mode: UI shell + selection + gizmos

## Objectif
Mode `--editor` avec UI (ImGui) et outils de sélection/gizmos.

## Dépendances
- M00.5
- M03.4

## Livrables
- Editor toggle
- ImGui integration
- Selection system

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Gizmos translate/rotate/scale; multi-select optionnel.

## Étapes d’implémentation
1. Intégrer ImGui dans renderer.
2. Créer panneaux: Scene, Inspector, Asset Browser (MVP).
3. Raycast selection (CPU) sur bounds/mesh proxy.
4. Implémenter gizmos transform.

## Critères de validation (DoD)
- [ ] Placer/selectionner/mover un objet dans la scène.

## Notes / pièges à éviter
- ID buffer sélection plus tard si besoin.
