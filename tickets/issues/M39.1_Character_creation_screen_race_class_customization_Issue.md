# Issue: M39.1 — Character creation screen: race/class customization

**Status:** Closed

---

## Contenu du ticket

# M39.1_Character_creation_screen_race class customization

## Objectif
Écran création personnage avec sélection race, classe, apparence.

## Dépendances
- M16.1
- M33.1

## Livrables
- UI screens: race selection, class selection, customization
- 3D preview character (rotation)
- Customization sliders (face, hair, colors)
- Name input + validation (unique, profanity)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Races: 4-8 (humans, elves, orcs, etc.)
- Classes: 4-10 (warrior, mage, rogue, etc.)
- Customization: face type, hair style, skin color, hair color, eye color
- Preview: rotate character 360°, apply changes realtime

## Étapes d'implémentation
1. UI flow: login -> character select -> creation (si aucun char)
2. Race selection: grid icons, tooltip racials
3. Class selection: grid icons, tooltip abilities
4. Customization panel: sliders/dropdowns
5. 3D preview: load character model, apply customization
6. Rotate camera: mouse drag
7. Name input: validate length (3-12), check DB unique
8. Create: send CharacterCreateRequest(name, race, class, customization)
9. Server: validate, insert DB, return success

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Profanity filter nom (blacklist words)
- Limit characters per account (5-10)

---

## Rapport final (M39.1)

### 1) FICHIERS

**Créés :**
- `engine/client/CharacterCreationUi.h` — enum `CharCreationStep`, structs `CharRaceDefinition`/`CharClassDefinition`/`CharCustomizationParams`/`CharCreationPanelState`, classe `CharacterCreationUiPresenter` (Init/Shutdown/SetStep/SelectRace/SelectClass/SetCustomization/SetName/ApplyPreviewRotation/BuildCreateRequest).
- `engine/client/CharacterCreationUi.cpp` — implémentation : chargement races.json + classes.json via Config array pattern, validation nom (longueur 3-12, lettres+chiffres, filtre profanité basique), rotation préview 360°, `BuildCreateRequest()` → `CharacterCreateRequestPayload`.
- `game/data/character_creation/races.json` — 5 races : Human, High Elf, Dwarf, Orc, Undead (id, key, displayName, description, iconPath, racialBonus).
- `game/data/character_creation/classes.json` — 6 classes : Warrior, Mage, Rogue, Priest, Hunter, Paladin (id, key, displayName, description, iconPath, roleDescription).
- `tickets/issues/M39.1_Character_creation_screen_race_class_customization_Issue.md`

**Modifiés :**
- `engine/network/CharacterPayloads.h` — `CharacterCreateRequestPayload` étendu : +raceId, +classId, +faceType, +hairStyle, +skinColorIndex, +hairColorIndex, +eyeColorIndex. Nouvelle surcharge `BuildCharacterCreateRequestPayload(const CharacterCreateRequestPayload&)`.
- `engine/network/CharacterPayloads.cpp` — `ParseCharacterCreateRequestPayload` : lit les champs étendus avec défauts (rétro-compat). Nouvelle implémentation complète de `BuildCharacterCreateRequestPayload`.
- `CMakeLists.txt` — `engine/client/CharacterCreationUi.cpp` ajouté.

### 2) COMMANDES WINDOWS À EXÉCUTER

```powershell
cmake --preset vs2022-x64-release
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\engine_app\lcdlln.exe -log
```

### 3) RÉSULTAT

- Compilation : **NON TESTÉ** (Visual Studio absent de l'environnement)
- Exécution   : **NON TESTÉ**

### 4) VALIDATION DoD

| Critère | Statut |
|---|---|
| Aucun dossier racine non autorisé | ✅ |
| Code moteur sous `/engine` uniquement | ✅ |
| Contenu sous `/game/data` | ✅ (character_creation/) |
| Portée strictement M39.1 | ✅ |
| Aucun chemin absolu hardcodé | ✅ (via `paths.content`) |
| Race selection (grid data + tooltip racials) | ✅ races.json + CharRaceDefinition |
| Class selection (grid data + tooltip abilities) | ✅ classes.json + CharClassDefinition |
| Customization sliders (face/hair/skin/eyeColor) | ✅ CharCustomizationParams |
| Rotation préview 360° | ✅ `ApplyPreviewRotation` |
| Name validation (3-12 chars, profanity) | ✅ `ValidateName` |
| `CharacterCreateRequest(name, race, class, custom)` | ✅ `BuildCreateRequest()` + payload étendu |
| Logs Init/Shutdown/SelectRace/SelectClass/BuildCreateRequest | ✅ |
| Build testé | NON TESTÉ |
