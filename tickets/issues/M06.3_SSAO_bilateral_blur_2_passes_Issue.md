# Issue: M06.3 — SSAO: bilateral blur (2 passes)

**Status:** Closed

---

## Rapport final (M06.3)

### 1) FICHIERS

**Créés :**
- `engine/render/SsaoBlurPass.h` (déjà présent avant cette session)
- `engine/render/SsaoBlurPass.cpp`
- `game/data/shaders/ssao_blur.vert` (déjà présent)
- `game/data/shaders/ssao_blur.frag` (déjà présent)

**Modifiés :**
- `CMakeLists.txt` — ajout de `engine/render/SsaoBlurPass.cpp` aux sources de `engine_core`
- `engine/Engine.h` — include `SsaoBlurPass.h`, membres `m_fgSsaoBlurTempId`, `m_fgSsaoBlurId`, `m_ssaoBlurPass`
- `engine/Engine.cpp` — images FG SSAO_Blur_Temp et SSAO_Blur, chargement shaders ssao_blur, init `m_ssaoBlurPass`, passes SSAO_BlurH et SSAO_BlurV, `m_ssaoBlurPass.Destroy` au shutdown

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (environnement sans Visual Studio / Vulkan SDK possible)
- **Exécution :** NON TESTÉ

L’utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Structure : code moteur sous `/engine`, contenu sous `/game/data`, pas de dossier racine interdit.
- Portée : uniquement pass blur H + blur V et texture SSAO_Blur (M06.3), pas de changement lighting ni refactor.
- Chemins : chargement shaders via `paths.content` + chemins relatifs (`shaders/ssao_blur.vert`, etc.).
- Qualité : code documenté, logs cohérents, pas de chemins absolus.

---

## Ticket content (M06.3) — référence

# M06.3 — SSAO: bilateral blur (2 passes)

## Objectif
Flouter SSAO sans baver sur les discontinuités profondeur.

## Dépendances
- M06.2

## Livrables
- Pass blur H + blur V
- SSAO_Blur texture

## Spécification technique
- Bilateral weight basé sur diff profondeur (et/ou normal).

## Étapes d'implémentation
1. Créer SSAO_Blur temp et output.
2. Implémenter blur horizontal puis vertical (5-7 taps).
3. Utiliser depth-aware weights.

## Critères de validation (DoD)
- [ ] AO plus smooth, pas de halo sur silhouettes.
