# M16.2 — HUD combat + target frame + cooldowns (MVP)

**Status:** Closed

## Contenu du ticket (intégral)

# M16.2 — HUD combat + target frame + cooldowns (MVP)

## Objectif
HUD jouable: HP/mana, cible, feedback combat minimal.

## Dépendances
- M16.1

## Livrables
- HUD widgets
- Target frame
- Damage float text (option)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Layout responsive; update à 60fps.

## Étapes d'implémentation
1. Créer panneau HUD.
2. Afficher barres stats et cible.
3. Afficher derniers hits (combat log mini).

## Critères de validation (DoD)
- [ ] En combat, HUD montre HP cible/joueur et updates.

## Notes / pièges à éviter
- Reste minimal; polish plus tard.

---

## Rapport final (M16.2)

### 1) FICHIERS

**Créés :**
- `engine/ui/GameHud.h`
- `engine/ui/GameHud.cpp`

**Modifiés :**
- `engine/core/Engine.h` — include GameHud.h, membres `m_gameHud`, `m_hudData`, API `SetHudData(const HudData&)`
- `engine/core/Engine.cpp` — init/shutdown GameHud quand `!m_editor`, RecreateFramebuffers, BeginFrame/Draw/EndFrame/Render, implémentation `SetHudData`
- `CMakeLists.txt` — ajout de `engine/ui/GameHud.cpp` à la lib `engine_core`

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe` (sans `--editor` pour afficher le HUD joueur/cible ; avec `config.json` sans `"editor": true` ou sans option editor)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l’environnement d’exécution)
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Code moteur sous `/engine`, pas de nouveau dossier racine, pas de chemins absolus. Panneau HUD (GameHud) avec barres joueur/cible et combat log mini ; layout responsive (DisplaySize) ; mise à jour chaque frame via `SetHudData` ; en combat le HUD affiche HP joueur/cible et se met à jour.

**Status:** Closed
