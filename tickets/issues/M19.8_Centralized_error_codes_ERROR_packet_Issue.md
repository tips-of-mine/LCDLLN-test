# M19.8 — Centralized error codes + ERROR packet handling

**Status:** Closed

## Objectif
Standardiser les erreurs réseau v1 avec codes numériques centralisés et un paquet `ERROR` unique, aligné sur la spec M19.1.

## Dépendances
- M19.1
- M19.2
- M00.1

## Livrables
- Enum `NetErrorCode` (stable, doc)
- Packet `ERROR`:
  - `uint32 error_code`
  - `string message` (optionnel)
  - `uint32 request_id_ref` (optionnel)
- Helpers build/parse ERROR
- Policy serveur:
  - quand renvoyer ERROR vs disconnect

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Codes stables (pas de réassign sans bump version doc).
- En prod, message optionnel doit éviter fuite d'infos internes.

## Étapes d'implémentation
1. Définir NetErrorCode + synchroniser doc.
2. Implémenter encode/decode ERROR.
3. Intégrer: unknown opcode, invalid packet, oversize.
4. Côté client: handler ERROR (log + hook UI future).

## Critères de validation (DoD)
- [ ] Unknown opcode → ERROR(UNKNOWN_OPCODE) ou policy.
- [ ] Invalid packet → ERROR(INVALID_PACKET) ou disconnect.
- [ ] Logs affichent code + contexte.

## Notes / pièges à éviter
- Ne pas spammer ERROR en boucle (couper après N occurrences).

---

## Rapport final (implémentation)

**Status:** Closed

### 1) FICHIERS

**Créés :**
- `engine/network/NetErrorCode.h` (enum NetErrorCode + codes stables)
- `engine/network/ErrorPacket.h` (ErrorPayload, BuildErrorPacket, ParseErrorPayload)
- `engine/network/ErrorPacket.cpp`
- `tickets/issues/M19.8_Centralized_error_codes_ERROR_packet_Issue.md`

**Modifiés :**
- `engine/network/ProtocolV1Constants.h` (kOpcodeError = 8)
- `engine/network/RequestResponseDispatcher.h` (ErrorHandler, SetErrorHandler)
- `engine/network/RequestResponseDispatcher.cpp` (traitement paquet ERROR: parse, log, hook, dispatch pending)
- `engine/server/NetServer.cpp` (Invalid → ERROR(INVALID_PACKET ou PACKET_OVERSIZE) puis disconnect, anti-spam kMaxErrorPacketsPerConnection)
- `CMakeLists.txt` (ErrorPacket.cpp dans engine_core)
- `engine/server/CMakeLists.txt` (ErrorPacket.cpp pour server_app UNIX)
- `tickets/docs/protocol_v1.md` (codes 97 PACKET_OVERSIZE, 98 UNKNOWN_OPCODE, 99 INVALID_PACKET)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset win64
cmake --build --preset win64
.\build\pkg\game\lcdlln.exe
```

(Serveur Linux : build preset adapté puis `./pkg/server/lcdlln_server`)

### 3) RÉSULTAT

- Compilation : NON TESTÉ (cmake non disponible dans l’environnement)
- Exécution   : NON TESTÉ

### 4) VALIDATION DoD

- Unknown opcode → ERROR(UNKNOWN_OPCODE) : outils fournis (BuildErrorPacket + NetErrorCode::UNKNOWN_OPCODE) ; le handler applicatif peut envoyer ERROR pour opcode inconnu (policy).
- Invalid packet → ERROR(INVALID_PACKET) ou disconnect : implémenté (serveur envoie ERROR puis disconnect ; oversize → ERROR(PACKET_OVERSIZE)).
- Logs code + contexte : LOG_WARN côté serveur (connId, code) et côté client (request_id, code, message) ; ErrorHandler optionnel pour hook UI.
- Anti-spam : kMaxErrorPacketsPerConnection = 10 ; après N ERROR envoyés sur une connexion, plus d’envoi puis disconnect.

→ **OUI** (points DoD respectés).
