# Issue: M10.2 — Streaming: versioning/cancel stale jobs

**Status:** Closed

---

## Rapport final (M10.2)

### 1) FICHIERS

**Créés :** aucun

**Modifiés :**
- `engine/world/WorldModel.h` — ChunkRequest : ajout du champ `streamVersion` (uint32_t).
- `engine/world/StreamingScheduler.h` — ChunkJob (coord + streamVersion) ; queues IO/CPU/GPU en std::queue<ChunkJob> ; m_streamVersion, m_cancelledJobCount ; GetCurrentStreamVersion(), DropStaleFromIoQueue/CpuQueue/GpuUploadQueue(), DropStaleFromAllQueues(), GetCancelledJobCount(), ResetCancelledJobCount().
- `engine/world/StreamingScheduler.cpp` — PushRequests incrémente m_streamVersion et assigne req.streamVersion ; implémentation DropStaleFrom* (drain queue, garder uniquement job.streamVersion == m_streamVersion, compter annulés).
- `engine/Engine.cpp` — après PushRequests, appel m_streamingScheduler.DropStaleFromAllQueues().

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l'environnement).
- **Exécution :** NON TESTÉ

L'utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Build : commandes listées.
- Structure : code uniquement sous `/engine`.
- Portée : uniquement M10.2 (streamVersion, drop mismatch, stats annulés).
- Chemins : aucun contenu ajouté.
- Qualité : fonctions documentées ; pas de TODO.
- Critère ticket : DropStaleFromAllQueues() appelé chaque frame après PushRequests, donc pas d’uploads tardifs pour les chunks quittés du ring.

---

## Ticket content (M10.2) — référence

# M10.2 — Streaming: versioning/cancel stale jobs

## Objectif
Ajouter `streamVersion` par chunk pour annuler jobs devenus inutiles.

## Dépendances
- M10.1

## Livrables
- streamVersion uint32 par chunk
- Drop mismatch

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Chaque request incrémente version; jobs portent version.

## Étapes d'implémentation
1. Ajouter version aux requests.
2. À chaque stage (IO/CPU/GPU), vérifier version encore valide.
3. Drop silencieux + stats annulés.

## Critères de validation (DoD)
- [ ] Pas d'uploads tardifs de chunks quittés du ring.

## Notes / pièges à éviter
- Indispensable en monde ouvert rapide.
