# M24.4 — Docker: images + local compose stack

**Status:** Closed

## Objectif
Simplifier le dev local via Docker (Master, Shard, MySQL, Prometheus optionnel).

## Dépendances
- M22
- M23.2

## Livrables
- `deploy/docker/Dockerfile.master`
- `deploy/docker/Dockerfile.shard`
- `deploy/docker/docker-compose.yml` (MySQL 8 + master + shard)
- Docs `docs/docker_dev.md`

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Images dev (pas hardening prod).
- Volumes pour config et logs.

## Étapes d'implémentation
1. Dockerfiles.
2. Compose.
3. Doc run local.

## Critères de validation (DoD)
- [ ] `docker compose up` démarre stack fonctionnelle.

---

## Rapport final (implémentation M24.4)

### 1) FICHIERS

**Créés :**
- `deploy/docker/Dockerfile.master`
- `deploy/docker/config/master.config.json`
- `deploy/docker/config/shard.config.json`
- `docs/docker_dev.md`

**Modifiés :**
- `deploy/docker/docker-compose.yml` (MySQL 8 + services `master` et `shard`, volumes config + logs, init schéma `../../db/schema.sql`)

**Note :** `deploy/docker/Dockerfile.shard` était déjà présent (créé précédemment dans le cadre du ticket).

### 2) COMMANDES WINDOWS À EXÉCUTER

Prérequis : placer les binaires Linux dans `deploy/docker/bin/` (`lcdlln_server`, `lcdlln_shard`).

```powershell
cd deploy\docker
docker compose build
docker compose up -d
```

Arrêt : `docker compose down`. Suppression des données MySQL : `docker compose down -v`.

### 3) RÉSULTAT

- Compilation (images Docker) : NON TESTÉ (environnement sans Docker disponible pour l’agent).
- Exécution : NON TESTÉ — l’utilisateur valide localement avec `docker compose up`.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md applicables au ticket sont respectés : **OUI** (structure du repo, portée limitée au ticket, livrables livrés, doc et rapport fournis). Le build/run du projet C++ n’est pas modifié ; la validation “stack fonctionnelle” se fait par `docker compose up` en local.
