# M09.2 — Budgets par chunk (triangles/drawcalls) + instrumentation

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M09/M09.2_ISSUE.md`
- `engine/world/ChunkStats.h`
- `engine/world/ChunkStats.cpp`

**Modifiés :**
- `CMakeLists.txt` — ajout de `engine/world/ChunkStats.cpp`
- `engine/core/Engine.h` — include `ChunkStats.h`, membre `m_chunkStats`
- `engine/core/Engine.cpp` — `BeginFrame()` avant frame graph, `RecordDraw()` dans la pass Geometry (tag chunk 0,0,0,0 + RingType::Active), `LogFrameStats()` toutes les 60 frames

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont respectés → **OUI**
- Stats par chunk/ring (compteurs + GetRingStats/GetChunkStats), cvars budgets (world.budget_*_drawcalls), affichage via log (LogFrameStats).

---

## Ticket (original)

### Objectif
Définir budgets et instrumenter pour mesurer triangles/drawcalls par ring/chunk.

### Dépendances
- M03.1
- M09.1

### Livrables
- Stats per chunk/ring
- Cvars budgets

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Active: <~1000 drawcalls, Visible <300, Far <50 (cibles).

### Étapes d'implémentation
1. Ajouter compteurs drawcalls/triangles dans renderer.
2. Associer draws à chunk id (tag).
3. Afficher stats debug overlay/log.

### Critères de validation (DoD)
- [ ] Les stats s'affichent et varient selon densité.

### Notes / pièges à éviter
- Budgets seront ajustés après premiers tests.
