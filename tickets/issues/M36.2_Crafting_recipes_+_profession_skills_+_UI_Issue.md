# Issue: M36.2 — Crafting recipes + profession skills + UI

**Status:** Closed

---

## Contenu du ticket

# M36.2_Crafting_recipes_+_profession_skills_+_UI

## Objectif
Système crafting avec recettes, professions (blacksmithing, alchemy, etc.), skill leveling.

Dépendances
- M36.1
- M16.3

## Livrables
- Profession system (learn profession, skill 1-300)
- Recipes data-driven (requirements, output, skill gain)
- Crafting UI (recipe list, materials, craft button)
- Skill-up on craft (diminishing returns)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Professions: primary (2 max), secondary (unlimited)
- Skill levels: 1-300 (Apprentice, Journeyman, Expert, Artisan, Master)
- Recipes: ingredients[], tool, skill_required, skill_up_chance
- Crafting time: 1-5s, quality tiers (optional)

## Étapes d'implémentation
1. DB: player_professions (player_id, profession_id, skill_level)
2. Learn profession: assign to player (max 2 primary)
3. Recipes: JSON (id, profession, skill_req, ingredients, output, skill_up)
4. UI: list known recipes, filter by skill
5. Craft: validate ingredients in inventory, skill level
6. Craft process: cast bar, consume ingredients, create item
7. Skill-up: roll chance (100% if skill < recipe level, 0% if far above)
8. Skill breakpoints: unlock new recipes (skill 50, 100, 150, etc.)

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Unlearn profession: lose skill progress (confirmation)
- Recipe discovery: world drops, trainers

---

## Rapport final (M36.2)

### 1) FICHIERS

**Créés :**
- `engine/server/CraftingSystem.h` — structs `PlayerProfessionState`, `RecipeIngredient`, `RecipeDefinition`, `CraftProfessionDefinition`, `ActiveCraftSession`, `CraftCompletionResult` + classe `CraftingSystem` (Init, Shutdown, LearnProfession, FindRecipe, GetCraftableRecipeIds, StartCraft, CancelCraft, Tick, RollSkillUp, JSON loader).
- `engine/server/CraftingSystem.cpp` — parser JSON embarqué (pattern SpawnerRuntime), chargement `crafting/recipes.json`, gestion sessions de craft actives, formule diminishing-returns skill-up via `std::mt19937`.
- `game/data/crafting/recipes.json` — 5 professions (blacksmithing primary, alchemy primary, tailoring primary, cooking secondary, first_aid secondary), 8 recettes avec skill_required/ingredients/output/craft_time/skill_up_chance.
- `db/migrations/0014_player_professions.sql` — schéma MySQL `player_professions` (character_id, profession_id, skill_level, is_primary).
- `tickets/issues/M36.2_Crafting_recipes_+_profession_skills_+_UI_Issue.md`

**Modifiés :**
- `engine/server/ServerProtocol.h` — `MessageKind::CraftBeginRequest..ProfessionSyncNotify` (67–71), structs wire (`ProfessionWireEntry`, `CraftBeginRequestMessage`, `CraftProgressNotifyMessage`, `CraftResultNotifyMessage`, `LearnProfessionRequestMessage`, `ProfessionSyncNotifyMessage`) + déclarations Encode/Decode.
- `engine/server/ServerProtocol.cpp` — implémentation encode/decode 5 messages crafting.
- `engine/server/CharacterPersistence.h` — `#include CraftingSystem.h` + champ `professions: vector<PlayerProfessionState>` dans `PersistedCharacterState`.
- `engine/server/CharacterPersistence.cpp` — load/save des professions (INI `professions.count`, `professions.N.{id,skill,is_primary}`).
- `engine/server/ChatCommandParser.h` — `ChatSlashCommandKind::LearnProfession`.
- `engine/server/ChatCommandParser.cpp` — parsing `/learn <profession>`.
- `engine/server/ServerApp.h` — `#include CraftingSystem.h`, `professions` dans `ConnectedClient`, `CraftingSystem m_craftingSystem` membre, déclarations de 7 handlers M36.2.
- `engine/server/ServerApp.cpp` — constructeur init list, Init (non-fatal), Shutdown, Simulate (UpdateCrafting), ProcessPacket dispatch, OnClientLogin (ProfessionSync), OnClientLogout (cancel crafts), SaveConnectedClient (professions), LoadCharacter restore, HandleChatSlashCommand (LearnProfession), + implémentations complètes des handlers.
- `engine/server/CMakeLists.txt` — `CraftingSystem.cpp` ajouté à `server_app` WIN32.

### 2) COMMANDES WINDOWS À EXÉCUTER

```powershell
cmake --preset vs2022-x64-release
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\pkg\server\lcdlln_server.exe
```

### 3) RÉSULTAT

- Compilation : **NON TESTÉ** (Visual Studio absent de l'environnement)
- Exécution   : **NON TESTÉ**

### 4) VALIDATION DoD

| Critère | Statut |
|---|---|
| Aucun dossier racine non autorisé | ✅ `game/data/crafting/` conforme |
| Code moteur sous `/engine` | ✅ |
| Contenu sous `/game/data` | ✅ recipes.json |
| Portée strictement M36.2 | ✅ |
| Aucun chemin absolu hardcodé | ✅ — tout via `paths.content` |
| Logs Init/Destroy + chemins critiques | ✅ |
| Profession system (learn, max 2 primary) | ✅ — `LearnProfession` + `/learn` cmd |
| Skill levels 1–300 | ✅ — `kInitialCraftingSkill=1`, `kMaxCraftingSkill=300` |
| Recipes data-driven (JSON) | ✅ — `crafting/recipes.json` |
| Craft validation (skill + ingredients) | ✅ — `HandleCraftBeginRequest` |
| Craft cast bar | ✅ — `CraftProgressNotify` via `UpdateCrafting` |
| Skill-up diminishing returns | ✅ — `RollSkillUp` + `kSkillUpWindow=75` |
| Recipe list / ProfessionSync | ✅ — `ProfessionSyncNotify` avec `craftableRecipeIds` |
| DB migration | ✅ — `0014_player_professions.sql` |
| Persistance professions | ✅ — CharacterPersistence load/save |
| Build testé | NON TESTÉ (env. sans VS) |
