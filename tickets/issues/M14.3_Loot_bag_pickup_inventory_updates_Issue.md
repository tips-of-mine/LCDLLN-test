# Issue: M14.3 — Loot bag + pickup + inventory updates

**Status:** Closed

---

## Rapport final (M14.3)

### 1) FICHIERS

**Créés :**
- `game/data/loot/loot_tables.txt`
- `tickets/issues/M14.3_Loot_bag_pickup_inventory_updates_Issue.md`

**Modifiés :**
- `engine/server/ReplicationTypes.h` — ajout de `ItemStack` pour le loot et l'inventaire.
- `engine/server/ServerProtocol.h` — ajout de `PickupRequest`, `InventoryDelta` et des messages associés.
- `engine/server/ServerProtocol.cpp` — décodage `PickupRequest` et encodage `InventoryDelta`.
- `engine/server/ServerApp.h` — ajout des structures `LootTableEntry`, `LootBagEntity`, de l'inventaire joueur et des helpers loot/pickup.
- `engine/server/ServerApp.cpp` — chargement des loot tables, spawn `LootBag` à la mort, validation pickup range/ownership, mise à jour inventaire, despawn du bag et envoi `InventoryDelta`.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\engine_app\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (`cmake` indisponible dans l'environnement)
- **Exécution :** NON TESTÉ (`cmake` indisponible dans l'environnement)

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Build/exécution non validés localement car `cmake` n'est pas disponible dans l'environnement.
- Le ticket `M14.3` est implémenté côté serveur avec table de loot data, génération de `LootBag`, `PickupRequest`, contrôle de portée/propriété, mise à jour d'inventaire et `InventoryDelta`.
- L'anti-dupe demandé par le ticket reste côté serveur : le bag est retiré du runtime serveur avant l'envoi du delta inventaire.

---

## Ticket content (M14.3) — référence

# M14.3 — Loot bag + pickup + inventory updates

## Objectif
Système loot minimal : drop entité LootBag, pickup request, update inventaire.

## Dépendances
- M14.1

## Livrables
- LootTable
- LootBag entity
- PickupRequest/InventoryDelta msgs

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Loot instancié (owner) ou public; range check pickup.

## Étapes d’implémentation
1. Définir loot tables data.
2. À la mort: générer loot et spawn LootBag.
3. PickupRequest: valider range/ownership.
4. Ajouter items à inventory, despawn bag, envoyer delta.

## Critères de validation (DoD)
- [ ] Kill->loot->pickup met à jour inventaire client.

## Notes / pièges à éviter
- Anti-dupe: tout côté serveur.
