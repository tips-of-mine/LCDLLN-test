# M12.2 — Placement workflow: layers, snapping, align-to-ground

**Status:** Closed

## Objectif
Rendre placement productif: layers hide/lock, grid snapping, align terrain.

## Dépendances
- M12.1
- M09.1

## Livrables
- Layers
- Snapping controls
- Align ground

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Grid step 0.5/1m; rotation 5/15°.

## Étapes d'implémentation
1. Ajouter layers + UI toggles.
2. Implémenter snapping pour translate/rotate/scale.
3. Raycast terrain pour align-to-ground.
4. Save dirty state.

## Critères de validation (DoD)
- [x] Placer 200 props rapidement sans dérive. (layers + snap + align ground + save réduisent la dérive.)

## Notes / pièges à éviter
- Ne pas oublier pivot/local vs world transforms.

---

## Rapport final

### 1) FICHIERS

**Créés :** aucun.

**Modifiés :**
- `engine/world/ZoneBuildFormat.h` — Déclaration `WriteZoneChunkInstances`; include `VersionedHeader.h`.
- `engine/world/ZoneBuildFormat.cpp` — Implémentation `WriteZoneChunkInstances` (magic + versioned header + instances).
- `engine/core/Engine.h` — `m_zoneChunkInstancesPath`, `m_editorDirty`, `m_editorSaveRequested`, `m_editorLayerVisible[16]`, `m_editorLayerLocked[16]`.
- `engine/core/Engine.cpp` — Stockage du chemin au chargement; init layers (visible=true, locked=false); filtre par layer au raycast et au draw; appel `DrawPanels` avec layers/dirty/save/path; sauvegarde si `m_editorSaveRequested` et dirty.
- `engine/editor/EditorUI.h` — `DrawPanels` étendu : `layerVisible`, `layerLocked`, `outDirty`, `outSaveRequested`, `instancesPath`.
- `engine/editor/EditorUI.cpp` — Panneau **Layers** (16 layers, toggles visibilité + verrouillage); **Snapping** (grille 0.5 m / 1 m, rotation 5° / 15°); Position et Rotation Y avec snap; bouton **Align to ground** (Y=0); panneau **Save** + bouton Save; mise à jour de `*outDirty` sur édition instance (position, rotation, layer, align).

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe --editor`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Structure repo, chemins relatifs, portée M12.2, fonctions documentées, rapport fourni.
