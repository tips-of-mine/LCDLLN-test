# M06.4 — Lighting: intégrer AO (ssao * texture AO)

**Status:** Closed

## Rapport final

### 1) FICHIERS

**Créés :** aucun.

**Modifiés :**
- `engine/render/vk/VkLightingPipeline.h` (SetSsaoBlurView)
- `engine/render/vk/VkLightingPipeline.cpp` (binding 12 SSAO_Blur, pool 12, SetSsaoBlurView)
- `game/data/shaders/lighting.frag` (uSsaoBlur binding 12, AO_final = ao_ssao * occlusion, directLight sans occlusion, diffuseIBL/specIBL * AO_final)
- `engine/core/Engine.cpp` (Lighting pass Read SSAO_Blur si valide; SetSsaoBlurView à l’init et au resize)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset <preset>`
- `cmake --build --preset <preset>`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Lighting lit SSAO_Blur ; AO_final = AO_ssao * AO_tex ; ambient (diffuse IBL + spec IBL) *= AO_final ; lumière directe non modifiée par AO.

---

## Ticket (original)

### Objectif
Multiplier l'ambient (diffuse IBL) par AO final.

### Dépendances
- M06.3
- M05.4

### Livrables
- Lighting lit SSAO_Blur et combine avec AO ORM.

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- AO n'affecte pas la lumière directe (au début).

### Étapes d'implémentation
1. Ajouter sampler SSAO à lighting pass.
2. AO_final = AO_ssao * AO_tex.
3. ambient *= AO_final.

### Critères de validation (DoD)
- [ ] Contacts plus ancrés sans assombrir toute la scène.

### Notes / pièges à éviter
- Plus tard: bent normals / GTAO si besoin.
