# M28.3 — Skills AoE targeting + ground placement

**Status:** Closed

## Objectif
Système targeting AoE (cercle, cone, ligne) avec preview au sol et validation serveur.

## Dépendances
- M28.1
- M27.1

## Livrables
- AoE shapes: circle, cone, line, ring
- Client preview (decal/particles)
- Server validation (position, targets hit)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Circle: center, radius
- Cone: origin, direction, angle, range
- Line: origin, direction, width, length
- Preview: projected decal au sol (texture + shader)

## Étapes d'implémentation
1. Skill JSON: ajout "aoeShape", "aoeRadius", etc.
2. Client: raycast mouse->world pour position placement
3. Afficher preview decal/particles
4. On confirm: send SkillUseRequest(skillId, targetPos, targetDir)
5. Server: validate position reachable, apply effects à tous dans AoE
6. Hit detection: distance check ou spatial query

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- LOS check optionnel (pillars bloquent AoE)
- Limiter targets max (10-20) pour perf

---
## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/client/AoEPreviewSystem.h`
- `engine/client/AoEPreviewSystem.cpp`
- `tickets/issues/M28.3_Skills_AoE_targeting_+_ground_placement_Issue.md` (issue)

**Modifiés :**
- `engine/gameplay/SkillSystem.h`
- `engine/gameplay/SkillSystem.cpp`
- `game/data/skills/fireball.json`
- `CMakeLists.txt`

**Supprimés (si explicitement demandé) :**
- (aucun)

### 2) COMMANDES WINDOWS À EXÉCUTER
- `cmake --preset vs2022-x64-release`
- `cmake --build --preset vs2022-x64-release`
- `.\build\vs2022-x64-release\engine_app\lcdlln.exe -log`

### 3) RÉSULTAT
- Compilation : NON TESTÉ
- Exécution   : NON TESTÉ

Cause (agent) : `cmake` est introuvable dans l’environnement PowerShell (`CommandNotFoundException`), donc la compilation et l’exécution n’ont pas pu être lancées.

### 4) VALIDATION DoD
- Tous les points de `tickets/DEFINITION_OF_DONE.md` sont-ils respectés ?
  → NON
- Explication : les points `A) Build & exécution` ne peuvent pas être validés car la commande `cmake` n’est pas disponible localement.

