# M02.4 — Offscreen SceneColor + blit/copy vers swapchain

**Status:** Closed

## Ticket (contenu)

# M02.4 — Offscreen SceneColor + blit/copy vers swapchain

## Objectif
Rendre vers une image offscreen (HDR/ LDR) puis copier vers swapchain pour préparer postprocess.

## Dépendances
- M02.3
- M01.3

## Livrables
- FG ressource `SceneColor` (offscreen)
- Pass Present: copy/blit vers swapchain
- Resize gère SceneColor

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Swapchain reste la dernière étape.
- SceneColor format configurable (HDR plus tard).

## Étapes d'implémentation
1. Créer SceneColor suivant extent swapchain.
2. Pass Clear/Draw écrit SceneColor.
3. Pass Present: transition SceneColor->TRANSFER_SRC, swapchain->TRANSFER_DST, copy, swapchain->PRESENT.
4. OnResize: rebuild graph resources.

## Critères de validation (DoD)
- [ ] Écran identique mais via offscreen.
- [ ] Resize reconstruit SceneColor sans crash.

## Notes / pièges à éviter
- Blit nécessite formats compatibles; sinon utiliser copy + full-screen pass plus tard.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- tickets/M02/M02.4_ISSUE.md

**Modifiés :**
- engine/Engine.cpp (OnResize: appeler m_frameGraph.destroy(device) avant VkSwapchain::Recreate pour reconstruire les ressources du graphe (SceneColor) au prochain execute)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset vs2022-x64`
- `cmake --build --preset vs2022-x64-debug`
- `.\build\vs2022-x64\Debug\engine_app.exe`

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- FG ressource SceneColor (offscreen) et Pass Present (CopyPresent) copy vers swapchain déjà en place (M02.1/M02.3). Ce ticket impose que le resize reconstruise les ressources du graphe : avant Recreate(swapchain) on appelle m_frameGraph.destroy(device), donc au prochain Render() execute() recrée SceneColor avec le nouvel extent (ensureImageResources). Écran identique via offscreen ; resize reconstruit SceneColor sans crash.
