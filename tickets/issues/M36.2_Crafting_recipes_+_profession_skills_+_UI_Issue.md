# Issue: M36.2 — Crafting recipes + profession skills + UI

**Status:** Closed

---

## Ticket content (M36.2) — référence

# M36.2_Crafting_recipes_+_profession_skills_+_UI

## Objectif
Système crafting avec recettes, professions (blacksmithing, alchemy, etc.), skill leveling.

## Dépendances
- M36.1
- M16.3

## Livrables
- Profession system (learn profession, skill 1-300)
- Recipes data-driven (requirements, output, skill gain)
- Crafting UI (recipe list, materials, craft button)
- Skill-up on craft (diminishing returns)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Professions: primary (2 max), secondary (unlimited)
- Skill levels: 1-300 (Apprentice, Journeyman, Expert, Artisan, Master)
- Recipes: ingredients[], tool, skill_required, skill_up_chance
- Crafting time: 1-5s, quality tiers (optional)

## Étapes d'implémentation
1. DB: player_professions (player_id, profession_id, skill_level)
2. Learn profession: assign to player (max 2 primary)
3. Recipes: JSON (id, profession, skill_req, ingredients, output, skill_up)
4. UI: list known recipes, filter by skill
5. Craft: validate ingredients in inventory, skill level
6. Craft process: cast bar, consume ingredients, create item
7. Skill-up: roll chance (100% if skill < recipe level, 0% if far above)
8. Skill breakpoints: unlock new recipes (skill 50, 100, 150, etc.)

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

---

## Rapport final (M36.2)

### 1) FICHIERS

**Créés :**
- `engine/server/CraftingSystem.h` — `RecipeIngredient`, `RecipeDefinition`, `ProfessionEntry`, `CraftingSessionState`, `CraftOpResult`, `LearnProfessionResult`, `CraftingSystem` (chargement JSON, gestion professions max 2 primary, validation skill+ingrédients, sessions craft, tick complétion, roll skill-up avec diminishing returns).
- `engine/server/CraftingSystem.cpp` — Parseur JSON inline (même pattern SpawnerRuntime), logs Gameplay, `SkillUpProbability` (linéaire 0→50 niveaux au-dessus), remove/add items sur ConnectedClient.
- `engine/client/CraftingUi.h` — `CraftRecipeRowRegion`, `CraftingPanelState`, `CraftingUiPresenter`.
- `engine/client/CraftingUi.cpp` — Layout panel 30%×55% viewport, onglets professions, rangées recettes, bouton Craft, cast bar, debug text.
- `game/data/crafting/recipes.json` — 7 recettes : 3 blacksmithing (iron_sword, iron_shield, iron_helmet), 3 alchemy (healing/mana/greater potions), 1 leatherworking.
- `db/migrations/0019_player_professions.sql` — Table `player_professions` (character_id, profession_key, skill_level, is_primary, FK → characters.id).
- `tickets/issues/M36.2_Crafting_recipes_+_profession_skills_+_UI_Issue.md`

**Modifiés :**
- `engine/server/ServerProtocol.h` — 9 nouvelles valeurs `MessageKind` (69–77), 9 structs de messages crafting/profession, 9 paires Encode/Decode déclarées.
- `engine/server/ServerProtocol.cpp` — Implémentations Encode/Decode M36.2 + helpers `WriteProfessionEntry`, `ReadProfessionEntry`, `WriteRecipeWireRow`, `ReadRecipeWireRow`.
- `engine/server/CharacterPersistence.h` — Include `CraftingSystem.h`, champ `professions` (`vector<ProfessionEntry>`) dans `PersistedCharacterState`.
- `engine/server/CharacterPersistence.cpp` — Load/save professions dans l'INI (avec clamp `kMaxProfessionSkillLevel`).
- `engine/server/ServerApp.h` — Include `CraftingSystem.h`, champ `professions` dans `ConnectedClient`, membre `m_craftingSystem`, 7 handlers M36.2 déclarés.
- `engine/server/ServerApp.cpp` — Init/Shutdown CraftingSystem, `TickCrafting()` dans Simulate, dispatch de 4 paquets client, restauration professions au login, persistence professions au save, 7 handlers implémentés.
- `engine/client/UIModel.h` — `UIModelChangeCrafting` (bit 15), `UIProfessionEntry`, `UICraftRecipeRow`, `UICraftingState`, champ `crafting` dans `UIModel`, 5 méthodes Apply + 5 scratch buffers.
- `engine/client/UIModel.cpp` — Dispatch 5 paquets M36.2 + implémentations Apply.
- `engine/server/CMakeLists.txt` — `CraftingSystem.cpp` ajouté dans `server_app` WIN32.
- `CMakeLists.txt` — `engine/client/CraftingUi.cpp` ajouté dans `engine_core`.

**Supprimés :** aucun

---

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
REM Serveur
.\build\vs2022-x64-release\pkg\server\lcdlln_server.exe
REM Client (smoke test)
.\build\vs2022-x64-release\pkg\game\lcdlln.exe -log
```

---

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake absent dans l'environnement de l'agent)
- **Exécution :** NON TESTÉ

---

### 4) VALIDATION DoD

| Critère | Statut |
|---|---|
| Build Windows OK (configure + build) | NON TESTÉ (cmake absent) |
| Exécutable lancé (smoke test) | NON TESTÉ |
| Aucun dossier racine non autorisé créé | ✅ |
| Rapport final complet | ✅ |
| Profession system (learn, skill 1-300, max 2 primary) | ✅ |
| Recipes data-driven (JSON: id, profession, skill_req, ingredients, output, skill_up) | ✅ |
| Crafting UI (recipe list, materials, craft button, cast bar) | ✅ |
| Skill-up on craft (diminishing returns : 100% → 0% sur 50 niveaux) | ✅ |
| Persistence professions (INI + SQL migration) | ✅ |
| Validation ingrédients serveur | ✅ |
| Chemins relatifs via paths.content | ✅ |
| Logs Init/Shutdown/chemins critiques | ✅ |
| Structure repo conforme | ✅ |
