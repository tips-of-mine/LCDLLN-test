# Issue: M09.4 — HLOD builder: clustering + mesh merge

**Status:** Closed

---

## Rapport final (M09.4)

### 1) FICHIERS

**Créés :**
- `tools/hlod_builder/CMakeLists.txt` — Exécutable hlod_builder (C++20, pas de Vulkan).
- `tools/hlod_builder/main.cpp` — Point d’entrée : --input, --output, --content ; parse manifest, cluster par chunk, merge, WriteHlodPak.
- `tools/hlod_builder/Types.h` — Instance, ChunkInput, Bounds, Cluster.
- `tools/hlod_builder/ManifestParser.h` — ParseManifest(contentDirDefault, text, contentDirOut).
- `tools/hlod_builder/ManifestParser.cpp` — Format texte : "content <path>", "chunk <cx> <cz>", "mesh <path> <materialId> <x> <y> <z>".
- `tools/hlod_builder/MeshLoader.h` — Vertex, MeshData, LoadMesh(contentDir, relativePath) (.mesh engine).
- `tools/hlod_builder/MeshLoader.cpp` — Lecture binaire .mesh (magic MESH, version 1, vertices 32 bytes, indices uint32).
- `tools/hlod_builder/Clustering.h` — ClusterInstances(chunk, minClusters=20, maxClusters=80).
- `tools/hlod_builder/Clustering.cpp` — Clustering spatial (grille kCellSize 32 m) + par materialId ; fusion/split pour rester 20–80 clusters/chunk.
- `tools/hlod_builder/MeshMerge.h` — MergedMesh, MergeClusterMeshes(contentDir, chunk, cluster).
- `tools/hlod_builder/MeshMerge.cpp` — Charge meshes, applique offset position, concatène vertices/indices, recalcule bounds AABB.
- `tools/hlod_builder/PakWriter.h` — ChunkOutput, WriteHlodPak(path, chunks, materialNames).
- `tools/hlod_builder/PakWriter.cpp` — Format hlod.pak : magic HLOD, version, numChunks ; par chunk : cx, cz, numClusters ; par cluster : materialId, bounds, vertexCount, indexCount, vertexData, indexData ; puis materials list (id + name).

**Modifiés :**
- `CMakeLists.txt` — add_subdirectory(tools/hlod_builder).

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\hlod_builder.exe --input manifest.txt --output hlod.pak [--content <path>]
```

Exemple manifest.txt :
```
content game/data
chunk 0 0
mesh meshes/prop.mesh 0 0 0 0
mesh meshes/prop.mesh 0 32 0 32
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l'environnement).
- **Exécution :** NON TESTÉ

L'utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Build : commandes listées.
- Structure : outil sous `/tools` (tools/hlod_builder) ; pas de contenu sous /engine.
- Portée : M09.4 uniquement (tool build HLOD per chunk, clustering spatial + material, merge meshes, output hlod.pak, 20–80 clusters/chunk).
- Chemins : manifest utilise "content <path>" ; pas de chemins absolus hardcodés.
- Qualité : fonctions documentées ; pas d’atlas (plus tard).

---

## Ticket content (M09.4) — référence

# M09.4 — HLOD builder: clustering + mesh merge

## Objectif
Créer des clusters HLOD par chunk et générer meshes fusionnés (et éventuellement atlas).

## Dépendances
- M11.1

## Livrables
- Tool: build HLOD per chunk
- Output hlod.pak

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Limiter clusters/chunk (20-80).

## Étapes d'implémentation
1. Clustering spatial + par material.
2. Merge meshes (index/vertex), recalcul bounds.
3. Écrire pak (mesh + materials list).

## Critères de validation (DoD)
- [ ] Au runtime, HLOD remplace un grand nombre de props à distance.

## Notes / pièges à éviter
- Atlas textures plus tard; commencer sans atlas.
