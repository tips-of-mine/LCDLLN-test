# Issue: M16.4 — Quest journal + tracker HUD + minimap zone (MVP)

**Status:** Closed

---

## Rapport final (M16.4)

### 1) FICHIERS

**Créés :**
- `engine/client/QuestUi.h`
- `engine/client/QuestUi.cpp`
- `game/data/ui/minimap_zones.txt`
- `tickets/issues/M16.4_Quest_journal_tracker_HUD_minimap_zone_MVP_Issue.md`

**Modifiés :**
- `engine/client/UIModel.h`
- `engine/client/UIModel.cpp`
- `CMakeLists.txt`

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64\pkg\game\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

Détail environnement :
- `cmake` n’est pas disponible dans l’environnement de travail courant, donc la configuration / compilation locale n’a pas pu être lancée ici.
- Les diagnostics IDE sur les fichiers modifiés ne remontent pas d’erreur.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Le scope demandé par `M16.4` a été implémenté au minimum via :
  - extension de `UIModel` pour exposer zone + position joueur/cible nécessaires à la minimap
  - `QuestUiPresenter` autonome avec journal de quêtes, détail de la quête sélectionnée, tracker HUD et widget minimap
  - conversion `xz -> uv` basée sur une data de zone chargée via `paths.content`
  - metadata minimale de minimap dans `game/data/ui/minimap_zones.txt`
- La validation complète DoD reste incomplète car :
  - la compilation n’a pas pu être exécutée localement sans `cmake`
  - l’exécution n’a pas pu être validée localement

---

## Ticket content (M16.4) — référence

# M16.4 — Quest journal + tracker HUD + minimap zone (MVP)

## Objectif
Journal quêtes + tracker et minimap (texture zone) avec POIs basiques.

## Dépendances
- M16.1
- M15.1
- M09.1

## Livrables
- Quest panel
- Tracker HUD
- Minimap widget

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Minimap: zone map baked; convert xz->uv.

## Étapes d’implémentation
1. Créer journal: liste + détails + steps.
2. Tracker HUD: quêtes suivies.
3. Minimap: afficher joueur + target + triggers (option).

## Critères de validation (DoD)
- [ ] Progression quête visible; minimap suit joueur.

## Notes / pièges à éviter
- Debug overlay chunks/cells utile.
