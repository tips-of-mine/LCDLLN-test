# M21.1 — MySQL schema.sql initial + schema_version

**Status:** Closed

## Objectif
Établir le schéma initial MySQL 8.0 et la table `schema_version` (source of truth).

## Dépendances
- M00.1
- MySQL 8.0

## Livrables
- `db/schema.sql`
- Tables minimales:
  - accounts (auth)
  - sessions (optionnel si persistant)
  - characters (placeholder, max 5 rule)
  - shards (pour M22)
  - schema_version
- Indexes & contraintes (unique email/login)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- MySQL 8.0 uniquement.
- `schema_version`:
  - `version INT PRIMARY KEY`
  - `applied_at TIMESTAMP`
  - `checksum CHAR(64)` (sha256)
- Transactions: utiliser InnoDB.

## Étapes d'implémentation
1. Définir tables + indexes.
2. Définir `schema_version` version=1.
3. Documenter comment initialiser DB.

## Critères de validation (DoD)
- [ ] schema.sql s'applique sur DB vierge.
- [ ] Indexes/uniques présents.

---

## Rapport final (M21.1)

### 1) FICHIERS

**Créés :**
- `tickets/issues/M21.1_schema_sql_initial_and_schema_version_table_Issue.md`
- `db/schema.sql`
- `db/README.md`

**Modifiés :** aucun.

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

Aucune commande CMake/build pour ce ticket (livrable = SQL + doc). Pour appliquer le schéma :

```bat
mysql -u root -p < db\schema.sql
```

(MySQL 8.0 client doit être installé et le serveur démarré.)

### 3) RÉSULTAT

- **Compilation :** N/A (pas de code C++).
- **Exécution :** NON TESTÉ (application du schéma à valider localement avec MySQL 8.0).

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI.
- `db/schema.sql` : tables accounts (UNIQUE email, UNIQUE login), sessions, characters (slot 0..4, UNIQUE account_id+slot), shards, schema_version (version INT PRIMARY KEY, applied_at TIMESTAMP, checksum CHAR(64)); moteur InnoDB; insertion version=1.
- Indexes et contraintes : uq_accounts_email, uq_accounts_login, uq_sessions_token, uq_characters_account_slot, uq_shards_name, chk_characters_slot.
- Documentation : `db/README.md` décrit comment initialiser la DB (mysql &lt; schema.sql ou SOURCE).
