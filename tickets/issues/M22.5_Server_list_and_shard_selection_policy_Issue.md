# M22.5 — Server list + shard selection policy (Master)

**Status:** Closed

## Contenu du ticket (référence)

- **Objectif** : Fournir au client la liste des shards disponibles et sélectionner un shard cible selon policy (load, capacity).
- **Livrables** : Opcodes SERVER_LIST_REQUEST/RESPONSE, payload (shard_id, status, current_load, max_capacity), policy (lowest load ratio, exclude offline/degraded), hooks nb persos.
- **Spec** : Clients consultent le Master ; Master source de vérité.

---

## Rapport final d’implémentation

### 1) FICHIERS

**Créés :**
- `engine/network/ServerListPayloads.h`
- `engine/network/ServerListPayloads.cpp`
- `engine/server/ServerListHandler.h`
- `engine/server/ServerListHandler.cpp`
- `engine/server/ServerListPolicyTests.cpp`

**Modifiés :**
- `engine/network/ProtocolV1Constants.h` — opcodes 19 (SERVER_LIST_REQUEST), 20 (SERVER_LIST_RESPONSE)
- `engine/server/ShardRegistry.h` — commentaire SelectShard (M22.5)
- `engine/server/ShardRegistry.cpp` — SelectShard : policy lowest load ratio, exclude Offline/Degraded (Online only)
- `engine/server/main_server_linux.cpp` — ServerListHandler branché, opcode 19
- `engine/server/CMakeLists.txt` — ServerListPayloads.cpp, ServerListHandler.cpp, test server_list_policy_tests

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset x64-debug
cmake --build --preset x64-debug
.\build\pkg\server\lcdlln_server.exe
```

Tests (Linux/UNIX) :

```bat
ctest --preset x64-debug -R server_list_policy_tests
```

### 3) RÉSULTAT

- **Compilation** : NON TESTÉ (cmake non disponible dans l’environnement).
- **Exécution** : NON TESTÉ.

L’utilisateur valide localement avec les commandes ci-dessus.

### 4) VALIDATION DoD

- **A) Build & exécution** : Commandes listées ; compilation/exécution à valider en local.
- **B) Structure** : Aucun nouveau dossier racine ; code sous `/engine`.
- **C) Portée** : Uniquement livrables M22.5 (server list, policy, hooks).
- **D) Chemins** : Aucun chemin absolu ; pas de contenu sous `/game/data`.
- **E) Qualité** : LOG_INFO/WARN/ERROR sur chemins critiques.
- **F) Rapport** : Présent dans cette Issue.

- **Critères ticket** : Server list reflète registry → handler construit la liste depuis ListShards(). Policy choisit shard correct → SelectShard() retourne le shard Online avec le plus faible ratio load/capacity ; test 2 shards vérifie le moins chargé.

→ **OUI** (sous réserve de validation build/run en local).
