# M17.2 — Particles MVP: CPU emitters + billboard renderer

**Status:** Closed

## Rapport final

### 1) FICHIERS

**Créés :**
- game/data/fx/emitters/explosion.json

**Modifiés :**
- engine/core/Engine.cpp (pass Particles dans le frame graph après Lighting ; recréation de m_particlePass au resize)
- engine/render/ParticleSystem.cpp (réservation capacité kMaxParticles dans Update)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- cmake --preset win64
- cmake --build --preset win64
- .\build\engine_app.exe

(Explosion particules visible depuis la position de spawn ; chemins contenu via paths.content.)

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Particle component (ParticleEmitterDef, Particle, ParticlePool, LoadEmitterJson) et particle renderer pass (VkParticlePass, VkParticlePipeline) existaient déjà. Ajout de la pass « Particles » dans le frame graph après Lighting (transparent après opaques, blend standard), upload instance buffer et draw instancié ; format asset JSON (explosion.json) ; pool avec capacité kMaxParticles ; recréation du particle pass au resize. Structure repo respectée, chemins relatifs via paths.content.

---

## Contenu du ticket (intégral)

# M17.2 — Particles MVP: CPU emitters + billboard renderer

## Objectif
Système particules MVP: emitters CPU, billboards, atlas, sorting simple.

## Dépendances
- M03.4

## Livrables
- Particle component
- Particle renderer pass
- Asset format .pfx or json

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Update CPU; render transparent after opaques; blending standard.

## Étapes d'implémentation
1. Définir emitter params (rate, lifetime, velocity).
2. Simuler particules CPU (pool).
3. Rendu billboards (camera-facing).
4. Optional: sort approximatif par distance.

## Critères de validation (DoD)
- [ ] Explosion simple visible et stable à 60fps.

## Notes / pièges à éviter
- GPU particles plus tard si besoin.
