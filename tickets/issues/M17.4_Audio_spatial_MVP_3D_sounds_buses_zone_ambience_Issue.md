# Issue: M17.4 — Audio spatial MVP: 3D sounds + buses + zone ambience

**Status:** Closed

---

## Rapport final (M17.4)

### 1) FICHIERS

**Créés :**
- `engine/audio/AudioEngine.h`
- `engine/audio/AudioEngine.cpp`
- `game/data/audio/zone_audio.json`
- `tickets/issues/M17.4_Audio_spatial_MVP_3D_sounds_buses_zone_ambience_Issue.md`

**Modifiés :**
- `CMakeLists.txt`
- `engine/Engine.h`
- `engine/Engine.cpp`

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64\pkg\game\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

Détail environnement :
- `cmake` n’est pas disponible dans l’environnement de travail courant, donc la configuration / compilation locale n’a pas pu être lancée ici.
- Les diagnostics IDE sur les fichiers modifiés ne remontent pas d’erreur.
- Le repo ne contient pas de dépendance FMOD/Wwise/OpenAL vendored ; l’implémentation livrée reste donc un wrapper MVP `NullAudioBackend` data-driven côté moteur, sans lecture audio réelle validée ici.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Le scope demandé par `M17.4` a été implémenté au minimum via :
  - wrapper audio moteur avec `Play3D(pos, soundId)`
  - buses `SFX`, `Music`, `UI`
  - chargement data-driven de `game/data/audio/zone_audio.json`
  - activation de boucle d’ambiance pour la zone de test
  - atténuation 3D min/max distance côté runtime
- La validation complète DoD reste incomplète car :
  - la compilation n’a pas pu être exécutée localement sans `cmake`
  - l’exécution n’a pas pu être validée localement
  - aucun backend audio tiers vendored n’est présent dans le repo pour confirmer une sortie audio réelle
  - le critère `Sons 3D se déplacent correctement; ambiance active par zone` ne peut donc pas être confirmé ici

---

## Ticket content (M17.4) — référence

# M17.4 — Audio spatial MVP: 3D sounds + buses + zone ambience

## Objectif
Audio 3D (position/rolloff) et ambiance de zone data-driven.

## Dépendances
- M00.4

## Livrables
- Audio engine wrapper (FMOD/Wwise/OpenAL)
- Buses SFX/Music/UI
- zone_audio.json

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Min/max distance; doppler optional.

## Étapes d’implémentation
1. Intégrer lib audio.
2. Implémenter Play3D(pos, soundId).
3. Définir buses et volumes.
4. Charger zone_audio et jouer loops.

## Critères de validation (DoD)
- [ ] Sons 3D se déplacent correctement; ambiance active par zone.

## Notes / pièges à éviter
- Éviter de charger des gros wav à la volée; streaming audio plus tard.
