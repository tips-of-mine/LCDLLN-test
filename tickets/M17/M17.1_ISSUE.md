# M17.1 — FX mapping: events->fx JSON definitions

**Status:** Closed

## Rapport final

### 1) FICHIERS

**Créés :**
- engine/fx/FxDef.h
- engine/fx/FxDef.cpp
- engine/fx/FxManager.h
- engine/fx/FxManager.cpp
- game/data/fx/fx.json

**Modifiés :**
- CMakeLists.txt (ajout lib engine_fx, lien client_app → engine_fx)
- main_client.cpp (include FxManager, Load fx/fx.json, appel OnCombatEvent après CombatEvent)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- cmake --preset win64
- cmake --build --preset win64
- .\build\client_app.exe 127.0.0.1 [characterId]

(Le client charge `game/data/fx/fx.json` via content root `game/data` ; à lancer depuis la racine du repo. Un hit/kill reçu déclenche le FX configuré dans le mapping.)

### 3) RÉSULTAT

- Compilation : NON TESTÉ (cmake non disponible dans l’environnement)
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Structure repo respectée (/engine pour le code, /game/data pour le contenu). Aucun chemin absolu ; contenu résolu via contentRoot + chemin relatif (fx/fx.json). Schema fx.json défini (definitions + mapping), FXManager avec SpawnParticle/SpawnDecal/SpawnSound et OnCombatEvent, mapping event→FX par type (CombatEvent → hit/kill). Un hit déclenche le FX configuré par JSON (client appelle fxManager.OnCombatEvent après ParseCombatEvent). FX non exécuté côté serveur (FxManager utilisé uniquement dans client_app).

---

## Contenu du ticket (intégral)

# M17.1 — FX mapping: events->fx JSON definitions

## Objectif
Mapper events réseau (CombatEvent, etc.) vers FX data-driven (particles/decals/sounds).

## Dépendances
- M13.3
- M16.1

## Livrables
- fx/*.json schema
- FX manager
- Event->FX mapping table

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- FX id strings; paramètres scale/duration; deterministic enough.

## Étapes d'implémentation
1. Définir schema fx.json.
2. Créer FXManager: spawn particle, decal, sound.
3. Créer mapping par type event.
4. Tester sur CombatEvent.

## Critères de validation (DoD)
- [ ] Un hit déclenche FX configuré par JSON.

## Notes / pièges à éviter
- Ne pas exécuter FX côté serveur.
