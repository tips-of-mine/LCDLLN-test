# M16.5 — Theme/skin switch: theme.json + style.qss integration

**Status:** Closed

## Contenu du ticket (intégral)

# M16.5 — Theme/skin switch: theme.json + style.qss integration

## Objectif
Appliquer thèmes UI par race via chargement theme.json + style.qss, hot-reload optionnel.

## Dépendances
- M16.1

## Livrables
- Theme manager
- Apply QSS
- Asset refs (fonts/icons/colors)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Widgets utilisent classes logiques (hud/panel/button/tooltip).

## Étapes d'implémentation
1. Définir mapping theme.json -> variables (colors/fonts).
2. Charger et appliquer style.qss.
3. Hook changement race/thème.
4. Option: hot reload fichier.

## Critères de validation (DoD)
- [ ] Changer thème modifie UI sans casser la logique.

## Notes / pièges à éviter
- Assurer fallback si fichier manquant.

---

## Rapport final (M16.5)

### 1) FICHIERS

**Créés :**
- `engine/ui/ThemeManager.h` — Theme manager: LoadTheme, Apply, GetColorVariable, GetFontPath, TryHotReload.
- `engine/ui/ThemeManager.cpp` — Chargement theme.json (colors, fonts), parsing style.qss (classes hud/panel/button/tooltip), application sur ImGui (WindowBg, Button, Text, PopupBg), fallback si fichier manquant, hot-reload via mtime.
- `game/data/ui/themes/default/theme.json` — Variables colors/fonts (asset refs).
- `game/data/ui/themes/default/style.qss` — Classes logiques .hud .panel .button .tooltip avec var(--*).

**Modifiés :**
- `engine/core/Engine.h` — Include ThemeManager, membre m_themeManager, m_themeName, API SetTheme(themeName).
- `engine/core/Engine.cpp` — LoadTheme au premier init GameHud (contentPath + ui/themes/default), Apply + TryHotReload avant Draw, SetTheme charge ui/themes/<themeName>.
- `CMakeLists.txt` — Ajout de engine/ui/ThemeManager.cpp à engine_core.

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe` (sans éditeur ; thème par défaut appliqué ; SetTheme("human") pour changer).

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Theme manager : chargement theme.json (mapping colors/fonts) + style.qss (classes hud/panel/button/tooltip). Apply() applique les couleurs au style ImGui. SetTheme(themeName) hook pour changement race/thème. Fallback si fichier manquant (valeurs par défaut). Option hot-reload via TryHotReload (mtime). Changer thème modifie l’UI (couleurs) sans casser la logique.

**Status:** Closed
