# Issue: M09.1 — World model: Zone 4km, Chunk 256m, rings

**Status:** Closed

---

## Rapport final (M09.1)

### 1) FICHIERS

**Créés :**
- `engine/world/WorldModel.h` — Constantes kZoneSize=4096, kChunkSize=256 ; structs ChunkCoord, ChunkBounds, ChunkRing, ChunkRequest, Zone, Chunk, World ; WorldToChunkCoord, ChunkBounds ; World::Update (hysteresis), GetPendingChunkRequests.
- `engine/world/WorldModel.cpp` — Implémentation WorldToChunkCoord (floor + clamp), ChunkBounds (min/max XZ), Update (rings Active 5x5, Visible 7x7, Far 11x11 ; hysteresis sur chunk centre joueur), GetPendingChunkRequests.

**Modifiés :**
- `engine/Engine.h` — include WorldModel.h ; membre `engine::world::World m_world`.
- `engine/Engine.cpp` — dans Update(), après FpsCameraController, appel `m_world.Update(out.camera.position)`.
- `CMakeLists.txt` — ajout de `engine/world/WorldModel.cpp` à la lib engine_core.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l'environnement).
- **Exécution :** NON TESTÉ

L'utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Build : commandes listées ; compilation/exécution à valider localement.
- Structure : code uniquement sous `/engine` (engine/world/).
- Portée : uniquement M09.1 (World, Zone, Chunk, pos→chunk, ChunkBounds, rings Active/Visible/Far, requests pour M10, hysteresis).
- Chemins : aucun contenu ajouté ; pas de chemins absolus.
- Qualité : fonctions documentées en en-tête ; pas de TODO ; pas de Vulkan dans ce ticket.

---

## Ticket content (M09.1) — référence

# M09.1 — World model: Zone 4km, Chunk 256m, rings

## Objectif
Implémenter modèle monde (Zone/Chunk) et calcul des rings autour du joueur.

## Dépendances
- M00.5

## Livrables
- `World`, `Zone`, `Chunk` structs
- Conversion pos->chunk coord
- Rings Active 5x5, Visible 7x7, Far HLOD

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Unités mètres; origine locale par zone.

## Étapes d'implémentation
1. Définir ZoneSize=4096, ChunkSize=256.
2. Fonctions: WorldToChunkCoord, ChunkBounds.
3. Calculer liste chunks requis par ring.
4. Émettre requests vers scheduler (M10).

## Critères de validation (DoD)
- [ ] Déplacement joueur met à jour chunks requis correctement.

## Notes / pièges à éviter
- Prévoir hysteresis pour éviter thrash.
