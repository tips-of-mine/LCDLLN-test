# M12.3 — Volumes gameplay: triggers/spawns/transitions placement

**Status:** Open

## Objectif
Permettre de placer volumes (box/sphere) pour triggers, spawns, transitions de zone.

## Dépendances
- M12.1

## Livrables
- Volume entities
- Inspector fields
- Visualization gizmos

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Types: trigger, spawnArea, zoneTransition.

## Étapes d'implémentation
1. Définir types volumes + propriétés.
2. UI création/édition.
3. Rendu debug volumes (lines).
4. Export dans layout.

## Critères de validation (DoD)
- [x] Un trigger et une transition sont visibles et exportés.

## Notes / pièges à éviter
- Les scripts d'actions peuvent être string IDs au début.

---

**Status:** Closed

---

## Rapport final M12.3

### 1) FICHIERS

**Créés :**
- `engine/world/GameplayVolume.h` — Types VolumeType (Trigger, SpawnArea, ZoneTransition), VolumeShape (Box, Sphere), struct GameplayVolume.
- `engine/world/VolumeFormat.h` — Déclarations ReadVolumesJson / WriteVolumesJson.
- `engine/world/VolumeFormat.cpp` — Lecture/écriture JSON volumes (volumes.json) pour export layout.
- `tickets/M12/M12.3_ISSUE.md` — Issue de suivi (ouverte puis fermée avec ce rapport).

**Modifiés :**
- `engine/math/Ray.h` — Ajout WorldToScreen (projection monde → écran pour debug draw) et RaySphere (intersection rayon/sphère pour sélection volumes).
- `engine/math/Ray.cpp` — Implémentation WorldToScreen, RaySphere ; ajout `#include <cmath>`.
- `engine/core/Engine.h` — m_zoneBasePath, m_zoneVolumes, m_editorSelectedVolumeIndex, m_editorExportVolumesRequested ; `#include "engine/world/GameplayVolume.h"`.
- `engine/core/Engine.cpp` — Include VolumeFormat.h ; chargement volumes (m_zoneBasePath, ReadVolumesJson) au chargement zone ; raycast sélection volumes (box/sphère) + mise à jour sélection instance/volume ; passage volumes/selectedVolumeIndex/zoneBasePath/outExportVolumesRequested à DrawPanels ; export volumes (WriteVolumesJson) quand m_editorExportVolumesRequested.
- `engine/editor/EditorUI.h` — DrawPanels étendu : volumes, selectedVolumeIndex, zoneBasePath, outExportVolumesRequested ; `#include "engine/world/GameplayVolume.h"`.
- `engine/editor/EditorUI.cpp` — Panneau Volumes (liste, Add Trigger/Spawn/Transition, Export volumes) ; Inspecteur pour volume (type, shape, position, halfExtents/radius, actionId) ; rendu debug volumes (lignes) : overlay plein écran, boîtes (12 arêtes), sphères (3 cercles) ; include Ray.h, cstdio, string.
- `CMakeLists.txt` — Ajout `engine/world/VolumeFormat.cpp` au target engine_core.

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe --editor` (avec config zone.build_path et paths.content pour charger une zone et afficher/éditer volumes)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l’environnement).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI.
- Structure repo respectée (/engine, /game/data, /tools, /tickets). Aucun chemin absolu ; volumes.json résolu via zone base (paths.content + zone.build_path). Portée limitée au ticket (volume entities, Inspector, gizmos lignes, export layout). Fonctions documentées. Aucune nouvelle dépendance.
