# Issue: M10.1 — Streaming scheduler: requests + priorities

**Status:** Closed

---

## Rapport final (M10.1)

### 1) FICHIERS

**Créés :**
- `engine/world/StreamingScheduler.h` — ComputeChunkPriority(), StreamingScheduler (PushRequests, GetPrioritizedRequests, queues request/io/cpu/gpuUpload).
- `engine/world/StreamingScheduler.cpp` — Implémentation priority (Active > Visible > Far; in front > behind), tri par priorité.

**Modifiés :**
- `engine/world/WorldModel.h` — ChunkRequest : ajout du champ `priority` (uint32_t).
- `engine/Engine.h` — include StreamingScheduler.h ; membre `m_streamingScheduler`.
- `engine/Engine.cpp` — Update() : calcul forward depuis view matrix, PushRequests(), BuildChunkDrawList utilise GetPrioritizedRequests().
- `CMakeLists.txt` — ajout de `engine/world/StreamingScheduler.cpp` à la lib engine_core.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l'environnement).
- **Exécution :** NON TESTÉ

L'utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Build : commandes listées ; compilation/exécution à valider localement.
- Structure : code uniquement sous `/engine` (engine/world/).
- Portée : uniquement M10.1 (scheduler, queues, priority function, ChunkRequest.priority, World → scheduler → draw list).
- Chemins : aucun contenu ajouté ; pas de chemins absolus.
- Qualité : fonctions documentées ; pas de TODO ; pas de Vulkan dans ce ticket.
- Critère ticket : en avançant, les chunks devant le joueur ont une priorité plus élevée (score base*2+inFront, inFront si dot(toChunkXZ, forwardXZ) > 0).

---

## Ticket content (M10.1) — référence

# M10.1 — Streaming scheduler: requests + priorities

## Objectif
Créer scheduler de streaming avec priorités (ring, distance, direction).

## Dépendances
- M00.3
- M09.1

## Livrables
- Queues request/io/cpu/gpuUpload
- Priority function

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Active > Visible > Far; devant joueur > derrière.

## Étapes d'implémentation
1. Définir `ChunkRequest` (chunkId, targetState, priority).
2. Implémenter priority score.
3. Mettre une file priority (heap) ou buckets.
4. Producer: World update push requests.

## Critères de validation (DoD)
- [ ] En avançant, chunks devant se chargent avant ceux derrière.

## Notes / pièges à éviter
- Éviter starvation: aging/boost après X sec.
