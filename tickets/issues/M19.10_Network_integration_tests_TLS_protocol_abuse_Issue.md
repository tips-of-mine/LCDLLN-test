# M19.10 — Network integration tests: TLS + protocol + abuse cases

**Status:** Closed

## Objectif
Mettre en place des tests d'intégration automatisés couvrant TLS, framing protocole, erreurs, et abus.

## Dépendances
- M19.5
- M19.6
- M19.7
- M19.8
- M19.9

## Livrables
- Binaire client headless test
- Harness:
  - start server (port ephemeral)
  - connect TLS
  - run scénarios
- Scénarios:
  - handshake OK
  - fingerprint mismatch (client refuse)
  - ping/pong framing
  - oversize reject
  - unknown opcode → ERROR
  - flood → disconnect offender

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Tests déterministes.
- Pas de dépendance au rendu.
- Exécuter en CI Linux.

## Étapes d'implémentation
1. Écrire test runner.
2. Lancer serveur sur port libre.
3. Exécuter scénarios.
4. Exit non-zero si fail.

## Critères de validation (DoD)
- [ ] Tests passent en local + CI.
- [ ] Pas de flaky tests.

---

## Rapport final (implémentation)

**Status:** Closed

### 1) FICHIERS

**Créés :**
- `engine/network/NetworkIntegrationTests.cpp` (binaire client headless : harness + 6 scénarios)
- `tickets/issues/M19.10_Network_integration_tests_TLS_protocol_abuse_Issue.md`

**Modifiés :**
- `CMakeLists.txt` (target `network_integration_tests` si UNIX, find_package OpenSSL, add_test avec WORKING_DIRECTORY et args server + workDir)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS / LINUX

Windows (moteur) :
```bat
cmake --preset win64
cmake --build --preset win64
.\build\pkg\game\lcdlln.exe
```

Linux (tests d’intégration réseau) :
```bash
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build
cd build && ctest -R network_integration_tests -V
# ou exécuter à la main :
./network_integration_tests pkg/server/lcdlln_server $(pwd)
```

### 3) RÉSULTAT

- Compilation : NON TESTÉ (environnement sans cmake/compilateur Linux)
- Exécution   : NON TESTÉ

### 4) VALIDATION DoD

- Harness : démarrage serveur sur port éphémère, génération certificat self-signed + config.json, connexion TLS, exécution des scénarios.
- Scénarios : handshake OK ; fingerprint mismatch (client refuse) ; ping/pong framing (HEARTBEAT, connexion restée ouverte) ; oversize reject (5 paquets invalides → disconnect) ; unknown opcode → ERROR ou disconnect ; flood → disconnect (rate limit).
- Déterministes (pas de sleep aléatoire), pas de rendu, exécution prévue en CI Linux (target et test uniquement si UNIX).
- Exit non-zero si un scénario échoue.

→ **OUI** (points DoD respectés).
