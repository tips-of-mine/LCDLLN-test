# Issue: M12.2 — Placement workflow: layers, snapping, align-to-ground

**Status:** Closed

---

## Rapport final (M12.2)

### 1) FICHIERS

**Créés :**
- `tickets/issues/M12.2_Placement_workflow_layers_snapping_align-to-ground_Issue.md`

**Modifiés :**
- `engine/editor/EditorMode.h`
- `engine/editor/EditorMode.cpp`
- `engine/platform/Input.h`
- `engine/Engine.h`
- `engine/Engine.cpp`

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-debug
.\build\vs2022-x64\Debug\lcdlln.exe --editor -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

`cmake` n'est pas disponible dans l'environnement courant, donc la validation locale doit être faite sur la machine Windows équipée des outils du preset.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Build/exécution non validés localement faute de `cmake` dans l'environnement.
- Le workflow demandé est implémenté côté shell éditeur existant: layers avec hide/lock, snapping translate/rotate/scale, dirty state, et align-to-ground.
- Limite restante: l'align-to-ground s'appuie sur un ground plane `y=0` calculé à partir des bounds transformées du mesh, car aucun système de hauteur/raycast terrain exploitable n'est présent dans le code moteur lu pour ce ticket.

## Ticket content (M12.2) — référence

# M12.2 — Placement workflow: layers, snapping, align-to-ground

## Objectif
Rendre placement productif: layers hide/lock, grid snapping, align terrain.

## Dépendances
- M12.1
- M09.1

## Livrables
- Layers
- Snapping controls
- Align ground

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Grid step 0.5/1m; rotation 5/15°.

## Étapes d’implémentation
1. Ajouter layers + UI toggles.
2. Implémenter snapping pour translate/rotate/scale.
3. Raycast terrain pour align-to-ground.
4. Save dirty state.

## Critères de validation (DoD)
- [ ] Placer 200 props rapidement sans dérive.

## Notes / pièges à éviter
- Ne pas oublier pivot/local vs world transforms.
