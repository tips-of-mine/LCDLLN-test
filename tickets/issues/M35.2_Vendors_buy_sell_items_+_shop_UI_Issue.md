# Issue: M35.2 — Vendors buy/sell items + shop UI

**Status:** Closed

---

## Rapport final (M35.2 — implémentation ticket)

### 1) Fichiers

**Créés**

- `engine/client/GameplayUdpClient.h`
- `engine/client/GameplayUdpClient.cpp`

**Modifiés**

- `CMakeLists.txt` — `engine_core` : `GameplayUdpClient.cpp`
- `config.json` — section `client.gameplay_udp` (désactivée par défaut)
- `engine/Engine.h` — `RenderState::gameplayHudDebugText`, sous-système M35.2 (UIModelBinding, ShopUi, InventoryUi, UDP client, pending sell)
- `engine/Engine.cpp` — `InitGameplayNet` / `ShutdownGameplayNet` / `PumpGameplayPackets` / `UpdateGameplayNet` ; HUD debug ; Escape ferme le shop ; resize rafraîchit presenters
- `engine/client/InventoryUi.h` — drag vendre (TryBeginDrag, CancelDrag, GetDragSource, SyncDragWithModel)
- `engine/client/InventoryUi.cpp` — implémentation drag + debug
- `engine/client/ShopUi.h` — régions hit-test (`ShopOfferRegion`, sell drop zone)
- `engine/client/ShopUi.cpp` — layout panneau gauche, hit tests, texte d’aide (clics/1–9 achat, **clic droit** drag vers zone SELL, Y/N confirmation)
- `engine/client/UIModel.h` — `UIModelBinding::CloseShop()`
- `engine/client/UIModel.cpp` — `CloseShop()`
- `engine/server/ServerProtocol.h` — `EncodeHello`
- `engine/server/ServerProtocol.cpp` — `EncodeHello`

**Supprimés**

- Aucun

### 2) Commandes Windows à exécuter

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
```

Serveur shard (dans un autre terminal, avec `server.listen_port` aligné sur le client) :

```bat
.\build\vs2022-x64-release\pkg\server\lcdlln_server.exe
```

Client : dans `config.json`, mettre `client.gameplay_udp.enabled` à `true`, puis :

```bat
.\build\vs2022-x64-release\pkg\game\lcdlln.exe
```

Contrôles M35.2 (avec shop ouvert côté HUD debug dans les logs) : **V** = parler au vendeur (`vendor_talk_target`, ex. `vendor:1`) ; **clic / touches 1–9** sur une ligne d’offre = achat qty 1 ; **clic droit + drag** depuis l’inventaire vers la zone SELL = proposition de revente (prix unitaire affiché = 25 % du prix d’achat catalogue, **Y/N** pour envoyer `ShopSellRequest`) ; **Escape** ferme le panneau shop localement.

### 3) Résultat

- **Compilation** : NON TESTÉ (CMake absent dans l’environnement agent)
- **Exécution** : NON TESTÉ

### 4) Validation DoD

- Build Windows via presets : **NON VÉRIFIÉ ICI** — à valider sur machine avec VS2022 + vcpkg + Vulkan + GLFW.
- Exécutable smoke test : **NON VÉRIFIÉ ICI**.
- Aucun dossier racine interdit créé : **OUI**.
- Rapport final dans l’issue : **OUI**.
- Hors ticket / périmètre : reset stock quotidien (optionnel dans le ticket) **non** implémenté (inchangé).

**Note** : Le binaire client n’inclut pas `VendorCatalog.cpp` ; le preview or vendre côté client reprend la même formule 25 % que le serveur (`VendorCatalog::ComputeSellPrice`).

---

## Ticket content (M35.2) — référence

# M35.2_Vendors_buy sell_items_+_shop_UI

## Objectif
NPCs vendors avec inventory, prix buy/sell, UI shop.

## Dépendances
- M35.1
- M16.3

## Livrables
- Vendor NPC component (shop inventory)
- Buy/sell transactions
- Shop UI panel (vendor items + prices)
- Sell-back mechanic (sell price = 25% buy price)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Vendor inventory: limited stock (optional) ou infinite
- Buy price: defined per item
- Sell price: 25% buy price (default)
- Stock refresh: daily reset (optional)

## Étapes d'implémentation
1. Vendor NPC data: vendorId, items[] (itemId, buyPrice, stock)
2. Interact NPC: open shop UI
3. UI: display vendor items grid, buy button
4. Buy: validate gold, add item inventory, subtract gold
5. Sell: drag item to vendor, offer sell price, confirm
6. Server: validate transactions (anti-dupe)
7. Stock tracking: decrement on buy, reset daily (cron)

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Vendor types: general, armor, weapons, potions, etc.
- Reputation discounts (phase 2)
