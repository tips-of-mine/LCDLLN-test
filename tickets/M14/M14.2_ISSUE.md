# M14.2 — Aggro + AI states (Idle/Patrol/Aggro/Return)

**Status:** Closed

## Objectif
IA mobs basique + threat table pour cibler joueur.

## Dépendances
- M11.3
- M14.1

## Livrables
- AI state machine
- Threat table
- Leash/return

## Spécification technique
- Threat += damage; target = max threat; leash distance configurable.

## Étapes d'implémentation
1. Définir states + transitions.
2. Mettre à jour threat à chaque CombatEvent.
3. Pathfind vers target (navmesh) si dans chunks actifs.
4. Return/reset threat hors leash.

## Critères de validation (DoD)
- [x] Mob agro, poursuit, attaque, puis reset s'il est kite trop loin.

## Notes
- Limiter update rate IA (ex 10Hz) pour perf.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- engine/ai/AiState.h
- engine/ai/ThreatTable.h
- engine/ai/ThreatTable.cpp
- engine/ai/MobAiUpdate.h
- engine/ai/MobAiUpdate.cpp

**Modifiés :**
- CMakeLists.txt (bibliothèque engine_ai, server_app lié à engine_ai)
- main_server.cpp (includes AI, Mob.spawnPosition/aiState, ThreatTable, AddThreat on damage, boucle UpdateMobAi à 10 Hz, attaque mob en Aggro à portée)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

- cmake --preset win64
- cmake --build --preset win64
- .\build\server_app.exe [contentPath]
- .\build\client_app.exe [contentPath]

### 3) RÉSULTAT

- Compilation : NON TESTÉ (CMake non disponible dans le PATH de l’environnement)
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Code moteur sous /engine (engine/ai). Pas de nouveau dossier racine. Pas de chemins absolus. Portée limitée au ticket (state machine, threat table, leash/return, update 10 Hz, mob attaque en Aggro). Pathfinding : déplacement direct vers la cible (pas de chargement navmesh côté serveur dans ce ticket).
