# M18.2 — GPU-driven: compute frustum culling + indirect draws

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- engine/render/DrawItem.h
- engine/render/DrawItem.cpp
- engine/render/vk/VkCullingPipeline.h
- engine/render/vk/VkCullingPipeline.cpp
- game/data/shaders/frustum_cull.comp

**Modifiés :**
- engine/render/vk/VkGeometryPipeline.h (Init: optional transformsSetLayout)
- engine/render/vk/VkGeometryPipeline.cpp (push 260 when 2 sets; pipeline layout avec 2 set layouts optionnel)
- game/data/shaders/geometry.vert (USE_INDIRECT_DRAWS: set 1 VisibleTransforms, useIndirect, gl_DrawID)
- engine/core/Engine.h (membres M18.2: buffers drawItem/indirect/count/visibleTransforms, culling pipeline, transforms set layout/pool/set)
- engine/core/Engine.cpp (transforms set layout; CreateCullingBuffers; init culling + transforms set; geometry pipeline avec variante shader; Geometry pass: build draw items, upload, dispatch culling, vkCmdDrawIndirectCount ou fallback CPU loop; shutdown culling)
- CMakeLists.txt (DrawItem.cpp, VkCullingPipeline.cpp)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset <preset>`
- `cmake --build --preset <preset>`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- DrawItem buffer, visible list append (compute atomic), indirect command buffer, vkCmdDrawIndirectCount dans le geometry pass. Fallback boucle CPU si culling non disponible. Structure repo respectée.

---

## Contenu du ticket (intégral)

# M18.2 — GPU-driven: compute frustum culling + indirect draws

## Objectif
Réduire coût CPU: GPU culling (compute) + draw indirect.

## Dépendances
- M18.1
- M09.3

## Livrables
- DrawItem buffer
- Visible list append
- Indirect command buffer

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).

## Spécification technique
- vkCmdDrawIndexedIndirect (ou indirect count).

## Étapes d'implémentation
1. Définir DrawItem struct GPU (mesh/material/transform/bounds).
2. Compute: frustum test -> write indirect commands.
3. Graphics: consume indirect in geometry pass.
4. Comparer perf vs CPU loop.

## Critères de validation (DoD)
- [ ] CPU render thread time baisse nettement en scène dense.

## Notes / pièges à éviter
- Commencer sans occlusion; ajouter Hi-Z ensuite.
