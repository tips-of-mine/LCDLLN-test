# Issue: M10.3 — Cache: CPU LRU + GPU deferred free

**Status:** Closed

---

## Rapport final (M10.3)

### 1) FICHIERS

**Créés :**
- `engine/world/StreamCache.h` — LRU cache (key asset/chunk path, blob, 1–4 GB configurable), Lookup/Insert, hit/miss stats.
- `engine/world/StreamCache.cpp` — Init(config), LRU eviction, streaming.cache_size_mb (default 1024, clamp 1024–4096 MB).
- `engine/render/vk/DeferredDestroyQueue.h` — PushBuffer/PushImage(fenceValue), Collect(device, completedFrameIndex).
- `engine/render/vk/DeferredDestroyQueue.cpp` — Queue (resource + fenceValue), Collect frees when fenceValue <= completedFrameIndex.

**Modifiés :**
- `engine/world/StreamingScheduler.h` — SetStreamCache(StreamCache*), TryGetCachedBlob(key) pour IO stage (hit cache -> skip disk).
- `engine/world/StreamingScheduler.cpp` — Implémentation TryGetCachedBlob (délègue au cache).
- `engine/Engine.h` — StreamCache, DeferredDestroyQueue ; includes.
- `engine/Engine.cpp` — Init m_streamCache, SetStreamCache sur le scheduler ; après vkWaitForFences appel m_deferredDestroyQueue.Collect(device, completedFrameIndex).
- `CMakeLists.txt` — StreamCache.cpp, DeferredDestroyQueue.cpp dans engine_core.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l'environnement).
- **Exécution :** NON TESTÉ

L'utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Build : commandes listées.
- Structure : code sous `/engine` (world + render/vk).
- Portée : uniquement M10.3 (LRU cache, DeferredDestroy queue, intégration cache dans IO stage via TryGetCachedBlob).
- Collect : appelé dans Render() juste après vkWaitForFences pour ne libérer qu’après fence signaled (spécification technique).
- Critère ticket : ré-entrer dans une zone récemment quittée évite re-IO massif grâce au cache LRU (hit -> skip disk via TryGetCachedBlob).

---

## Ticket content (M10.3) — référence

# M10.3 — Cache: CPU LRU + GPU deferred free

## Objectif
Ajouter cache RAM (blobs décompressés) et destruction GPU différée (fences).

## Dépendances
- M10.1
- M00.2

## Livrables
- LRU cache
- DeferredDestroy queue

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Cache 1-4GB configurable; GPU destroy après fence signaled.

## Étapes d'implémentation
1. Implémenter cache LRU keyed par asset/chunk file.
2. Intégrer dans IO stage (hit cache -> skip disk).
3. Implémenter deferred free list (resource + fenceValue).
4. Collect lors de BeginFrame.

## Critères de validation (DoD)
- [ ] Ré-entrer dans une zone récemment quittée évite re-IO massif.

## Notes / pièges à éviter
- Mesurer: hit rate et memory usage.
