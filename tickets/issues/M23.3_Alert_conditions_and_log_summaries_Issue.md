# M23.3 — Alert conditions (rules) + log summaries

**Status:** Closed

## Objectif
Définir conditions d'alerte (documentation + hooks logs) pour incidents critiques (shard down, DB inaccessible, auth spike).

## Dépendances
- M23.2

## Livrables
- `docs/alerts.md`:
  - shard heartbeat timeout
  - db down
  - high auth failures rate
  - high disconnect rate
- Logs résumés périodiques (toutes 60s) pour état cluster

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Alerte "down" basée sur métriques (Prometheus rule) + logs côté serveur.
- Ne pas spammer; summary throttled.

## Étapes d'implémentation
1. Définir règles.
2. Implémenter summary periodic logs.
3. Vérifier avec scénarios (kill shard, stop DB).

## Critères de validation (DoD)
- [x] Docs utilisables pour config Alertmanager.
- [x] Summary logs lisibles.

---

## Rapport final (implémentation M23.3)

### 1) FICHIERS

**Créés :**
- `docs/alerts.md`

**Modifiés :**
- `engine/server/main_server_linux.cpp` (logs résumés cluster toutes les 60 s)

**Supprimés :** aucun.

### 2) COMMANDES À EXÉCUTER

- `cmake --preset linux-x64-release`
- `cmake --build --preset linux-x64-release`
- Lancer le Master : `./build/linux-x64-release/pkg/server/lcdlln_server` — les logs résumés apparaissent toutes les 60 s avec le préfixe `[ServerMain] cluster summary:`.

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI.**
- `docs/alerts.md` : décrit les 4 conditions (shard heartbeat timeout, db down, high auth failures rate, high disconnect rate), exemples de règles Prometheus/Alertmanager et hooks logs côté serveur.
- Summary logs : une ligne LOG_INFO toutes les 60 s avec conn_active, sessions_active, shards_online, auth_success, auth_fail, db_ok (throttled, pas de spam).

