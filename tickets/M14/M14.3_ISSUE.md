# M14.3 — Loot bag + pickup + inventory updates

**Status:** Closed

## Objectif
Système loot minimal : drop entité LootBag, pickup request, update inventaire.

## Dépendances
- M14.1

## Livrables
- LootTable
- LootBag entity
- PickupRequest/InventoryDelta msgs

## Spécification technique
- Loot instancié (owner) ou public; range check pickup.

## Étapes d'implémentation
1. Définir loot tables data.
2. À la mort: générer loot et spawn LootBag.
3. PickupRequest: valider range/ownership.
4. Ajouter items à inventory, despawn bag, envoyer delta.

## Critères de validation (DoD)
- [x] Kill->loot->pickup met à jour inventaire client.

## Notes
- Anti-dupe: tout côté serveur.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- engine/loot/LootTable.h
- engine/loot/LootTable.cpp
- engine/network/LootProtocol.h
- engine/network/LootProtocol.cpp

**Modifiés :**
- engine/network/Protocol.h (MsgType::PickupRequest = 9, InventoryDelta = 10)
- CMakeLists.txt (engine_loot, LootProtocol.cpp dans engine_network, server_app lié engine_loot)
- main_server.cpp (LootTable, LootBag, spawn LootBag à la mort du mob, PickupRequest: range/ownership, inventaire, Despawn, InventoryDelta)
- main_client.cpp (InventoryDelta, PickupRequest, inventaire local, affichage inv_item1)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

- cmake --preset win64
- cmake --build --preset win64
- .\build\server_app.exe [contentPath]
- .\build\client_app.exe [host]

### 3) RÉSULTAT

- Compilation : NON TESTÉ (CMake non disponible dans le PATH)
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Code moteur sous /engine. LootTable + LootBag + PickupRequest/InventoryDelta. À la mort du mob : génération loot, spawn LootBag (archetypeId 2). Pickup : vérification range (kPickupRange) et ownership (ownerId 0 = public). Inventaire serveur mis à jour, bag despawn, delta envoyé au client. Anti-dupe côté serveur.
