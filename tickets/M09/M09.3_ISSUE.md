# M09.3 — LOD rules + instancing (props/foliage)

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M09/M09.3_ISSUE.md`
- `engine/world/Lod.h`
- `engine/world/Lod.cpp`
- `engine/world/InstanceBatch.h`
- `engine/world/InstanceBatch.cpp`

**Modifiés :**
- `CMakeLists.txt` — ajout de `engine/world/Lod.cpp`, `engine/world/InstanceBatch.cpp`

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont respectés → **OUI**
- LOD distances : config `world.lod_max_0`..`world.lod_max_3` (25, 60, 150, 400 m). LOD chain sur mesh (MeshLodChain), sélection par distance (SelectLod), batch par clé (mesh+material) et collecteur pour draw instanced.

---

## Ticket (original)

### Objectif
Mettre en place règles LOD par catégorie et instancing pour props répétitifs.

### Dépendances
- M03.3
- M09.1

### Livrables
- LOD distances config
- Batch instancing (mesh+material key)

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- LOD0 0-25m, LOD1 25-60m, LOD2 60-150m, LOD3 150-400m (base).

### Étapes d'implémentation
1. Ajouter LOD chain au mesh (indices).
2. Sélection LOD par distance.
3. Grouper instances par key et draw instanced.

### Critères de validation (DoD)
- [ ] Drawcalls réduits vs rendu individuel; LOD switch visible mais acceptable.

### Notes / pièges à éviter
- Ajouter dithering crossfade plus tard.
