# M02.1 — Frame Graph: API ressources + passes + registry

**Status:** Closed

## Ticket (contenu)

# M02.1 — Frame Graph: API ressources + passes + registry

## Objectif
Mettre en place un Frame Graph minimal : création images/buffers logiques, passes avec read/write, registry pour handles Vulkan.

## Dépendances
- M01.4
- M00.2

## Livrables
- `FrameGraph.*` (addPass, createImage/createBuffer)
- `PassBuilder` (declare reads/writes)
- `Registry` (resolve FG id -> VkImage/VkBuffer/View)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Ressources transientes par défaut (lifetime frame).
- Pass = setup + execute(cmd, registry).
- Nommage des ressources/passes pour debug.

## Étapes d'implémentation
1. Définir IDs (uint32) et descriptors (ImageDesc/BufferDesc).
2. Implémenter PassBuilder read/write.
3. Implémenter Registry simple (map id->resource).
4. Exécuter passes dans l'ordre d'ajout (temporaire).

## Critères de validation (DoD)
- [ ] Créer une image SceneColor + une pass clear + une pass copy/present.
- [ ] Registry retourne des handles valides pendant la frame.

## Notes / pièges à éviter
- Commencer simple: pas d'aliasing mémoire au tout début.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- engine/render/FrameGraph.h (ResourceId, ImageDesc, BufferDesc, Registry, PassBuilder, FrameGraph)
- engine/render/FrameGraph.cpp (implémentation createImage/createBuffer/createExternalImage, addPass, execute, destroy)
- tickets/M02/M02.1_ISSUE.md

**Modifiés :**
- CMakeLists.txt (ajout engine/render/FrameGraph.cpp)
- engine/Engine.h (include FrameGraph.h ; m_frameGraph, m_fgRegistry, m_fgSceneColorId, m_fgBackbufferId)
- engine/Engine.cpp (construction du graphe : SceneColor + Backbuffer externe, pass Clear, pass CopyPresent ; Render() utilise le Frame Graph au lieu du render pass swapchain direct ; destruction m_frameGraph avant DestroyFrameResources)
- engine/render/vk/VkSwapchain.h (GetImage, GetImageView ; m_images)
- engine/render/vk/VkSwapchain.cpp (stockage m_images, GetImage/GetImageView ; imageUsage avec TRANSFER_DST_BIT)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset vs2022-x64`
- `cmake --build --preset vs2022-x64-debug`
- `.\build\vs2022-x64\Debug\engine_app.exe`

### 3) RÉSULTAT

- Compilation : NON TESTÉ (cmake/outils de build non disponibles dans l'environnement)
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Code moteur sous /engine ; pas de nouveau dossier racine ; pas de chemin absolu.
- FrameGraph : createImage, createBuffer, createExternalImage, addPass (PassBuilder read/write), Registry (bindImage, bindBuffer, getImage, getImageView, getBuffer), execute dans l’ordre d’ajout.
- Validation ticket : image SceneColor + pass clear (vkCmdClearColorImage) + pass copy/present (vkCmdCopyImage vers backbuffer) ; Registry fournit des handles valides pendant la frame.
