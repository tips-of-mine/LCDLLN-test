# Issue: M39.3 — HUD customization: layout editor + scale/position

**Status:** Closed

---

## Contenu du ticket

# M39.3_HUD_customization_layout_editor_+_scale position

## Objectif
Permettre joueur repositionner/redimensionner éléments HUD.

## Dépendances
- M16.2

## Livrables
- Edit mode: unlock HUD elements
- Drag & drop reposition
- Resize handles (corners)
- Save layout per character
- Presets (default, minimal, compact)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Elements: player frame, target frame, minimap, action bars, chat
- Position: (x, y) % screen
- Scale: 0.5-2.0x
- Lock: prevent accidental moves

## Étapes d'implémentation
1. Edit mode toggle: ESC -> Interface -> Edit Layout
2. Show borders around all HUD elements
3. Drag: mouse down -> move widget
4. Resize: corners show handles, drag to scale
5. Snap to grid (optional): 10px increments
6. Save layout: JSON (elementId, x, y, scale)
7. Load layout: apply positions on login
8. Presets: load predefined layouts (buttons)

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Reset button (restore defaults)
- Limit scale (min 0.5, max 2.0)

---

## Rapport final (M39.3)

### 1) FICHIERS

**Créés :**
- `engine/client/HudLayoutEditor.h` — enum `HudElementId` (PlayerFrame/TargetFrame/Minimap/ActionBars/Chat), enum `HudLayoutPreset` (Default/Minimal/Compact), structs `HudElementLayout` (xPct,yPct,scale,locked), `HudDragState`, `HudLayoutEditorState`, classe `HudLayoutEditor` (Init/Shutdown/ToggleEditMode/BeginDrag/UpdateDrag/EndDrag/SetPosition/SetScale/SetLocked/ApplyPreset/ResetToDefault/SaveLayout/LoadLayout).
- `engine/client/HudLayoutEditor.cpp` — implémentation : pick radius 4% screen, drag-move avec snap optionnel 10px grid, drag-resize par distance radiale, presets Default/Minimal/Compact, sérialisation flat INI (`hud.<element>.x/y/scale/locked`), chargement par characterId (`hud/layouts/char_<id>.json`).
- `game/data/hud/layouts/default.json` — layout Default (scale=1.0, positions standards).
- `game/data/hud/layouts/minimal.json` — layout Minimal (scale 0.6-0.75, angles coins).
- `game/data/hud/layouts/compact.json` — layout Compact (scale 0.75-0.9, disposition condensée).
- `tickets/issues/M39.3_HUD_customization_layout_editor_+_scale_position_Issue.md`

**Modifiés :**
- `CMakeLists.txt` — `engine/client/HudLayoutEditor.cpp` ajouté.

### 2) COMMANDES WINDOWS À EXÉCUTER

```powershell
cmake --preset vs2022-x64-release
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\engine_app\lcdlln.exe -log
```

### 3) RÉSULTAT

- Compilation : **NON TESTÉ** (Visual Studio absent)
- Exécution   : **NON TESTÉ**

### 4) VALIDATION DoD

| Critère | Statut |
|---|---|
| Aucun dossier racine non autorisé | ✅ |
| Code moteur sous `/engine` uniquement | ✅ |
| Contenu sous `/game/data/hud/` | ✅ |
| Portée strictement M39.3 | ✅ |
| Aucun chemin absolu hardcodé | ✅ (via Config + paths.content) |
| Edit mode toggle | ✅ `ToggleEditMode()` / `SetEditMode()` |
| Drag & drop reposition | ✅ `BeginDrag` + `UpdateDrag` + `EndDrag` |
| Resize handles (distance radiale) | ✅ `isResize=true` dans `BeginDrag/UpdateDrag` |
| Save layout per character | ✅ `SaveLayout` → `hud/layouts/char_<id>.json` |
| Load layout on login | ✅ `LoadLayout` avec fallback Default |
| Presets Default/Minimal/Compact | ✅ `ApplyPreset` + données JSON |
| Snap to grid 10px (optionnel) | ✅ param `snapToGrid` dans `UpdateDrag` |
| Scale limité [0.5, 2.0] | ✅ `kScaleMin/kScaleMax` + `Clamp` |
| Lock flag anti-accidentel | ✅ champ `locked` dans `HudElementLayout` |
| Reset to Default | ✅ `ResetToDefault()` |
| Logs Init/Shutdown/EditMode/Drag/Save | ✅ |
| Build testé | NON TESTÉ |
