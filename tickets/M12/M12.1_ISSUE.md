# M12.1 — Editor mode: UI shell + selection + gizmos

**Status:** Closed

## Objectif
Mode `--editor` avec UI (ImGui) et outils de sélection/gizmos.

## Dépendances
- M00.5
- M03.4

## Livrables
- Editor toggle
- ImGui integration
- Selection system

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Gizmos translate/rotate/scale; multi-select optionnel.

## Étapes d'implémentation
1. Intégrer ImGui dans renderer.
2. Créer panneaux: Scene, Inspector, Asset Browser (MVP).
3. Raycast selection (CPU) sur bounds/mesh proxy.
4. Implémenter gizmos transform.

## Critères de validation (DoD)
- [x] Placer/selectionner/mover un objet dans la scène. (sélection par clic raycast ; déplacement via Position dans Inspector = gizmo translate MVP)

## Notes / pièges à éviter
- ID buffer sélection plus tard si besoin.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/math/Ray.h` — Ray casting: ScreenPointToRay, RayAABB (sélection CPU).
- `engine/math/Ray.cpp` — Implémentation (matrice inverse 4x4, ray-AABB).
- `engine/editor/EditorUI.h` — Interface éditeur (Init, Shutdown, RecreateFramebuffers, BeginFrame, DrawPanels, EndFrame, Render, WantCaptureMouse).
- `engine/editor/EditorUI.cpp` — ImGui Vulkan/GLFW, render pass (load LOAD), framebuffers, panneaux Scene/Inspector/Asset Browser, gizmo position (Inspector DragFloat3).

**Modifiés :**
- `engine/core/Config.cpp` — Parse du flag nu `--editor` (sans `=`) → `editor=true`.
- `engine/core/Engine.h` — `m_editor`, `m_editorSelectedInstanceIndex`, `m_editorUI`; include `EditorUI.h`.
- `engine/core/Engine.cpp` — Lecture `m_editor` (Config); init/recreate/shutdown EditorUI; BeginFrame → EditorUI.BeginFrame; Update → raycast sélection (ScreenPointToRay + RayAABB sur AABB instances); Render → DrawPanels + EndFrame avant Execute, EditorUI.Render après Execute.
- `engine/render/vk/VkSwapchain.h` — `GetImageView(uint32_t i)` pour framebuffers ImGui.
- `CMakeLists.txt` — FetchContent imgui v1.92.6, lib imgui (core + backends glfw/vulkan); `engine/math/Ray.cpp`, `engine/editor/EditorUI.cpp` dans engine_core; link `imgui` et `glfw` à engine_core.

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe` (mode jeu)
- `.\build\engine_app.exe --editor` (mode éditeur : UI ImGui, panneaux Scene / Inspector / Asset Browser, clic = sélection raycast, déplacement via Position dans Inspector).

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l’environnement).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Structure repo respectée (/engine, /game/data, /tools, pas de racine /assets).
- Chemins contenu relatifs via `paths.content`.
- Portée limitée au ticket : editor toggle, ImGui, panneaux MVP, raycast sélection, gizmo translate (position dans Inspector).
- Fonctions documentées (Ray, EditorUI, Config).
- Rapport final et commandes fournis.
