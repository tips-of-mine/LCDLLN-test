# Issue M39.3 — HUD customization: layout editor + scale/position

**Status:** Closed

---

## Ticket content

# M39.3_HUD_customization_layout_editor_+_scale position

## Objectif
Permettre joueur repositionner/redimensionner éléments HUD.

## Dépendances
- M16.2

## Livrables
- Edit mode: unlock HUD elements
- Drag & drop reposition
- Resize handles (corners)
- Save layout per character
- Presets (default, minimal, compact)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Elements: player frame, target frame, minimap, action bars, chat
- Position: (x, y) % screen
- Scale: 0.5-2.0x
- Lock: prevent accidental moves

## Étapes d'implémentation
1. Edit mode toggle: ESC -> Interface -> Edit Layout
2. Show borders around all HUD elements
3. Drag: mouse down -> move widget
4. Resize: corners show handles, drag to scale
5. Snap to grid (optional): 10px increments
6. Save layout: JSON (elementId, x, y, scale)
7. Load layout: apply positions on login
8. Presets: load predefined layouts (buttons)

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Reset button (restore defaults)
- Limit scale (min 0.5, max 2.0)

---

## Rapport final

### 1) Fichiers

**Créés :**
- `engine/client/HudLayoutEditor.h` — Déclaration de la classe `HudLayoutEditor`, des structures `HudElementLayout`, `HudLayoutEditorState`, de l'enum `HudResizeCorner`, et des constantes `kHudScaleMin`/`kHudScaleMax`/`kHudSnapGridPx`.
- `engine/client/HudLayoutEditor.cpp` — Implémentation complète : lifecycle (Init/Shutdown), gestion de l'edit mode, chargement/sauvegarde JSON (presets + par personnage), drag & drop, resize par coin, snap-to-grid optionnel, debug text.
- `game/data/hud/layouts/default.json` — Preset "default" (5 éléments : player_frame, target_frame, minimap, action_bars, chat).
- `game/data/hud/layouts/minimal.json` — Preset "minimal" (éléments réduits en taille et repositionnés).
- `game/data/hud/layouts/compact.json` — Preset "compact" (positions intermédiaires, scale ~0.85).
- `tickets/issues/M39.3_HUD_customization_layout_editor_+_scale_position_Issue.md` — Cette issue.

**Modifiés :**
- `CMakeLists.txt` — Ajout de `engine/client/HudLayoutEditor.cpp` à la liste des sources.

**Supprimés :** aucun.

---

### 2) Commandes Windows à exécuter

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
```

---

### 3) Résultat

- **Compilation :** NON TESTÉ (outillage non disponible dans l'environnement d'exécution)
- **Exécution :** NON TESTÉ

---

### 4) Validation DoD

| Critère | État |
|---|---|
| Build Windows OK | NON TESTÉ |
| Exécutable sans crash | NON TESTÉ |
| Aucun dossier racine non autorisé | ✅ |
| Rapport final complet | ✅ |
| Edit mode toggle | ✅ `ToggleEditMode()` |
| Drag & drop reposition | ✅ `BeginDrag/UpdateDrag/EndDrag/CancelDrag` |
| Resize handles (corners) | ✅ `BeginResize/UpdateResize/EndResize/CancelResize`, `HudResizeCorner` enum |
| Save layout per character | ✅ `SaveCharacterLayout(config, characterId)` → `hud/layouts/character_<id>.json` |
| Load layout on login | ✅ `LoadCharacterLayout(config, characterId)` |
| Presets (default, minimal, compact) | ✅ JSON dans `game/data/hud/layouts/` + `LoadPreset(config, name)` |
| Reset to default | ✅ `ResetToDefault(config)` |
| Scale [0.5, 2.0] | ✅ `kHudScaleMin/kHudScaleMax`, `ClampScale()` |
| Position (x,y) % screen | ✅ normalised [0,1] posX/posY, `ClampPos()` |
| Lock element | ✅ `SetElementLocked()`, vérifié dans BeginDrag/BeginResize |
| Snap to grid optionnel (10px) | ✅ `SetSnapToGrid()`, `SnapPosition()` |
| Chemins contenu via paths.content | ✅ utilise `FileSystem::ReadAllText/WriteAllTextContent` + config |
| Logs Init/Shutdown/chemins critiques | ✅ |
