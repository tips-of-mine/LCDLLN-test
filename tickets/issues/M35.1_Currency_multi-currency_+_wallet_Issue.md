# Issue: M35.1 — Currency multi-currency + wallet

**Status:** Closed

---

## Rapport final (M35.1)

### 1) FICHIERS

**Créés :**
- `db/migrations/0012_player_wallet_currency.sql` — tables `player_wallet` et `player_currency_log` (FK `characters.id`, audit delta/balance_after).
- `game/data/config/currencies.json` — devises (id, key, name, max_amount, icon relatif) + conversion optionnelle copper/silver/gold.
- `engine/server/CurrencyConfig.h` — définitions et chargeur JSON (`paths.content` / `config/currencies.json`).
- `engine/server/CurrencyConfig.cpp` — parsing JSON minimal + défauts + logs.
- `engine/server/PlayerWalletService.h` — ids de devises, Add / Subtract / Transfer (arithmétique non signée, plafonds).
- `engine/server/PlayerWalletService.cpp` — implémentation sur `ConnectedClient`.
- `tickets/issues/M35.1_Currency_multi-currency_+_wallet_Issue.md`

**Modifiés :**
- `engine/server/CharacterPersistence.h` — champs honor, badges, premiumCurrency dans `PersistedCharacterState`.
- `engine/server/CharacterPersistence.cpp` — load/save `.ini` pour les nouveaux champs.
- `engine/server/ServerApp.h` — `ConnectedClient` étendu, `CurrencyConfig`, `PlayerWalletService`, API publique TryAdd/Subtract/Transfer, `SendWalletUpdate`, `FindConnectedClient`.
- `engine/server/ServerApp.cpp` — init config devises, persistance handshake, récompenses quêtes/événements via `AddCurrency` (or), `SendWalletUpdate`, implémentations API.
- `engine/server/ServerProtocol.h` — `MessageKind::WalletUpdate`, `WalletUpdateMessage`, encode/decode déclarés.
- `engine/server/ServerProtocol.cpp` — `EncodeWalletUpdate` / `DecodeWalletUpdate`.
- `engine/server/CMakeLists.txt` — sources `CurrencyConfig.cpp`, `PlayerWalletService.cpp` pour `server_app` (Windows).
- `engine/client/UIModel.h` — `UIWalletState`, `UIModelChangeWallet`, `ApplyWalletUpdate`, scratch `m_walletScratch`.
- `engine/client/UIModel.cpp` — dispatch paquet, dump debug, reset.
- `engine/client/CombatHud.h` — état wallet (bounds + ligne or).
- `engine/client/CombatHud.cpp` — layout, `UpdateWalletFromModel`, debug text.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\pkg\server\lcdlln_server.exe
```

Client / smoke moteur (BUILD_CHECK) :

```bat
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\pkg\game\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (`cmake` absent dans l’environnement d’exécution de l’agent)
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → **NON** (build et exécution non validés ici ; le reste du ticket est implémenté côté code et migrations).
- **Note :** le shard Windows n’utilise pas MySQL runtime ; la migration `0012` est fournie pour les déploiements MySQL (transactions atomiques documentées dans le SQL). La persistance runtime des soldes sur le serveur de jeu repose sur les fichiers `.ini` existants + nouveaux champs.

---

## Ticket content (M35.1) — référence

# M35.1_Currency_multi-currency_+_wallet
## Objectif
Système multi-currency (gold, silver, copper, gems, badges) avec wallet tracking.

## Dépendances
- M14.4
- M16.1

## Livrables
- Currency types config (id, name, max, conversion)
- Wallet per player (DB)
- Transactions (add, subtract, transfer)
- UI wallet display

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Currencies: gold (main), honor, badges, premium_currency
- Max: gold=999,999,999 (uint32), honor=100,000
- Conversion: 100 copper = 1 silver, 100 silver = 1 gold (optional)
- Transactions atomic (DB transaction)

## Étapes d'implémentation
1. Define currency types (JSON config)
2. DB: player_wallet (player_id, currency_id, amount)
3. API: AddCurrency(playerId, currencyId, amount)
4. Validate: check max cap, overdraft prevention
5. Log transactions (player_currency_log for audit)
6. UI: display wallet (gold icon + amount)
7. Events: CurrencyChanged -> update UI

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Signed overflow: use unsigned types
- Negative amounts check (server validation)
