# Issue: M07.2 — TAA: history ping-pong textures

**Status:** Closed

---

## Rapport final (M07.2)

### 1) FICHIERS

**Créés :** aucun

**Modifiés :**
- `engine/Engine.h` — `m_fgHistoryAId`, `m_fgHistoryBId` ; `m_taaHistoryEverFilled` ; `GetTaaHistoryPrevId()`, `GetTaaHistoryNextId()` (prev=idx^1, next=idx).
- `engine/Engine.cpp` — Création des images FG HistoryA/HistoryB (R8G8B8A8_UNORM, COLOR_ATTACHMENT | SAMPLED | TRANSFER_DST) ; pass TAA_InitHistory (lecture LDR, écriture HistoryA + HistoryB) : premier frame copie LDR → les deux, sur reset copie LDR → history next uniquement ; implémentation des getters prev/next.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

L’utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Structure : code moteur sous `/engine`, aucun nouveau dossier racine.
- Portée : uniquement images HistoryA/HistoryB, gestion prev/next (next=frame%2, prev=next^1), init premier frame (copie LDR → les deux), reset (copie LDR → history next).
- Chemins : aucun nouveau chemin de contenu.
- Qualité : code et commentaires cohérents.

---

## Ticket content (M07.2) — référence

# M07.2 — TAA: history ping-pong textures

## Objectif
Créer HistoryA/HistoryB et ping-pong par frame.

## Dépendances
- M07.1

## Livrables
- FG images HistoryA/HistoryB format LDR (ou HDR)
- Gestion indices prev/next

## Spécification technique
- History format = input TAA format.

## Étapes d'implémentation
1. Créer 2 images history.
2. À chaque frame: prev=idx^1, next=idx.
3. Init history = current lors du premier frame.

## Critères de validation (DoD)
- [ ] Pas d'artefacts au démarrage; ping-pong correct.

## Notes / pièges à éviter
- Sur reset, copier current -> history next.
