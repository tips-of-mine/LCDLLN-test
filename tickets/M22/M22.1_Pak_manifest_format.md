# M22.1 — Packaging — Format manifest + layout .pak (MVP)

## Objectif
Définir un format manifest de contenu (JSON) qui décrit:
- liste des fichiers contenus,
- hash (xxhash/sha256), taille,
- version (semver),
- “chunks” ou “bundles” (ex: zone_01.pak).

Aucun vrai packer binaire n’est requis ici: on définit le format + un générateur simple.

## Dépendances
- M12.1


## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Code jeu** : uniquement sous `/game/...`
- **Contenu** (textures/meshes/json/audio/shaders) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- **Interdit** : créer des dossiers racine du type `/assets`, `/textures`, `/data`, `/shaders`, etc.
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json). Pas de chemins absolus.


## Livrables
- `tools/pak/manifest_schema.md` (documentation)
- `tools/pak/generate_manifest.py` (scan game/data)
- Exemple `game/data/manifest.json` généré

## Validation
- Le générateur produit un manifest déterministe
- Les chemins dans le manifest sont relatifs à game/data

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD
