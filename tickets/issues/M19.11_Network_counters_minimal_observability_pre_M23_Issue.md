# M19.11 — Network counters & minimal observability (pre-M23)

**Status:** Closed

## Objectif
Ajouter des compteurs et logs réseau exploitables dès M19, avant l'observabilité complète (M23).

## Dépendances
- M19.3
- M19.4
- M19.5
- M00.1

## Livrables
- Counters:
  - connections active/total
  - handshake success/fail
  - bytes in/out
  - packets in/out
  - packets dropped
  - disconnect reasons histogram
- Logs contrôlés (throttle si nécessaire)
- Option CLI debug `--net.stats` (ou équivalent)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Préférer aggregation per-thread puis merge périodique.
- Ne pas logguer par paquet en prod.

## Étapes d'implémentation
1. Struct stats global + per-thread.
2. Instrumenter connect/accept/handshake/read/write/disconnect.
3. Ajouter dump périodique en debug.
4. Ajouter commande/flag pour snapshot.

## Critères de validation (DoD)
- [ ] Stats cohérentes sur 1000 connexions.
- [ ] Logs restent lisibles (pas de spam).

---

## Rapport final (implémentation)

**Status:** Closed

### 1) FICHIERS

**Créés :**
- `tickets/issues/M19.11_Network_counters_minimal_observability_pre_M23_Issue.md`

**Modifiés :**
- `engine/server/NetServer.h` (struct NetServerStats, GetNetworkStats())
- `engine/server/NetServer.cpp` (atomics: connectionsTotal, handshakeSuccess/Fail, bytesIn/Out, packetsIn/Out, packetsDropped ; instrumentation accept/read/write/handshake/decode/rate-limit ; GetNetworkStats() impl + stub non-Linux)
- `engine/server/main_server_linux.cpp` (parse --net.stats / -net.stats, dump périodique 10s + snapshot final avec histogramme disconnect)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS / LINUX

Windows (moteur) :
```bat
cmake --preset win64
cmake --build --preset win64
.\build\pkg\game\lcdlln.exe
```

Linux (serveur avec stats) :
```bash
./pkg/server/lcdlln_server   # sans stats
./pkg/server/lcdlln_server --net.stats   # dump stats toutes les 10s + final
```

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution   : NON TESTÉ

### 4) VALIDATION DoD

- Counters : connectionsActive (déjà connectionCount), connectionsTotal, handshakeSuccess, handshakeFail, bytesIn, bytesOut, packetsIn, packetsOut, packetsDropped, disconnectByReason[] (histogram).
- Logs contrôlés : pas de log par paquet ; dump stats uniquement si --net.stats et toutes les 10s + une fois en fin de run.
- Option CLI : --net.stats et -net.stats pour activer le snapshot périodique et le dump final (avec histogramme disconnect).
- Stats cohérentes : compteurs mis à jour côté IO thread (atomics), lecture via GetNetworkStats() sans contention excessive.

→ **OUI** (points DoD respectés).
