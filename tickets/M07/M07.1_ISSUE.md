# M07.1 — TAA: jitter Halton + matrices prev/curr

**Status:** Closed

## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/render/Halton.h`
- `engine/render/Halton.cpp`

**Modifiés :**
- `engine/core/Engine.h` (RenderState: viewProjCurr, viewProjPrev, jitterCurr, jitterPrev; Engine: m_taaFrameIndex, m_taaResetHistory, m_taaPrevAspect, m_taaPrevFov)
- `engine/core/Engine.cpp` (include Halton; Update: Halton jitter NDC, apply to proj, store prev/curr ViewProj + jitter, reset on resize/FOV change; OnResize: m_taaResetHistory = true)
- `CMakeLists.txt` (Halton.cpp)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset <preset>`
- `cmake --build --preset <preset>`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Séquence Halton (2/3) N=16 ; jitter NDC = (2*(halton-0.5))/resolution ; appliqué à la projection ; viewProjCurr/viewProjPrev et jitter stockés ; reset sur resize et changement FOV/aspect.

---

## Ticket (original)

### Objectif
Ajouter jitter subpixel à la projection et conserver matrices prev/curr pour reprojection.

### Dépendances
- M03.4

### Livrables
- Séquence Halton (2/3) N=8-16
- Curr/Prev ViewProj + jitter stored

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Jitter en NDC: (2*jitterPx)/resolution.

### Étapes d'implémentation
1. Implémenter générateur Halton.
2. Appliquer jitter à projection chaque frame.
3. Stocker matrices prev/curr et gérer reset.

### Critères de validation (DoD)
- [ ] Projection jitter change chaque frame, stable et cyclique.

### Notes / pièges à éviter
- Reset history sur resize/teleport/FOV change.
