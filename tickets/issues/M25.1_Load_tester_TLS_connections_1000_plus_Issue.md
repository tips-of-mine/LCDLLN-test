# M25.1 — Load tester: 1000+ TLS connections

**Status:** Closed

## Objectif
Créer un outil de test de charge capable de simuler 1000+ connexions TLS et scénarios auth/heartbeat.

## Dépendances
- M19.5/M19.6 (TLS)
- M20.5/M20.6 (auth/heartbeat)

## Livrables
- `tools/load_tester/`:
  - scénarios: connect-only, auth-only, heartbeat-only, mix
  - reporting: success/fail, latency p50/p95
- Paramétrage: nb clients, ramp-up, durée

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Ne pas saturer une seule machine: possibilité multi-process.
- Mesurer latence handshake + requests.

## Étapes d’implémentation
1. Implémenter client headless multi-conn.
2. Ajouter scénarios.
3. Ajouter reporting.

## Critères de validation (DoD)
- [ ] 1000 connexions stables localement (hardware dépendant, mais sans crash).

---
## Rapport final

### 1) FICHIERS

**Créés :**
- `tools/load_tester/CMakeLists.txt`
- `tools/load_tester/LoadTester.h`
- `tools/load_tester/LoadTester.cpp`
- `tools/load_tester/main.cpp`

**Modifiés :**
- `CMakeLists.txt`

**Supprimés (si explicitement demandé) :**
- (aucun)

### 2) COMMANDES WINDOWS À EXÉCUTER

```powershell
cmake --preset vs2022-x64-release
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64\pkg\tools\load_tester.exe --scenario mix --clients 1000 --ramp-up-sec 10 --duration-sec 30 --master-host localhost --master-port 3840 --server-fingerprint <sha256_hex> --login testuser --client-hash <argon2_hash>
```

### 3) RÉSULTAT

- Compilation : ERREUR
- Exécution   : NON TESTÉ

Raison compilation : `cmake` n’est pas trouvé dans l’environnement d’exécution de l’agent (`CommandNotFoundException`).

### 4) VALIDATION DoD

- Tous les points de `DEFINITION_OF_DONE.md` sont-ils respectés ? → NON
- Explication : le build/run n’a pas pu être validé localement (commande `cmake` indisponible côté agent), donc les items A) Build & exécution restent non validés.

