# Issue: M10.4 — GPU upload budget/frame + staging

**Status:** Closed

---

## Rapport final (M10.4)

### 1) FICHIERS

**Créés :**
- `engine/render/GpuUploadQueue.h` — UploadCategory (TerrainHlod, TextureMip), UploadJob, GpuUploadQueue (Init, Enqueue, PlanFrameUploads, GetBudgetBytes, GetBudgetUsedThisFrame).
- `engine/render/GpuUploadQueue.cpp` — Budget depuis config (streaming.upload_budget_mb, défaut 32), plan terrain puis textures.
- `engine/render/vk/StagingAllocator.h` — Ring de 2 buffers persistants, Allocate, BeginFrame, GetCurrentBuffer, Destroy.
- `engine/render/vk/StagingAllocator.cpp` — VMA host-visible TRANSFER_SRC, taille = budget/frame.

**Modifiés :**
- `engine/Engine.h` — GpuUploadQueue, StagingAllocator ; includes.
- `engine/Engine.cpp` — Init m_gpuUploadQueue ; après VMA init StagingAllocator.Init(device, vma, budget) ; dans Render après Collect : StagingAllocator.BeginFrame(frameIndex), GpuUploadQueue.PlanFrameUploads() ; à la destruction : StagingAllocator.Destroy avant vmaDestroyAllocator.
- `CMakeLists.txt` — GpuUploadQueue.cpp, StagingAllocator.cpp.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l'environnement).
- **Exécution :** NON TESTÉ

L'utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Build : commandes listées.
- Structure : code sous `/engine` (render + render/vk).
- Portée : uniquement M10.4 (UploadQueue + budget, Staging allocator, plan terrain puis textures, stats budget used).
- Critère ticket : budget par frame (32 MB configurable) limite les uploads donc évite les gros spikes de frame time lors du streaming.

---

## Ticket content (M10.4) — référence

# M10.4 — GPU upload budget/frame + staging

## Objectif
Limiter uploads GPU par frame (MB ou ms) pour éviter stutter.

## Dépendances
- M10.1
- M01.4

## Livrables
- UploadQueue + budget
- Staging allocator

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Budget initial: 32MB/frame (configurable).

## Étapes d'implémentation
1. Créer staging buffer ring (persistant).
2. Planifier uploads en fonction budget.
3. Uploader d'abord terrain/HLOD puis textures mip bas.
4. Report stats budget used.

## Critères de validation (DoD)
- [ ] Pas de gros spikes frame time lors de streaming.

## Notes / pièges à éviter
- Plus tard: timeline semaphores pour mieux pipeliner.
