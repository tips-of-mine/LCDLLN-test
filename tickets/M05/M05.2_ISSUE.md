# M05.2 — IBL: irradiance cubemap convolution

**Status:** Closed

## Ticket (original)

### Objectif
Générer cubemap irradiance (diffuse) à partir d'un env HDR cubemap.

### Dépendances
- M05.1
- M03.3

### Livrables
- Irradiance cubemap (64-128) format RGBA16F
- Compute ou raster convolution

### Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Sampling hémisphère (Lambert); importance sampling optionnel.

### Étapes d'implémentation
1. Charger env HDR cubemap source.
2. Créer irradiance cubemap target.
3. Convolution (compute) par face.
4. Barrières vers sampled.

### Critères de validation (DoD)
- [ ] Diffuse ambient réagit à la direction N (visible sur sphère).

### Notes / pièges à éviter
- Commencer avec peu de samples (64) puis augmenter.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/render/vk/VkIrradianceCubemap.h`
- `engine/render/vk/VkIrradianceCubemap.cpp`
- `game/data/shaders/irradiance_conv.comp`

**Modifiés :**
- `engine/render/vk/VkTextureLoader.h` — `LoadCubemapHDR`, `CreateDefaultCubemap`
- `engine/render/vk/VkTextureLoader.cpp` — `LoadCubemapHDR` / `LoadCubemapHDRInternal` (6 faces HDR), `CreateDefaultCubemap` / `CreateDefaultCubemapInternal` (1x1 cube fallback)
- `engine/render/vk/VkLightingPipeline.h` — `SetIrradianceView`
- `engine/render/vk/VkLightingPipeline.cpp` — binding 10 (irradiance cubemap), `SetIrradianceView`, pool 10 image samplers
- `game/data/shaders/lighting.frag` — `samplerCube uIrradianceMap`, `irradiance = texture(uIrradianceMap, N).rgb`, `diffuseAmbient = irradiance * albedo * occlusion`
- `engine/core/Engine.h` — `VkIrradianceCubemap`, `m_envCubemapSampler`
- `engine/core/Engine.cpp` — init env sampler, load env HDR cubemap, init/convolve irradiance, `SetIrradianceView` (réel ou fallback), shutdown irradiance + sampler
- `CMakeLists.txt` — `VkIrradianceCubemap.cpp`
- `config.json` — `env.base_path` (optionnel, défaut `"env"`)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset windows-default`
- `cmake --build --preset windows-default`
- `.\build\engine_app.exe` (ou chemin équivalent selon preset)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l’environnement).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Build : commandes listées ; structure repo respectée ; code sous `/engine`, contenu sous `/game/data` ; chemins via `paths.content` ; pas de dérive hors ticket ; rapport final fourni.
- **Critère ticket :** L’ambiant diffuse réagit à la direction N : le fragment utilise `irradiance = texture(uIrradianceMap, N).rgb` et `diffuseAmbient = irradiance * albedo * occlusion`, donc visible sur une sphère (ou toute géométrie avec N variée).

### Détails techniques

- **Chargement env HDR :** `VkTextureLoader::LoadCubemapHDR(basePath)` charge 6 faces : `basePath/posx.hdr`, `negx.hdr`, `posy.hdr`, `negy.hdr`, `posz.hdr`, `negz.hdr` via `stbi_loadf_from_memory`, crée une image 6 layers (cube compatible) R32G32B32A32_SFLOAT, vue type CUBE.
- **Irradiance target :** `VkIrradianceCubemap` crée une image 64×64×6 RGBA16F (storage + sampled), vues 2D array (compute) et CUBE (lighting).
- **Convolution :** compute `irradiance_conv.comp` — 64 échantillons hémisphère (Lambert), par face (dispatch 8×8×6), barrières UNDEFINED→GENERAL puis GENERAL→SHADER_READ_ONLY_OPTIMAL.
- **Fallback :** si l’env n’est pas chargé, `CreateDefaultCubemap()` (1x1 cube gris) + `SetIrradianceView` pour garder le binding 10 valide.
