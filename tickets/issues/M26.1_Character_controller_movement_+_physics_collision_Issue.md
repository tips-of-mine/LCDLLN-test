# M26.1 — Character controller movement + physics collision

**Status:** Closed

## Objectif
Implémenter character controller avec collision capsule, gravity, slope handling.

## Dépendances
- M00.4
- M09.1

## Livrables
- `engine/gameplay/CharacterController.*`
- Capsule collision shape
- Movement avec acceleration/friction
- Slope/step handling

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Capsule (rayon 0.3m, hauteur 1.8m typique)
- Walk speed 4-6 m/s, run 8-10 m/s
- Gravity -20 m/s²
- Max slope 45°, max step 0.3m

## Étapes d'implémentation
1. Définir CharacterController (velocity, isGrounded, capsule dims)
2. Update: appliquer input -> desired velocity
3. Appliquer gravity si !isGrounded
4. Collision detection capsule vs world (sweep test)
5. Slide along obstacles (project velocity)
6. Step up detection

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Utiliser sweep test, pas discrete collision
- Séparer logique movement de physics (fixed timestep optionnel)

---
## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/gameplay/CharacterController.h`
- `engine/gameplay/CharacterController.cpp`
- `tickets/issues/M26.1_Character_controller_movement_+_physics_collision_Issue.md` (issue)

**Modifiés :**
- `CMakeLists.txt`

**Supprimés (si explicitement demandé) :**
- (aucun)

### 2) COMMANDES WINDOWS À EXÉCUTER
- `cmake --preset vs2022-x64-release`
- `cmake --build --preset vs2022-x64-release`
- `.\build\vs2022-x64-release\engine_app\lcdlln.exe -log`

### 3) RÉSULTAT
- Compilation : ERREUR
- Exécution   : NON TESTÉ

Cause (agent) : commande `cmake` indisponible (`CommandNotFoundException`), donc pas de compilation/smoke test possible ici.

### 4) VALIDATION DoD
- Tous les points de `DEFINITION_OF_DONE.md` sont-ils respectés ?
  → NON
- Explication : configure/build/run et smoke test non exécutés localement via l’agent (absence de `cmake` dans l’environnement).

