# Issue: M37.3 — Water underwater post-effects + fog

**Status:** Closed

---

## Contenu du ticket

# M37.3_Water_underwater_post-effects_+_fog

## Objectif
Post-effects underwater (tint blue, fog distance, blur).

## Dépendances
- M37.1
- M08.2

## Livrables
- Underwater detection (camera below water plane)
- Color grading (blue tint)
- Fog (exponential depth-based)
- Blur vignette (optional)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Detect: camera.y < water.y
- Tint: lerp color vers blue (depth-based)
- Fog: exp(-density * depth)
- Blur: slight gaussian (simule diffusion light)

## Étapes d'implémentation
1. Check camera position vs water planes
2. If underwater: enable post-FX
3. Tint shader: color *= vec3(0.5, 0.7, 1.0)
4. Fog: fogFactor = exp(-0.05 * depth), lerp(color, fogColor, fogFactor)
5. Optional blur: apply light gaussian
6. Disable when camera exits water

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Oxygen timer (M26.3 extend)
- God rays underwater (M19.2 adapt)

---

## Rapport final (M37.3)

### 1) FICHIERS

**Créés :**
- `engine/render/UnderwaterPass.h` — classe `UnderwaterPass` avec `UnderwaterParams` (32 bytes), méthode statique `IsUnderwater(cameraY, waterY)`, pipeline fullscreen alpha-blend sur SceneColorHDR, binding depth.
- `engine/render/UnderwaterPass.cpp` — implémentation complète : sampler NEAREST pour depth, render pass LOAD_OP_LOAD+alpha blend, descriptor set (1 binding : depth), push constants 32 bytes (fragment only), pipeline fullscreen triangle (draw 3 vertices, no VBO).
- `game/data/shaders/underwater.vert` — fullscreen triangle shader (même pattern que tonemap.vert).
- `game/data/shaders/underwater.frag` — effet : depth linearisation, brouillard exponentiel `fogFactor = 1 - exp(-density * linearDepth)`, tint bleu `vec3(0.5, 0.7, 1.0)`, vignette optionnelle, sortie alpha-blend overlay.
- `tickets/issues/M37.3_Water_underwater_post-effects_+_fog_Issue.md`

**Modifiés :**
- `engine/render/DeferredPipeline.h` — `#include UnderwaterPass.h`, membre `UnderwaterPass m_underwaterPass`, getter `GetUnderwaterPass()`.
- `engine/render/DeferredPipeline.cpp` — Init UnderwaterPass après WaterPass (non-fatal, WARN si shaders absents) ; Destroy() inclut `m_underwaterPass.Destroy(device)`.
- `CMakeLists.txt` — `engine/render/UnderwaterPass.cpp` ajouté.

### 2) COMMANDES WINDOWS À EXÉCUTER

```powershell
# Compiler les shaders (depuis game/data/shaders/)
glslangValidator -V underwater.vert -o underwater.vert.spv
glslangValidator -V underwater.frag -o underwater.frag.spv

# Build moteur
cmake --preset vs2022-x64-release
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\engine_app\lcdlln.exe -log
```

### 3) RÉSULTAT

- Compilation : **NON TESTÉ** (Visual Studio / Vulkan SDK absents de l'environnement)
- Exécution   : **NON TESTÉ**

### 4) VALIDATION DoD

| Critère | Statut |
|---|---|
| Aucun dossier racine non autorisé | ✅ |
| Code moteur sous `/engine` | ✅ |
| Contenu sous `/game/data` (shaders) | ✅ |
| Portée strictement M37.3 | ✅ |
| Aucun chemin absolu hardcodé | ✅ |
| Underwater detection (`IsUnderwater(cameraY, waterY)`) | ✅ |
| Blue tint (`color *= vec3(0.5, 0.7, 1.0)` via fogColor) | ✅ |
| Fog exponentiel (`exp(-density * depth)`) | ✅ |
| Blur vignette optionnel (`vignetteStrength`) | ✅ |
| Logs Init/Destroy conformes | ✅ |
| DeferredPipeline : intégration non-fatale | ✅ |
| Build testé | NON TESTÉ |
