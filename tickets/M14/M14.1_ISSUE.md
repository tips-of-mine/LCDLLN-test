# M14.1 — Combat serveur: validation + damage + events

**Status:** Closed

## Ticket (contenu)

# M14.1 — Combat serveur: validation + damage + events

## Objectif
Implémenter combat minimal authoritative : AttackRequest, validation, dégâts, CombatEvent.

## Dépendances
- M13.3

## Livrables
- AttackRequest msg
- Server combat system
- CombatEvent broadcast

## Spécification technique
- MVP hit=always; range check; cooldown simple.

## Étapes d'implémentation
1. Ajouter composants Stats/Combat aux entités.
2. Handler AttackRequest: vérifier cible + range + cooldown.
3. Appliquer damage, HP<=0 -> death state.
4. Broadcast CombatEvent aux clients intéressés.

## Critères de validation (DoD)
- [ ] Un joueur peut attaquer un mob et voir HP descendre côté client.

## Notes
- Pas de prédiction nécessaire au MVP.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- engine/network/Combat.h
- engine/network/Combat.cpp

**Modifiés :**
- engine/network/Protocol.h (MsgType AttackRequest = 7, CombatEvent = 8)
- CMakeLists.txt (Combat.cpp dans engine_network)
- main_server.cpp (EntityCombat, Mob, entityCombat map, 1 mob zone 0 à (50,0,50), handler AttackRequest: cible + range + cooldown, dégâts, death, broadcast CombatEvent à tous les clients; mobs ajoutés aux zoneGrids)
- main_client.cpp (Combat.h, entityHp, réception CombatEvent, envoi AttackRequest périodique vers mob 1000, affichage mob_hp dans les stats; position initiale (48,0,50) pour être à portée du mob)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- cmake --preset win64
- cmake --build --preset win64
- .\build\server_app.exe [content_path]
- .\build\client_app.exe [host]

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- AttackRequest (attackerId, targetId) et CombatEvent (attackerId, targetId, damage, targetHpRemaining, targetIsDead); serveur: composants Stats/Combat (EntityCombat: hp, maxHp, lastAttackTick, isDead), un mob de test, validation cible + range 5m + cooldown 20 ticks, dégâts 10, HP<=0 → death, broadcast CombatEvent à tous les clients; client envoie AttackRequest vers le mob et affiche mob_hp qui descend.
