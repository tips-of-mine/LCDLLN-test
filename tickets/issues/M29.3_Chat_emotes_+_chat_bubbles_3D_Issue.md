# M29.3 — Chat emotes + chat bubbles 3D

**Status:** Closed

# M29.3_Chat_emotes_+_chat_bubbles_3D

## Objectif
Emotes textuelles (/dance, /laugh) et chat bubbles au-dessus personnages.

## Dépendances
- M29.1
- M25.3

## Livrables
- Emote commands (/dance, /wave, etc.)
- Animation playback pour emotes
- 3D chat bubbles (texte au-dessus tête)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Emotes: /dance, /wave, /laugh, /cry, /sit, etc.
- Animation play once (ou loop si /sit)
- Chat bubbles: durée 3-5s, fade out
- Billboard texte face caméra

## Étapes d'implémentation
1. Emote command: trigger animation state (M25.3)
2. Broadcast EmoteEvent(playerId, emoteId) aux clients proches
3. Clients: play animation correspondante
4. Chat bubble: on say/yell, spawn billboard text au-dessus tête
5. Render text 3D (world space UI ou shader)
6. Auto-destroy après timeout

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Limiter bubbles actives (max 10 visible)
- Culling bubbles hors screen

---

## Rapport final (agent)

### 1) FICHIERS

**Créés :**
- `engine/net/ChatEmotes.h` / `engine/net/ChatEmotes.cpp`
- `engine/client/ChatWorldVisualPresenter.h` / `engine/client/ChatWorldVisualPresenter.cpp`
- `tickets/issues/M29.3_Chat_emotes_+_chat_bubbles_3D_Issue.md`

**Modifiés :**
- `CMakeLists.txt`
- `engine/server/CMakeLists.txt`
- `engine/server/ServerProtocol.h` / `engine/server/ServerProtocol.cpp`
- `engine/server/ServerApp.h` / `engine/server/ServerApp.cpp`
- `engine/client/UIModel.h` / `engine/client/UIModel.cpp`

**Supprimés :** *(aucun)*

### 2) COMMANDES WINDOWS

```bat
cmake --preset vs2022-x64-release
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64\Release\pkg\game\lcdlln.exe
.\build\vs2022-x64\Release\pkg\server\lcdlln_server.exe -log -console
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (`cmake` absent sur l’agent)
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

**NON** — build/run non exécutés ici.

**Périmètre livré (conforme ticket) :**
- Emotes `/dance`, `/wave`, `/laugh`, `/cry`, `/sit`, `/bow`, `/point`, `/salute` : parsing `ChatEmotes`, diffusion **EmoteRelay** (proximité Say), `UIModelBinding::ApplyEmoteRelay`, état `activeEmotes` + durées one-shot / loop pour `/sit`.
- Bulles Say/Yell : file d’attente max **10**, durée **4 s**, fade **3–4 s**, frustum cull, projection billboard (NDC + monde) via `TickChatWorldVisuals` (appelant fournit caméra / viewProj / viewport).
- Pas de nouveau contenu sous `/game/data` ; pas de chemins absolus.
- **Note dépendance M25.3** du ticket : dans le backlog, M25.3 = pooling réseau ; la « lecture animation » côté client est préparée par `emoteWireId` (branchement futur graphe d’anim).

