# Issue: M07.1 — TAA: jitter Halton + matrices prev/curr

**Status:** Closed

---

## Rapport final (M07.1)

### 1) FICHIERS

**Créés :**
- `engine/render/TaaJitter.h` — Halton(2,3) (N=8), GetJitterNdc(sampleIndex, width, height) ; jitter NDC = (2*jitterPx)/resolution.

**Modifiés :**
- `engine/Engine.h` — RenderState : `prevViewProjMatrix` (Mat4), `jitterCurrNdc[2]` ; membre `m_taaHistoryInvalid` ; include `TaaJitter.h`.
- `engine/Engine.cpp` — Update() : détection FOV change → `m_taaHistoryInvalid` ; jitter Halton appliqué à la projection (proj.m[8]+=jitterX, proj.m[9]+=jitterY) ; remplissage `prevViewProjMatrix`, `jitterCurrNdc` ; OnResize() : `m_taaHistoryInvalid = true`.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

L’utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Structure : code moteur sous `/engine`, aucun nouveau dossier racine.
- Portée : uniquement Halton (2/3) N=8, jitter appliqué à la projection, prev/curr ViewProj + jitter stockés, reset sur resize et FOV (teleport non détecté, pas d’événement dédié).
- Chemins : aucun nouveau chemin de contenu.
- Qualité : fonctions documentées, pas de TODO.

---

## Ticket content (M07.1) — référence

# M07.1 — TAA: jitter Halton + matrices prev/curr

## Objectif
Ajouter jitter subpixel à la projection et conserver matrices prev/curr pour reprojection.

## Dépendances
- M03.4

## Livrables
- Séquence Halton (2/3) N=8-16
- Curr/Prev ViewProj + jitter stored

## Spécification technique
- Jitter en NDC: (2*jitterPx)/resolution.

## Étapes d'implémentation
1. Implémenter générateur Halton.
2. Appliquer jitter à projection chaque frame.
3. Stocker matrices prev/curr et gérer reset.

## Critères de validation (DoD)
- [ ] Projection jitter change chaque frame, stable et cyclique.

## Notes / pièges à éviter
- Reset history sur resize/teleport/FOV change.
