# Issue: M17.3 — Deferred decals MVP: albedo-only projection

**Status:** Closed

---

## Rapport final (M17.3)

### 1) FICHIERS

**Créés :**
- `engine/render/DecalSystem.h`
- `engine/render/DecalSystem.cpp`
- `engine/render/DecalPass.h`
- `engine/render/DecalPass.cpp`
- `game/data/shaders/decal.vert`
- `game/data/shaders/decal.frag`
- `tickets/issues/M17.3_Deferred_decals_MVP_albedo-only_projection_Issue.md`

**Modifiés :**
- `CMakeLists.txt`
- `engine/Engine.h`
- `engine/Engine.cpp`
- `engine/render/DeferredPipeline.h`
- `engine/render/DeferredPipeline.cpp`
- `engine/render/LightingPass.h`
- `engine/render/LightingPass.cpp`
- `game/data/shaders/lighting.frag`

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64\pkg\game\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

Détail environnement :
- `cmake` n’est pas disponible dans l’environnement de travail courant, donc la configuration / compilation locale n’a pas pu être lancée ici.
- Les diagnostics IDE sur les fichiers modifiés ne remontent pas d’erreur.
- La passe decals charge `shaders/decal.vert.spv` et `shaders/decal.frag.spv` au runtime ; si votre workflow ne les génère pas automatiquement, il faudra les précompiler localement avant validation visuelle.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Le scope demandé par `M17.3` a été implémenté au minimum via :
  - composant decal avec volume cube, texture albedo, lifetime et fade
  - système runtime avec tri par distance et culling
  - passe deferred decals entre geometry et lighting
  - écriture albedo dans un overlay séparé, puis composition dans `LightingPass`
- La validation complète DoD reste incomplète car :
  - la compilation n’a pas pu être exécutée localement sans `cmake`
  - l’exécution n’a pas pu être validée localement
  - le critère visuel `Impact laisse une marque au sol/mesh` n’a donc pas pu être confirmé ici

---

## Ticket content (M17.3) — référence

# M17.3 — Deferred decals MVP: albedo-only projection

## Objectif
Projeter decals en deferred (au moins albedo) pour impacts.

## Dépendances
- M03.1

## Livrables
- Decal component
- Decal pass writing GBufferA (or separate)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Volume cube + projection; lifetime + fade.

## Étapes d’implémentation
1. Définir decal volume + material.
2. Pass decals après geometry avant lighting.
3. Écrire albedo (option roughness).

## Critères de validation (DoD)
- [ ] Impact laisse une marque au sol/mesh.

## Notes / pièges à éviter
- Gérer sorting decals et distance culling.
