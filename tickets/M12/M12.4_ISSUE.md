# M12.4 — Export layout.json + chunk overlay debug

**Status:** Open

## Objectif
Exporter layout.json versionné et afficher overlay grille chunks/cells.

## Dépendances
- M12.2
- M12.3
- M11.2

## Livrables
- layout.json export
- Overlay chunks 256m + cells 64m

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- layout.json conforme schema, stable ordering pour diff git.

## Étapes d'implémentation
1. Serializer instances/volumes/spawns/transitions.
2. Écrire fichier avec tri par id/type.
3. Overlay debug (toggle) : chunk grid + ring highlight.

## Critères de validation (DoD)
- [x] Builder consomme layout exporté sans erreurs.

## Notes / pièges à éviter
- Maintenir compat version; migrer si schema change.

---

**Status:** Closed

---

## Rapport final M12.4

### 1) FICHIERS

**Créés :**
- `engine/world/LayoutExport.h` — Déclaration WriteLayoutJson, kLayoutJsonVersion.
- `engine/world/LayoutExport.cpp` — Export layout.json : instances triées (assetId, index), volumes triés (type, index) ; schema version + instances (guid, position, mesh, material, assetId) + volumes.
- `tickets/M12/M12.4_ISSUE.md` — Issue de suivi (ouverte puis fermée avec ce rapport).

**Modifiés :**
- `engine/core/Engine.h` — m_editorExportLayoutRequested.
- `engine/core/Engine.cpp` — Include LayoutExport.h ; traitement export layout (WriteLayoutJson sur m_editorExportLayoutRequested) ; passage outExportLayoutRequested et cameraPosition à DrawPanels.
- `engine/editor/EditorUI.h` — DrawPanels : paramètres outExportLayoutRequested, cameraPosition[3].
- `engine/editor/EditorUI.cpp` — Include World.h ; bouton « Export layout » (panneau Volumes) ; panneau « Debug overlay » avec checkbox « Chunk grid (256m + 64m cells) » ; overlay dessin grille chunks 256m, cells 64m, et surbrillance ring Active (WorldToChunkCoord, ChunkBounds, GetChunksForRing).
- `tools/zone_builder.cpp` — LayoutInstance : useExplicitAssetId, explicitAssetId ; lecture optionnelle « assetId » dans LoadLayoutJson ; rec.assetId = useExplicitAssetId ? explicitAssetId : ResolveMeshToAssetId(...).
- `CMakeLists.txt` — Ajout engine/world/LayoutExport.cpp au target engine_core.

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe --editor` (export layout depuis panneau Volumes ; overlay via Debug overlay)
- `.\build\zone_builder.exe <layout.json> <scene.gltf|scene.glb> [outputDir]` (vérifier que le layout exporté est consommé sans erreur)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l’environnement).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI.
- Structure repo respectée. Chemins relatifs (layout.json sous zone base). Portée limitée au ticket (export layout, overlay chunks/cells). Builder accepte layout avec optionnel assetId. Fonctions documentées.
