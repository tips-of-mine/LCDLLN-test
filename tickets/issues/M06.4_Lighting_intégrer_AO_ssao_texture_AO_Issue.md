# Issue: M06.4 — Lighting: intégrer AO (ssao * texture AO)

**Status:** Closed

---

## Rapport final (M06.4)

### 1) FICHIERS

**Créés :** aucun

**Modifiés :**
- `engine/render/LightingPass.h` — Record() : ajout du paramètre `ResourceId idSsaoBlur` ; commentaire descriptor set 8 samplers (SSAO_Blur).
- `engine/render/LightingPass.cpp` — Layout 8 bindings, pool 8×maxFrames, Record() récupère la vue SSAO_Blur et met à jour le binding 7.
- `game/data/shaders/lighting.frag` — binding 7 `ssaoTex` (SSAO_Blur), `ao_final = ao_ssao * ao_tex`, ambient multiplié par `ao_final`.
- `engine/Engine.cpp` — Pass Lighting : `b.read(m_fgSsaoBlurId, ...)` ; appel `Record(..., m_fgSsaoBlurId, ...)`.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

L’utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Structure : code moteur sous `/engine`, contenu shaders sous `/game/data` ; aucun nouveau dossier racine.
- Portée : uniquement lecture SSAO_Blur et combinaison AO_ssao × AO_tex pour l’ambient ; pas de modification de la lumière directe.
- Chemins : aucun nouveau chemin ; shaders existants sous `game/data/shaders`.
- Qualité : code et commentaires cohérents.

---

## Ticket content (M06.4) — référence

# M06.4 — Lighting: intégrer AO (ssao * texture AO)

## Objectif
Multiplier l'ambient (diffuse IBL) par AO final.

## Dépendances
- M06.3
- M05.4

## Livrables
- Lighting lit SSAO_Blur et combine avec AO ORM.

## Spécification technique
- AO n'affecte pas la lumière directe (au début).

## Étapes d'implémentation
1. Ajouter sampler SSAO à lighting pass.
2. AO_final = AO_ssao * AO_tex.
3. ambient *= AO_final.

## Critères de validation (DoD)
- [ ] Contacts plus ancrés sans assombrir toute la scène.

## Notes / pièges à éviter
- Plus tard: bent normals / GTAO si besoin.
