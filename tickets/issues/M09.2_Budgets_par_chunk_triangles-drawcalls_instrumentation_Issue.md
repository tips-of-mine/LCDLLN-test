# Issue: M09.2 — Budgets par chunk (triangles/drawcalls) + instrumentation

**Status:** Closed

---

## Rapport final (M09.2)

### 1) FICHIERS

**Créés :**
- `engine/world/ChunkBudgetStats.h` — RingStats (drawcalls, triangles) ; ChunkBudgetStats : Init(config), RecordDraw(chunkId, ring, drawcallCount, triangleCount), ResetPerFrame(), GetRingStats/GetBudgetDrawcalls, LogStats().
- `engine/world/ChunkBudgetStats.cpp` — Init lit world.budget_active_drawcalls (1000), world.budget_visible_drawcalls (300), world.budget_far_drawcalls (50) ; RecordDraw accumule par ring ; LogStats émet LOG_INFO par ring avec budget et indicateur OVER si dépassement.

**Modifiés :**
- `engine/world/WorldModel.h` — GetRingForChunk(ChunkCoord) const.
- `engine/world/WorldModel.cpp` — Implémentation GetRingForChunk (distance à m_lastCenterChunk, mêmes rayons Active/Visible/Far).
- `engine/Engine.h` — include ChunkBudgetStats.h ; membre m_chunkStats.
- `engine/Engine.cpp` — Init m_chunkStats dans le ctor ; BeginFrame : m_chunkStats.ResetPerFrame() ; dans la lambda geometry pass : WorldToChunkCoord(camera.position), GetRingForChunk(chunk), RecordDraw(chunk, ring, 1, indexCount/3) ; idem dans chaque lambda shadow pass (4 cascades) ; EndFrame : toutes les 60 frames LogStats().
- `CMakeLists.txt` — ajout de engine/world/ChunkBudgetStats.cpp à engine_core.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l'environnement).
- **Exécution :** NON TESTÉ

L'utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Build : commandes listées.
- Structure : code uniquement sous `/engine` (engine/world/).
- Portée : uniquement M09.2 (stats per chunk/ring, cvars budgets, compteurs dans le renderer, association draw→chunk/ring, affichage stats en log).
- Chemins : aucun contenu ajouté ; pas de chemins absolus.
- Qualité : fonctions documentées ; logs cohérents.

---

## Ticket content (M09.2) — référence

# M09.2 — Budgets par chunk (triangles/drawcalls) + instrumentation

## Objectif
Définir budgets et instrumenter pour mesurer triangles/drawcalls par ring/chunk.

## Dépendances
- M03.1
- M09.1

## Livrables
- Stats per chunk/ring
- Cvars budgets

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Active: <~1000 drawcalls, Visible <300, Far <50 (cibles).

## Étapes d'implémentation
1. Ajouter compteurs drawcalls/triangles dans renderer.
2. Associer draws à chunk id (tag).
3. Afficher stats debug overlay/log.

## Critères de validation (DoD)
- [ ] Les stats s'affichent et varient selon densité.

## Notes / pièges à éviter
- Budgets seront ajustés après premiers tests.
