# M13.4 — Zone transitions: server authoritative + client preload hook

**Status:** Closed

## Ticket (contenu)

# M13.4 — Zone transitions: server authoritative + client preload hook

## Objectif
Permettre changement de zone via volume transition, validation serveur, respawn position.

## Dépendances
- M13.3
- M11.2

## Livrables
- Message ZoneChange
- Server zone assignment
- Client clears + requests spawns

## Spécification technique
- Client ne décide pas la zone; serveur valide.

## Étapes d'implémentation
1. Serveur: détecter entrée volume transition.
2. Envoyer ZoneChange (zoneId + spawn pos).
3. Client: reset streaming, précharger nouvelle zone, reset TAA history.
4. Serveur: recalculer interest set dans nouvelle zone.

## Critères de validation (DoD)
- [ ] Passer zone_001 -> zone_002 sans crash; entités se resynchronisent.

## Notes
- Masquer transition par tunnel/brume côté level design.

---

## Rapport final

### 1) FICHIERS

**Créés :** —

**Modifiés :**
- engine/network/Protocol.h (MsgType ZoneChange = 6)
- engine/network/Replication.h (SerializeZoneChange, ParseZoneChange)
- engine/network/Replication.cpp (implémentation ZoneChange)
- engine/streaming/StreamingScheduler.h (ClearAllQueues)
- engine/streaming/StreamingScheduler.cpp (implémentation ClearAllQueues)
- engine/core/Engine.h (OnZoneChange, m_zonePathOverride)
- engine/core/Engine.cpp (zone load avec m_zonePathOverride, OnZoneChange: clear streaming, preload zone, TAA reset)
- engine/world/VolumeFormat.h (PointInVolume)
- engine/world/VolumeFormat.cpp (PointInVolume box/sphere)
- main_server.cpp (chargement volumes par zone, clientZoneIds, détection transition, envoi ZoneChange, CellGrid par zone)
- main_client.cpp (réception ZoneChange, clear entities, mise à jour position/spawn)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- cmake --preset win64
- cmake --build --preset win64
- .\build\server_app.exe [content_path]
- .\build\client_app.exe [host]
- engine_app: appel possible à Engine::OnZoneChange(zoneId, spawnPos) lorsque le client réseau reçoit ZoneChange (reset streaming, préchargement zone, reset TAA).

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Message ZoneChange (zoneId + spawn pos); serveur assigne la zone et envoie ZoneChange à l’entrée d’un volume ZoneTransition; client clear + mise à jour position (main_client); Engine::OnZoneChange fait reset streaming, préchargement nouvelle zone (m_zonePathOverride + m_zoneBuildLoaded=false), reset TAA; serveur recalcule interest set par zone (CellGrid par zoneId). Passer zone_001 -> zone_002 sans crash et resync entités est couvert par le flux implémenté.
