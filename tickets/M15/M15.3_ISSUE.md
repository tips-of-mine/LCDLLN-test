# M15.3 — Dynamic events: phases + rewards + notifications

**Status:** Closed

## Contenu du ticket
- **Objectif:** Système événements dynamiques par zone avec phases (waves/boss) et rewards.
- **Dépendances:** M15.2, M16.1
- **Livrables:** Event JSON, EventState server, EventState msg client
- **Spécification technique:** Triggers time/random; phases séquentielles; cooldown.
- **Étapes:** 1) Définir schema event. 2) Server: scheduler trigger + state machine phases. 3) Spawn waves via spawner-like API. 4) Client: notifier UI (texte + progression).
- **DoD:** Un event se déclenche, progresse, finit et récompense.
- **Notes:** Persistance event optionnelle (cooldown).

---

## RAPPORT FINAL

### 1) FICHIERS
- **Créés :**
  - engine/event/EventDef.h
  - engine/event/EventDef.cpp
  - engine/network/EventProtocol.h
  - engine/network/EventProtocol.cpp
  - game/data/zones/zone_000/events.json
- **Modifiés :**
  - CMakeLists.txt (engine_event, EventProtocol dans engine_network, server_app → engine_event)
  - engine/network/Protocol.h (MsgType::EventState = 14)
  - main_server.cpp (chargement events par zone, EventInstanceState, scheduler trigger time/random, state machine phases, spawn waves, rewards + InventoryDelta, envoi EventState)
  - main_client.cpp (EventProtocol, handler EventState + affichage texte + progression)
- **Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER
- cmake --preset win64
- cmake --build --preset win64
- .\build\server_app.exe
- .\build\client_app.exe 127.0.0.1 [character_id]

### 3) RÉSULTAT
- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD
- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Structure respectée, chemins relatifs via contentPath, pas de dérive hors ticket. Event se déclenche (time/random), progresse par phases, spawn waves, finit et récompense (rewards + notification EventState).
