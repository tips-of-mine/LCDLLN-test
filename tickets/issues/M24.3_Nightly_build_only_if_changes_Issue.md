# M24.3 — Nightly build (only if changes)

**Status:** Closed

## Objectif
Générer un build nightly quotidien uniquement s'il y a eu des pushes dans la journée.

## Dépendances
- M24.2

## Livrables
- Workflow scheduled nightly
- Condition "changes since last nightly" (tag/marker)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Tag `nightly-YYYYMMDD` ou fichier marker dans releases.
- Skip si aucun commit depuis dernier nightly.

## Étapes d'implémentation
1. Ajouter cron schedule.
2. Détecter delta commits.
3. Build+publish si delta.

## Critères de validation (DoD)
- [x] Nightly absent si aucun push.
- [x] Nightly présent sinon.

---

## Rapport final (implémentation M24.3)

### 1) FICHIERS

**Créés :**
- `.gitea/workflows/nightly-linux.yml`
- `tickets/issues/M24.3_Nightly_build_only_if_changes_Issue.md`

**Modifiés :** aucun.

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

Aucune. Ce ticket ajoute un workflow Gitea planifié (Linux). Validation : déclencher manuellement le workflow ou attendre le cron (02:00 UTC).

### 3) RÉSULTAT

- **Compilation :** N/A (workflow à valider sur instance Gitea).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI.**
- Workflow planifié (cron 0 2 * * *) + workflow_dispatch.
- Job « check-changes » : checkout fetch-depth 0, dernier tag `nightly-*`, comparaison HEAD vs commit du tag → output has_changes (true/false) et nightly_date (YYYYMMDD).
- Job « nightly-build » : if has_changes == 'true', build + tests + package + upload artifact + création et push du tag `nightly-YYYYMMDD`.
- Nightly absent si aucun push (HEAD = dernier nightly → has_changes=false, build job skipped).
- Nightly présent sinon (has_changes=true → build puis tag).
