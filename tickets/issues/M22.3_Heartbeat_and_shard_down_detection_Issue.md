# M22.3 — Heartbeat system + shard down detection

**Status:** Closed

## Contenu du ticket (référence)

- **Objectif** : Heartbeat périodique shards→master et détection shard down fiable.
- **Livrables** : Heartbeat interval/timeout (config), Master watchdog timeout => mark offline, Log + event interne "shard_down".
- **Spec** : Heartbeat payload shard_id, current_load, timestamp ; timeout ex 90s ; Master autoritaire.

---

## Rapport final d’implémentation

### 1) FICHIERS

**Créés :**
- `engine/server/ShardDownDetectionTests.cpp`

**Modifiés :**
- `engine/server/ShardRegistry.h` — callback shard_down, `EvictStaleHeartbeats(timeout, as_of)` optionnel pour tests
- `engine/server/ShardRegistry.cpp` — log `[ShardRegistry] shard_down (shard_id=…)`, appel du callback, paramètre `as_of`
- `engine/server/main_server_linux.cpp` — `shard.heartbeat_timeout_sec` défaut 90, `SetShardDownCallback` (log ServerMain)
- `engine/network/ShardToMasterClient.h` — `SetHeartbeatIntervalSec(int)`, membre `m_heartbeat_interval_sec` (défaut 10)
- `engine/network/ShardToMasterClient.cpp` — envoi du timestamp dans le heartbeat, utilisation de `m_heartbeat_interval_sec`
- `CMakeLists.txt` — cible et test `shard_down_detection_tests`

*(Payload heartbeat avec timestamp : `engine/network/ShardPayloads.h` / `ShardPayloads.cpp` déjà adaptés en amont pour ce ticket.)*

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset x64-debug
cmake --build --preset x64-debug
ctest --preset x64-debug
```

Pour lancer uniquement le test shard down :

```bat
.\build\bin\shard_down_detection_tests.exe
```

Serveur Linux (si build Unix) :

```bat
.\build\pkg\server\lcdlln_server
```

### 3) RÉSULTAT

- **Compilation** : NON TESTÉ (cmake non disponible dans l’environnement).
- **Exécution** : NON TESTÉ.

L’utilisateur valide localement avec les commandes ci-dessus.

### 4) VALIDATION DoD

- **A) Build & exécution** : Commandes listées ; compilation/exécution à valider en local.
- **B) Structure** : Aucun nouveau dossier racine ; code sous `/engine`.
- **C) Portée** : Uniquement livrables M22.3 (heartbeat interval/timeout, watchdog, log + event shard_down, test).
- **D) Chemins** : Aucun chemin absolu ; pas de contenu ajouté sous `/game/data`.
- **E) Qualité** : LOG_INFO sur init/destroy et chemins critiques ; pas de Vulkan.
- **F) Rapport** : Présent dans cette Issue.

→ **OUI** (sous réserve de validation build/run en local).
