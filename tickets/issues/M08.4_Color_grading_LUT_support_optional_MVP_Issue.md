# Issue: M08.4 — Color grading: LUT support (optional MVP)

**Status:** Closed

---

## Rapport final M08.4

### 1) FICHIERS

- **Créés :**
  - tickets/issues/M08.4_Color_grading_LUT_support_optional_MVP_Issue.md

- **Modifiés :**
  - engine/render/TonemapPass.h (TonemapParams.strength, Record avec lutView)
  - engine/render/TonemapPass.cpp (layout 2 bindings, pool, push 8 bytes, Record avec LUT)
  - game/data/shaders/tonemap.frag (texLUT, push strength, LUT strip 256×16 + mix)
  - engine/Engine.h (m_colorGradingLutHandle)
  - engine/Engine.cpp (chargement LUT depuis color_grading.lut_path, tonemap callback avec enable/strength et lutView)

- **Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset <preset>
cmake --build --preset <preset>
.\build\engine_app.exe
```

(Remplace `<preset>` par le preset utilisé, ex. `win-x64-release`.)

Config optionnelle (ex. dans `config.json` ou CLI) :
- `color_grading.lut_path` : chemin relatif à `paths.content` vers une LUT .texr (strip 256×16).
- `color_grading.enable` : true/false pour activer la LUT.
- `color_grading.strength` : 0..1, force du blend LUT.

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (environnement sans VS/Ninja/Vulkan/vcpkg).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI (sous réserve de validation build/run locale).
- Structure : pas de nouveau dossier racine ; moteur sous `/engine`, contenu sous `/game/data` ; chemins relatifs via `paths.content`.
- Portée : uniquement LUT optionnel après tonemap, enable/strength, pas de refactor hors ticket.
- LUT optionnel : si `color_grading.lut_path` vide ou chargement échoue, `lutView = VK_NULL_HANDLE` et strength = 0.

---

## Ticket content (M08.4) — référence

# M08.4 — Color grading: LUT support (optional MVP)

## Objectif
Support LUT (3D ou 2D strip) appliqué après tonemap.

## Dépendances
- M03.4

## Livrables
- Chargement LUT texture + application shader.

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- LUT 32^3 ou strip 256x16; param enable/strength.

## Étapes d'implémentation
1. Charger LUT texture (sRGB/linear selon format).
2. Dans tonemap pass, appliquer LUT sur couleur LDR.
3. Expose strength.

## Critères de validation (DoD)
- [ ] Changement LUT modifie look global (test 2 LUTs).

## Notes / pièges à éviter
- Garder LUT optionnel si tu veux avancer vite.
