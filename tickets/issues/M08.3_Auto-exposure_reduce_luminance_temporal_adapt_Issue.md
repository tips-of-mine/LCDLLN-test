# Issue: M08.3 — Auto-exposure: reduce luminance + temporal adapt

**Status:** Closed

---

## Rapport final (M08.3)

### 1) FICHIERS

**Créés :**
- `engine/render/AutoExposure.h` — AutoExposure class: luminance buffer, staging, persistent exposure buffer, Init/Record/Update/GetExposure/Destroy.
- `engine/render/AutoExposure.cpp` — Compute pipeline (image + storage buffer), reduction 64x64 log(L), copy to staging; Update: readback, log-avg, target = key/log_avg, exposure = lerp(prev, target, 1-exp(-dt*speed)), write exposure buffer.
- `game/data/shaders/luminance_reduce.comp` — Compute: sample HDR on grid, write log(L+eps) to buffer.

**Modifiés :**
- `CMakeLists.txt` — ajout de `engine/render/AutoExposure.cpp`.
- `engine/Engine.h` — include AutoExposure.h, membre `m_autoExposure`.
- `engine/Engine.cpp` — chargement luminance_reduce.comp, init `m_autoExposure` ; pass FG "AutoExposure_Luminance" (read SceneColor_HDR_WithBloom, execute Record) ; après vkWaitForFences appel `Update(device, dt, key, speed)` avec config exposure.key (0.18) / exposure.speed ; Tonemap utilise `GetExposure()` quand auto-exposure valide ; `m_autoExposure.Destroy` au shutdown.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

L’utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Structure : code sous `/engine`, shader sous `/game/data/shaders`.
- Portée : M08.3 uniquement (compute reduce luminance → 1 value, buffer exposure persistant, param key/speed, exposure dans tonemap).
- Chemins : shader via paths.content.

---

## Ticket content (M08.3) — référence

# M08.3 — Auto-exposure: reduce luminance + temporal adapt

## Objectif
Calculer exposure dynamique (log-average) via compute reduction et lissage temporel.

## Dépendances
- M08.2

## Livrables
- Compute pass reduce luminance -> 1 value
- Persistent buffer exposure
- Param key/speed

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- exposure = lerp(prev, target, 1-exp(-dt*speed)); key=0.18.

## Étapes d'implémentation
1. Compute luminance buffer (downsample) ou direct reduction.
2. Calcul log-average, targetExposure.
3. Adapter temporellement et stocker.
4. Utiliser exposure dans tonemap.

## Critères de validation (DoD)
- [ ] Entrée/sortie zone sombre: adaptation progressive.

## Notes / pièges à éviter
- Plus tard: histogram percentile pour robustesse.
