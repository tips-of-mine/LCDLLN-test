# Issue: M09.5 — Runtime HLOD switching + culling

**Status:** Closed

---

## Rapport final (M09.5)

### 1) FICHIERS

**Créés :**
- `engine/world/HlodRuntime.h` — HlodRuntime (Init, ShouldUseHlod(distance), GetThresholdMeters, IsVisible frustum+distance) ; ChunkDrawDecision ; BuildChunkDrawList(...) → debug string + decisionsOut.
- `engine/world/HlodRuntime.cpp` — Init lit world.hlod_distance_threshold_m (200) ; IsVisible (frustum.TestAABB + distance à l’AABB) ; BuildChunkDrawList (par chunk: bounds 3D, distance, cull, useHlod, remplissage decisions + string "HLOD: X inst: Y culled: Z").

**Modifiés :**
- `engine/Engine.h` — include HlodRuntime.h ; RenderState.hlodDebugText (std::string) ; m_hlodRuntime, m_chunkDrawDecisions.
- `engine/Engine.cpp` — m_hlodRuntime.Init(m_cfg) ; dans Update(), après frustum : BuildChunkDrawList(GetPendingChunkRequests(), camera, frustum, m_hlodRuntime, maxDrawDistance, m_chunkDrawDecisions), out.hlodDebugText = résultat ; LOG_DEBUG toutes les 60 frames pour overlay.
- `CMakeLists.txt` — engine/world/HlodRuntime.cpp.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

Config optionnel : world.hlod_distance_threshold_m (défaut 200), world.max_draw_distance_m (0 = pas de cull distance).

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l'environnement).
- **Exécution :** NON TESTÉ

L'utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Build : commandes listées.
- Structure : code sous `/engine` uniquement.
- Portée : M09.5 uniquement (switch props→HLOD selon distance, culling CPU frustum+distance, build draw list decisions, overlay debug HLOD state).
- Chemins : pas de chemins absolus.
- Qualité : fonctions documentées ; culling stable (déterministe).

---

## Ticket content (M09.5) — référence

# M09.5 — Runtime HLOD switching + culling

## Objectif
Au-delà d'un seuil, rendre HLOD à la place des props individuels + culling frustum/distance.

## Dépendances
- M09.4
- M10.4

## Livrables
- Switch props->HLOD selon distance
- Culling CPU stable

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Seuil HLOD ~200m (base).

## Étapes d'implémentation
1. Dans build draw lists, choisir HLOD vs instances.
2. Culling bounding volumes.
3. Debug: overlay HLOD state.

## Critères de validation (DoD)
- [ ] Drawcalls chutent nettement quand on s'éloigne.

## Notes / pièges à éviter
- Occlusion culling plus tard (M18).
