# M17.3 — Deferred decals MVP: albedo-only projection

**Status:** Open

## Contenu du ticket (intégral)

# M17.3 — Deferred decals MVP: albedo-only projection

## Objectif
Projeter decals en deferred (au moins albedo) pour impacts.

## Dépendances
- M03.1

## Livrables
- Decal component
- Decal pass writing GBufferA (or separate)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Volume cube + projection; lifetime + fade.

## Étapes d'implémentation
1. Définir decal volume + material.
2. Pass decals après geometry avant lighting.
3. Écrire albedo (option roughness).

## Critères de validation (DoD)
- [ ] Impact laisse une marque au sol/mesh.

## Notes / pièges à éviter
- Gérer sorting decals et distance culling.

---

## Rapport final (M17.3)

**Status:** Closed

### 1) FICHIERS

**Créés :**
- `tickets/M17/M17.3_ISSUE.md`
- `engine/render/DecalComponent.h`
- `engine/render/DecalComponent.cpp`
- `engine/render/vk/VkDecalPass.h`
- `engine/render/vk/VkDecalPass.cpp`
- `engine/render/vk/VkDecalPipeline.h`
- `engine/render/vk/VkDecalPipeline.cpp`
- `game/data/shaders/decal.vert`
- `game/data/shaders/decal.frag`

**Modifiés :**
- `CMakeLists.txt` (sources DecalComponent, VkDecalPass, VkDecalPipeline)
- `engine/core/Engine.h` (membres decal: DecalList, VkDecalPass, VkDecalPipeline, cube VB/IB, albedo view)
- `engine/core/Engine.cpp` (CreateDecalCubeBuffers, init decal pass/pipeline/cube/texte, pass Decals dans le frame graph après Geometry, Update decals, shutdown, recreate pass au resize)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe` (ou exécutable produit par le preset)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake/outils de build non disponibles dans l’environnement).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Code moteur sous `/engine`, contenu sous `/game/data` (shaders). Chemins relatifs via `paths.content`. Decal component (volume + lifetime + fade), decal pass écrivant GBuffer A après geometry avant lighting, albedo seul (roughness non implémenté, optionnel). Pas de refactor hors scope. Fonctions documentées.
