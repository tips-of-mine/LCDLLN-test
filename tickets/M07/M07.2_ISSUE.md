# M07.2 — TAA: history ping-pong textures

**Status:** Closed

## Ticket (original)

### Objectif
Créer HistoryA/HistoryB et ping-pong par frame.

### Dépendances
- M07.1

### Livrables
- FG images HistoryA/HistoryB format LDR (ou HDR)
- Gestion indices prev/next

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- History format = input TAA format.

### Étapes d'implémentation
1. Créer 2 images history.
2. À chaque frame: prev=idx^1, next=idx.
3. Init history = current lors du premier frame.

### Critères de validation (DoD)
- [ ] Pas d'artefacts au démarrage; ping-pong correct.

### Notes / pièges à éviter
- Sur reset, copier current -> history next.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/render/vk/VkTaaHistory.h`
- `engine/render/vk/VkTaaHistory.cpp`
- `tickets/M07/M07.2_ISSUE.md` (issue + rapport)

**Modifiés :**
- `engine/core/Engine.h` — include VkTaaHistory ; membres `m_vkTaaHistory`, `m_fgTaaHistoryAId`, `m_fgTaaHistoryBId`, `m_taaHistoryIdx`, `m_taaFirstFrame`, `m_taaCopyHistoryOnReset`
- `engine/core/Engine.cpp` — init/shutdown VkTaaHistory ; FG HistoryA/HistoryB et passes TAA_CopyToHistoryA/B ; enregistrement registry ; swap `m_taaHistoryIdx` ; copy first frame + reset ; OnResize + Recreate + `m_taaCopyHistoryOnReset` ; Update `m_taaCopyHistoryOnReset` quand reset
- `CMakeLists.txt` — ajout `engine/render/vk/VkTaaHistory.cpp`

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset <preset>`
- `cmake --build --preset <preset>`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Structure repo respectée ; code moteur sous `/engine` ; pas de contenu sous `/engine` ; pas de chemins absolus ; portée limitée au ticket ; fonctions documentées.
