# M08.1 — Bloom: prefilter + downsample pyramid

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M08/M08.1_ISSUE.md`
- `engine/render/vk/VkBloomPyramid.h`
- `engine/render/vk/VkBloomPyramid.cpp`
- `engine/render/vk/VkBloomPrefilterPipeline.h`
- `engine/render/vk/VkBloomPrefilterPipeline.cpp`
- `engine/render/vk/VkBloomDownsamplePipeline.h`
- `engine/render/vk/VkBloomDownsamplePipeline.cpp`
- `game/data/shaders/bloom_prefilter.vert`
- `game/data/shaders/bloom_prefilter.frag`
- `game/data/shaders/bloom_downsample.vert`
- `game/data/shaders/bloom_downsample.frag`

**Modifiés :**
- `engine/core/Engine.h` (includes + membres bloom pyramid, prefilter, downsample, m_fgBloomMipId)
- `engine/core/Engine.cpp` (init pyramid + pipelines, FG ressources BloomMip0..4, passes Bloom_Prefilter + Bloom_Downsample_1..4, registry, OnResize, Shutdown)
- `CMakeLists.txt` (VkBloomPyramid, VkBloomPrefilterPipeline, VkBloomDownsamplePipeline)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l’environnement)
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Structure repo respectée (/engine, /game/data), pas de chemins absolus, chemins contenu via config. Portée limitée au ticket (BloomMip0..N, prefilter threshold+knee, downsample box/tent). Fonctions documentées.

---

## Ticket (original)

### Objectif
Extraire hautes lumières HDR et construire pyramide downsample (1/2..1/32).

### Dépendances
- M03.2

### Livrables
- BloomMip0..N textures
- Pass prefilter (threshold + knee)
- Downsample passes

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Threshold 1.0, knee 0.5 (defaults).

### Étapes d'implémentation
1. Créer mip textures (FG) selon extent.
2. Prefilter: sample HDR, soft threshold.
3. Downsample chain (box/tent).

### Critères de validation (DoD)
- [ ] Bloom mips remplis, pas de banding majeur.

### Notes / pièges à éviter
- Limiter N pour perf (jusqu'à 1/32 suffisant).
