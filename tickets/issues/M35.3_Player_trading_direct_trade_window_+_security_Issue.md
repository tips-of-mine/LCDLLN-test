# Issue: M35.3 — Player trading: direct trade window + security

**Status:** Closed

---

## Contenu du ticket

# M35.3_Player_trading_direct_trade_window_+_security

## Objectif
Échange direct player-to-player avec fenêtre trade, confirmation, anti-scam.

## Dépendances
- M35.1
- M16.3

## Livrables
- Trade request/accept
- Trade window UI (both players items + gold)
- Lock & confirm mechanism
- Trade logging (audit)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Trade flow:
  1. Player A requests trade
  2. Player B accepts
  3. Both add items/gold
  4. Both lock (ready)
  5. Both confirm (final)
  6. Server validates, swaps items atomically
- Lock: prevent adding more items (review phase)
- Confirm: final accept (irreversible)

## Étapes d'implémentation
1. /trade <name>: send TradeRequest
2. Accept: create Trade session (both players)
3. UI: split window (left=self, right=other)
4. Add items: drag&drop, update Trade session
5. Lock button: set ready flag
6. Both locked: enable Confirm button
7. Both confirm: server validates (items exist, not equipped), swap inventories
8. Log trade (player_trade_log)
9. On cancel: return items

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Scam prevention: show items clearly, 5s review after lock
- Range check: both players <10m

---

## Rapport final (M35.3)

### 1) FICHIERS

**Créés :**
- `engine/server/PlayerTradeSession.h` — structs `TradeSlot`, `TradeSession`, `TradePhase`, constantes (`kMaxTradeItemSlots`, `kTradeMaxRangeMeters`, `kTradeLockReviewTicks`), classe `PlayerTradeManager` (pending requests + sessions actives).
- `engine/server/PlayerTradeSession.cpp` — implémentation `PlayerTradeManager` (Init, Shutdown, AddPendingRequest, FindPendingRequestInitiator, CancelPendingRequest, CreateSession, FindSession, RemoveSession, CancelSessionsForClient).
- `tickets/issues/M35.3_Player_trading_direct_trade_window_+_security_Issue.md` — cette issue.

**Modifiés :**
- `engine/server/ServerProtocol.h` — ajout `MessageKind::TradeRequest..TradeResult` (52–62), `TradeResultCode`, structs wire (`TradeItemSlotWire`, `TradeOfferWire`, `TradeRequestMessage`, …, `TradeResultMessage`), déclarations Encode/Decode.
- `engine/server/ServerProtocol.cpp` — implémentation encode/decode pour tous les messages trade (avec helper `WriteTradeOffer`/`ReadTradeOffer`).
- `engine/server/ChatCommandParser.h` — ajout `ChatSlashCommandKind::Trade`, `TradeAccept`, `TradeDecline` + déclaration `ParseTradeTargetName`.
- `engine/server/ChatCommandParser.cpp` — parsing `/trade <name>`, `/trade accept`, `/trade decline` + implémentation `ParseTradeTargetName`.
- `engine/server/SecurityAuditLog.h` — ajout `LogTradeCompleted(...)`.
- `engine/server/SecurityAuditLog.cpp` — implémentation `LogTradeCompleted` (ligne TRADE_COMPLETED dans le fichier d'audit rotatif).
- `engine/server/ServerApp.h` — `#include PlayerTradeSession.h`, membres `PlayerTradeManager m_tradeManager`, déclarations handlers M35.3.
- `engine/server/ServerApp.cpp` — Init/Shutdown du trade manager, dispatch packets (TradeAccept..TradeConfirm), UpdateTradeSessions dans Simulate, gestion `/trade` dans HandleChatSlashCommand, cancel session au logout, implémentation complète des handlers (HandleTradeCommand, HandleTradeAcceptCommand, HandleTradeDeclineCommand, HandleTradeAccept, HandleTradeDecline, HandleTradeAddItem, HandleTradeRemoveItem, HandleTradeSetGold, HandleTradeLock, HandleTradeConfirm, BroadcastTradeUpdate, BroadcastTradeResult, CommitTrade, UpdateTradeSessions).
- `engine/server/CMakeLists.txt` — ajout `PlayerTradeSession.cpp` dans `server_app` WIN32.

### 2) COMMANDES WINDOWS À EXÉCUTER

```powershell
# Configurer
cmake --preset vs2022-x64-release

# Compiler
cmake --build --preset vs2022-x64-release

# Lancer (smoke test)
.\build\vs2022-x64-release\engine_app\lcdlln.exe -log

# Lancer le serveur de jeu
.\build\vs2022-x64-release\pkg\server\lcdlln_server.exe
```

### 3) RÉSULTAT

- Compilation : **NON TESTÉ** (pas de Visual Studio/Ninja disponible dans l'environnement CI)
- Exécution   : **NON TESTÉ**

### 4) VALIDATION DoD

| Critère | Statut |
|---|---|
| A) Build Windows OK via presets existants | NON TESTÉ (environnement sans VS) |
| A) Exécutable lancé, pas de crash | NON TESTÉ |
| B) Aucun dossier racine non autorisé | ✅ OUI — aucun dossier racine créé |
| B) Code moteur sous /engine | ✅ OUI — tous les fichiers sous engine/server/ |
| B) Contenu sous /game/data | ✅ OUI — aucun contenu créé |
| C) Aucune fonctionnalité hors scope | ✅ OUI — strictement M35.3 |
| C) Aucun refactor global | ✅ OUI |
| D) Aucun chemin absolu hardcodé | ✅ OUI |
| E) Logs init/destroy sur chaque module | ✅ OUI — PlayerTradeManager Init/Shutdown, handlers |
| E) Logs chemins critiques | ✅ OUI — WARN sur rejet, INFO sur création/commit, FATAL absent (aucune condition irrécupérable) |
| F) Rapport final présent | ✅ OUI — cette section |
