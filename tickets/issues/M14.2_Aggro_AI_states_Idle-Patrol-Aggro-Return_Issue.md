# Issue: M14.2 — Aggro + AI states (Idle/Patrol/Aggro/Return)

**Status:** Closed

---

## Rapport final (M14.2)

### 1) FICHIERS

**Créés :**
- `tickets/issues/M14.2_Aggro_AI_states_Idle-Patrol-Aggro-Return_Issue.md`

**Modifiés :**
- `engine/server/ServerApp.h` — ajout des états IA mob `Idle/Patrol/Aggro/Return`, de la table de menace et des helpers serveur pour aggro, leash et retour.
- `engine/server/ServerApp.cpp` — boucle IA limitée à 10 Hz configurable, mise à jour de la menace à chaque `CombatEvent`, poursuite/attaque du joueur cible, leash/return et reset de la menace.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\engine_app\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (`cmake` indisponible dans l'environnement)
- **Exécution :** NON TESTÉ (`cmake` indisponible dans l'environnement)

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Build/exécution non validés localement car `cmake` n'est pas disponible dans l'environnement.
- Le ticket est implémenté côté serveur avec état IA, menace, aggro, poursuite, attaque et reset hors leash.
- La partie "pathfind vers target (navmesh) si dans chunks actifs" n'a pas pu être reliée à une API runtime navmesh explicite dans la surface de code serveur actuellement présente ; l'implémentation utilise le déplacement autoritatif existant via la partition spatiale du serveur.

---

## Ticket content (M14.2) — référence

# M14.2 — Aggro + AI states (Idle/Patrol/Aggro/Return)

## Objectif
IA mobs basique + threat table pour cibler joueur.

## Dépendances
- M11.3
- M14.1

## Livrables
- AI state machine
- Threat table
- Leash/return

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Threat += damage; target = max threat; leash distance configurable.

## Étapes d’implémentation
1. Définir states + transitions.
2. Mettre à jour threat à chaque CombatEvent.
3. Pathfind vers target (navmesh) si dans chunks actifs.
4. Return/reset threat hors leash.

## Critères de validation (DoD)
- [ ] Mob agro, poursuit, attaque, puis reset s'il est kite trop loin.

## Notes / pièges à éviter
- Limiter update rate IA (ex 10Hz) pour perf.
