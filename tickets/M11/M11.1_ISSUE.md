# M11.1 — Zone Builder: import glTF + layout.json

**Status:** Closed

## Ticket (original)

### Objectif
Outil CLI qui lit glTF (assets) + layout.json (placements) pour construire une zone.

### Dépendances
- M00.4

### Livrables
- `tools/zone_builder`
- Parser layout.json
- Importer glTF (tinygltf)

### Spécification technique
- layout.json versionné; positions en mètres; GUID instances.

### Étapes d'implémentation
1. Créer exécutable tool, parsing args.
2. Charger layout.json, valider schema minimal.
3. Charger glTF references (meshes/materials).
4. Émettre liste instances + assets.

### Critères de validation (DoD)
- [ ] Tool charge une zone simple et liste instances/meshes.

### Notes
- Ne pas faire FBX au début; glTF d'abord.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M11/M11.1_ISSUE.md`
- `tools/zone_builder.cpp`

**Modifiés :**
- `CMakeLists.txt` (FetchContent TinyGLTF v2.6.3, exécutable `zone_builder`, lien tinygltf + nlohmann_json)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64` (télécharge TinyGLTF au configure si besoin)
- `cmake --build --preset win64`
- `.\build\engine_app.exe` [options]
- **Zone builder :** `.\build\zone_builder.exe <layout.json> <scene.gltf|scene.glb>`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans le PATH).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- `tools/zone_builder` : exécutable CLI, parsing des arguments (layout.json + glTF).
- Parser layout.json : chargement + validation minimale (champ `version` requis, tableau `instances` avec `guid`, `position` en mètres, `mesh`/`material` optionnels).
- Importer glTF : chargement via TinyGLTF (LoadASCIIFromFile / LoadBinaryFromFile), liste des meshes et materials.
- Émission : liste des instances (guid, position en m, mesh/material) et des assets (meshes, materials) sur stdout.
- Structure repo respectée ; pas de nouveau dossier racine ; chemins relatifs.
