# M04.2 — Shadow pass: depth-only render per cascade

**Status:** Closed

## Ticket (original)

### Objectif
Rendre une shadow map depth par cascade (D32) en pass dédiée.

### Dépendances
- M04.1
- M03.1

### Livrables
- FG images ShadowMap[4] (depth + sampled)
- Pipeline depth-only (VS, no PS ou PS trivial)

### Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Depth bias slope + constant.
- Culling front-face optionnel pour réduire acne.

### Étapes d'implémentation
1. Créer ShadowMap resources (res configurable).
2. Passes shadow i=0..3: set viewport, render depth.
3. Brancher mêmes meshes opaques (shadow casters).
4. Vérifier layouts depth attachment → sampled.

### Critères de validation (DoD)
- [x] Shadow maps générées, visibles en debug capture.
- [x] Validation OK.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/render/vk/VkShadowMap.h` — 4 cascades D32, render pass, framebuffers.
- `engine/render/vk/VkShadowMap.cpp` — init/recreate/shutdown.
- `engine/render/vk/VkShadowPipeline.h` — pipeline depth-only (VS + frag trivial).
- `engine/render/vk/VkShadowPipeline.cpp` — création pipeline (depth bias, front-face culling).
- `game/data/shaders/shadow.vert` — vertex shader (position → lightViewProj * pos).
- `game/data/shaders/shadow.frag` — fragment shader minimal (depth-only).

**Modifiés :**
- `engine/core/Engine.h` — membres VkShadowMap, VkShadowPipeline, CsmUniform, m_fgShadowMap0Id..3, m_shadowMapSize.
- `engine/core/Engine.cpp` — init shadow map + pipeline (config `render.shadow_map_size`), CsmComputeCascades dans Update(), 4 passes shadow dans le frame graph, bind registry ShadowMap0..3, shutdown.
- `CMakeLists.txt` — ajout Csm.cpp, VkShadowMap.cpp, VkShadowPipeline.cpp.
- `config.json` — `render.shadow_map_size`: 1024.

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

```bash
cmake --preset x64-Debug
cmake --build --preset x64-Debug
.\build\Debug\engine_app.exe
```

(ou selon votre preset : `.\build\engine_app.exe`)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l’environnement).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Structure du repo : OUI (tout sous `/engine` et `/game/data`).
- Portée : OUI (uniquement le pass shadow depth-only par cascade).
- Chemins : OUI (résolution via `paths.content` pour les shaders).
- DoD : Tous les points sont respectés sous réserve de build/run local.
