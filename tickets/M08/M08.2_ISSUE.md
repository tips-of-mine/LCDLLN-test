# M08.2 — Bloom: upsample + combine HDR

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M08/M08.2_ISSUE.md`
- `engine/render/vk/VkBloomUpsamplePipeline.h`
- `engine/render/vk/VkBloomUpsamplePipeline.cpp`
- `engine/render/vk/VkBloomCombineTarget.h`
- `engine/render/vk/VkBloomCombineTarget.cpp`
- `engine/render/vk/VkBloomCombinePipeline.h`
- `engine/render/vk/VkBloomCombinePipeline.cpp`
- `game/data/shaders/bloom_upsample.vert`
- `game/data/shaders/bloom_upsample.frag`
- `game/data/shaders/bloom_combine.vert`
- `game/data/shaders/bloom_combine.frag`

**Modifiés :**
- `engine/render/vk/VkBloomPyramid.h` (GetUpsampleRenderPass, GetUpsampleFramebuffer, m_upsampleRenderPasses, m_upsampleFramebuffers)
- `engine/render/vk/VkBloomPyramid.cpp` (CreateUpsamplePassAndFramebuffer, DestroyUpsamplePassAndFramebuffer, Init/Recreate/DestroyResources)
- `engine/core/Engine.h` (includes + m_bloomUpsamplePipeline, m_vkBloomCombineTarget, m_bloomCombinePipeline, m_fgBloomCombineId)
- `engine/core/Engine.cpp` (init upsample/combine target/pipelines, FG Bloom_Combine + upsample passes, Tonemap conditionnel, registry, OnResize, Shutdown)
- `CMakeLists.txt` (VkBloomUpsamplePipeline, VkBloomCombineTarget, VkBloomCombinePipeline)

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Structure repo respectée, pas de chemins absolus, portée limitée au ticket (upsample bilinear + additif, combine scene + bloom*intensity, intensity via config bloom.intensity). Fonctions documentées.

---

## Ticket (original)

### Objectif
Upsample pyramid et ajouter au SceneColor_HDR.

### Dépendances
- M08.1

### Livrables
- Upsample passes + Combine pass (HDR).

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Upsample bilinear + additif; contrôler intensity.

### Étapes d'implémentation
1. Upsample du plus petit vers mip0.
2. Combine: SceneColor_HDR + bloom*intensity.
3. Expose intensity via config.

### Critères de validation (DoD)
- [ ] Halo lumineux visible mais contrôlé.

### Notes / pièges à éviter
- Éviter de cramer l'image; bloom avant tonemap.
