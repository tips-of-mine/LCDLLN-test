# M02.3 — Frame Graph: barrières Vulkan (MVP) + tracking layouts

**Status:** Closed

## Ticket (contenu)

# M02.3 — Frame Graph: barrières Vulkan (MVP) + tracking layouts

## Objectif
Insérer les barrières nécessaires entre passes (layout transitions + access masks).

## Dépendances
- M02.2
- M01.4

## Livrables
- Tracking dernier usage par ressource (layout/stage/access)
- Barrières `vkCmdPipelineBarrier2` (si sync2) ou legacy

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- MVP acceptable: barrière conservative entre passes.
- Objectif: transitions correctes pour COLOR_ATTACHMENT, SAMPLED, TRANSFER.

## Étapes d'implémentation
1. Définir états d'usage (ColorWrite, DepthWrite, SampledRead, TransferSrc/Dst).
2. Mapper usage -> layout + stage/access.
3. Avant chaque pass, insérer barrières pour ressources utilisées.
4. Tester avec SceneColor offscreen + copy.

## Critères de validation (DoD)
- [ ] Validation sans erreurs de layout.
- [ ] Capture RenderDoc montrant transitions correctes (si dispo).

## Notes / pièges à éviter
- Conservateur d'abord; optimiser plus tard.

---

## Rapport final

### 1) FICHIERS

**Créés :**
- tickets/M02/M02.3_ISSUE.md

**Modifiés :**
- engine/render/FrameGraph.h (ImageUsage, ResourceUsageState, GetUsageState; PassBuilder read(id,usage)/write(id,usage), ReadWriteList; Pass reads/writes avec usage; emitBarriersBeforePass)
- engine/render/FrameGraph.cpp (GetUsageState; PassBuilder read/write avec usage; compile avec .first; emitBarriersBeforePass + lastUsage, vkCmdPipelineBarrier legacy; execute avec lastUsage et appel emitBarriersBeforePass avant chaque pass)
- engine/Engine.cpp (Clear: write(SceneColor, TransferDst), plus de barrières dans le lambda; CopyPresent: read(TransferSrc), write(TransferDst), barrières d’entrée supprimées, garde uniquement transition finale vers PRESENT_SRC_KHR; PostRead: read(SceneColor, SampledRead))

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset vs2022-x64`
- `cmake --build --preset vs2022-x64-debug`
- `.\build\vs2022-x64\Debug\engine_app.exe`

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Tracking dernier usage : lastUsage (layout/stage/access) par ResourceId, mis à jour après chaque barrière.
- Barrières : vkCmdPipelineBarrier (legacy) insérées avant chaque pass pour les ressources lues/écrites ; usage → layout/stage/access via GetUsageState (ColorWrite, DepthWrite, SampledRead, TransferSrc, TransferDst).
- Transitions correctes pour COLOR_ATTACHMENT, SAMPLED, TRANSFER ; SceneColor offscreen + copy testés via les passes existantes.
