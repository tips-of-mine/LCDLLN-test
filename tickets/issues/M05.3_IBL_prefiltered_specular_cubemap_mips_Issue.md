# Issue: M05.3 — IBL prefiltered specular cubemap (mips)

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `game/data/shaders/specular_prefilter.comp`
- `engine/render/SpecularPrefilterPass.h`
- `engine/render/SpecularPrefilterPass.cpp`
- `tickets/issues/M05.3_IBL_prefiltered_specular_cubemap_mips_Issue.md`

**Modifiés :**
- `CMakeLists.txt` (ajout de `engine/render/SpecularPrefilterPass.cpp`)
- `engine/Engine.h` (include + membre `m_specularPrefilterPass`)
- `engine/Engine.cpp` (init M05.3 après M05.1, Destroy M05.3 avant M05.1)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset vs2022-x64`
- `cmake --build --preset vs2022-x64-debug`
- `.\build\vs2022-x64\Debug\lcdlln.exe` (ou `engine_app.exe` selon la cible)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l’environnement d’exécution).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI** (sous réserve de build/run local).
- Structure : code sous `/engine`, contenu shader sous `/game/data/shaders/`, pas de dossier racine interdit, chemins relatifs via `paths.content`.
- Portée : uniquement livrables M05.3 (cubemap préfiltrée RGBA16F + mips, compute GGX importance sampling, roughness = mip/(mipCount-1)).
- Generate() n’est pas appelé tant qu’aucune cubemap env source n’est fournie (ex. M05.2) ; le pass est initialisé et prêt.

---

## Ticket content (M05.3)

# M05.3 — IBL: prefiltered specular cubemap (mips)

## Objectif
Générer cubemap specular préfiltrée (GGX) avec mipmaps = roughness.

## Dépendances
- M05.1
- M03.3

## Livrables
- Prefiltered cubemap (256-512) RGBA16F + mips
- Compute shader GGX importance sampling

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Roughness -> mip = r*(mipCount-1).

## Étapes d'implémentation
1. Créer cubemap avec mips.
2. Pour chaque mip, dispatch compute avec roughness correspondant.
3. Écrire face/mip.
4. Transition en sampled.

## Critères de validation (DoD)
- [ ] Reflets deviennent plus flous quand roughness augmente.

## Notes / pièges à éviter
- Prévoir temps de build; possibilité de pré-bake offline.
