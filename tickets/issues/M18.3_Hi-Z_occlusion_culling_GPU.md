# Issue: M18.3 — Hi-Z occlusion culling GPU

**Status:** Closed

---

# M18.3 — Hi-Z occlusion culling GPU

## Objectif
Construire depth pyramid et tester bounds pour occlusion GPU.

## Dépendances
- M18.2

## Livrables
- Depth pyramid build pass
- Occlusion test in culling compute

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Pyramid mip chain (min depth).

## Étapes d’implémentation
1. Compute build Hi-Z à partir depth.
2. Dans culling compute, projeter bounds, tester contre Hi-Z.
3. Marquer visible/invisible.

## Critères de validation (DoD)
- [ ] Draws cachés derrière murs/relief diminuent.

## Notes / pièges à éviter
- Tuning important; fallback conservative.

---

## Rapport final

### 1) FICHIERS
- Créés :
  - `engine/render/HiZPyramidPass.h`
  - `engine/render/HiZPyramidPass.cpp`
  - `game/data/shaders/hiz_build.comp`
  - `tickets/issues/M18.3_Hi-Z_occlusion_culling_GPU.md`
- Modifiés :
  - `CMakeLists.txt`
  - `engine/Engine.cpp`
  - `engine/render/DeferredPipeline.h`
  - `engine/render/DeferredPipeline.cpp`
  - `engine/render/GpuDrivenCullingPass.h`
  - `engine/render/GpuDrivenCullingPass.cpp`
  - `game/data/shaders/gpu_cull.comp`
- Supprimés :
  - Aucun

### 2) COMMANDES WINDOWS À EXÉCUTER
- `glslangValidator -V -S comp -o game/data/shaders/gpu_cull.comp.spv game/data/shaders/gpu_cull.comp`
- `glslangValidator -V -S comp -o game/data/shaders/hiz_build.comp.spv game/data/shaders/hiz_build.comp`
- `cmake --preset vs2022-x64`
- `cmake --build --preset vs2022-x64-debug`
- `.\build\vs2022-x64\pkg\game\lcdlln.exe`

### 3) RÉSULTAT
- Compilation : NON TESTÉ
- Exécution   : NON TESTÉ

### 4) VALIDATION DoD
- Tous les points de `DEFINITION_OF_DONE.md` sont-ils respectés ?
  - NON
- Pourquoi :
  - `cmake` n'est pas disponible dans l'environnement courant, donc la configuration/compilation/exécution Windows n'ont pas pu être vérifiées ici.
  - Le critère ticket "Draws cachés derrière murs/relief diminuent" n'a pas pu être validé sans exécution locale.
  - `BUILD_CHECK.md` n'a pas pu être relu ici car le chemin résolu par l'environnement n'était pas lisible.
