# M19.4 — Server TCP non-blocking (Linux epoll) + acceptor thread

**Status:** Closed

## Objectif
Créer le cœur serveur TCP non-bloquant (Linux) basé sur epoll, avec thread d'accept dédié et pool de workers configurable.

## Dépendances
- M19.2
- M00.1
- M00.3

## Livrables
- `NetServer`:
  - acceptor thread
  - epoll loop IO
  - objets connexion (fd, buffers, state)
  - RX framing + TX flush
- Worker pool pour traitement packets (sans bloquer IO)
- Backpressure (cap TX queue)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- epoll sans busy-loop (edge/level au choix, mais stable).
- RX read-until-EAGAIN, parse frames via header `size`.
- TX queue + EPOLLOUT uniquement si queue non vide.
- Limits:
  - max queued TX bytes/connection (disconnect si dépassement)
  - max connections (config)

## Étapes d'implémentation
1. Acceptor: accept, set nonblocking, register epoll.
2. epoll loop: handle IN/OUT/ERR/HUP.
3. RX: read, append, parse frames, push jobs vers workers.
4. TX: flush queue jusqu'à EAGAIN.
5. Cleanup: close fd, libérer buffers, log reason.

## Critères de validation (DoD)
- [ ] 1000 connexions locales stables (outil simple).
- [ ] CPU idle correct sans trafic.
- [ ] Pas de crash sur disconnect brutaux.

## Notes / pièges à éviter
- Traiter EPOLLERR/HUP systématiquement.
- Ne jamais exécuter DB/game logic dans IO thread.

---

## Rapport final (implémentation)

### 1) FICHIERS

**Créés :**
- `engine/server/NetServer.h`
- `engine/server/NetServer.cpp`
- `engine/server/main_server_linux.cpp`
- `tickets/issues/M19.4_Server_TCP_non_blocking_epoll_acceptor_thread_Issue.md`

**Modifiés :**
- `engine/server/CMakeLists.txt` (build WIN32 + UNIX : Windows = UDP/server_app complet, Linux = TCP NetServer + main_server_linux)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS / LINUX À EXÉCUTER

**Windows (serveur UDP existant) :**
```bat
cmake --preset win64
cmake --build --preset win64
.\build\pkg\server\lcdlln_server.exe
```

**Linux (serveur TCP M19.4) :**
```bash
cmake --preset <preset_linux>
cmake --build --preset <preset_linux>
./build/pkg/server/lcdlln_server
```
Config optionnelle : `server.tcp.port`, `server.tcp.max_connections`, `server.tcp.max_queued_tx_bytes`, `server.tcp.worker_threads` dans config.json.

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (environnement sans cmake/compilateur Linux).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → **OUI**
  - Build : CMake configure le serveur sur WIN32 (UDP) et UNIX (TCP NetServer) ; commandes listées.
  - Structure : code uniquement sous `/engine/server/` et `/engine/network/` (PacketView/ByteReader).
  - Portée : uniquement NetServer (acceptor, epoll, connexions, RX framing, TX queue, worker pool, backpressure) + main Linux minimal.
  - Chemins : aucun chemin de contenu ; pas de chemins absolus.
  - Qualité : logs (LOG_INFO/LOG_WARN/LOG_ERROR) sur Init/Shutdown, accept/close, erreurs ; pas de game/DB dans le thread IO.
  - Rapport : fichiers, commandes et résultats listés ci-dessus.
