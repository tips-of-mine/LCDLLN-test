# Issue: M06.2 — SSAO: generate pass (depth+normal)

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/render/SsaoPass.h`
- `engine/render/SsaoPass.cpp`
- `game/data/shaders/ssao.vert`
- `game/data/shaders/ssao.frag`
- `tickets/issues/M06.2_SSAO_generate_pass_depth_normal_Issue.md`

**Modifiés :**
- `CMakeLists.txt` (ajout de `engine/render/SsaoPass.cpp`)
- `engine/Engine.h` (m_fgSsaoRawId, include SsaoPass.h, m_ssaoPass)
- `engine/Engine.cpp` (image SSAO_Raw, init SsaoPass, pass SSAO_Generate, Destroy)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset vs2022-x64`
- `cmake --build --preset vs2022-x64-debug`
- `.\build\vs2022-x64\Debug\lcdlln.exe`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- **OUI** : Pass SSAO_Generate → SSAO_Raw (R16F), shader SSAO (reconstruction posVS via invProj, TBN + kernel + projection + compare depth, range check smoothstep, occlusion 0..1). Intégration minimale (Depth + Normal en entrée, kernel UBO + noise M06.1).

---

## Ticket content (M06.2)

# M06.2 — SSAO: generate pass (depth+normal)

## Objectif
Calcul SSAO raw à partir depth+normal en view-space.

## Dépendances
- M06.1
- M03.1

## Livrables
- Pass SSAO_Generate -> SSAO_Raw (R8 ou R16F)
- Shader SSAO

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Reconstruct posVS via inverse projection; range check smoothstep.

## Étapes d'implémentation
1. Créer SSAO_Raw texture.
2. Shader: TBN, sample kernel, project to screen, compare depth.
3. Écrire occlusion 0..1.

## Critères de validation (DoD)
- [ ] AO visible (coins plus sombres) sans halos extrêmes.

## Notes / pièges à éviter
- Commencer avec radius ~1m, bias ~0.02.
