# M13.3 — Streaming — Intégration Job System (I/O async)

> **Type**: Ticket (Engineering)  
> **Priority**: High  
> **Owner**: Codex  
> **Status**: TODO  
> **Created**: 2026-02-08

## Objectif
Brancher le job system existant pour exécuter les chargements chunks en arrière-plan:
- lecture fichier via VFS,
- parsing chunk,
- publication thread-safe dans la structure world.

Important:
- ne pas toucher au renderer ici,
- seulement le chargement et la disponibilité CPU.

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Code jeu** : uniquement sous `/game/...`
- **Contenu** (textures/meshes/json/audio/shaders) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- **Interdit** : créer des dossiers racine du type `/assets`, `/textures`, `/data`, `/shaders`, etc.
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json). Pas de chemins absolus.


## Livrables
- `engine/world/ChunkStreamingJobs.*`
- Queue de jobs + priorité par ring (near > far)
- Synchronisation minimale (mutex/lockfree)

## Validation
- Les jobs s’exécutent sans deadlock
- La priorité near charge d’abord les chunks proches

## Commandes (Windows)
- Configure: `cmake --preset win-ninja-vcpkg`
- Build: `cmake --build --preset win-build`
- Run: `./build/engine_app.exe <args>`

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD
