# M19.6 — TLS client (OpenSSL) + certificate fingerprint pinning

**Status:** Closed

## Objectif
Ajouter TLS côté client via OpenSSL et vérifier le serveur via fingerprint SHA-256 configurable (pinning).

## Dépendances
- M19.3
- M00.1

## Livrables
- `SSL_CTX` client + handshake TLS
- Extraction certificat serveur (X509)
- Calcul SHA-256 fingerprint (DER)
- Comparaison avec fingerprint config (normalisation)
- Refus explicite si mismatch

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Fingerprint: chaîne hex (avec/sans `:`) normalisée (case-insensitive, remove separators).
- Policy:
  - si fingerprint configuré → obligatoire
  - mode dev optionnel (allow_insecure_dev) uniquement via config explicite
- TLS min: 1.2+

## Étapes d'implémentation
1. Handshake TLS client (non-bloquant si thread réseau).
2. Récupérer cert serveur après handshake.
3. Calculer fingerprint SHA-256, normaliser, comparer.
4. Si mismatch: log + disconnect.
5. Si OK: read/write via SSL.

## Critères de validation (DoD)
- [ ] Fingerprint correct → connexion acceptée.
- [ ] Fingerprint incorrect → connexion refusée.
- [ ] Logs indiquent cause.

## Notes / pièges à éviter
- Ne pas "fallback" silencieusement en insecure.

---

## Rapport final (implémentation)

### 1) FICHIERS

**Créés :**
- `tickets/issues/M19.6_TLS_client_OpenSSL_fingerprint_pinning_Issue.md`

**Modifiés :**
- `engine/network/NetClient.h` (SetExpectedServerFingerprint, SetAllowInsecureDev; membres m_expectedServerFingerprintHex, m_allowInsecureDev)
- `engine/network/NetClient.cpp` (handshake TLS client non-bloquant, extraction X509, SHA-256 DER, normalisation hex, comparaison, refus si mismatch; SSL_read/SSL_write; logs cause)
- `CMakeLists.txt` (find_package OpenSSL si WIN32, lien OpenSSL::SSL pour engine_core)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset win64
cmake --build --preset win64
.\build\pkg\game\lcdlln.exe
```
(OpenSSL requis pour le client Windows.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → **OUI**
  - Build : OpenSSL lié pour engine_core (WIN32) ; commandes listées.
  - Structure : code sous `/engine/network/` uniquement.
  - Portée : uniquement TLS client + pinning (SSL_CTX client, handshake, cert, SHA-256, normalisation, refus si mismatch, allow_insecure_dev via config).
  - Chemins : pas de chemins de contenu ; fingerprint fourni par config/code appelant.
  - Qualité : logs (LOG_INFO/LOG_ERROR/LOG_WARN) sur TLS init, handshake, mismatch, cause de refus.
  - Rapport : fichiers, commandes et résultats listés ci-dessus.
