# Issue: M08.2 — Bloom: upsample + combine HDR

**Status:** Closed

---

## Rapport final (M08.2)

### 1) FICHIERS

**Créés :**
- `game/data/shaders/bloom_upsample.vert` — Fullscreen triangle pour upsample.
- `game/data/shaders/bloom_upsample.frag` — Sample mip (bilinear), output ajouté via blend additif.
- `game/data/shaders/bloom_combine.vert` — Fullscreen triangle pour combine.
- `game/data/shaders/bloom_combine.frag` — SceneColor_HDR + bloom * intensity → HDR.

**Modifiés :**
- `engine/render/BloomPass.h` — BloomUpsamplePass (LOAD + additive blend), BloomCombinePass (2 samplers + intensity).
- `engine/render/BloomPass.cpp` — Init/Record/Destroy pour BloomUpsamplePass et BloomCombinePass.
- `engine/Engine.h` — m_fgSceneColorHDRWithBloomId ; m_bloomUpsamplePass, m_bloomCombinePass.
- `engine/Engine.cpp` — Création FG SceneColor_HDR_WithBloom ; chargement bloom_upsample / bloom_combine shaders ; init des deux passes ; passes FG Bloom_Upsample_4..0 (ordre petit→mip0), Bloom_Combine ; Tonemap lit m_fgSceneColorHDRWithBloomId ; Destroy des deux passes.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

L’utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Structure : code sous `/engine`, shaders sous `/game/data/shaders`.
- Portée : uniquement M08.2 (upsample bilinear + additif, combine HDR, intensity via config bloom.intensity, bloom avant tonemap).
- Chemins : shaders via paths.content.

---

## Ticket content (M08.2) — référence

# M08.2 — Bloom: upsample + combine HDR

## Objectif
Upsample pyramid et ajouter au SceneColor_HDR.

## Dépendances
- M08.1

## Livrables
- Upsample passes + Combine pass (HDR).

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Upsample bilinear + additif; contrôler intensity.

## Étapes d'implémentation
1. Upsample du plus petit vers mip0.
2. Combine: SceneColor_HDR + bloom*intensity.
3. Expose intensity via config.

## Critères de validation (DoD)
- [ ] Halo lumineux visible mais contrôlé.

## Notes / pièges à éviter
- Éviter de cramer l'image; bloom avant tonemap.
