# Issue: M36.1 — Gathering resource nodes + harvesting

**Status:** Closed

---

## Contenu du ticket

# M36.1_Gathering_resource_nodes_+_harvesting

## Objectif
Nodes ressources (mining, herbalism, skinning) avec spawn/despawn, harvesting mini-game.

## Dépendances
- M09.1
- M16.3

## Livrables
- Resource node spawner (positions, type, respawn timer)
- Harvesting interaction (progress bar)
- Loot table par node
- Skill level requirement (optional)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Node types: ore_vein, herb, tree, animal_corpse (skinnable)
- Harvesting time: 2-5s (cast bar)
- Respawn: 5-15min per node
- Loot: 1-5 resources, quality variable

## Étapes d'implémentation
1. Define node types (JSON: id, mesh, loot_table, respawn_time)
2. Spawn nodes dans monde (zone config)
3. Interaction: E key proximity check
4. Start harvest: cast bar 3s
5. Interruption: movement/damage cancels
6. On complete: roll loot table, add to inventory
7. Despawn node, start respawn timer
8. Server: validate proximity, prevent teleport-harvesting

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Phasing: concurrent players voir same node (race condition)
- Shared loot vs individual (individual = moins frustration)

---

## Rapport final (M36.1)

### 1) FICHIERS

**Créés :**
- `engine/server/ResourceNodeRuntime.h` — structs `ResourceNodeLootEntry`, `ResourceNodeDefinition`, `ResourceNodeInstance`, `ResourceNodeState`, `ResourceHarvestResult` + classe `ResourceNodeRuntime` (Init/Shutdown, JSON loader, tick, loot roll, cancel, progress).
- `engine/server/ResourceNodeRuntime.cpp` — implémentation complète avec parser JSON embarqué (pattern SpawnerRuntime), chargement definitions + instances par zone, machine d'état Available→Harvesting→Depleted→Available, loot roll `std::mt19937`.
- `game/data/gathering/nodes.json` — 4 types : ore_vein (archetypeId 300), herb (301), tree (302), animal_corpse (303), avec harvestTimeSec, respawnTimeSec, lootTable.
- `game/data/zones/zone_0/gathering_nodes.json` — 5 instances (instanceId 1001–1005) dans zone_0.
- `tickets/issues/M36.1_Gathering_resource_nodes_+_harvesting_Issue.md`

**Modifiés :**
- `engine/server/ServerProtocol.h` — `MessageKind::HarvestBeginRequest..HarvestCancelNotify` (63–66), structs wire + déclarations Encode/Decode.
- `engine/server/ServerProtocol.cpp` — implémentation encode/decode 4 messages harvest.
- `engine/server/ServerApp.h` — `#include ResourceNodeRuntime.h`, membre `m_resourceNodeRuntime`, déclarations `InitResourceNodes`, `UpdateResourceNodes`, `HandleHarvestBeginRequest`, `SendHarvestProgressNotify`, `SendHarvestResultNotify`, `SendHarvestCancelNotify`.
- `engine/server/ServerApp.cpp` — constructeur (init list), Init (non-fatal), Shutdown, Simulate (UpdateResourceNodes), ProcessPacket (dispatch), HandleInput (interruption mouvement), TryMobAttackPlayer (interruption dommages), OnClientLogout (cancel on disconnect), + implémentation de tous les handlers.
- `engine/server/CMakeLists.txt` — `ResourceNodeRuntime.cpp` ajouté à `server_app` WIN32.

### 2) COMMANDES WINDOWS À EXÉCUTER

```powershell
cmake --preset vs2022-x64-release
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\pkg\server\lcdlln_server.exe
```

### 3) RÉSULTAT

- Compilation : **NON TESTÉ** (Visual Studio absent de l'environnement d'exécution)
- Exécution   : **NON TESTÉ**

### 4) VALIDATION DoD

| Critère | Statut |
|---|---|
| Build Windows OK | NON TESTÉ |
| Aucun dossier racine non autorisé | ✅ `game/data/gathering/` conforme |
| Code moteur sous `/engine` | ✅ |
| Contenu sous `/game/data` | ✅ nodes.json, gathering_nodes.json |
| Portée strictement M36.1 | ✅ |
| Aucun chemin absolu | ✅ — tout via `paths.content` |
| Logs Init/Destroy sur chaque module | ✅ — ResourceNodeRuntime + handlers |
| Logs chemins critiques | ✅ — WARN sur rejet proximity/node unavailable, INFO sur start/complete/cancel/respawn |
| Resource node spawner (positions, type, respawn) | ✅ |
| Harvesting interaction (progress bar protocol) | ✅ HarvestProgressNotify (0–100%) |
| Loot table par node | ✅ weighted random roll via `std::mt19937` |
| Interruption mouvement | ✅ dans HandleInput — > 1m from start |
| Interruption dommages | ✅ dans TryMobAttackPlayer |
| Anti teleport-harvesting | ✅ proximity check 5m dans HandleHarvestBeginRequest |
| Skill level requirement | N/A (marqué "optional" dans le ticket) |
| Rapport final présent | ✅ |
