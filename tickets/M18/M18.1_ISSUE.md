# M18.1 — Profiler HUD: CPU scopes + GPU timestamps per pass

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- engine/core/Profiler.h
- engine/core/Profiler.cpp
- engine/render/vk/VkTimestampPool.h
- engine/render/vk/VkTimestampPool.cpp
- engine/ui/ProfilerOverlay.h
- engine/ui/ProfilerOverlay.cpp

**Modifiés :**
- engine/render/FrameGraph.h (Execute avec pool timestamps optionnel ; GetExecutedPassCount ; GetPassNameByExecutionIndex)
- engine/render/FrameGraph.cpp (écriture vkCmdWriteTimestamp avant/après chaque pass ; implémentation GetPassNameByExecutionIndex)
- engine/core/Engine.h (Profiler, VkTimestampPool, ProfilerOverlay ; membres m_timestampPool, m_lastGpuPassMs, m_hasSubmittedTimestampFrame)
- engine/core/Engine.cpp (PROFILE_SCOPE("Frame"), init/reset/GetResults timestamp pool, DrawProfilerOverlay, Profiler::EndFrame, shutdown pool)
- CMakeLists.txt (Profiler.cpp, VkTimestampPool.cpp, ProfilerOverlay.cpp dans engine_core)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset <preset>`
- `cmake --build --preset <preset>`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Structure repo respectée (/engine, /game/data), pas de chemins absolus, pas de dérive hors ticket. PROFILE_SCOPE, GPU query pool, overlay implémentés ; coût GBuffer/Shadows/SSAO/etc visible en ms dans la fenêtre Profiler.

---

## Contenu du ticket (intégral)

# M18.1 — Profiler HUD: CPU scopes + GPU timestamps per pass

## Objectif
Profiler intégré: scopes CPU multi-thread + GPU timestamps (frame graph passes) + HUD overlay.

## Dépendances
- M02.2
- M00.3

## Livrables
- PROFILE_SCOPE macros
- GPU query pool
- Debug overlay

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).

## Spécification technique
- Afficher ms total + breakdown top passes; export json optionnel.

## Étapes d'implémentation
1. Implémenter CPU profiler (ring buffer events).
2. Implémenter GPU timestamps (vkCmdWriteTimestamp).
3. Associer passes frame graph à scopes GPU.
4. UI overlay pour lecture.

## Critères de validation (DoD)
- [ ] Voir coût GBuffer/Shadows/SSAO/etc en ms.

## Notes / pièges à éviter
- Attention au nombre de queries; recycler.
