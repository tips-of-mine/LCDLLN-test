# M16.4 — Quest journal + tracker HUD + minimap zone (MVP)

**Status:** Closed

## Contenu du ticket (intégral)

# M16.4 — Quest journal + tracker HUD + minimap zone (MVP)

## Objectif
Journal quêtes + tracker et minimap (texture zone) avec POIs basiques.

## Dépendances
- M16.1
- M15.1
- M09.1

## Livrables
- Quest panel
- Tracker HUD
- Minimap widget

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Minimap: zone map baked; convert xz->uv.

## Étapes d'implémentation
1. Créer journal: liste + détails + steps.
2. Tracker HUD: quêtes suivies.
3. Minimap: afficher joueur + target + triggers (option).

## Critères de validation (DoD)
- [ ] Progression quête visible; minimap suit joueur.

## Notes / pièges à éviter
- Debug overlay chunks/cells utile.

---

## Rapport final (M16.4)

### 1) FICHIERS

**Créés :** —

**Modifiés :**
- `engine/ui/GameHud.h` — `QuestDisplayEntry`, `HudData`: `questEntries`, `trackedQuestIds`, `playerPositionXZ`, `zoneSize`, `targetPositionXZ`, `poiPositions`.
- `engine/ui/GameHud.cpp` — Quest panel (journal: liste + détails + steps), Tracker HUD (quêtes suivies via `trackedQuestIds`), Minimap (xz→uv, joueur + target + POIs optionnels).
- `engine/core/Engine.cpp` — `SetHudData` copie les champs quêtes et minimap.

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe` (sans éditeur ; alimenter `SetHudData` avec `questEntries`/`trackedQuestIds` depuis UIModel + définitions quêtes, et `playerPositionXZ`/`zoneSize` pour que la minimap suive le joueur).

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Quest panel : journal avec liste, détails (step, counter, completed), steps (currentStepLabel). Tracker HUD : affichage des quêtes dont l’id est dans `trackedQuestIds`. Minimap : conversion xz→uv (zoneSize), point joueur (suit joueur), point cible et POIs en option. Progression quête visible dans le journal et le tracker ; minimap suit le joueur via `playerPositionXZ`.

**Status:** Closed
