# Issue: M16.5 — Theme/skin switch: theme.json + style.qss integration

**Status:** Closed

---

## Rapport final (M16.5)

### 1) FICHIERS

**Créés :**
- `engine/client/ThemeManager.h`
- `engine/client/ThemeManager.cpp`
- `game/data/ui/themes/default/theme.json`
- `game/data/ui/themes/default/style.qss`
- `game/data/ui/races/humains/theme.json`
- `game/data/ui/races/humains/style.qss`
- `tickets/issues/M16.5_Theme-skin_switch_theme.json_style.qss_integration_Issue.md`

**Modifiés :**
- `CMakeLists.txt`

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64\pkg\game\lcdlln.exe -log
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

Détail environnement :
- `cmake` n’est pas disponible dans l’environnement de travail courant, donc la configuration / compilation locale n’a pas pu être lancée ici.
- Les diagnostics IDE sur les fichiers modifiés ne remontent pas d’erreur.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Le scope demandé par `M16.5` a été implémenté au minimum via :
  - `ThemeManager` autonome avec chargement de `theme.json`
  - chargement et résolution de `style.qss` avec variables `${...}`
  - hooks `SetTheme()` et `SetRace()` avec fallback si le thème de race est manquant
  - refs d’assets/couleurs/polices exposées dans la définition active
  - contenu minimal conforme sous `game/data/ui/themes` et `game/data/ui/races`
- La validation complète DoD reste incomplète car :
  - la compilation n’a pas pu être exécutée localement sans `cmake`
  - l’exécution n’a pas pu être validée localement

---

## Ticket content (M16.5) — référence

# M16.5 — Theme/skin switch: theme.json + style.qss integration

## Objectif
Appliquer thèmes UI par race via chargement theme.json + style.qss, hot-reload optionnel.

## Dépendances
- M16.1

## Livrables
- Theme manager
- Apply QSS
- Asset refs (fonts/icons/colors)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Widgets utilisent classes logiques (hud/panel/button/tooltip).

## Étapes d’implémentation
1. Définir mapping theme.json -> variables (colors/fonts).
2. Charger et appliquer style.qss.
3. Hook changement race/thème.
4. Option: hot reload fichier.

## Critères de validation (DoD)
- [ ] Changer thème modifie UI sans casser la logique.

## Notes / pièges à éviter
- Assurer fallback si fichier manquant.
