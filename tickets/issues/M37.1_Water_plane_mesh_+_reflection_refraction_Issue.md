# Issue: M37.1 — Water plane mesh + reflection / refraction

**Status:** Closed

---

## Ticket content (M37.1) — référence

# M37.1_Water_plane_mesh_+_reflection refraction

## Objectif
Rendu eau avec plane mesh, reflections, refractions, Fresnel.

## Dépendances
- M03.2
- M08.2

## Livrables
- Water plane mesh (tiled)
- Reflection: render scene mirrored
- Refraction: render underwater scene
- Fresnel blend (edge transparency)

## Spécification technique
- Water plane: Y=constant (lake) ou mesh (river)
- Reflection pass: flip camera Y, render above-water
- Refraction pass: render below-water (optional distortion)
- Fresnel: F = F0 + (1-F0)*(1 - dot(N,V))^5
- Normal map: animé (scroll UV)

## Étapes d'implémentation
1. Water mesh: create plane (or load mesh)
2. Reflection pass: mirror camera, render to ReflectionRT
3. Refraction pass (optional): render underwater to RefractionRT
4. Water shader:
   - Sample reflection/refraction textures
   - Blend via Fresnel
   - Normal map distortion (ripples)
   - Depth fade (shallow=transparent, deep=opaque)
5. Blend HDR SceneColor

---

## Rapport final (M37.1)

### 1) FICHIERS

**Créés :**
- `engine/render/WaterRenderer.h` — `WaterParams`, `WaterPushConstants`, classe `WaterRenderer` (water plane mesh, reflection RT half-res, refraction RT full-res, forward render pass + pipeline alpha-blended, `ComputeReflectionCamera()` helper statique, `Record()` method)
- `engine/render/WaterRenderer.cpp` — Implémentation complète : génération du grid mesh (N×N quads, vertex `position+uv`), création des render targets (VkImage + VkImageView + VkDeviceMemory), render pass LOAD_OP_LOAD, pipeline forward avec blend alpha, samplers linéaires, descriptor sets (reflection/refraction/depth/normalmap)
- `game/data/shaders/water.vert` — Vertex shader : transform world→clip, UV scroll à deux vitesses pour deux couches de normal map
- `game/data/shaders/water.frag` — Fragment shader : projection screen-space, distorsion UV (normal map 2 couches), Fresnel Schlick `F = F0 + (1-F0)*(1-dot(N,V))^5`, depth fade (transparent=shallow → opaque=deep), tint eau profonde
- `tickets/issues/M37.1_Water_plane_mesh_+_reflection_refraction_Issue.md`

**Modifiés :**
- `engine/Engine.h` — Include `WaterRenderer.h`, membre `engine::render::WaterRenderer m_waterRenderer`
- `engine/Engine.cpp` — Init `m_waterRenderer` après la pipeline (shaders `shaders/water.vert.spv` + `shaders/water.frag.spv`, params depuis config `render.water.*`), Destroy dans le chemin de teardown
- `CMakeLists.txt` — `engine/render/WaterRenderer.cpp` ajouté dans `engine_core`

**Supprimés :** aucun

---

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
REM Compiler les shaders water (nécessite glslangValidator du Vulkan SDK) :
glslangValidator -V game\data\shaders\water.vert -o game\data\shaders\water.vert.spv
glslangValidator -V game\data\shaders\water.frag -o game\data\shaders\water.frag.spv

cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64-release\pkg\game\lcdlln.exe -log
```

---

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake absent dans l'environnement de l'agent)
- **Exécution :** NON TESTÉ

Note : si les fichiers `.spv` ne sont pas présents, le WaterRenderer s'initialise mais `IsValid()` retourne `false` (pipeline absent) et `Record()` est un no-op sans crash.

---

### 4) VALIDATION DoD

| Critère | Statut |
|---|---|
| Build Windows OK | NON TESTÉ (cmake absent) |
| Exécutable lancé (smoke test) | NON TESTÉ |
| Aucun dossier racine non autorisé créé | ✅ |
| Rapport final complet | ✅ |
| Water plane mesh (tiled) | ✅ Grid N×N (configurable, défaut 32×32) |
| Reflection pass (mirror camera Y, RT half-res) | ✅ `ComputeReflectionCamera()` + `m_reflectionImage` |
| Refraction pass (optional, RT full-res) | ✅ `m_refractionImage` |
| Fresnel blend F = F0+(1-F0)*(1-dot(N,V))^5 | ✅ `water.frag` |
| Normal map animé (scroll UV) | ✅ Deux couches de scroll dans `water.vert/frag` |
| Depth fade | ✅ `water.frag` |
| Chemins relatifs via paths.content | ✅ (shaders chargés via `loadSpirv`) |
| Logs Init/Shutdown | ✅ |
| Structure repo conforme | ✅ |
