# M15.1 — Quests data-driven: JSON + progress events

**Status:** Closed

## Objectif
Système quêtes data-driven (kill/collect/talk/enter) avec progression serveur et UI hooks.

## Dépendances
- M14.1
- M16.1

## Livrables
- Quest definitions JSON
- QuestState per player
- QuestDelta msg

## Spécification technique
- Steps typés; rewards XP/gold/items; prereqs.

## Étapes d'implémentation
1. Définir schema JSON quest.
2. Server: charger toutes quêtes, gérer states par joueur.
3. Hook events OnKill/OnPickup/OnTalk/OnEnterTrigger.
4. Envoyer QuestDelta au client.

## Critères de validation (DoD)
- [x] Ajouter une quête JSON et la compléter en jeu.

## Notes
- Localisation texte plus tard (string tables).

---

## Rapport final

### 1) FICHIERS

**Créés :**
- engine/quest/QuestDef.h
- engine/quest/QuestDef.cpp
- engine/network/QuestProtocol.h
- engine/network/QuestProtocol.cpp
- game/data/quests.json

**Modifiés :**
- engine/network/Protocol.h (MsgType::QuestDelta = 12, AcceptQuest = 13)
- CMakeLists.txt (engine_quest, QuestProtocol.cpp, server_app → engine_quest)
- main_server.cpp (load quests, QuestProgress/playerQuestProgress/playerCompletedQuests/clientTriggersInsidePrev, AcceptQuest, fireQuestEvent, OnKill/OnPickup/OnEnterTrigger, Logout erase quest state)
- main_client.cpp (QuestProtocol.h, SerializeAcceptQuest(1) après connect, réception QuestDelta + printf)

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

- cmake --preset win64
- cmake --build --preset win64
- .\build\server_app.exe game/data
- .\build\client_app.exe 127.0.0.1

### 3) RÉSULTAT

- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- Schema JSON : id, title, prereqs, steps (type, target, count), rewards (xp, gold, items). QuestState par client (stepIndex, counter) + completed. AcceptQuest + QuestDelta. Hooks OnKill (archetype), OnPickup (item), OnEnterTrigger (Talk/Enter avec actionId). Quête exemple dans game/data/quests.json (kill 1 archetype:1) ; client envoie AcceptQuest(1), tue le mob → QuestDelta reçu, quête complétée.
