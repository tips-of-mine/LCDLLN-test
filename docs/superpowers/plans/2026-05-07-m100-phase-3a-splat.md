# M100 Phase 3a — Splat Map + Painting Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal :** Livrer M100.9 (Splat Map System, 8 layers 257² par chunk, format `splat.bin` magic SLAT, shader Vulkan terrain_chunk, drawcall dans `GeometryPass`) et M100.10 (Splat Painting Brushes, manuel + auto-rules, undo coalescé, couture multi-chunks) dans une seule PR. Phase 3a résout aussi la dette technique Task 11 reportée de Phase 2 (drawcall mesh-terrain par chunk).

**Architecture :** Mode "couche au-dessus" du legacy `engine::render::terrain::TerrainSplatting` (4 layers RGBA8 1024² magic SLAP, sert encore `demo_plains` au boot du jeu). Le nouveau code vit en parallèle dans `engine::world::terrain::SplatMap` (data) et `engine::editor::world::SplatPaintTool` (tools). Pipeline Vulkan `terrain_chunk` neuf (descriptor set 2 = splatMap + 3 texture2DArray albedo/normal/ARM). Couture inter-chunks reproduit le pattern M100.6 (sculpt seam) : la rangée bord d'un chunk = rangée bord du voisin, écriture des deux côtés dans la même `ICommand`.

**Tech Stack :** C++17/20 + Vulkan, ImGui (déjà intégré), GLSL pour les shaders, `engine::core::Config`/`Log`, `OutputVersionHeader` + `ComputeXxHash64` (déjà au repo), `stb_image_write` (déjà au repo via `external/stb/`) pour le mini-tool placeholder. Tests via le framework REQUIRE maison (cf. `engine/editor/world/tests/CommandStackTests.cpp`).

**Entrées :**
- Specs détaillées : [tickets/M100/M100.9-SplatMapSystem.md](../../../tickets/M100/M100.9-SplatMapSystem.md), [M100.10](../../../tickets/M100/M100.10-SplatPaintingBrushes.md).
- Design : [docs/superpowers/specs/2026-05-07-m100-phase-3a-splat-design.md](../specs/2026-05-07-m100-phase-3a-splat-design.md). 8 décisions verrouillées (coexistence SLAP/SLAT, drawcall livré, placeholders colorés au build, screenshot test stubbé, 1 PR).
- Audit : [docs/superpowers/audits/2026-05-06-m100-gap-analysis.md](../audits/2026-05-06-m100-gap-analysis.md) sections M100.9 + M100.10. Verdicts : tous deux `partiel L`.
- Pré-requis Phase 1 + Phase 2 : déjà mergés sur origin/main. `engine::world::terrain::TerrainChunk`, `TerrainMeshBuilder`, `TerrainDocument`, `WorldEditorShell` (avec `enum class ActiveTool { None, TerrainSculpt, TerrainStamp }`), `CommandStack`, `ICommand`, `ChunkSegment::Terrain` dans `ChunkPackageLayout`.

---

## File Structure

| Fichier | Rôle | Ticket | Action |
|---------|------|--------|--------|
| `engine/world/terrain/SplatMap.h` | Constantes (magic SLAT, version, kSplatLayerCount=8) + struct `SplatMap` + free funcs `SaveSplatBin`/`LoadSplatBin` | M100.9 | Create |
| `engine/world/terrain/SplatMap.cpp` | Impl `MakeUniform`, `IsValid` (somme=255), Save/Load | M100.9 | Create |
| `engine/world/terrain/LayerPalette.h` | Struct `LayerPalette` + 8 entrées `LayerEntry` + parser JSON | M100.9 | Create |
| `engine/world/terrain/LayerPalette.cpp` | Parse `layer_palette.json` via le parser JSON existant (`engine/core/Config` ou `tools/zone_builder/lib/JsonDocument`) | M100.9 | Create |
| `engine/world/terrain/tests/SplatMapTests.cpp` | Round-trip byte-exact + MakeUniform somme=255 + reject bad magic / sum != 255 + LayerPalette parse | M100.9 | Create |
| `engine/render/TerrainChunkPipeline.h` | API publique : `Init(device, ...)`, `Record(cmd, viewProj, chunkMesh, splatTexture, layerArrays)` | M100.9 | Create |
| `engine/render/TerrainChunkPipeline.cpp` | Pipeline Vulkan + descriptor set 2 + uniforms tilingScale[8] | M100.9 | Create |
| `game/data/shaders/terrain.vert` | Vertex shader (transforme position chunk-local → clip space) | M100.9 | Create |
| `game/data/shaders/terrain.frag` | Fragment 8-layer blend (sample splatMap → 8 poids, blend albedo/normal/ARM) | M100.9 | Create |
| `assets/terrain/layer_palette.json` | 8 entrées (dirt/grass_dry/grass_wet/mud/sand/rock/snow/lava_cooled), `surfaceType` par défaut "Dirt" jusqu'à M100.11 | M100.9 | Create |
| `assets/terrain/placeholders/` | Dir des PNG 4×4 colorés générés au build (8 fichiers, ~quelques Ko chacun) | M100.9 | Create (auto-build) |
| `tools/gen_terrain_placeholders/main.cpp` | Mini-tool C++ standalone, écrit 8 PNG 4×4 unicolore (utilise `stb_image_write`) | M100.9 | Create |
| `tools/gen_terrain_placeholders/CMakeLists.txt` | Target tool + custom command POST_BUILD qui génère les PNG dans `assets/terrain/placeholders/` | M100.9 | Create |
| `engine/editor/world/SplatPaintTool.h/.cpp` | Outil principal + dispatch input | M100.10 | Create |
| `engine/editor/world/SplatPaintCommand.h/.cpp` | `ICommand` delta sparse (prev[8]/next[8] par cellule) + somme=255 invariant | M100.10 | Create |
| `engine/editor/world/SplatRules.h/.cpp` | Auto-rules pente/altitude (calcule slope depuis `TerrainChunk::SampleHeight` voisinage) | M100.10 | Create |
| `engine/editor/world/tests/SplatPaintTests.cpp` | Manual + auto-rules + somme=255 + seam + history coalescing | M100.10 | Create |
| `engine/world/StreamCache.h/.cpp` | Modif : `LoadSplatMap(GlobalChunkCoord)` + cache key `chunks/<i>_<j>/splat.bin` | M100.9 | Modify |
| `engine/world/ChunkPackageLayout.h/.cpp` | Modif : ajouter `ChunkSegment::Splat` (entre `Terrain` et `Tex`) + `kChunkMetaHasSplat = 1u << 6` flag | M100.9 | Modify |
| `engine/world/StreamingScheduler.cpp` | Aucune modif requise (passe automatiquement par `GetChunkLoadOrder()` qui inclut le nouveau segment) | M100.9 | (no change) |
| `engine/render/GeometryPass.cpp` | Modif : drawcall terrain_chunk pour chaque chunk visible avec `TerrainMeshGpu` + `SplatMapGpu` présents (sans branche `m_editorEnabled`) | M100.9 | Modify |
| `tools/zone_builder/lib/Public/zone_builder/ChunkPackageWriter.h` | Modif : forward-declare `SplatMap` + signature `WriteSplatMap` | M100.9 | Modify |
| `tools/zone_builder/lib/ChunkPackageWriter.cpp` | Modif : impl `WriteSplatMap` (pattern `WriteTerrainChunk` de Phase 2) | M100.9 | Modify |
| `tools/zone_builder/lib/CMakeLists.txt` | Modif : ajouter `SplatMap.cpp` + `LayerPalette.cpp` aux sources dupliquées (cf. fix `1c790b6` du Phase 2) | M100.9 | Modify |
| `engine/editor/world/TerrainDocument.h/.cpp` | Modif : ajouter `EnsureSplatLoaded(cfg, x, z)` retournant `shared_ptr<SplatMap>` + `MarkSplatDirty(coord)` + `SaveDirtySplatToDisk(cfg)` | M100.9 | Modify |
| `engine/editor/world/WorldEditorShell.h/.cpp` | Modif : ajouter `TerrainStamp` → `SplatPaint` dans `enum class ActiveTool`, membre `SplatPaintTool m_splatPaintTool`, accessor `MutableSplatPaintTool()`, raccourci `P` dans `HandleShortcut(ctrl, shift)` | M100.10 | Modify |
| `engine/editor/world/panels/ToolPropertiesPanel.cpp` | Modif : render UI splat paint quand `GetActiveTool() == SplatPaint` | M100.10 | Modify |
| `CMakeLists.txt` (racine) | Modif : ajouter sources à `engine_core` + 2 nouveaux test targets `splat_map_tests` + `splat_paint_tests` + custom command pour copier `layer_palette.json` à côté des binaires + invoquer `gen_terrain_placeholders` | tous | Modify |
| `tickets/M100/INDEX.md` | Marquer M100.9 + M100.10 « Done partial » (assets PBR fallback placeholders) | tous | Modify |

**Conventions transverses (CLAUDE.md) :**
- Toute fonction nouvelle/modifiée a un commentaire `///` Doxygen au-dessus de la déclaration (rôle, params non-évidents, effets de bord, contraintes thread).
- Code en français pour les commentaires, anglais pour les identifiants.
- TDD strict : test red commit → impl green commit (commits séparés) sauf cas trivial.
- Aucun `--no-verify` sur les commits.
- Framework de test : **REQUIRE macro maison** (cf. `engine/editor/world/tests/CommandStackTests.cpp` ou `engine/world/terrain/tests/TerrainChunkTests.cpp` pour le pattern). PAS Catch2.

**Anti-duplication serveur :**
- `engine/world/terrain/SplatMap.cpp` + `LayerPalette.cpp` exclus du target serveur (vérification : `grep -RIn "engine::world::terrain::SplatMap" engine/server/` doit retourner 0).
- `engine/render/TerrainChunkPipeline.cpp` + shaders : déjà exclus (le serveur n'a pas Vulkan).
- `tools/zone_builder/lib/CMakeLists.txt` : ajouter `SplatMap.cpp` + `LayerPalette.cpp` aux sources dupliquées (sinon link error sur `WriteSplatMap`, comme le bug `1c790b6` qu'on a déjà corrigé pour TerrainChunk en Phase 2).

---

## Tasks

### Task 1 : Reconnaissance — confirmer base et APIs Phase 1+2

**Files :**
- Read seulement.

- [ ] **Step 1 : Confirmer la branche et les prérequis Phase 1+2 sur main**

```bash
git status
git log origin/main --oneline | head -5
ls engine/world/terrain/
ls engine/editor/world/
```

Expected : branche `claude/m100-phase-3a-splat`, main contient les commits Phase 1 (M100.1-4) + Phase 2 (M100.5-8) mergés. `engine/world/terrain/` contient `TerrainChunk.{h,cpp}`, `TerrainChunkLoader.{h,cpp}`, `TerrainLodChain.{h,cpp}`, `TerrainLodWorker.{h,cpp}`, `TerrainMeshBuilder.{h,cpp}`. `engine/editor/world/` contient `CommandStack.{h,cpp}`, `WorldEditorShell.{h,cpp}`, `TerrainDocument.{h,cpp}`, `TerrainBrush.{h,cpp}`, `TerrainSculptCommand.{h,cpp}`, `TerrainSculptTool.{h,cpp}`, `TerrainStampCommand.{h,cpp}`, `TerrainStampTool.{h,cpp}`, `StampLibrary.{h,cpp}`, `ProceduralStampGenerators.{h,cpp}`, `panels/`, `tests/`.

- [ ] **Step 2 : Lire les sources existantes touchées**

```bash
cat engine/world/terrain/TerrainChunk.h | head -80
cat engine/world/ChunkPackageLayout.h
cat engine/editor/world/TerrainDocument.h
cat engine/editor/world/WorldEditorShell.h | head -130
cat engine/render/DeferredPipeline.cpp | head -100
cat engine/render/ShaderCompiler.h
ls game/data/shaders/ | head -20
```

Notes attendues :
- `ChunkSegment` enum a déjà `Geo, Terrain, Tex, Instances, Nav, Probes` (Phase 2 a ajouté `Terrain`). Cette PR ajoutera `Splat` après `Terrain`.
- `kChunkSegmentCount` = 6, devra passer à 7.
- `kChunkMetaHasTerrain` est déjà défini (`1u << 5`). Cette PR ajoute `kChunkMetaHasSplat = 1u << 6`.
- `WorldEditorShell::ActiveTool` enum : `None, TerrainSculpt, TerrainStamp`. Cette PR ajoute `SplatPaint`.
- `TerrainDocument::EnsureLoaded(cfg, x, z)` retourne `shared_ptr<TerrainChunk>`. Cette PR ajoute `EnsureSplatLoaded(cfg, x, z)` retournant `shared_ptr<SplatMap>` (pattern miroir).
- `DeferredPipeline.cpp` est le pattern de référence pour la pipeline Vulkan.
- Shaders : convention `game/data/shaders/*.vert/.frag`, **pas** `engine/render/shaders/`.

- [ ] **Step 3 : Confirmer existence stb_image_write**

```bash
grep -n "stb_image_write" external/stb/stb_image.h external/stb/*.h 2>&1 | head -5
ls external/stb/
```

Expected : si `stb_image_write.h` est dans `external/stb/`, le tool placeholder l'utilise. Sinon, il fait du PNG canonique à la main (~80 lignes, faisable). Note : `WorldMapIo.cpp` / `TexturePreviewCache.cpp` / `AssetRegistry.cpp` définissent déjà `STB_IMAGE_IMPLEMENTATION` ; pour `STB_IMAGE_WRITE_IMPLEMENTATION`, vérifier qu'il n'est défini nulle part avant de l'introduire.

Si `stb_image_write.h` est absent, deux options :
- (A) Implémenter PNG 4×4 canonique à la main dans le tool (~80 lignes, déterministe, aucune dépendance).
- (B) Ajouter `external/stb/stb_image_write.h` (un seul header public domain, ~1500 lignes).

**Décision recommandée : (A)** pour ne pas grossir le repo. Code source PNG simple : signature, IHDR (width=4 height=4 bitdepth=8 colortype=2 RGB), IDAT compressé via zlib (mais zlib n'est pas dispo ?). Si zlib absent, fallback (B).

- [ ] **Step 4 : Pas de commit** — reconnaissance pure.

---

### Task 2 : `SplatMap.h` — constantes + struct (M100.9)

**Files :**
- Create : `engine/world/terrain/SplatMap.h`

- [ ] **Step 1 : Créer le header**

```cpp
// engine/world/terrain/SplatMap.h
#pragma once

#include "engine/world/OutputVersion.h"

#include <cstdint>
#include <span>
#include <string>
#include <vector>

namespace engine::world::terrain
{
	/// Magic du fichier `chunks/chunk_i_j/splat.bin` ("SLAT" little-endian).
	/// Distinct de `kTerrainSplatFileMagic` (= "SLAP") du legacy
	/// `engine::render::terrain::TerrainSplatting` qui sert le mode démo
	/// single-zone non-chunked. Coexistence intentionnelle.
	constexpr uint32_t kSplatMagic = 0x54414C53u;
	/// Version courante du payload `splat.bin` (M100.9).
	constexpr uint32_t kSplatVersion = 1u;
	/// Résolution par chunk (alignée sur `TerrainChunk` 257²).
	constexpr uint32_t kSplatResolution = 257u;
	/// Nombre de layers par cellule (8 octets de poids).
	constexpr uint32_t kSplatLayerCount = 8u;

	/// Splat-map éditable d'un chunk monde (M100.9). Stockage row-major
	/// `weights[(z * resolution + x) * layerCount + layer]`. Invariant : pour
	/// chaque cellule (x, z), la somme des `layerCount` poids vaut exactement
	/// `255` (validé au `Load` et après chaque commit `SplatPaintCommand`).
	struct SplatMap
	{
		uint32_t resolution = kSplatResolution;
		uint32_t layerCount = kSplatLayerCount;
		std::vector<uint8_t> weights; // taille = resolution * resolution * layerCount

		/// Initialise une splat-map où toutes les cellules ont `layerIndex=255`
		/// et les autres layers à 0. Somme=255 invariant satisfait par
		/// construction.
		/// \param layerIndex 0..(kSplatLayerCount-1).
		static SplatMap MakeUniform(uint32_t layerIndex);

		/// Vérifie l'invariant somme=255 par cellule. Coût O(resolution²).
		/// \return true si toutes les cellules respectent l'invariant.
		bool IsValid() const;
	};

	/// Sérialise `splat` au format `splat.bin` (M100.9). Header
	/// `OutputVersionHeader` (magic=`kSplatMagic`, version=`kSplatVersion`,
	/// `contentHash` = xxhash64 du payload post-header), puis :
	///   uint32 resolution + uint32 layerCount + uint8[res²*layers] weights.
	/// \return true si écriture OK et `IsValid()` est vrai.
	bool SaveSplatBin(const SplatMap& splat, std::vector<uint8_t>& outBytes, std::string& outError);

	/// Désérialise un `splat.bin`. Valide magic, version, contentHash,
	/// resolution (== `kSplatResolution`), layerCount (== `kSplatLayerCount`),
	/// puis l'invariant somme=255 par cellule.
	bool LoadSplatBin(std::span<const uint8_t> bytes, SplatMap& outSplat, std::string& outError);
}
```

- [ ] **Step 2 : Commit**

```bash
git add engine/world/terrain/SplatMap.h
git commit -m "feat(world/terrain): declare SplatMap + format binaire SLAT (M100.9)"
```

---

### Task 3 : Tests `SplatMap` (TDD red — M100.9)

**Files :**
- Create : `engine/world/terrain/tests/SplatMapTests.cpp`

- [ ] **Step 1 : Écrire les 5 tests core**

```cpp
// engine/world/terrain/tests/SplatMapTests.cpp
#include "engine/world/terrain/SplatMap.h"

#include <cstdio>
#include <cstring>
#include <vector>

namespace
{
	int g_failed = 0;

	#define REQUIRE(cond) do { \
		if (!(cond)) { \
			std::fprintf(stderr, "[FAIL] %s:%d  %s\n", __FILE__, __LINE__, #cond); \
			++g_failed; \
		} \
	} while (0)

	using engine::world::terrain::SplatMap;
	using engine::world::terrain::SaveSplatBin;
	using engine::world::terrain::LoadSplatBin;
	using engine::world::terrain::kSplatLayerCount;
	using engine::world::terrain::kSplatResolution;

	/// MakeUniform(0) : layer 0 = 255 partout, autres = 0, somme = 255 partout.
	void Test_MakeUniform_SumIs255()
	{
		auto splat = SplatMap::MakeUniform(0u);
		REQUIRE(splat.resolution == kSplatResolution);
		REQUIRE(splat.layerCount == kSplatLayerCount);
		REQUIRE(splat.weights.size() ==
			static_cast<size_t>(kSplatResolution) * kSplatResolution * kSplatLayerCount);
		REQUIRE(splat.IsValid());
		// Vérifie quelques cellules : layer 0 = 255, layers 1..7 = 0.
		for (uint32_t z : {0u, 100u, kSplatResolution - 1u})
		{
			for (uint32_t x : {0u, 100u, kSplatResolution - 1u})
			{
				const size_t base = (z * kSplatResolution + x) * kSplatLayerCount;
				REQUIRE(splat.weights[base + 0] == 255);
				for (uint32_t layer = 1; layer < kSplatLayerCount; ++layer)
					REQUIRE(splat.weights[base + layer] == 0);
			}
		}
	}

	/// MakeUniform avec layer hors bornes → fallback layer 0.
	void Test_MakeUniform_OutOfBoundsClampsToZero()
	{
		auto splat = SplatMap::MakeUniform(99u);
		REQUIRE(splat.IsValid());
		const size_t base = 0;
		REQUIRE(splat.weights[base + 0] == 255);
	}

	/// Save → Load redonne la même splat byte-exact.
	void Test_SaveLoad_Roundtrip()
	{
		auto src = SplatMap::MakeUniform(3u);
		std::vector<uint8_t> bytes;
		std::string err;
		REQUIRE(SaveSplatBin(src, bytes, err));
		REQUIRE(err.empty());

		SplatMap dst;
		REQUIRE(LoadSplatBin(bytes, dst, err));
		REQUIRE(err.empty());
		REQUIRE(dst.resolution == src.resolution);
		REQUIRE(dst.layerCount == src.layerCount);
		REQUIRE(std::memcmp(dst.weights.data(), src.weights.data(),
			src.weights.size()) == 0);
	}

	/// Magic patché → Load doit échouer.
	void Test_Load_RejectsBadMagic()
	{
		auto src = SplatMap::MakeUniform(0u);
		std::vector<uint8_t> bytes;
		std::string err;
		REQUIRE(SaveSplatBin(src, bytes, err));
		bytes[0] = 'X'; bytes[1] = 'X'; bytes[2] = 'X'; bytes[3] = 'X';
		SplatMap dst;
		REQUIRE(!LoadSplatBin(bytes, dst, err));
		REQUIRE(!err.empty());
	}

	/// Cellule avec somme != 255 → Load doit échouer.
	void Test_Load_RejectsSumNot255()
	{
		auto src = SplatMap::MakeUniform(0u);
		// Casse la cellule (50, 50) : layer 0 = 200 au lieu de 255.
		const size_t base = (50u * kSplatResolution + 50u) * kSplatLayerCount;
		src.weights[base + 0] = 200; // somme cellule = 200, pas 255

		std::vector<uint8_t> bytes;
		std::string err;
		// SaveSplatBin doit échouer aussi (validate avant écriture).
		REQUIRE(!SaveSplatBin(src, bytes, err));

		// Forcer une écriture corrompue : on save d'abord la bonne, puis on patche.
		auto good = SplatMap::MakeUniform(0u);
		REQUIRE(SaveSplatBin(good, bytes, err));
		// Trouve l'offset post-header (24) + uint32 resolution (4) + uint32 layerCount (4) = 32.
		const size_t cellOffset = 32u + base;
		bytes[cellOffset + 0] = 200; // casse l'invariant
		// Recalcule le contentHash sinon Load échoue d'abord sur le hash.
		// Plus simple : skip la validation hash et Load doit quand même rejeter
		// sur l'invariant somme. → Le test Load doit échouer pour somme OU pour hash,
		// donc dans tous les cas !LoadSplatBin.
		SplatMap dst;
		REQUIRE(!LoadSplatBin(bytes, dst, err));
	}
}

int main()
{
	Test_MakeUniform_SumIs255();
	Test_MakeUniform_OutOfBoundsClampsToZero();
	Test_SaveLoad_Roundtrip();
	Test_Load_RejectsBadMagic();
	Test_Load_RejectsSumNot255();

	if (g_failed == 0)
	{
		std::printf("[PASS] SplatMapTests (5/5)\n");
		return 0;
	}
	std::printf("[FAIL] SplatMapTests: %d failure(s)\n", g_failed);
	return 1;
}
```

- [ ] **Step 2 : Commit (red — pas encore d'impl)**

```bash
git add engine/world/terrain/tests/SplatMapTests.cpp
git commit -m "test(world/terrain): SplatMap tests M100.9 (TDD red)"
```

---

### Task 4 : `SplatMap.cpp` impl + green (M100.9)

**Files :**
- Create : `engine/world/terrain/SplatMap.cpp`

- [ ] **Step 1 : Impl**

```cpp
// engine/world/terrain/SplatMap.cpp
#include "engine/world/terrain/SplatMap.h"

#include <cstring>

namespace engine::world::terrain
{
	SplatMap SplatMap::MakeUniform(uint32_t layerIndex)
	{
		SplatMap s;
		const size_t cellCount = static_cast<size_t>(kSplatResolution) * kSplatResolution;
		s.weights.assign(cellCount * kSplatLayerCount, 0u);
		const uint32_t safeLayer = (layerIndex < kSplatLayerCount) ? layerIndex : 0u;
		for (size_t cell = 0; cell < cellCount; ++cell)
			s.weights[cell * kSplatLayerCount + safeLayer] = 255u;
		return s;
	}

	bool SplatMap::IsValid() const
	{
		if (resolution != kSplatResolution || layerCount != kSplatLayerCount)
			return false;
		const size_t cellCount = static_cast<size_t>(resolution) * resolution;
		if (weights.size() != cellCount * layerCount)
			return false;
		for (size_t cell = 0; cell < cellCount; ++cell)
		{
			uint32_t sum = 0;
			for (uint32_t layer = 0; layer < layerCount; ++layer)
				sum += weights[cell * layerCount + layer];
			if (sum != 255u) return false;
		}
		return true;
	}

	namespace
	{
		uint8_t* Write32(uint8_t* dst, uint32_t v)
		{ std::memcpy(dst, &v, sizeof(v)); return dst + sizeof(v); }
		uint8_t* Write64(uint8_t* dst, uint64_t v)
		{ std::memcpy(dst, &v, sizeof(v)); return dst + sizeof(v); }
	}

	bool SaveSplatBin(const SplatMap& splat, std::vector<uint8_t>& outBytes, std::string& outError)
	{
		if (!splat.IsValid())
		{ outError = "SplatMap: IsValid() failed (somme=255 invariant)"; return false; }

		const size_t headerSize = 24u;
		const size_t metaSize = 4u + 4u; // resolution + layerCount
		const size_t weightsSize =
			static_cast<size_t>(splat.resolution) * splat.resolution * splat.layerCount;
		const size_t totalSize = headerSize + metaSize + weightsSize;
		outBytes.assign(totalSize, 0u);

		// Post-header : metadata + weights.
		uint8_t* p = outBytes.data() + headerSize;
		p = Write32(p, splat.resolution);
		p = Write32(p, splat.layerCount);
		std::memcpy(p, splat.weights.data(), weightsSize);

		// ContentHash sur payload post-header.
		std::span<const uint8_t> payload(outBytes.data() + headerSize, totalSize - headerSize);
		const uint64_t contentHash = engine::world::ComputeXxHash64(payload);

		uint8_t* h = outBytes.data();
		h = Write32(h, kSplatMagic);
		h = Write32(h, kSplatVersion);
		h = Write32(h, engine::world::kZoneBuilderVersion);
		h = Write32(h, engine::world::kZoneEngineVersion);
		h = Write64(h, contentHash);
		(void)h;
		return true;
	}

	bool LoadSplatBin(std::span<const uint8_t> bytes, SplatMap& outSplat, std::string& outError)
	{
		const size_t headerSize = 24u;
		const size_t metaSize = 8u;
		const size_t weightsSize =
			static_cast<size_t>(kSplatResolution) * kSplatResolution * kSplatLayerCount;
		const size_t expectedSize = headerSize + metaSize + weightsSize;

		if (bytes.size() != expectedSize)
		{ outError = "splat.bin: unexpected size"; return false; }

		engine::world::OutputVersionHeader hdr;
		if (!engine::world::ReadOutputVersionHeader(bytes, hdr, outError)) return false;
		if (hdr.magic != kSplatMagic)
		{ outError = "splat.bin: bad magic"; return false; }
		if (hdr.formatVersion != kSplatVersion)
		{ outError = "splat.bin: bad version"; return false; }

		std::span<const uint8_t> payload = bytes.subspan(headerSize);
		if (engine::world::ComputeXxHash64(payload) != hdr.contentHash)
		{ outError = "splat.bin: contentHash mismatch"; return false; }

		const uint8_t* p = bytes.data() + headerSize;
		std::memcpy(&outSplat.resolution, p, 4); p += 4;
		std::memcpy(&outSplat.layerCount, p, 4); p += 4;
		if (outSplat.resolution != kSplatResolution)
		{ outError = "splat.bin: resolution mismatch"; return false; }
		if (outSplat.layerCount != kSplatLayerCount)
		{ outError = "splat.bin: layerCount mismatch"; return false; }

		outSplat.weights.assign(weightsSize, 0u);
		std::memcpy(outSplat.weights.data(), p, weightsSize);

		if (!outSplat.IsValid())
		{ outError = "splat.bin: somme!=255 invariant violated"; return false; }
		return true;
	}
}
```

- [ ] **Step 2 : Ajouter source à engine_core dans `CMakeLists.txt`**

Trouver le bloc `add_library(engine_core …)` et ajouter `engine/world/terrain/SplatMap.cpp` après les autres sources `engine/world/terrain/*.cpp`.

- [ ] **Step 3 : Ajouter test target `splat_map_tests`**

Pattern identique à `terrain_chunk_tests` (cf. `CMakeLists.txt` autour de la ligne où `terrain_chunk_tests` est défini) :

```cmake
# M100.9 — Tests unitaires CPU pour SplatMap (8-layer splat per chunk).
if(WIN32)
  add_executable(splat_map_tests engine/world/terrain/tests/SplatMapTests.cpp)
  target_include_directories(splat_map_tests PRIVATE ${CMAKE_SOURCE_DIR})
  target_link_libraries(splat_map_tests PRIVATE engine_core)
  if(MSVC)
    target_compile_options(splat_map_tests PRIVATE /W4 /permissive- /Zc:preprocessor)
  endif()
  add_test(NAME splat_map_tests COMMAND splat_map_tests)
endif()
```

- [ ] **Step 4 : Commit**

```bash
git add engine/world/terrain/SplatMap.cpp CMakeLists.txt
git commit -m "feat(world/terrain): implemente SplatMap + Save/LoadSplatBin (M100.9)"
```

---

### Task 5 : `LayerPalette.h/.cpp` (M100.9)

**Files :**
- Create : `engine/world/terrain/LayerPalette.h`
- Create : `engine/world/terrain/LayerPalette.cpp`

- [ ] **Step 1 : Header**

```cpp
// engine/world/terrain/LayerPalette.h
#pragma once

#include <array>
#include <cstdint>
#include <filesystem>
#include <string>

namespace engine::world::terrain
{
	/// Une entrée de la `LayerPalette` (M100.9). 8 layers, indexées 0..7.
	struct LayerEntry
	{
		uint32_t index = 0;
		std::string name;
		std::string albedoPath;
		std::string normalPath;
		std::string armPath;
		float tilingMeters = 4.0f;
		std::string surfaceType; // M100.11 : enum string ; ici simple string
	};

	/// Palette des 8 layers terrain. Lue depuis `assets/terrain/layer_palette.json`.
	struct LayerPalette
	{
		uint32_t version = 1u;
		std::array<LayerEntry, 8> layers;
	};

	/// Charge `path` (JSON) dans `outPalette`. Format : `{"version":1, "layers":[
	/// {"index":0,"name":"dirt","albedo":"...","normal":"...","arm":"...",
	///  "tilingMeters":4.0,"surfaceType":"Dirt"}, …8 entrées]}`.
	/// \return true si OK, sinon `outError` renseigné.
	bool LoadLayerPalette(const std::filesystem::path& path,
		LayerPalette& outPalette, std::string& outError);
}
```

- [ ] **Step 2 : Impl avec parser JSON existant**

Le repo utilise `engine/core/Config` (parser JSON simple) pour la plupart des configs. Pour la palette qui est un tableau de structs, le plus simple est de lire le fichier en string et de faire un parse minimal (le format est fixé). Alternative : utiliser un parser JSON tiers si présent. **Décision** : implémenter un mini-parser linéaire qui lit la structure attendue.

```cpp
// engine/world/terrain/LayerPalette.cpp
#include "engine/world/terrain/LayerPalette.h"

#include <fstream>
#include <sstream>

namespace engine::world::terrain
{
	namespace
	{
		/// Parser JSON minimal : trouve la valeur d'une clé string dans un objet
		/// (ex. `"name": "dirt"` → "dirt"). Pas de support objets imbriqués.
		std::string ExtractStringField(const std::string& obj, const std::string& key, const std::string& fallback = "")
		{
			const std::string searchKey = "\"" + key + "\"";
			size_t keyPos = obj.find(searchKey);
			if (keyPos == std::string::npos) return fallback;
			size_t colonPos = obj.find(':', keyPos);
			if (colonPos == std::string::npos) return fallback;
			size_t startQuote = obj.find('"', colonPos + 1);
			if (startQuote == std::string::npos) return fallback;
			size_t endQuote = obj.find('"', startQuote + 1);
			if (endQuote == std::string::npos) return fallback;
			return obj.substr(startQuote + 1, endQuote - startQuote - 1);
		}

		/// Idem pour une valeur numérique (ex. `"tilingMeters": 4.0` → 4.0f).
		float ExtractFloatField(const std::string& obj, const std::string& key, float fallback = 0.0f)
		{
			const std::string searchKey = "\"" + key + "\"";
			size_t keyPos = obj.find(searchKey);
			if (keyPos == std::string::npos) return fallback;
			size_t colonPos = obj.find(':', keyPos);
			if (colonPos == std::string::npos) return fallback;
			std::string remainder = obj.substr(colonPos + 1);
			try { return std::stof(remainder); } catch (...) { return fallback; }
		}

		uint32_t ExtractUintField(const std::string& obj, const std::string& key, uint32_t fallback = 0u)
		{
			const std::string searchKey = "\"" + key + "\"";
			size_t keyPos = obj.find(searchKey);
			if (keyPos == std::string::npos) return fallback;
			size_t colonPos = obj.find(':', keyPos);
			if (colonPos == std::string::npos) return fallback;
			std::string remainder = obj.substr(colonPos + 1);
			try { return static_cast<uint32_t>(std::stoul(remainder)); } catch (...) { return fallback; }
		}
	}

	bool LoadLayerPalette(const std::filesystem::path& path,
		LayerPalette& outPalette, std::string& outError)
	{
		std::ifstream f(path);
		if (!f.good()) { outError = "LayerPalette: cannot open " + path.string(); return false; }

		std::stringstream buf;
		buf << f.rdbuf();
		const std::string content = buf.str();

		outPalette.version = ExtractUintField(content, "version", 1u);

		// Parse les 8 objets layer. On découpe le tableau "layers": [ ... ].
		// Format attendu : objets séparés par '},' ou '}\n'.
		const std::string layersKey = "\"layers\"";
		size_t arrStart = content.find('[', content.find(layersKey));
		if (arrStart == std::string::npos)
		{ outError = "LayerPalette: missing 'layers' array"; return false; }

		size_t cursor = arrStart;
		for (uint32_t i = 0; i < 8u; ++i)
		{
			size_t objStart = content.find('{', cursor);
			if (objStart == std::string::npos)
			{ outError = "LayerPalette: missing layer entry " + std::to_string(i); return false; }
			size_t objEnd = content.find('}', objStart);
			if (objEnd == std::string::npos)
			{ outError = "LayerPalette: unterminated layer entry " + std::to_string(i); return false; }
			std::string obj = content.substr(objStart, objEnd - objStart + 1);

			LayerEntry& e = outPalette.layers[i];
			e.index        = ExtractUintField(obj, "index", i);
			e.name         = ExtractStringField(obj, "name");
			e.albedoPath   = ExtractStringField(obj, "albedo");
			e.normalPath   = ExtractStringField(obj, "normal");
			e.armPath      = ExtractStringField(obj, "arm");
			e.tilingMeters = ExtractFloatField(obj, "tilingMeters", 4.0f);
			e.surfaceType  = ExtractStringField(obj, "surfaceType", "Dirt");

			cursor = objEnd + 1;
		}
		return true;
	}
}
```

**Note** : le parser est rudimentaire mais suffit pour le format fixé. Si plus tard le format évolue (objets imbriqués, escape sequences), migrer vers un parser propre.

- [ ] **Step 3 : Ajouter à CMake (engine_core sources + sources dupliquées zone_builder_lib)**

Dans `CMakeLists.txt` racine, ajouter `engine/world/terrain/LayerPalette.cpp` à `engine_core`.

Dans `tools/zone_builder/lib/CMakeLists.txt`, ajouter `${CMAKE_SOURCE_DIR}/engine/world/terrain/SplatMap.cpp` ET `${CMAKE_SOURCE_DIR}/engine/world/terrain/LayerPalette.cpp` aux sources dupliquées (sinon link error sur `WriteSplatMap`, comme le bug `1c790b6` qu'on a déjà corrigé pour TerrainChunk).

- [ ] **Step 4 : Commit**

```bash
git add engine/world/terrain/LayerPalette.h engine/world/terrain/LayerPalette.cpp \
        CMakeLists.txt tools/zone_builder/lib/CMakeLists.txt
git commit -m "feat(world/terrain): LayerPalette JSON parser (M100.9)"
```

---

### Task 6 : `assets/terrain/layer_palette.json` (M100.9)

**Files :**
- Create : `assets/terrain/layer_palette.json`

- [ ] **Step 1 : Écrire les 8 layers**

```json
{
  "version": 1,
  "layers": [
    { "index": 0, "name": "dirt",
      "albedo": "tex/terrain/dirt_albedo.texr",
      "normal": "tex/terrain/dirt_normal.texr",
      "arm":    "tex/terrain/dirt_arm.texr",
      "tilingMeters": 4.0, "surfaceType": "Dirt" },
    { "index": 1, "name": "grass_dry",
      "albedo": "tex/terrain/grass_dry_albedo.texr",
      "normal": "tex/terrain/grass_dry_normal.texr",
      "arm":    "tex/terrain/grass_dry_arm.texr",
      "tilingMeters": 3.0, "surfaceType": "Grass" },
    { "index": 2, "name": "grass_wet",
      "albedo": "tex/terrain/grass_wet_albedo.texr",
      "normal": "tex/terrain/grass_wet_normal.texr",
      "arm":    "tex/terrain/grass_wet_arm.texr",
      "tilingMeters": 3.0, "surfaceType": "Grass" },
    { "index": 3, "name": "mud",
      "albedo": "tex/terrain/mud_albedo.texr",
      "normal": "tex/terrain/mud_normal.texr",
      "arm":    "tex/terrain/mud_arm.texr",
      "tilingMeters": 4.0, "surfaceType": "Mud" },
    { "index": 4, "name": "sand",
      "albedo": "tex/terrain/sand_albedo.texr",
      "normal": "tex/terrain/sand_normal.texr",
      "arm":    "tex/terrain/sand_arm.texr",
      "tilingMeters": 4.0, "surfaceType": "Sand" },
    { "index": 5, "name": "rock",
      "albedo": "tex/terrain/rock_albedo.texr",
      "normal": "tex/terrain/rock_normal.texr",
      "arm":    "tex/terrain/rock_arm.texr",
      "tilingMeters": 5.0, "surfaceType": "Rock" },
    { "index": 6, "name": "snow",
      "albedo": "tex/terrain/snow_albedo.texr",
      "normal": "tex/terrain/snow_normal.texr",
      "arm":    "tex/terrain/snow_arm.texr",
      "tilingMeters": 4.0, "surfaceType": "Snow" },
    { "index": 7, "name": "lava_cooled",
      "albedo": "tex/terrain/lava_cooled_albedo.texr",
      "normal": "tex/terrain/lava_cooled_normal.texr",
      "arm":    "tex/terrain/lava_cooled_arm.texr",
      "tilingMeters": 6.0, "surfaceType": "LavaCooled" }
  ]
}
```

**Note** : les paths `.texr` ne pointent pas vers des fichiers existants. Le shader fragment fera un fallback vers les placeholders `assets/terrain/placeholders/<name>.png` si le `.texr` est absent (cf. Task 14).

- [ ] **Step 2 : Ajouter le test `Test_LayerPalette_LoadJson` dans `SplatMapTests.cpp`**

```cpp
// Ajouter à SplatMapTests.cpp avant int main() :

#include "engine/world/terrain/LayerPalette.h"
#include <filesystem>

void Test_LayerPalette_LoadJson()
{
	using engine::world::terrain::LayerPalette;
	using engine::world::terrain::LoadLayerPalette;

	const std::filesystem::path path = "assets/terrain/layer_palette.json";
	if (!std::filesystem::exists(path))
	{
		std::printf("[SKIP] Test_LayerPalette_LoadJson: %s missing\n", path.string().c_str());
		return;
	}
	LayerPalette palette;
	std::string err;
	REQUIRE(LoadLayerPalette(path, palette, err));
	REQUIRE(err.empty());
	REQUIRE(palette.version == 1u);
	REQUIRE(palette.layers[0].name == "dirt");
	REQUIRE(palette.layers[7].name == "lava_cooled");
	REQUIRE(palette.layers[1].tilingMeters > 0.0f);
}

// Et dans main(), avant le PASS :
//   Test_LayerPalette_LoadJson();
```

- [ ] **Step 3 : Commit**

```bash
git add assets/terrain/layer_palette.json engine/world/terrain/tests/SplatMapTests.cpp
git commit -m "feat(world/terrain): assets/terrain/layer_palette.json (8 layers) + parse test (M100.9)"
```

---

### Task 7 : `tools/gen_terrain_placeholders` (M100.9)

**Files :**
- Create : `tools/gen_terrain_placeholders/main.cpp`
- Create : `tools/gen_terrain_placeholders/CMakeLists.txt`

- [ ] **Step 1 : Mini-tool C++ qui écrit 8 PNG 4×4 unicolore**

Format PNG canonique 4×4, color_type=2 (RGB), bit_depth=8. Chunks : IHDR + IDAT (zlib-compressed pixels) + IEND. **zlib** est tirée transitivement via spdlog/OpenSSL ; on l'utilise en lien direct si disponible. Sinon fallback : écrire des PNG 4×4 avec compression méthode 0 (no compression — header zlib + raw deflate stored block).

Pour réduire la complexité, on encode les pixels en **deflate stored block** (section RFC1951 §3.2.4), pas de besoin de zlib. C'est ~80 lignes au total.

```cpp
// tools/gen_terrain_placeholders/main.cpp
//
// Génère 8 PNG 4×4 unicolore dans `assets/terrain/placeholders/` (chemin
// passé en argv[1], défaut `./assets/terrain/placeholders/`). Utilisé par la
// custom command CMake pour fournir des placeholders du shader 8-layer
// terrain (M100.9). Remplaçables transparente quand les vrais PBR arriveront.
//
// Format : PNG canonique color_type=2 (RGB), bit_depth=8, taille 4x4.
// IDAT compressé en deflate "stored block" (mode 0, pas de compression),
// pour ne dépendre d'aucune bibliothèque zlib.

#include <array>
#include <cstdint>
#include <cstdio>
#include <cstring>
#include <filesystem>
#include <fstream>
#include <string>
#include <vector>

namespace
{
	// Couleurs RGB 8-bit par layer (dirt, grass_dry, grass_wet, mud, sand, rock, snow, lava_cooled).
	constexpr std::array<std::array<uint8_t, 3>, 8> kLayerColors {{
		{{139,  90,  43}}, // dirt — brun
		{{100, 145,  60}}, // grass_dry — vert moyen
		{{ 60, 130,  60}}, // grass_wet — vert foncé
		{{ 78,  50,  30}}, // mud — brun très foncé
		{{220, 200, 130}}, // sand — beige
		{{120, 120, 120}}, // rock — gris
		{{240, 240, 250}}, // snow — blanc bleuté
		{{ 30,  20,  20}}, // lava_cooled — noir grenat
	}};
	constexpr std::array<const char*, 8> kLayerNames {
		"dirt", "grass_dry", "grass_wet", "mud", "sand", "rock", "snow", "lava_cooled"
	};

	uint32_t SwapBE32(uint32_t v) {
		return ((v & 0xFFu) << 24) | ((v & 0xFF00u) << 8)
		     | ((v & 0xFF0000u) >> 8) | ((v >> 24) & 0xFFu);
	}

	// CRC32 standard PNG (polynôme 0xEDB88320).
	uint32_t Crc32(const uint8_t* data, size_t len, uint32_t crc = 0xFFFFFFFFu)
	{
		static uint32_t table[256] = {0};
		static bool init = false;
		if (!init) {
			for (uint32_t i = 0; i < 256; ++i) {
				uint32_t c = i;
				for (int k = 0; k < 8; ++k)
					c = (c & 1u) ? (0xEDB88320u ^ (c >> 1)) : (c >> 1);
				table[i] = c;
			}
			init = true;
		}
		for (size_t i = 0; i < len; ++i)
			crc = table[(crc ^ data[i]) & 0xFFu] ^ (crc >> 8);
		return crc ^ 0xFFFFFFFFu;
	}

	void WriteChunk(std::vector<uint8_t>& out, const char type[4], const std::vector<uint8_t>& data)
	{
		const uint32_t lenBE = SwapBE32(static_cast<uint32_t>(data.size()));
		const uint8_t* lenBytes = reinterpret_cast<const uint8_t*>(&lenBE);
		out.insert(out.end(), lenBytes, lenBytes + 4);
		out.insert(out.end(), reinterpret_cast<const uint8_t*>(type), reinterpret_cast<const uint8_t*>(type) + 4);
		out.insert(out.end(), data.begin(), data.end());

		// CRC sur type + data.
		std::vector<uint8_t> crcBuf;
		crcBuf.insert(crcBuf.end(), reinterpret_cast<const uint8_t*>(type), reinterpret_cast<const uint8_t*>(type) + 4);
		crcBuf.insert(crcBuf.end(), data.begin(), data.end());
		const uint32_t crc = Crc32(crcBuf.data(), crcBuf.size());
		const uint32_t crcBE = SwapBE32(crc);
		const uint8_t* crcBytes = reinterpret_cast<const uint8_t*>(&crcBE);
		out.insert(out.end(), crcBytes, crcBytes + 4);
	}

	// Adler32 standard zlib (somme rolling pour zlib stream).
	uint32_t Adler32(const uint8_t* data, size_t len)
	{
		uint32_t a = 1, b = 0;
		for (size_t i = 0; i < len; ++i) {
			a = (a + data[i]) % 65521u;
			b = (b + a) % 65521u;
		}
		return (b << 16) | a;
	}

	/// Encode `raw` (taille rawLen) en stream zlib avec un seul stored block
	/// non compressé. Header zlib (CMF=0x78, FLG=0x01), bloc stored (BFINAL=1,
	/// BTYPE=00), LEN/NLEN little-endian, raw bytes, Adler32 trailing big-endian.
	std::vector<uint8_t> ZlibStored(const std::vector<uint8_t>& raw)
	{
		std::vector<uint8_t> out;
		out.push_back(0x78); // CMF
		out.push_back(0x01); // FLG (no preset dict, fastest level)

		// Stored block : BFINAL=1, BTYPE=00 → 1 byte = 0x01
		out.push_back(0x01);
		const uint16_t len = static_cast<uint16_t>(raw.size());
		const uint16_t nlen = ~len;
		out.push_back(static_cast<uint8_t>(len & 0xFFu));
		out.push_back(static_cast<uint8_t>((len >> 8) & 0xFFu));
		out.push_back(static_cast<uint8_t>(nlen & 0xFFu));
		out.push_back(static_cast<uint8_t>((nlen >> 8) & 0xFFu));
		out.insert(out.end(), raw.begin(), raw.end());

		// Adler32 trailing.
		const uint32_t adler = Adler32(raw.data(), raw.size());
		const uint32_t adlerBE = SwapBE32(adler);
		const uint8_t* adlerBytes = reinterpret_cast<const uint8_t*>(&adlerBE);
		out.insert(out.end(), adlerBytes, adlerBytes + 4);
		return out;
	}

	void WritePng(const std::filesystem::path& path, uint8_t r, uint8_t g, uint8_t b, uint32_t size = 4)
	{
		std::vector<uint8_t> png;
		// Signature PNG.
		const uint8_t sig[] = {0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A};
		png.insert(png.end(), sig, sig + 8);

		// IHDR : width(4) + height(4) + bitdepth(1) + colortype(1) + compression(1) + filter(1) + interlace(1) = 13 octets.
		std::vector<uint8_t> ihdr;
		const uint32_t wBE = SwapBE32(size), hBE = SwapBE32(size);
		const uint8_t* wBytes = reinterpret_cast<const uint8_t*>(&wBE);
		const uint8_t* hBytes = reinterpret_cast<const uint8_t*>(&hBE);
		ihdr.insert(ihdr.end(), wBytes, wBytes + 4);
		ihdr.insert(ihdr.end(), hBytes, hBytes + 4);
		ihdr.push_back(8); // bit depth
		ihdr.push_back(2); // color type RGB
		ihdr.push_back(0); // compression
		ihdr.push_back(0); // filter
		ihdr.push_back(0); // interlace
		WriteChunk(png, "IHDR", ihdr);

		// IDAT : pixels filtre 0 (None) par scanline, puis zlib stored.
		std::vector<uint8_t> raw;
		for (uint32_t y = 0; y < size; ++y)
		{
			raw.push_back(0); // filter type None
			for (uint32_t x = 0; x < size; ++x)
			{
				raw.push_back(r); raw.push_back(g); raw.push_back(b);
			}
		}
		std::vector<uint8_t> idat = ZlibStored(raw);
		WriteChunk(png, "IDAT", idat);

		// IEND.
		WriteChunk(png, "IEND", {});

		std::ofstream f(path, std::ios::binary | std::ios::trunc);
		f.write(reinterpret_cast<const char*>(png.data()), static_cast<std::streamsize>(png.size()));
	}
}

int main(int argc, char* argv[])
{
	std::filesystem::path outDir = (argc > 1) ? argv[1] : "assets/terrain/placeholders";
	std::error_code ec;
	std::filesystem::create_directories(outDir, ec);
	if (ec)
	{
		std::fprintf(stderr, "[gen_terrain_placeholders] mkdir failed: %s\n", ec.message().c_str());
		return 1;
	}

	for (size_t i = 0; i < 8; ++i)
	{
		const std::filesystem::path file = outDir / (std::string(kLayerNames[i]) + ".png");
		WritePng(file, kLayerColors[i][0], kLayerColors[i][1], kLayerColors[i][2]);
	}
	std::printf("[gen_terrain_placeholders] wrote 8 PNG to %s\n", outDir.string().c_str());
	return 0;
}
```

- [ ] **Step 2 : CMake du tool**

```cmake
# tools/gen_terrain_placeholders/CMakeLists.txt
cmake_minimum_required(VERSION 3.20)
add_executable(gen_terrain_placeholders main.cpp)
if(MSVC)
  target_compile_options(gen_terrain_placeholders PRIVATE /W4 /permissive- /Zc:preprocessor)
else()
  target_compile_options(gen_terrain_placeholders PRIVATE -Wall -Wextra -Wpedantic)
endif()

# Custom command : exécute le tool POST_BUILD pour générer les PNG dans
# assets/terrain/placeholders/. Tourne sur Linux + Windows (cwd = source root).
add_custom_command(TARGET gen_terrain_placeholders POST_BUILD
  COMMAND $<TARGET_FILE:gen_terrain_placeholders>
          "${CMAKE_SOURCE_DIR}/assets/terrain/placeholders"
  WORKING_DIRECTORY ${CMAKE_SOURCE_DIR}
  COMMENT "Generation des placeholders terrain (8 PNG 4x4 colores)..."
)
```

- [ ] **Step 3 : Ajouter `add_subdirectory` racine**

Dans `CMakeLists.txt` racine, après les autres `add_subdirectory(tools/...)` :

```cmake
add_subdirectory(tools/gen_terrain_placeholders)
```

- [ ] **Step 4 : Build le tool + vérifier que les PNG sortent**

```bash
cmake --build build --target gen_terrain_placeholders
ls assets/terrain/placeholders/
```

Expected : 8 PNG (`dirt.png`, `grass_dry.png`, …, `lava_cooled.png`), chacun ~80 octets.

- [ ] **Step 5 : Ajouter `assets/terrain/placeholders/` au `.gitignore`**

Les PNG sont générés au build, pas commités.

```bash
echo "assets/terrain/placeholders/*.png" >> .gitignore
```

- [ ] **Step 6 : Commit**

```bash
git add tools/gen_terrain_placeholders/ CMakeLists.txt .gitignore
git commit -m "feat(tools): gen_terrain_placeholders 8 PNG 4x4 colores (M100.9)"
```

---

### Task 8 : `ChunkSegment::Splat` (M100.9)

**Files :**
- Modify : `engine/world/ChunkPackageLayout.h`
- Modify : `engine/world/ChunkPackageLayout.cpp`

- [ ] **Step 1 : Ajouter `Splat` à l'enum + flag**

Édit `engine/world/ChunkPackageLayout.h` : ajouter `Splat` après `Terrain` dans `enum class ChunkSegment`, passer `kChunkSegmentCount` de 6 à 7, ajouter flag :

```cpp
enum class ChunkSegment : uint8_t
{
	Geo,
	Terrain,
	Splat,    // splat.bin (M100.9, 8 layers 257²)
	Tex,
	Instances,
	Nav,
	Probes
};

constexpr uint32_t kChunkSegmentCount = 7u;

// ...

constexpr uint32_t kChunkMetaHasSplat = 1u << 6;
```

- [ ] **Step 2 : Étendre `GetChunkSegmentFilename` + `GetChunkLoadOrder`**

Édit `ChunkPackageLayout.cpp` :

```cpp
std::string_view GetChunkSegmentFilename(ChunkSegment segment)
{
	switch (segment)
	{
	case ChunkSegment::Geo:       return "geo.pak";
	case ChunkSegment::Terrain:   return "terrain.bin";
	case ChunkSegment::Splat:     return "splat.bin";
	case ChunkSegment::Tex:       return "tex.pak";
	case ChunkSegment::Instances: return "instances.bin";
	case ChunkSegment::Nav:       return "navmesh.bin";
	case ChunkSegment::Probes:    return "probes.bin";
	}
	return "geo.pak";
}

std::array<ChunkSegment, kChunkSegmentCount> GetChunkLoadOrder()
{
	return {{
		ChunkSegment::Geo,
		ChunkSegment::Terrain,
		ChunkSegment::Splat,
		ChunkSegment::Tex,
		ChunkSegment::Instances,
		ChunkSegment::Nav,
		ChunkSegment::Probes
	}};
}
```

- [ ] **Step 3 : Commit**

```bash
git add engine/world/ChunkPackageLayout.h engine/world/ChunkPackageLayout.cpp
git commit -m "feat(world): add ChunkSegment::Splat dans le load order (M100.9)"
```

---

### Task 9 : `StreamCache::LoadSplatMap` (M100.9)

**Files :**
- Modify : `engine/world/StreamCache.h`
- Modify : `engine/world/StreamCache.cpp`

- [ ] **Step 1 : Header**

Forward-declare `SplatMap` + ajouter méthode :

```cpp
// engine/world/StreamCache.h - dans le namespace engine::world::terrain forward declarations :
namespace terrain { struct TerrainChunk; struct TerrainLodChain; struct SplatMap; }

// Dans la classe StreamCache, après LoadTerrainLods :
/// Charge `splat.bin` du chunk `(chunkX, chunkZ)` (M100.9). Tente le cache,
/// sinon lit depuis disque (`<paths.content>/chunks/chunk_<i>_<j>/splat.bin`)
/// et l'insère dans le cache.
/// \return shared_ptr<SplatMap>, ou nullptr si fichier absent / corrompu.
std::shared_ptr<engine::world::terrain::SplatMap> LoadSplatMap(
	const engine::core::Config& config, int chunkX, int chunkZ);
```

- [ ] **Step 2 : Impl**

```cpp
// Dans StreamCache.cpp - ajouter après LoadTerrainLods (et include nécessaires en haut) :
#include "engine/world/terrain/SplatMap.h"

// Méthode :
std::shared_ptr<engine::world::terrain::SplatMap>
StreamCache::LoadSplatMap(const engine::core::Config& config, int chunkX, int chunkZ)
{
	std::ostringstream keyStream;
	keyStream << "chunks/chunk_" << chunkX << "_" << chunkZ << "/splat.bin";
	const std::string cacheKey = keyStream.str();

	auto cached = Lookup(cacheKey);
	if (cached.has_value())
	{
		auto splat = std::make_shared<engine::world::terrain::SplatMap>();
		std::span<const uint8_t> bytes(cached->data(), cached->size());
		std::string err;
		if (engine::world::terrain::LoadSplatBin(bytes, *splat, err)) return splat;
		LOG_WARN(World, "[StreamCache] cached splat.bin invalid: {}", err);
	}

	const std::string contentRoot = config.GetString("paths.content", "game/data");
	const std::string fullPath = contentRoot + "/" + cacheKey;
	std::ifstream f(fullPath, std::ios::binary);
	if (!f.good()) return nullptr;
	f.seekg(0, std::ios::end);
	const std::streamsize fileSize = f.tellg();
	f.seekg(0, std::ios::beg);
	if (fileSize <= 0) return nullptr;
	std::vector<uint8_t> blob(static_cast<size_t>(fileSize));
	f.read(reinterpret_cast<char*>(blob.data()), fileSize);
	if (!f.good() && !f.eof()) return nullptr;

	auto splat = std::make_shared<engine::world::terrain::SplatMap>();
	std::span<const uint8_t> bytes(blob.data(), blob.size());
	std::string err;
	if (!engine::world::terrain::LoadSplatBin(bytes, *splat, err))
	{
		LOG_WARN(World, "[StreamCache] LoadSplatBin fail ({}): {}", fullPath, err);
		return nullptr;
	}
	Insert(cacheKey, blob);
	return splat;
}
```

- [ ] **Step 3 : Commit**

```bash
git add engine/world/StreamCache.h engine/world/StreamCache.cpp
git commit -m "feat(world): StreamCache::LoadSplatMap (M100.9)"
```

---

### Task 10 : `zone_builder::WriteSplatMap` (M100.9)

**Files :**
- Modify : `tools/zone_builder/lib/Public/zone_builder/ChunkPackageWriter.h`
- Modify : `tools/zone_builder/lib/ChunkPackageWriter.cpp`

- [ ] **Step 1 : API + impl**

Forward-declare `SplatMap` dans le header, ajouter signature :

```cpp
namespace engine::world::terrain { struct TerrainChunk; struct TerrainLodChain; struct SplatMap; }

// Dans namespace tools::zone_builder, après WriteTerrainLods :
bool WriteSplatMap(std::string_view outputRootDir, int32_t chunkX, int32_t chunkZ,
	const engine::world::terrain::SplatMap& splat, std::string& outError);
```

Impl dans `.cpp` (pattern identique à `WriteTerrainChunk`) :

```cpp
#include "engine/world/terrain/SplatMap.h"

// Après WriteTerrainLods :
bool WriteSplatMap(std::string_view outputRootDir, int32_t chunkX, int32_t chunkZ,
	const engine::world::terrain::SplatMap& splat, std::string& outError)
{
	std::filesystem::path chunkDir = std::filesystem::path(outputRootDir)
		/ "chunks"
		/ ("chunk_" + std::to_string(chunkX) + "_" + std::to_string(chunkZ));
	std::error_code ec;
	std::filesystem::create_directories(chunkDir, ec);
	if (ec) { outError = "WriteSplatMap: mkdir failed: " + ec.message(); return false; }

	std::vector<uint8_t> bytes;
	if (!engine::world::terrain::SaveSplatBin(splat, bytes, outError)) return false;

	const std::filesystem::path file = chunkDir / "splat.bin";
	std::ofstream out(file, std::ios::binary | std::ios::trunc);
	if (!out.good()) { outError = "WriteSplatMap: open failed: " + file.string(); return false; }
	out.write(reinterpret_cast<const char*>(bytes.data()),
		static_cast<std::streamsize>(bytes.size()));
	if (!out.good()) { outError = "WriteSplatMap: write failed: " + file.string(); return false; }
	return true;
}
```

- [ ] **Step 2 : Commit**

```bash
git add tools/zone_builder/lib/Public/zone_builder/ChunkPackageWriter.h \
        tools/zone_builder/lib/ChunkPackageWriter.cpp
git commit -m "feat(zone_builder): WriteSplatMap pour splat.bin (M100.9)"
```

---

### Task 11 : `TerrainDocument` extension splat (M100.9)

**Files :**
- Modify : `engine/editor/world/TerrainDocument.h`
- Modify : `engine/editor/world/TerrainDocument.cpp`

- [ ] **Step 1 : Ajouter méthodes splat**

Dans le header, après `EnsureLoaded`/`MarkDirty`/`SaveDirtyToDisk` (terrain) :

```cpp
// Inclure SplatMap.h
#include "engine/world/terrain/SplatMap.h"

// Méthodes :
/// Charge la `SplatMap` du chunk `(x, z)` depuis disque, ou crée une splat
/// uniforme layer 0 (= dirt) si fichier absent. Retourne le shared_ptr stable.
/// Idempotent.
std::shared_ptr<engine::world::terrain::SplatMap> EnsureSplatLoaded(
	const engine::core::Config& config, int chunkX, int chunkZ);

/// Marque la splat-map du chunk `coord` comme dirty.
void MarkSplatDirty(engine::world::GlobalChunkCoord coord);

/// Sauvegarde toutes les splat-maps dirty sur disque sous
/// `<paths.content>/chunks/chunk_<i>_<j>/splat.bin`.
size_t SaveDirtySplatToDisk(const engine::core::Config& config);

// Ajouter dans la struct privée ChunkSlot un champ splat + dirty flag,
// OU mieux : créer un second hashmap m_splats parallèle à m_chunks (plus simple).

// Membre privé :
struct SplatSlot {
	std::shared_ptr<engine::world::terrain::SplatMap> splat;
	bool dirty = false;
};
std::unordered_map<uint64_t, SplatSlot> m_splats;
```

- [ ] **Step 2 : Impl**

Pattern identique à `EnsureLoaded` / `MarkDirty` / `SaveDirtyToDisk` (terrain) mais sur `m_splats`. Si le fichier `splat.bin` est absent, créer une `SplatMap::MakeUniform(0)` (= 100 % dirt).

Skeleton attendu :

```cpp
std::shared_ptr<engine::world::terrain::SplatMap>
TerrainDocument::EnsureSplatLoaded(const engine::core::Config& config, int chunkX, int chunkZ)
{
	const engine::world::GlobalChunkCoord coord{chunkX, chunkZ};
	const uint64_t key = PackCoord(coord);
	auto it = m_splats.find(key);
	if (it != m_splats.end()) return it->second.splat;

	const std::string contentRoot = config.GetString("paths.content", "game/data");
	std::ostringstream pathStream;
	pathStream << contentRoot << "/chunks/chunk_" << chunkX << "_" << chunkZ << "/splat.bin";
	const std::string fullPath = pathStream.str();

	std::shared_ptr<engine::world::terrain::SplatMap> splat;
	std::ifstream f(fullPath, std::ios::binary);
	if (f.good())
	{
		f.seekg(0, std::ios::end);
		const std::streamsize size = f.tellg();
		f.seekg(0, std::ios::beg);
		if (size > 0)
		{
			std::vector<uint8_t> blob(static_cast<size_t>(size));
			f.read(reinterpret_cast<char*>(blob.data()), size);
			auto loaded = std::make_shared<engine::world::terrain::SplatMap>();
			std::span<const uint8_t> bytes(blob.data(), blob.size());
			std::string err;
			if (engine::world::terrain::LoadSplatBin(bytes, *loaded, err))
				splat = std::move(loaded);
			else
				LOG_WARN(EditorWorld, "[TerrainDocument] LoadSplatBin fail ({}): {}", fullPath, err);
		}
	}
	if (!splat)
		splat = std::make_shared<engine::world::terrain::SplatMap>(
			engine::world::terrain::SplatMap::MakeUniform(0u));

	m_splats.emplace(key, SplatSlot{splat, false});
	return splat;
}

void TerrainDocument::MarkSplatDirty(engine::world::GlobalChunkCoord coord)
{
	auto it = m_splats.find(PackCoord(coord));
	if (it == m_splats.end()) return;
	it->second.dirty = true;
}

size_t TerrainDocument::SaveDirtySplatToDisk(const engine::core::Config& config)
{
	size_t written = 0;
	const std::string contentRoot = config.GetString("paths.content", "game/data");
	for (auto& [key, slot] : m_splats)
	{
		if (!slot.dirty) continue;
		const uint32_t hi = static_cast<uint32_t>(key >> 32);
		const uint32_t lo = static_cast<uint32_t>(key & 0xFFFFFFFFu);
		const int chunkX = static_cast<int>(static_cast<int32_t>(hi));
		const int chunkZ = static_cast<int>(static_cast<int32_t>(lo));

		std::ostringstream dirStream;
		dirStream << contentRoot << "/chunks/chunk_" << chunkX << "_" << chunkZ;
		const std::filesystem::path dir(dirStream.str());
		std::error_code ec;
		std::filesystem::create_directories(dir, ec);
		if (ec) continue;

		std::vector<uint8_t> bytes;
		std::string err;
		if (!engine::world::terrain::SaveSplatBin(*slot.splat, bytes, err))
		{
			LOG_WARN(EditorWorld, "[TerrainDocument] SaveSplatBin fail: {}", err);
			continue;
		}
		const std::filesystem::path file = dir / "splat.bin";
		std::ofstream out(file, std::ios::binary | std::ios::trunc);
		if (!out.good()) continue;
		out.write(reinterpret_cast<const char*>(bytes.data()),
			static_cast<std::streamsize>(bytes.size()));
		if (!out.good()) continue;
		slot.dirty = false;
		++written;
	}
	return written;
}
```

- [ ] **Step 3 : Commit**

```bash
git add engine/editor/world/TerrainDocument.h engine/editor/world/TerrainDocument.cpp
git commit -m "feat(editor/world): TerrainDocument splat slot (M100.9)"
```

---

### Task 12 : Shaders GLSL `terrain.vert/.frag` (M100.9)

**Files :**
- Create : `game/data/shaders/terrain.vert`
- Create : `game/data/shaders/terrain.frag`

- [ ] **Step 1 : terrain.vert**

```glsl
// game/data/shaders/terrain.vert (M100.9)
// Vertex shader du pipeline `terrain_chunk` : transforme position chunk-local
// (x, y=hauteur, z) en clip space via viewProj. Passe UV 0..1 et worldPos
// au fragment pour le sampling splat + tiling layers.
#version 450

layout(location = 0) in vec3 inPosition;
layout(location = 1) in vec3 inNormal;
layout(location = 2) in vec2 inUV;

layout(set = 0, binding = 0) uniform CameraUBO {
    mat4 viewProj;
} u_camera;

layout(push_constant) uniform PushConstants {
    vec3 chunkOriginWorld; // origine du chunk en monde (m)
    float pad0;
} u_chunk;

layout(location = 0) out vec3 v_normal;
layout(location = 1) out vec2 v_uv;
layout(location = 2) out vec3 v_worldPos;

void main()
{
    vec3 worldPos = u_chunk.chunkOriginWorld + inPosition;
    v_worldPos = worldPos;
    v_normal = inNormal;
    v_uv = inUV;
    gl_Position = u_camera.viewProj * vec4(worldPos, 1.0);
}
```

- [ ] **Step 2 : terrain.frag**

```glsl
// game/data/shaders/terrain.frag (M100.9)
// Fragment shader 8-layer blend. Échantillonne splatMap (RGBA8 + RGBA8
// déclarés via 2 samplers séparés ou un sampler R8G8B8A8 dédoublé) pour
// récupérer 8 poids, puis blend 8 layers PBR (albedo/normal/ARM) tilées
// individuellement.
#version 450

layout(location = 0) in vec3 v_normal;
layout(location = 1) in vec2 v_uv;
layout(location = 2) in vec3 v_worldPos;

layout(set = 2, binding = 0) uniform sampler2D u_splatMap0; // RGBA = layers 0..3 weights
layout(set = 2, binding = 1) uniform sampler2D u_splatMap1; // RGBA = layers 4..7 weights
layout(set = 2, binding = 2) uniform sampler2DArray u_albedoArray;
layout(set = 2, binding = 3) uniform sampler2DArray u_normalArray;
layout(set = 2, binding = 4) uniform sampler2DArray u_armArray;

layout(set = 2, binding = 5) uniform LayerParams {
    float tilingScale[8]; // mètres → 1/tilingMeters = scale UV
} u_layers;

layout(location = 0) out vec4 outAlbedo;
layout(location = 1) out vec4 outNormal;
layout(location = 2) out vec4 outArm;

void main()
{
    vec4 a = texture(u_splatMap0, v_uv);
    vec4 b = texture(u_splatMap1, v_uv);
    float w[8] = float[8](a.r, a.g, a.b, a.a, b.r, b.g, b.b, b.a);

    vec3 albedo = vec3(0.0);
    vec3 normal = vec3(0.0);
    vec3 arm = vec3(0.0);
    float totalW = 0.0;
    for (int i = 0; i < 8; ++i)
    {
        if (w[i] <= 0.001) continue;
        vec2 tiledUV = v_worldPos.xz * u_layers.tilingScale[i];
        albedo += texture(u_albedoArray, vec3(tiledUV, float(i))).rgb * w[i];
        normal += texture(u_normalArray, vec3(tiledUV, float(i))).rgb * w[i];
        arm    += texture(u_armArray,    vec3(tiledUV, float(i))).rgb * w[i];
        totalW += w[i];
    }
    if (totalW > 0.001) { albedo /= totalW; normal /= totalW; arm /= totalW; }

    outAlbedo = vec4(albedo, 1.0);
    outNormal = vec4(normalize(normal * 2.0 - 1.0), 0.0);
    outArm    = vec4(arm, 1.0);
}
```

- [ ] **Step 3 : Commit**

```bash
git add game/data/shaders/terrain.vert game/data/shaders/terrain.frag
git commit -m "feat(shaders): terrain.vert/.frag 8-layer blend (M100.9)"
```

---

### Task 13 : `TerrainChunkPipeline` Vulkan (M100.9)

**Files :**
- Create : `engine/render/TerrainChunkPipeline.h`
- Create : `engine/render/TerrainChunkPipeline.cpp`

**Note importante :** ce task est le plus lourd de la PR (~600-1000 lignes Vulkan boilerplate). Recommandé de **dispatcher un subagent dédié** avec accès au pattern `engine/render/DeferredPipeline.cpp` pour produire un pipeline équivalent adapté au terrain.

- [ ] **Step 1 : Header — API publique minimale**

```cpp
// engine/render/TerrainChunkPipeline.h
#pragma once

#include <vulkan/vulkan.h>

#include <cstdint>
#include <vector>

namespace engine::world::terrain { struct TerrainMeshGpu; struct LayerPalette; }

namespace engine::render
{
	/// Pipeline Vulkan pour rendre un chunk terrain avec splat 8-layers
	/// (M100.9). Crée le pipeline + descriptor set layout + pipeline layout +
	/// pipeline graphique au `Init`, free au `Shutdown`. Le caller fournit le
	/// render pass cible (compatible avec gbuffer mrt = albedo, normal, arm).
	class TerrainChunkPipeline
	{
	public:
		bool Init(VkDevice device, VkPhysicalDevice physDev, VkRenderPass renderPass,
			VkDescriptorSetLayout cameraSetLayout,
			const std::string& shaderRootPath,
			std::string& outError);
		void Shutdown(VkDevice device);

		/// Bind le pipeline et émet le draw indexed pour un chunk donné.
		/// \param cmd command buffer en recording.
		/// \param cameraSet descriptor set 0 (camera UBO).
		/// \param splatSet descriptor set 2 (splat samplers + layer arrays).
		/// \param mesh mesh CPU-uploaded (vertex + index buffers).
		/// \param chunkOriginWorld origine du chunk en mètres monde (push constant).
		void RecordChunkDraw(VkCommandBuffer cmd,
			VkDescriptorSet cameraSet, VkDescriptorSet splatSet,
			const engine::world::terrain::TerrainMeshGpu& mesh,
			float chunkOriginX, float chunkOriginY, float chunkOriginZ);

		VkDescriptorSetLayout GetSplatSetLayout() const { return m_splatSetLayout; }

	private:
		VkPipeline m_pipeline = VK_NULL_HANDLE;
		VkPipelineLayout m_pipelineLayout = VK_NULL_HANDLE;
		VkDescriptorSetLayout m_splatSetLayout = VK_NULL_HANDLE;
		VkShaderModule m_vertShader = VK_NULL_HANDLE;
		VkShaderModule m_fragShader = VK_NULL_HANDLE;
	};
}
```

- [ ] **Step 2 : Impl Vulkan**

L'impl complète (~600 lignes) doit :
1. Compiler les SPIR-V via le `ShaderCompiler` existant (depuis `<shaderRootPath>/terrain.vert/.frag`).
2. Créer `m_splatSetLayout` avec 6 bindings : `(0) sampler2D splatMap0`, `(1) sampler2D splatMap1`, `(2) sampler2DArray albedoArray`, `(3) sampler2DArray normalArray`, `(4) sampler2DArray armArray`, `(5) UBO layerParams`.
3. Créer `m_pipelineLayout` avec 3 set layouts (camera = set 0, vide ou material = set 1, splat = set 2) + push constant `chunkOriginWorld` (16 bytes).
4. Vertex input binding : 32 octets (`TerrainVertex` du M100.5), 3 attributs (pos float3, normal float3, uv float2).
5. Render pass : MRT 3 cibles (albedo R8G8B8A8, normal A2R10G10B10, arm R8G8B8A8).
6. `RecordChunkDraw` : `vkCmdBindPipeline` + `vkCmdBindDescriptorSets` (2 sets : camera + splat) + `vkCmdPushConstants(chunkOriginWorld)` + `vkCmdBindVertexBuffers` + `vkCmdBindIndexBuffer(VK_INDEX_TYPE_UINT32)` + `vkCmdDrawIndexed`.

**Référence à copier-coller** : `engine/render/DeferredPipeline.cpp` (~700 lignes, structure pipeline GBuffer).

- [ ] **Step 3 : CMake + commit**

Ajouter `engine/render/TerrainChunkPipeline.cpp` à `engine_core` dans `CMakeLists.txt`.

```bash
git add engine/render/TerrainChunkPipeline.h engine/render/TerrainChunkPipeline.cpp CMakeLists.txt
git commit -m "feat(render): TerrainChunkPipeline Vulkan 8-layer (M100.9)"
```

---

### Task 14 : `GeometryPass` drawcall terrain par chunk (M100.9)

**Files :**
- Modify : `engine/render/GeometryPass.cpp`

**Note :** ce task résout la dette technique Task 11 reportée de Phase 2. À dispatcher en **subagent** ou faire avec attention.

- [ ] **Step 1 : Récupérer les terrain chunks visibles + leurs splat maps**

Repérer dans `GeometryPass::Record` la boucle des chunks visibles (via `WorldModel` ou équivalent). Pour chaque chunk visible :
- `auto mesh = chunkRuntime->GetTerrainMeshGpu();` (à exposer si pas déjà)
- `auto splat = chunkRuntime->GetSplatMapGpu();` (idem)
- Si les deux sont valides : invoquer `m_terrainChunkPipeline.RecordChunkDraw(cmd, cameraSet, splatSet, mesh, chunkOriginWorld)`.

- [ ] **Step 2 : Vérifier absence de branche éditeur**

Critère M100.9 : `grep -n "m_editorEnabled" engine/render/GeometryPass.cpp` ne doit montrer aucune nouvelle occurrence côté terrain.

- [ ] **Step 3 : Commit**

```bash
git add engine/render/GeometryPass.cpp
git commit -m "feat(render): GeometryPass drawcall terrain_chunk par chunk (M100.9, resout Task 11)"
```

---

### Task 15 : `SplatMapTests` parity (M100.9)

**Files :**
- Modify : `engine/world/terrain/tests/SplatMapTests.cpp`

- [ ] **Step 1 : Ajouter le test de parité**

```cpp
// Ajouter dans le namespace anonyme :
#include <fstream>
#include <filesystem>

void Test_Parity_EditorWritesClientReadsIdentical()
{
	// Construit une SplatMap déterministe.
	auto src = SplatMap::MakeUniform(0u);
	// Modifie quelques cellules pour avoir un pattern non trivial (somme=255 conservée).
	for (uint32_t z = 0; z < 10; ++z)
	{
		for (uint32_t x = 0; x < 10; ++x)
		{
			const size_t base = (z * kSplatResolution + x) * kSplatLayerCount;
			src.weights[base + 0] = 100; // dirt
			src.weights[base + 5] = 155; // rock
			// somme = 255 OK
		}
	}
	REQUIRE(src.IsValid());

	std::vector<uint8_t> bytes;
	std::string err;
	REQUIRE(SaveSplatBin(src, bytes, err));

	// Round-trip : reload et compare bit-à-bit.
	SplatMap dst;
	REQUIRE(LoadSplatBin(bytes, dst, err));
	REQUIRE(std::memcmp(dst.weights.data(), src.weights.data(), src.weights.size()) == 0);
}

// Et dans main(), l'ajouter :
//   Test_Parity_EditorWritesClientReadsIdentical();
//   et bumper le compteur PASS si nécessaire (4/4 → 6/6 avec les 6 tests).
```

- [ ] **Step 2 : Commit**

```bash
git add engine/world/terrain/tests/SplatMapTests.cpp
git commit -m "test(world/terrain): SplatMap parity test M100.9"
```

---

## ─── Bloc M100.10 : Splat Painting Brushes ───

### Task 16 : `SplatPaintCommand` (M100.10)

**Files :**
- Create : `engine/editor/world/SplatPaintCommand.h`
- Create : `engine/editor/world/SplatPaintCommand.cpp`

- [ ] **Step 1 : Header**

```cpp
// engine/editor/world/SplatPaintCommand.h
#pragma once

#include "engine/editor/world/CommandStack.h"
#include "engine/editor/world/TerrainDocument.h"
#include "engine/world/WorldModel.h"

#include <array>
#include <vector>

namespace engine::editor::world
{
	/// Delta sparse d'une cellule : prev[8] et next[8] poids. La cellule est
	/// localisée par (coord chunk, x local, z local).
	struct SplatDeltaCell
	{
		uint16_t x;
		uint16_t z;
		std::array<uint8_t, 8> prev;
		std::array<uint8_t, 8> next;
	};

	struct SplatDeltaChunk
	{
		engine::world::GlobalChunkCoord coord{0, 0};
		std::vector<SplatDeltaCell> cells;
	};

	/// `ICommand` qui applique un delta sparse multi-chunk sur la splat-map
	/// (M100.10). Maintient l'invariant somme=255 par cellule (la `SplatPaintTool`
	/// précalcule les `next[8]` correctement). `mergeKey` non-nul stable durée
	/// du brushstroke ; deux ticks consécutifs partagent la même clé et sont
	/// fusionnés via `TryMerge` (concat des cellules par chunk, dernier write
	/// gagne par cellule).
	class SplatPaintCommand final : public ICommand
	{
	public:
		SplatPaintCommand(TerrainDocument& doc,
			std::vector<SplatDeltaChunk> deltas,
			CommandMergeKey strokeKey);

		const char* GetLabel() const override { return "Splat paint"; }
		size_t GetMemoryFootprint() const override;
		CommandMergeKey GetMergeKey() const override { return m_mergeKey; }
		void Execute() override;
		void Undo() override;
		bool TryMerge(const ICommand& other) override;

	private:
		TerrainDocument* m_doc;
		std::vector<SplatDeltaChunk> m_deltas;
		CommandMergeKey m_mergeKey;
		bool m_applied = false;
	};
}
```

- [ ] **Step 2 : Impl** (pattern identique à `TerrainSculptCommand`)

`Execute` parcourt les delta chunks, charge la splat via `m_doc->EnsureSplatLoaded`, écrit `next[8]` dans `splat->weights[(z*res+x)*8 + layer]` pour chaque layer, marque dirty. `Undo` écrit `prev[8]`.

`TryMerge` : si `other` a même mergeKey et est un `SplatPaintCommand`, fusionne par chunk. Pour la même cellule, `next` prend la valeur la plus récente (= celle de `other`), `prev` reste celui d'origine (du current).

- [ ] **Step 3 : Commit**

```bash
git add engine/editor/world/SplatPaintCommand.h engine/editor/world/SplatPaintCommand.cpp
git commit -m "feat(editor/world): SplatPaintCommand delta sparse + merge (M100.10)"
```

---

### Task 17 : `SplatRules` auto-rules (M100.10)

**Files :**
- Create : `engine/editor/world/SplatRules.h`
- Create : `engine/editor/world/SplatRules.cpp`

- [ ] **Step 1 : Header + impl**

```cpp
// engine/editor/world/SplatRules.h
#pragma once

#include "engine/world/terrain/TerrainChunk.h"

#include <cstdint>

namespace engine::editor::world
{
	/// Calcule la pente (en degrés) au point de cellule (x, z) du chunk.
	/// Utilise les voisins immédiats pour estimer le gradient.
	float ComputeSlopeDeg(const engine::world::terrain::TerrainChunk& chunk,
		uint32_t x, uint32_t z);

	/// Vrai si la cellule (x, z) du chunk satisfait les rules (pente ∈
	/// [slopeMinDeg, slopeMaxDeg] ET altitude ∈ [altMin, altMax]).
	bool MatchesRules(const engine::world::terrain::TerrainChunk& chunk,
		uint32_t x, uint32_t z,
		float slopeMinDeg, float slopeMaxDeg,
		float altMin, float altMax);
}
```

```cpp
// engine/editor/world/SplatRules.cpp
#include "engine/editor/world/SplatRules.h"

#include <algorithm>
#include <cmath>

namespace engine::editor::world
{
	float ComputeSlopeDeg(const engine::world::terrain::TerrainChunk& chunk,
		uint32_t x, uint32_t z)
	{
		const uint32_t xL = (x == 0) ? 0u : x - 1u;
		const uint32_t xR = std::min(x + 1u, chunk.resolutionX - 1u);
		const uint32_t zL = (z == 0) ? 0u : z - 1u;
		const uint32_t zR = std::min(z + 1u, chunk.resolutionZ - 1u);
		const float hL = chunk.heights[z * chunk.resolutionX + xL];
		const float hR = chunk.heights[z * chunk.resolutionX + xR];
		const float hD = chunk.heights[zL * chunk.resolutionX + x];
		const float hU = chunk.heights[zR * chunk.resolutionX + x];
		const float spanX = static_cast<float>(xR - xL) * chunk.cellSizeMeters;
		const float spanZ = static_cast<float>(zR - zL) * chunk.cellSizeMeters;
		const float dhdx = (spanX > 0.0f) ? (hR - hL) / spanX : 0.0f;
		const float dhdz = (spanZ > 0.0f) ? (hU - hD) / spanZ : 0.0f;
		const float slope = std::sqrt(dhdx * dhdx + dhdz * dhdz);
		// slope = tan(angle), donc angle = atan(slope) en radians.
		return std::atan(slope) * 180.0f / 3.14159265358979323846f;
	}

	bool MatchesRules(const engine::world::terrain::TerrainChunk& chunk,
		uint32_t x, uint32_t z,
		float slopeMinDeg, float slopeMaxDeg,
		float altMin, float altMax)
	{
		const float h = chunk.heights[z * chunk.resolutionX + x];
		if (h < altMin || h > altMax) return false;
		const float slope = ComputeSlopeDeg(chunk, x, z);
		if (slope < slopeMinDeg || slope > slopeMaxDeg) return false;
		return true;
	}
}
```

- [ ] **Step 2 : CMake + commit**

```bash
git add engine/editor/world/SplatRules.h engine/editor/world/SplatRules.cpp CMakeLists.txt
git commit -m "feat(editor/world): SplatRules slope/altitude (M100.10)"
```

---

### Task 18 : `SplatPaintTool` (M100.10)

**Files :**
- Create : `engine/editor/world/SplatPaintTool.h`
- Create : `engine/editor/world/SplatPaintTool.cpp`

- [ ] **Step 1 : Header**

```cpp
// engine/editor/world/SplatPaintTool.h
#pragma once

#include "engine/editor/world/CommandStack.h"
#include "engine/editor/world/SplatPaintCommand.h"
#include "engine/editor/world/TerrainDocument.h"

#include <cstdint>
#include <vector>

namespace engine::core { class Config; }
namespace engine::render { class Camera; }

namespace engine::editor::world
{
	struct SplatPaintParams
	{
		bool autoRules = false;
		uint8_t activeLayer = 0;
		float radiusMeters = 6.0f;
		float strength = 0.5f;       // 0..1
		float falloff = 0.7f;
		float slopeMinDeg = 0.0f;
		float slopeMaxDeg = 90.0f;
		float altMin = -1024.0f;
		float altMax =  8192.0f;
	};

	class SplatPaintTool
	{
	public:
		bool Init(CommandStack& stack, TerrainDocument& doc);
		void SetParams(const SplatPaintParams& p) { m_params = p; }
		const SplatPaintParams& GetParams() const { return m_params; }

		/// Démarre un brushstroke. Allouera mergeKey unique.
		void OnMouseDown(const engine::render::Camera& cam, int sx, int sy, int vw, int vh,
			const engine::core::Config& cfg);
		/// Continue le brushstroke tant que la souris est pressée.
		void OnMouseMove(const engine::render::Camera& cam, int sx, int sy, int vw, int vh,
			const engine::core::Config& cfg);
		/// Commit le brushstroke en push d'un `SplatPaintCommand`.
		void OnMouseUp();

		/// Applique les auto-rules à un chunk entier (mode auto). Push une
		/// commande non-fusionnable (mergeKey unique).
		void ApplyAutoRulesToChunk(const engine::core::Config& cfg, int chunkX, int chunkZ);

	private:
		CommandStack* m_stack = nullptr;
		TerrainDocument* m_doc = nullptr;
		SplatPaintParams m_params;
		std::vector<SplatDeltaChunk> m_inFlight;
		bool m_pressing = false;
		uint64_t m_strokeId = 0;
	};
}
```

- [ ] **Step 2 : Impl**

`OnMouseMove` (si pressing) :
1. Raycast caméra → terrain (réutilise `engine::editor::world::TerrainRaycast` du M100.6).
2. Pour chaque chunk dans la box `[radius]` autour du point P :
   - `EnsureSplatLoaded(cfg, x, z)` + `EnsureLoaded(cfg, x, z)` (pour le terrain heightmap si auto-rules).
   - Pour chaque cellule à distance ≤ radius :
     - Calculer `weight = falloff(dist, radius) * strength * dt`
     - Si autoRules : skip si `!MatchesRules(...)`.
     - Calculer `delta = uint8(weight * 255)` ajouté au layer actif, redistribuer pour somme=255.
     - Stocker `prev[8]` (avant) et `next[8]` (après) dans le `SplatDeltaCell` du chunk concerné dans `m_inFlight`.
     - **Couture** : si la cellule est sur le bord d'un chunk, écrire aussi la cellule miroir du chunk voisin.

`OnMouseUp` : si `m_inFlight` non vide, push `SplatPaintCommand` avec `m_strokeId`. Le `CommandStack::Push` va fusionner avec le top via `TryMerge`. Reset `m_inFlight`.

- [ ] **Step 3 : Commit**

```bash
git commit -m "feat(editor/world): SplatPaintTool manuel + auto-rules + seam (M100.10)"
```

---

### Task 19 : `WorldEditorShell` raccourci `P` (M100.10)

**Files :**
- Modify : `engine/editor/world/WorldEditorShell.h`
- Modify : `engine/editor/world/WorldEditorShell.cpp`

- [ ] **Step 1 : Étendre l'enum `ActiveTool`**

Dans `WorldEditorShell.h`, ajouter `SplatPaint` à l'enum :

```cpp
enum class ActiveTool : uint8_t { None, TerrainSculpt, TerrainStamp, SplatPaint };
```

Ajouter membre `SplatPaintTool m_splatPaintTool;` + accessor `MutableSplatPaintTool() { return m_splatPaintTool; }`.

- [ ] **Step 2 : Init dans `WorldEditorShell::Init`**

Après l'init de `m_stampTool` :

```cpp
if (!m_splatPaintTool.Init(m_commandStack, m_terrainDoc))
{
	LOG_WARN(EditorWorld, "[WorldEditorShell] SplatPaintTool init failed");
}
```

- [ ] **Step 3 : Branche `'P'` dans `HandleShortcut(virtualKey, ctrl, shift)`**

Avant la fallback à `HandleShortcut(virtualKey)` :

```cpp
if (!ctrl && !shift && virtualKey == 'P')
{
	SetActiveTool(ActiveTool::SplatPaint);
	return true;
}
```

- [ ] **Step 4 : Commit**

```bash
git commit -m "feat(editor/world): raccourci P -> SplatPaintTool (M100.10)"
```

---

### Task 20 : `ToolPropertiesPanel` UI splat paint (M100.10)

**Files :**
- Modify : `engine/editor/world/panels/ToolPropertiesPanel.cpp`

- [ ] **Step 1 : Render UI quand `GetActiveTool() == SplatPaint`**

UI selon `tickets/M100/visuals/M100.10-SplatPaintingBrushes.html` :
- Radio `Manual` / `Auto-Rules`
- Combo Active layer (0..7, lit `LayerPalette` pour les noms)
- Slider Radius (1..50m), Strength (0..1), Falloff (0..1)
- Si Auto-Rules : sliders Slope min/max (0..90°), Alt min/max
- Bouton `Apply to chunk` (si Auto-Rules)

- [ ] **Step 2 : Commit**

```bash
git commit -m "feat(editor/world): ToolPropertiesPanel UI splat paint (M100.10)"
```

---

### Task 21 : `SplatPaintTests` (M100.10)

**Files :**
- Create : `engine/editor/world/tests/SplatPaintTests.cpp`

- [ ] **Step 1 : 5 tests selon spec M100.10**

Pattern REQUIRE maison + `int main()`. Tests :
- `Test_ManualBrush_PreservesSum255` : applique un brush sur une cellule, vérifie que somme = 255 après.
- `Test_AutoRules_PaintsOnlyMatchingCells` : pose un terrain avec moitié plate (slope=0) et moitié pente forte (slope=45°) ; auto-rule slope ∈ [30, 90] doit toucher uniquement la moitié pentue.
- `Test_Falloff_RadialMonotone` : weight au centre > weight à r/2 > weight à r-1 > weight au bord.
- `Test_CrossChunk_PreservesSeam` : pose deux chunks adjacents, peint sur la frontière, vérifie que la cellule x=256 chunk(0,0) == cellule x=0 chunk(1,0).
- `Test_Stroke_OneHistoryEntry` : push 2 SplatPaintCommand avec même mergeKey → CommandStack ne contient qu'1 entrée.

- [ ] **Step 2 : CMake target `splat_paint_tests`**

Pattern identique à `terrain_sculpt_tests`.

- [ ] **Step 3 : Commit**

```bash
git commit -m "test(editor/world): SplatPaintTests 5 cas (M100.10)"
```

---

## ─── Bloc final ───

### Task 22 : Build + run all tests

- [ ] **Step 1 : Build all**

```bash
cmake --build build 2>&1 | tail -30
```

- [ ] **Step 2 : Run new tests**

```bash
ctest --test-dir build -R "splat_map_tests|splat_paint_tests" --output-on-failure
```

Expected : tests verts.

- [ ] **Step 3 : Anti-dup serveur**

```bash
grep -RIn "engine::world::terrain::SplatMap\|engine::editor::world::SplatPaintTool" engine/server/ 2>&1 | head -5
```

Expected : zéro ligne.

- [ ] **Step 4 : Pas de commit** — validation pure.

---

### Task 23 : Marquer M100.9/.10 « Done » dans INDEX.md

**Files :**
- Modify : `tickets/M100/INDEX.md`

- [ ] **Step 1 : Mise à jour Statut**

| M100.9 → Done partiel (CI pending — assets PBR placeholders ; vrais assets en follow-up)
| M100.10 → Done (CI pending)

- [ ] **Step 2 : Commit**

```bash
git commit -m "docs(tickets/M100): marque M100.9/.10 Done (Phase 3a, CI pending)"
```

---

### Task 24 : Récap PR

- [ ] **Step 1 : Préparer le résumé**

```bash
git diff origin/main..HEAD --stat | tail -3
git log origin/main..HEAD --oneline | wc -l
```

Résumé : 4 tickets résolus en partiel ou full (M100.5 dette résolue + M100.9 + M100.10), ~5000 lignes nouvelles, ~10-12 tests neufs.

**Déploiement** : ✅ client/éditeur uniquement, pas de redéploiement serveur.

---

## Self-Review Checklist (avant ouverture PR)

- [ ] Spec M100.9 : SplatMap struct + format SLAT + LayerPalette + shaders + pipeline + GeometryPass + parity → Tasks 2-15 ✓.
- [ ] Spec M100.10 : SplatPaintTool + Command + auto-rules + seam + raccourci P + UI → Tasks 16-21 ✓.
- [ ] Anti-dup serveur : Task 22 step 3.
- [ ] Tests round-trip M100.9 : Task 15.
- [ ] Pas de placeholder TODO dans le plan.
- [ ] Tous les fichiers du Files Structure sont référencés par au moins une Task.

---

## Hand-off

Plan complet et sauvegardé dans `docs/superpowers/plans/2026-05-07-m100-phase-3a-splat.md`. Deux options d'exécution :

1. **Subagent-Driven (recommandé)** — surtout pour Task 13 (TerrainChunkPipeline Vulkan, ~600-1000 lignes) et Task 14 (GeometryPass integration). Active via `superpowers:subagent-driven-development`.
2. **Inline Execution** — pour les tasks plus mécaniques (Tasks 2-12, 15-21).

Choix utilisateur attendu avant exécution.
