# Issue M39.2 — Settings menu: graphics / audio / controls / keybindings

**Status:** Closed

---

## Ticket source

```markdown
# M39.2_Settings_menu_graphics audio controls keybindings

## Objectif
Menu settings complet avec onglets graphics, audio, controls, gameplay.

## Dépendances
- M16.1
- M20.2

## Livrables
- Settings UI panel (tabs)
- Graphics: resolution, quality preset, vsync, FOV
- Audio: master/sfx/music/voice volumes, device selection
- Controls: mouse sensitivity, invert Y, keybindings
- Gameplay: auto-loot, combat text, camera distance

## Spécification technique
- Persist settings: local config file + server-side (cloud save)
- Apply changes: realtime ou restart required (label)
- Keybindings: rebindable, detect conflicts

## Étapes d'implémentation
1. UI: tabbed panel (graphics, audio, controls, gameplay)
2. Graphics tab: resolution dropdown, quality preset (Low/Med/High/Ultra), vsync, fullscreen, FOV [60-120]
3. Audio tab: sliders master/sfx/music volumes [0-100%], dropdown output device
4. Controls tab: slider mouse sensitivity, checkbox invert Y, keybindings list (click to rebind)
5. Gameplay tab: checkboxes auto-loot/combat text/timestamps, slider camera distance [2-10m]
6. Save settings: write user_settings.json (local), cloud (server) placeholder
7. Apply: update engine params, reload if needed
```

---

## Rapport final

### 1) Fichiers

**Créés :**
- `tickets/issues/M39.2_Settings_menu_graphics_audio_controls_keybindings_Issue.md`
- `engine/client/SettingsMenuUi.h` — Header : enums `SettingsTab`, structs `GraphicsSettings`, `AudioSettings`, `ControlsSettings` (avec `KeyBinding`), `GameplaySettings`, commandes `*SettingsCmd`, classe `SettingsMenuPresenter`
- `engine/client/SettingsMenuUi.cpp` — Implémentation : init depuis Config, setters avec clamp, rebind avec détection de conflits, reset aux défauts, `ConsumePending*()`, `SaveToFile()` (JSON plat compatible `user_settings.json`), `BuildDebugText()`
- `game/data/settings/default_keybindings.json` — 13 actions par défaut (WASD, jump, interact, inventory, map, character, skills, chat, auto-run, screenshot)

**Modifiés :**
- `CMakeLists.txt` — `engine/client/SettingsMenuUi.cpp` ajouté à la liste des sources

### 2) Commandes Windows à exécuter

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release
```

### 3) Résultat

- **Compilation :** NON TESTÉ (outillage non disponible dans l'environnement)
- **Exécution :** NON TESTÉ

### 4) Validation DoD

| Point | État |
|---|---|
| Build Windows OK | NON TESTÉ |
| Exécutable sans crash | NON TESTÉ |
| Aucun dossier racine non autorisé | ✅ |
| Rapport final complet | ✅ |
| Settings UI panel 4 onglets | ✅ `SettingsTab` enum + `SettingsMenuPresenter` |
| Graphics : resolution, quality preset, vsync, fullscreen, FOV | ✅ `GraphicsSettings` + setters |
| Audio : master/sfx/music/voice volumes, output device | ✅ `AudioSettings` + setters |
| Controls : mouse sensitivity, invert Y, keybindings rebindables | ✅ `ControlsSettings` + `BeginRebind/OnKeyPressed/CancelRebind/ResetToDefault` |
| Keybindings : détection conflits | ✅ `OnKeyPressed` parcourt les bindings existants |
| Keybindings : reset button | ✅ `ResetKeybindingsToDefault()` |
| Gameplay : auto-loot, combat text, timestamps, camera distance [2-10m] | ✅ `GameplaySettings` + setters |
| Persist local (user_settings.json) | ✅ `SaveToFile()` — JSON plat compatible avec `LoadUserSettings` |
| Apply commands consommables par le moteur | ✅ `ConsumePending*()` pattern |
| Chemins contenu relatifs via `paths.content` | ✅ `default_keybindings.json` résolu via config |
| Logs Init/Shutdown/chemins critiques | ✅ |

---

## Contenu du ticket (M39.2)

# M39.2_Settings_menu_graphics audio controls keybindings

## Objectif
Menu settings complet avec onglets graphics, audio, controls, gameplay.

## Dépendances
- M16.1
- M20.2

## Livrables
- Settings UI panel (tabs)
- Graphics: resolution, quality preset, vsync, FOV
- Audio: master/sfx/music/voice volumes, device selection
- Controls: mouse sensitivity, invert Y, keybindings
- Gameplay: auto-loot, combat text, camera distance

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Persist settings: local config file + server-side (cloud save)
- Apply changes: realtime ou restart required (label)
- Keybindings: rebindable, detect conflicts

## Étapes d'implémentation
1. UI: tabbed panel (graphics, audio, controls, gameplay)
2. Graphics tab:
   - Resolution dropdown (detect supported)
   - Quality preset: Low/Med/High/Ultra (M20.2)
   - Checkboxes: vsync, fullscreen
   - Slider: FOV (60-120)
3. Audio tab:
   - Sliders: master/sfx/music volumes (0-100%)
   - Dropdown: output device
4. Controls tab:
   - Slider: mouse sensitivity
   - Checkbox: invert Y
   - Keybindings list: action -> key (click to rebind)
5. Gameplay tab:
   - Checkboxes: auto-loot, combat text, timestamps
   - Slider: camera distance (2-10m)
6. Save settings: write config.json (local), upload to server (cloud)
7. Apply: update engine params, reload if needed

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Default keybinds always accessible (reset button)
- Validate settings (e.g., unsupported resolution)

