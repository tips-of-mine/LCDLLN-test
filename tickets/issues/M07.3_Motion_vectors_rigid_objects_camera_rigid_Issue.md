# Issue: M07.3 — Motion vectors: rigid objects (camera+rigid)

**Status:** Closed

---

## Rapport final (M07.3)

### 1) FICHIERS

**Créés :** aucun

**Modifiés :**
- `engine/Engine.h` — `m_fgGBufferVelocityId` (ResourceId).
- `engine/Engine.cpp` — Image FG GBufferVelocity (R16G16_SFLOAT) ; Init geometry pass avec `formatVelocity` ; pass Geometry écrit aussi velocity ; Record avec `prevViewProjMatrix.m`, `viewProjMatrix.m`.
- `engine/render/GeometryPass.h` — Init avec `formatVelocity` ; Record avec `idVelocity`, `prevViewProjMat4`, `viewProjMat4`.
- `engine/render/GeometryPass.cpp` — Render pass 5 attachments (4 color + depth), 4e = velocity ; push 128 B (prevViewProj + viewProj) ; Record 5 vues, 5 clear, push 2×64 B.
- `game/data/shaders/gbuffer_geometry.vert` — Push prevViewProj + viewProj ; sorties vPrevClip, vCurrClip ; prevClip/currClip = prevViewProj/viewProj * worldPos.
- `game/data/shaders/gbuffer_geometry.frag` — Entrées vPrevClip, vCurrClip ; location 3 out outVelocity ; velocity = currNDC - prevNDC, écrit dans outVelocity.rg.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

L’utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Structure : code moteur sous `/engine`, shaders sous `/game/data/shaders`.
- Portée : GBufferVelocity R16G16F, motion vectors = currNDC - prevNDC ; prev/curr ViewProj (caméra) depuis RenderState (M07.1) ; pas de prev model par objet (un seul mesh, caméra seule = rigid sans déplacement d’objet).
- Chemins : aucun nouveau chemin hardcodé.
- Qualité : commentaires et noms cohérents.

---

## Ticket content (M07.3) — référence

# M07.3 — Motion vectors: rigid objects (camera+rigid)

## Objectif
Produire un velocity buffer (R16G16F) au moins pour la caméra et objets rigides.

## Dépendances
- M03.1
- M07.1

## Livrables
- GBufferVelocity (R16G16F)
- Shaders: output motion vectors

## Spécification technique
- Velocity = currNDC - prevNDC (ou pixels).

## Étapes d'implémentation
1. Stocker prev transform par renderable (rigid).
2. Dans geometry pass, calculer clip prev/curr.
3. Écrire velocity au pixel.

## Critères de validation (DoD)
- [ ] Objets en mouvement reprojettent correctement (moins de ghost).

## Notes / pièges à éviter
- Skinned plus tard (nécessite prev bones).
