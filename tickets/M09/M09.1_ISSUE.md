# M09.1 — World model: Zone 4km, Chunk 256m, rings

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M09/M09.1_ISSUE.md`
- `engine/world/World.h`
- `engine/world/World.cpp`

**Modifiés :**
- `CMakeLists.txt` — ajout de `engine/world/World.cpp` à `engine_core`

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont respectés → **OUI**
- Modèle monde sous `/engine/world/` ; unités en mètres ; origine locale par zone ; callback d’émission pour M10 ; hysteresis laissée au caller.

---

## Ticket (original)

### Objectif
Implémenter modèle monde (Zone/Chunk) et calcul des rings autour du joueur.

### Dépendances
- M00.5

### Livrables
- `World`, `Zone`, `Chunk` structs
- Conversion pos->chunk coord
- Rings Active 5x5, Visible 7x7, Far HLOD

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- Unités mètres; origine locale par zone.

### Étapes d'implémentation
1. Définir ZoneSize=4096, ChunkSize=256.
2. Fonctions: WorldToChunkCoord, ChunkBounds.
3. Calculer liste chunks requis par ring.
4. Émettre requests vers scheduler (M10).

### Critères de validation (DoD)
- [ ] Déplacement joueur met à jour chunks requis correctement.

### Notes / pièges à éviter
- Prévoir hysteresis pour éviter thrash.
