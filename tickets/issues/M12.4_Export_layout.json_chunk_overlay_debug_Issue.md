# Issue: M12.4 — Export layout.json + chunk overlay debug

**Status:** Closed

---

## Rapport final (M12.4)

### 1) FICHIERS

**Créés :**
- `tickets/issues/M12.4_Export_layout.json_chunk_overlay_debug_Issue.md`

**Modifiés :**
- `engine/editor/EditorMode.h`
- `engine/editor/EditorMode.cpp`
- `engine/platform/Input.h`

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-debug
.\build\vs2022-x64\Debug\lcdlln.exe --editor -log
.\build\vs2022-x64\Debug\zone_builder.exe --layout layout.json --output build\zone_0 --config config.json
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

`cmake` n'est pas disponible dans l'environnement courant, donc la validation locale doit être faite sur la machine Windows équipée des outils du preset.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → NON
- Build/exécution non validés localement faute de `cmake` dans l'environnement.
- L'export est désormais écrit dans `paths.content/layout.json` avec `version`, `instances`, `volumes`, `spawns`, `transitions` et tri stable par `id/type` sur les entités volumes.
- Compatibilité builder prise en compte pour `instances` : le schéma minimal `guid/gltf/position` attendu par `tools/zone_builder` est conservé, avec tableau vide tant que l'éditeur MVP ne manipule pas d'asset glTF.
- Limite restante : l'overlay debug chunks/cells est fourni dans le shell éditeur (titre de fenêtre/logs/panels) avec toggle `C`, highlight de chunk et ring (`active/visible/far`), pas en rendu grille scène Vulkan.

## Ticket content (M12.4) — référence

# M12.4 — Export layout.json + chunk overlay debug

## Objectif
Exporter layout.json versionné et afficher overlay grille chunks/cells.

## Dépendances
- M12.2
- M12.3
- M11.2

## Livrables
- layout.json export
- Overlay chunks 256m + cells 64m

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- layout.json conforme schema, stable ordering pour diff git.

## Étapes d’implémentation
1. Serializer instances/volumes/spawns/transitions.
2. Écrire fichier avec tri par id/type.
3. Overlay debug (toggle) : chunk grid + ring highlight.

## Critères de validation (DoD)
- [ ] Builder consomme layout exporté sans erreurs.

## Notes / pièges à éviter
- Maintenir compat version; migrer si schema change.
