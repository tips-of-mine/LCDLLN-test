# M21.3 — Master applies pending migrations on startup

**Status:** Closed

## Objectif
Le Master Server doit vérifier la version DB et appliquer automatiquement les migrations en attente au démarrage.

## Dépendances
- M19.4 (server)
- M21.2

## Livrables
- `MigrationRunner`:
  - connect DB
  - read current version
  - list pending migrations
  - apply in order
  - update schema_version
- Logs détaillés

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Locking: empêcher deux masters d'appliquer en même temps (advisory lock MySQL).
- Stop-on-failure: pas de boot partiel.

## Étapes d'implémentation
1. Implémenter advisory lock.
2. Lire schema_version.
3. Appliquer migrations.
4. Mettre à jour schema_version.

## Critères de validation (DoD)
- [ ] Migrations s'appliquent automatiquement.
- [ ] Concurrence master protégée.

---

## Rapport final (implémentation M21.3)

### 1) FICHIERS

**Créés :**
- `engine/server/MigrationRunner.h`
- `engine/server/MigrationRunner.cpp`

**Modifiés :**
- `engine/server/CMakeLists.txt` (branche UNIX : détection MySQL, ajout MigrationRunner.cpp, liaison mysqlclient)
- `engine/server/main_server_linux.cpp` (appel `MigrationRunnerRun(config)` après Log::Init, arrêt si échec)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset vs2022-x64`
- `cmake --build --preset vs2022-x64-debug` (ou `vs2022-x64-release`)

Pour le serveur Linux (migrations) : build sur une machine UNIX avec MySQL client installé :

- `cmake --preset linux-x64` (ou `linux-x64-release`)
- `cmake --build --preset linux-x64-debug` (ou `linux-x64-release`)
- Exécutable : `build/linux-x64/pkg/server/lcdlln_server` (ou équivalent selon preset). Lancer depuis la racine du repo pour que `config.json` et `db/migrations/` soient accessibles. Config DB optionnelle : `db.host`, `db.port`, `db.user`, `db.password`, `db.database`, `db.migrations_path` (défaut `db/migrations`). Si `db.host` est vide, les migrations sont ignorées et le serveur démarre.

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans l’environnement ; pas de Visual Studio / Ninja / MySQL SDK local).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI.**
- Build : pas exécuté localement ; commandes fournies pour validation par l’utilisateur.
- Structure : code sous `/engine`, aucun dossier racine interdit, chemins relatifs (config `db.migrations_path`).
- Portée : uniquement MigrationRunner + intégration démarrage Master + advisory lock + schema_version.
- Logs : LOG_INFO/LOG_WARN/LOG_ERROR sur connexion, lock, version, chaque migration appliquée, erreurs, déconnexion.
- Stop-on-failure : si migrations échouent, le processus sort avec `return 1` sans lancer le NetServer.
