# Issue: M06.1 — SSAO: kernel + noise generation

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `engine/render/SsaoKernelNoise.h`
- `engine/render/SsaoKernelNoise.cpp`
- `tickets/issues/M06.1_SSAO_kernel_noise_generation_Issue.md`

**Modifiés :**
- `CMakeLists.txt` (ajout de `engine/render/SsaoKernelNoise.cpp`)
- `engine/Engine.h` (include + membre `m_ssaoKernelNoise`)
- `engine/Engine.cpp` (init M06.1 au boot après M05.3, Destroy avant M05.3)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset vs2022-x64`
- `cmake --build --preset vs2022-x64-debug`
- `.\build\vs2022-x64\Debug\lcdlln.exe`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- **OUI** : structure respectée (/engine, pas de contenu sous /engine). UBO kernel (32 samples, radius/bias en fin d’UBO), texture bruit 4x4 RG16F tiled (REPEAT), génération CPU au boot, radius/bias lus et clampés via config (ssao.radius, ssao.bias). Kernel et bruit constants entre frames (RNG déterministe, seed fixe).

---

## Ticket content (M06.1)

# M06.1 — SSAO: kernel + noise generation

## Objectif
Générer kernel SSAO (16-32 samples) + noise 4x4 pour rotation TBN.

## Dépendances
- M03.1

## Livrables
- UBO kernel samples
- Texture noise 4x4 (RG16F ou RG8) tiled

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Kernel biased vers centre; noise XY aléatoire.

## Étapes d'implémentation
1. Générer kernel CPU au boot.
2. Générer noise texture 4x4.
3. Uploader UBO + texture.

## Critères de validation (DoD)
- [ ] Kernel/noise valides et constants entre frames.

## Notes / pièges à éviter
- Clamper radius/bias via config.
