# M11.5 — Versioning: builderVersion/engineVersion/contentHash

**Status:** Closed

## Objectif
Ajouter versioning dans chaque output pour invalidation cache et compatibilité.

## Dépendances
- M11.2

## Livrables
- Headers versionnés dans bin/pak/meta
- content hash (xxhash)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Refuser de charger si version incompatible (log clair).

## Étapes d'implémentation
1. Définir header commun (magic, version, hash).
2. Calculer hash inputs (layout + referenced assets timestamps/hashes).
3. Écrire header dans chaque fichier.
4. Runtime: valider avant load.

## Critères de validation (DoD)
- [x] Modifier layout -> hash change; runtime détecte mismatch.

## Notes / pièges à éviter
- Éviter hash des gros fichiers à chaque build (cache).

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M11/M11.5_ISSUE.md` (issue de suivi)
- `engine/world/VersionedHeader.h` (header commun : formatVersion, builderVersion, engineVersion, contentHash ; constantes ; ValidateVersionedHeader)
- `engine/world/VersionedHeader.cpp` (validation + LOG_ERROR si version incompatible)

**Modifiés :**
- `CMakeLists.txt` (ajout `engine/world/VersionedHeader.cpp` ; FetchContent xxHash, lib xxhash statique ; zone_builder link xxhash)
- `engine/world/ZoneBuildFormat.h` (magic `kInstancesBinMagic` pour instances.bin)
- `engine/world/ZoneBuildFormat.cpp` (lecture du VersionedHeader après magic pour zone.meta, chunk.meta, instances.bin ; validation ; refus avec log si incompatible)
- `engine/world/ProbesFormat.cpp` (lecture VersionedHeader après magic pour probes.bin ; validation)
- `tools/zone_builder.cpp` (M11.5 : include VersionedHeader + xxhash ; ComputeContentHash(layout contenu + gltf mtime) ; écriture magic + VersionedHeader dans zone.meta, chunk.meta, instances.bin, probes.bin ; kInstancesBinMagic pour instances.bin)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe`
- `.\build\zone_builder.exe <layout.json> <scene.gltf> <outputDir>` (génère sorties versionnées ; modifier layout ou gltf change le contentHash)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible dans le PATH).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Headers versionnés : chaque bin/meta (zone.meta, chunk.meta, instances.bin, probes.bin) commence par magic (4) + VersionedHeader (formatVersion, builderVersion, engineVersion, contentHash 8 bytes xxhash).
- Content hash : xxHash (FetchContent xxHash) ; hash = layout file content + glTF mtime (pas de hash des gros fichiers à chaque build).
- Runtime : lecture du header puis ValidateVersionedHeader ; si formatVersion ou engineVersion incompatible, LOG_ERROR et refus de chargement.
- Modifier layout (ou mtime gltf) → contentHash différent ; runtime charge uniquement si versions compatibles (mismatch de hash n’est pas un refus, le refus est sur version).
