# Issue: M08.1 — Bloom: prefilter + downsample pyramid

**Status:** Closed

---

## Rapport final (M08.1)

### 1) FICHIERS

**Créés :**
- `engine/render/BloomPass.h` — BloomPrefilterPass, BloomDownsamplePass, kBloomMipCount=6, PrefilterParams (threshold, knee).
- `engine/render/BloomPass.cpp` — Init/Record/Destroy pour prefilter (fullscreen + push constants) et downsample (box 2x2).
- `game/data/shaders/bloom_prefilter.vert` — Fullscreen triangle (NDC, UV [0,1]²).
- `game/data/shaders/bloom_prefilter.frag` — Sample HDR, soft threshold (threshold + knee), output HDR.
- `game/data/shaders/bloom_downsample.vert` — Fullscreen triangle.
- `game/data/shaders/bloom_downsample.frag` — Box 2x2 (4 taps), output vers mip suivant.

**Modifiés :**
- `engine/render/FrameGraph.h` — ImageDesc : ajout de `extentScalePower` (0 = extent global, >0 = extent >> power) pour pyramide bloom.
- `engine/render/FrameGraph.cpp` — ensureImageResources : calcul width/height avec extentScalePower (bloom mips 1/2..1/32).
- `CMakeLists.txt` — ajout de `engine/render/BloomPass.cpp`.
- `engine/Engine.h` — include BloomPass.h ; membres `m_bloomPrefilterPass`, `m_bloomDownsamplePass`, `m_fgBloomMipIds[kBloomMipCount]`.
- `engine/Engine.cpp` — création FG BloomMip_0..5 (R16G16B16A16_SFLOAT, extentScalePower 0..5) ; chargement/compilation bloom_prefilter/downsample shaders ; init des deux passes ; pass FG "Bloom_Prefilter" (read SceneColor_HDR, write BloomMip0, params threshold/knee depuis config) ; passes FG "Bloom_Downsample_0".."4" (read BloomMip_i, write BloomMip_{i+1}) ; Destroy des deux passes au shutdown.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake/outils non disponibles dans l’environnement).
- **Exécution :** NON TESTÉ

L’utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Structure : code sous `/engine`, contenu shaders sous `/game/data/shaders`.
- Portée : uniquement M08.1 (BloomMip0..N, prefilter threshold+knee, downsample chain box 2x2, N limité à 6 = 1/32).
- Chemins : shaders chargés via `paths.content` + chemins relatifs.
- Aucun dossier racine interdit ; pas de chemins absolus.

---

## Ticket content (M08.1) — référence

# M08.1 — Bloom: prefilter + downsample pyramid

## Objectif
Extraire hautes lumières HDR et construire pyramide downsample (1/2..1/32).

## Dépendances
- M03.2

## Livrables
- BloomMip0..N textures
- Pass prefilter (threshold + knee)
- Downsample passes

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Threshold 1.0, knee 0.5 (defaults).

## Étapes d'implémentation
1. Créer mip textures (FG) selon extent.
2. Prefilter: sample HDR, soft threshold.
3. Downsample chain (box/tent).

## Critères de validation (DoD)
- [ ] Bloom mips remplis, pas de banding majeur.

## Notes / pièges à éviter
- Limiter N pour perf (jusqu'à 1/32 suffisant).
