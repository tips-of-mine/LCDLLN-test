# Issue: M07.4 — TAA pass: reprojection + clamp anti-ghost

**Status:** Closed

---

## Rapport final (M07.4)

### 1) FICHIERS

**Créés :**
- `engine/render/TaaPass.h` — Classe TAA fullscreen, TaaParams (alpha), Init/Record/Destroy.
- `engine/render/TaaPass.cpp` — Render pass 1 color (R8G8B8A8_UNORM), 4 combined image samplers (current, history, velocity, depth), push 16 B, pipeline fullscreen.
- `game/data/shaders/taa.vert` — Fullscreen triangle.
- `game/data/shaders/taa.frag` — uvHistory = uv - velocity*0.5 ; sample history ; 3x3 min/max current, clamp history ; out = lerp(current, historyClamped, alpha). Off-screen uvHistory → fallback current.

**Modifiés :**
- `CMakeLists.txt` — ajout de `engine/render/TaaPass.cpp`.
- `engine/Engine.h` — include `TaaPass.h`, membre `m_taaPass`.
- `engine/Engine.cpp` — création history avec TRANSFER_SRC ; chargement taa.vert/taa.frag, init `m_taaPass` ; pass TAA (read LDR, HistoryA/B, Velocity, Depth ; write HistoryA/B ; callback Record avec prev/next) ; CopyPresent lit HistoryNext (ou LDR si TAA désactivé), dépendances HistoryA + HistoryB + LDR ; `m_taaPass.Destroy` au shutdown.

**Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER

```bat
cmake --preset vs2022-x64
cmake --build build/vs2022-x64 --config Debug
.\build\vs2022-x64\Debug\lcdlln.exe
```

(Pour Release : `--config Release` et dossier `Release`.)

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

L’utilisateur valide localement.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI
- Structure : code moteur sous `/engine`, shaders sous `/game/data/shaders`.
- Portée : pass TAA (reprojection, clamp 3x3, blend alpha ~0.9), output HistoryNext ; CopyPresent affiche HistoryNext (ou LDR en fallback).
- Chemins : shaders chargés via `paths.content` + chemins relatifs.
- Qualité : code et commentaires cohérents.

---

## Ticket content (M07.4) — référence

# M07.4 — TAA pass: reprojection + clamp anti-ghost

## Objectif
Implémenter pass TAA : reproject history via velocity, clamp neighborhood, blend.

## Dépendances
- M07.2
- M07.3

## Livrables
- Pass TAA: read SceneColor_LDR + HistoryPrev + Velocity + Depth
- Output HistoryNext (et/ou TAA_Output)

## Spécification technique
- Clamp history dans min/max neighborhood 3x3; alpha ~0.9.

## Étapes d'implémentation
1. Créer pass TAA dans FG.
2. Shader: uvHistory = uv - velocity; sample history.
3. Clamp via neighborhood min/max current.
4. Blend out = lerp(current, historyClamped, alpha).

## Critères de validation (DoD)
- [ ] Image plus stable, scintillement réduit, ghosting modéré.

## Notes / pièges à éviter
- Traiter uvHistory hors écran -> fallback current.
