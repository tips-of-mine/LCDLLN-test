# M16.3 — Inventory UI: grid + tooltip + pickup feedback

**Status:** Closed

## Contenu du ticket (intégral)

# M16.3 — Inventory UI: grid + tooltip + pickup feedback

## Objectif
Inventaire: slots, stacks, tooltips, update via InventoryDelta.

## Dépendances
- M16.1
- M14.3

## Livrables
- Inventory panel
- Tooltip
- Stack rendering

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Drag/drop optionnel plus tard.

## Étapes d'implémentation
1. Créer grid slots.
2. Afficher item icon/id + qty.
3. Tooltip simple sur hover.

## Critères de validation (DoD)
- [ ] Pickup loot met à jour l'inventaire UI.

## Notes / pièges à éviter
- Prévoir mapping itemId -> icon/desc via data.

---

## Rapport final (M16.3)

### 1) FICHIERS

**Créés :**
- `game/data/items.json` — Exemple de données pour mapping itemId → name/desc (chargé par le jeu pour remplir HudData.itemLabels).

**Modifiés :**
- `engine/ui/GameHud.h` — `InventorySlot`, `HudData.inventorySlots`, `HudData.itemLabels`; commentaire Draw (M16.3).
- `engine/ui/GameHud.cpp` — Panneau Inventory : grille 8×8, affichage item id + qty (stack), tooltip au survol (itemLabels ou "Item &lt;id&gt;").
- `engine/core/Engine.cpp` — `SetHudData` copie `inventorySlots` et `itemLabels`.

**Supprimés :** —

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe` (sans mode éditeur ; le jeu alimente l’inventaire via `SetHudData` avec `inventorySlots` dérivés de UIModel après InventoryDelta).

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Inventory panel (grille de slots), tooltip au survol, stack (item id + count). Mise à jour via InventoryDelta : le jeu appelle `SetHudData` avec `inventorySlots` remplis à partir de `UIModel::GetInventory()` après chaque `ApplyInventoryDelta`, donc le pickup loot met à jour l’UI. Mapping itemId → desc prévu via `HudData.itemLabels` et données optionnelles (ex. `game/data/items.json`).

**Status:** Closed
