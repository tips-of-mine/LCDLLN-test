# M27.2 — Camera smooth follow + look-ahead

**Status:** Closed

## Objectif
Améliorer feeling caméra avec follow smooth et anticipation direction mouvement.

## Dépendances
- M27.1
- M26.1

## Livrables
- Spring-damper follow
- Look-ahead offset basé vélocité
- Transition camera states (combat/exploration)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Spring-damper: stiffness/damping configurable
- Look-ahead: offset = velocity * 0.5-1.0s
- Combat mode: resserrer distance, lock target optionnel

## Étapes d'implémentation
1. Remplacer lerp par spring-damper (smoother)
2. Calculer look-ahead offset: character velocity projected
3. Target position = character pos + look-ahead + up offset
4. Mode combat: override distance/pitch
5. Exposer params config camera

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Tuning spring critical: trop soft = laggy, trop stiff = jittery
- Clamp look-ahead offset (max 2-3m)

---
## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/issues/M27.2_Camera_smooth_follow_+_look-ahead_Issue.md` (issue)

**Modifiés :**
- `engine/gameplay/ThirdPersonCamera.h`
- `engine/gameplay/ThirdPersonCamera.cpp`

**Supprimés (si explicitement demandé) :**
- (aucun)

### 2) COMMANDES WINDOWS À EXÉCUTER
- `cmake --preset vs2022-x64-release`
- `cmake --build --preset vs2022-x64-release`
- `.\build\vs2022-x64-release\engine_app\lcdlln.exe -log`

### 3) RÉSULTAT
- Compilation : NON TESTÉ
- Exécution   : NON TESTÉ

Cause (agent) : `cmake` introuvable dans l’environnement PowerShell (CommandNotFoundException / where.exe ne trouve pas `cmake*`), donc configure/build et smoke test non exécutés.

### 4) VALIDATION DoD
- Tous les points de `DEFINITION_OF_DONE.md` sont-ils respectés ?
  → NON
- Explication : le projet n’a pas été configuré/compilé/exécuté localement à cause de l’absence de `cmake`, donc les checks A) Build & exécution ne peuvent pas être validés.

