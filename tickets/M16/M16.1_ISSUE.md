# M16.1 — UI model binding: stats/inv/quests/events

**Status:** Closed

## Contenu du ticket
- **Objectif:** Créer UI Model (données) et binding depuis messages réseau.
- **Dépendances:** M13.3
- **Livrables:** UIModel struct, Handlers messages -> UIModel, Observer callbacks
- **Spécification technique:** Séparer UI logique vs style; model thread-safe (main thread).
- **Étapes:** 1) Définir UIModel (player stats, inventory, quest list, events). 2) Brancher handlers réseau pour snapshots/deltas. 3) Notifier vues (signals) ou pull-based. 4) Debug view dumps.
- **DoD:** Changements réseau reflétés instantanément dans le model.
- **Notes:** Éviter allocations massives à chaque delta; réutiliser buffers.

---

## RAPPORT FINAL

### 1) FICHIERS
- **Créés :**
  - engine/ui/UIModel.h
  - engine/ui/UIModel.cpp
- **Modifiés :**
  - CMakeLists.txt (engine_ui, client_app → engine_ui)
  - main_client.cpp (UIModel, Apply* depuis handlers, position depuis model, DumpToDebug pour stats)
- **Supprimés :** aucun

### 2) COMMANDES WINDOWS À EXÉCUTER
- cmake --preset win64
- cmake --build --preset win64
- .\build\client_app.exe 127.0.0.1 [character_id]

### 3) RÉSULTAT
- Compilation : NON TESTÉ
- Exécution : NON TESTÉ

### 4) VALIDATION DoD
- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? → OUI
- UIModel (stats, inv, quests, events) défini sous /engine ; handlers message → Apply* ; observers (Register/Unregister) ; debug dump (DumpToDebug). Client branché : ConnectAck, Snapshot, CombatEvent, InventoryDelta, QuestDelta, EventState, ZoneChange mettent à jour le model ; affichage périodique via DumpToDebug.
