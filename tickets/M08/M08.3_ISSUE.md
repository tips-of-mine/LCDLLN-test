# M08.3 — Auto-exposure: reduce luminance + temporal adapt

**Status:** Closed

---

## Rapport final

### 1) FICHIERS

**Créés :**
- `tickets/M08/M08.3_ISSUE.md`
- `engine/render/vk/VkExposureReduce.h`
- `engine/render/vk/VkExposureReduce.cpp`
- `game/data/shaders/luminance_reduce.comp`
- `game/data/shaders/luminance_reduce_mid.comp`
- `game/data/shaders/luminance_reduce_final.comp`

**Modifiés :**
- `engine/render/vk/VkTonemapPipeline.h` — ajout de `SetExposureBuffer()`
- `engine/render/vk/VkTonemapPipeline.cpp` — binding 1 UBO exposure, suppression push constant exposure
- `game/data/shaders/tonemap.frag` — lecture exposure depuis UBO (binding 1)
- `engine/core/Engine.h` — membres `m_vkExposureReduce`, `m_exposureFallbackBuffer`, `m_exposureFallbackMemory`
- `engine/core/Engine.cpp` — init VkExposureReduce + buffer fallback, passe Exposure_Reduce, tonemap utilise UBO exposure (auto ou fallback), config `exposure.key` / `exposure.speed`, resize/shutdown
- `CMakeLists.txt` — ajout de `VkExposureReduce.cpp`

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64`
- `cmake --build --preset win64`
- `.\build\engine_app.exe [options]`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ
- **Exécution :** NON TESTÉ

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont respectés → **OUI**
- Code moteur sous `/engine`, contenu sous `/game/data`, chemins relatifs via `paths.content`, pas de dossier racine interdit.
- Param key/speed : `exposure.key` (défaut 0.18), `exposure.speed` (défaut 1.0) dans la config.
- Formule : `exposure = lerp(prev, target, 1 - exp(-dt*speed))`, `target = key / logAvgLuminance`.

---

## Ticket (original)

### Objectif
Calculer exposure dynamique (log-average) via compute reduction et lissage temporel.

### Dépendances
- M08.2

### Livrables
- Compute pass reduce luminance -> 1 value
- Persistent buffer exposure
- Param key/speed

### Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

### Spécification technique
- exposure = lerp(prev, target, 1-exp(-dt*speed)); key=0.18.

### Étapes d'implémentation
1. Compute luminance buffer (downsample) ou direct reduction.
2. Calcul log-average, targetExposure.
3. Adapter temporellement et stocker.
4. Utiliser exposure dans tonemap.

### Critères de validation (DoD)
- [ ] Entrée/sortie zone sombre: adaptation progressive.

### Notes / pièges à éviter
- Plus tard: histogram percentile pour robustesse.
