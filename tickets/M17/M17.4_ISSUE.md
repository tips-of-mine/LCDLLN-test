# M17.4 — Audio spatial MVP: 3D sounds + buses + zone ambience

**Status:** Open

## Contenu du ticket (intégral)

# M17.4 — Audio spatial MVP: 3D sounds + buses + zone ambience

## Objectif
Audio 3D (position/rolloff) et ambiance de zone data-driven.

## Dépendances
- M00.4

## Livrables
- Audio engine wrapper (FMOD/Wwise/OpenAL)
- Buses SFX/Music/UI
- zone_audio.json

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Min/max distance; doppler optional.

## Étapes d'implémentation
1. Intégrer lib audio.
2. Implémenter Play3D(pos, soundId).
3. Définir buses et volumes.
4. Charger zone_audio et jouer loops.

## Critères de validation (DoD)
- [ ] Sons 3D se déplacent correctement; ambiance active par zone.

## Notes / pièges à éviter
- Éviter de charger des gros wav à la volée; streaming audio plus tard.

---

## Rapport final (M17.4)

**Status:** Closed

### 1) FICHIERS

**Créés :**
- `tickets/M17/M17.4_ISSUE.md`
- `engine/audio/IAudioListenerUpdate.h`
- `engine/audio/AudioEngine.h`
- `engine/audio/AudioEngine.cpp`
- `game/data/audio/zone_audio.json`

**Modifiés :**
- `CMakeLists.txt` (find_package OpenAL, lib engine_audio, engine_core link engine_audio)
- `engine/core/Engine.h` (include IAudioListenerUpdate, Run/RunInternal avec IAudioListenerUpdate*, m_audioListener)
- `engine/core/Engine.cpp` (Run crée et init AudioEngine si nullptr, LoadZoneAudio + PlayZoneAmbience("default"), Update appelle SetListener)
- `main.cpp` (reste minimal : Engine::Run(argc, argv))

**Supprimés :**
- `engine/core/IAudioListenerUpdate.h` (déplacé vers engine/audio)

### 2) COMMANDES WINDOWS À EXÉCUTER

- `cmake --preset win64` (nécessite OpenAL, ex. `vcpkg install openal-soft`)
- `cmake --build --preset win64`
- `.\build\engine_app.exe`

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake/OpenAL non vérifiés dans l’environnement).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI**
- Code moteur sous `/engine`, contenu sous `/game/data` (zone_audio.json, chemins relatifs via paths.content). Wrapper OpenAL : Init, LoadSound, Play3D(pos, soundId, minDist, maxDist, bus), SetListener, SetBusVolume(SFX/Music/UI), LoadZoneAudio, PlayZoneAmbience. Buses et volumes définis. zone_audio.json chargé et boucles d’ambiance par zone. Pas de refactor hors scope. Fonctions documentées.
