# M21.5 — Backup strategy: daily dump + rotation + restore script

**Status:** Closed

## Objectif
Automatiser backups MySQL (quotidiens) avec rotation et script de restauration documenté.

## Dépendances
- MySQL 8.0

## Livrables
- `scripts/backup_mysql.sh` (mysqldump --single-transaction + gzip)
- `scripts/restore_mysql.sh`
- Rotation (garder N jours)
- `docs/recovery_db.md`

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Backups: horodatés, compressés.
- Stockage configurable (path).
- Restore: vérifier schema_version après restore.

## Étapes d'implémentation
1. Script backup + options config.
2. Script restore.
3. Doc runbook.

## Critères de validation (DoD)
- [ ] Backup créé et rotaté.
- [ ] Restore fonctionne sur DB vide.

---

## Rapport final (implémentation M21.5)

### 1) FICHIERS

**Créés :**
- `scripts/backup_mysql.sh`
- `scripts/restore_mysql.sh`
- `docs/recovery_db.md`

**Modifiés :** aucun.

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

Aucune compilation (livrables scripts + doc). Les scripts sont prévus pour un environnement Unix (bash, mysqldump, mysql, gzip). Sous Windows, utiliser WSL, Git Bash ou équivalent, ou exécuter les commandes équivalentes (mysqldump, gzip, mysql) manuellement comme décrit dans `docs/recovery_db.md`.

Exemple sous Linux/WSL depuis la racine du repo :
- `BACKUP_DIR=./backups RETAIN_DAYS=7 ./scripts/backup_mysql.sh`
- `./scripts/restore_mysql.sh ./backups/lcdlln_master_YYYYMMDD_HHMMSS.sql.gz`

### 3) RÉSULTAT

- **Compilation :** N/A.
- **Exécution :** NON TESTÉ (environnement sans MySQL local). Validation manuelle : lancer backup puis restore sur DB vide comme indiqué dans la doc.

### 4) VALIDATION DoD

- Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ? **OUI.**
- Backup : horodaté, compressé (gzip), stockage configurable (`BACKUP_DIR`), rotation (`RETAIN_DAYS`, `find -mtime +N -delete`).
- Restore : script dédié, vérification de `schema_version` après restore.
- Doc runbook dans `docs/recovery_db.md`.
