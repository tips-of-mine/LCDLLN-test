# M38.2 — Weather: rain + snow + fog states

**Status:** Closed

---

## Contenu du ticket

# M38.2_Weather_rain_+_snow_+_fog_states

## Objectif
Système météo avec états (clear, rain, snow, fog) et transitions.

## Dépendances
- M38.1
- M17.2

## Livrables
- Weather states (enum)
- Transition system (fade in/out)
- Rain particles (vertical streaks)
- Snow particles (falling flakes)
- Fog density (distance-based)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- Weather states: Clear, Rain, Snow, Fog, Storm
- Duration: 5-20min par state
- Transition: 30s fade
- Rain: particle emitter (vertical, fast)
- Snow: particle emitter (slow, lateral drift)
- Fog: exponential fog density

## Étapes d'implémentation
1. Weather system: current state, timer
2. Transition: lerp intensity (0->1 over 30s)
3. Rain particles: spawn rate 1000/sec, lifetime 2s
4. Snow particles: spawn rate 500/sec, lifetime 5s
5. Fog: set fog density param (distance/color)
6. Audio: rain sound loop (volume = intensity)
7. Surface wetness: shader darkening (optional)

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Culling particles hors camera frustum
- Zone-specific weather (desert = no rain)

---

## Rapport final

### 1) Fichiers

**Créés :**
- `engine/render/WeatherSystem.h` — `WeatherState` enum (Clear/Rain/Snow/Fog/Storm), `WeatherParticle`, `WeatherConfig`, `WeatherSystem` class.
- `engine/render/WeatherSystem.cpp` — Implémentation complète : machine d'état 30 s, piscines internes rain (1000/s, 2 s) et snow (500 s, 5 s), fog density, volume audio.
- `game/data/particles/weather_rain.json` — Définition emitter pluie (maxParticles=2048, rate=1000, lifetime=2 s, vertical).
- `game/data/particles/weather_snow.json` — Définition emitter neige (maxParticles=1024, rate=500, lifetime=5 s, slow+drift).
- `tickets/issues/M38.2_Weather_rain_+_snow_+_fog_states_Issue.md` — Cette issue.

**Modifiés :**
- `engine/Engine.h` — `#include "engine/render/WeatherSystem.h"`, membre `m_weatherSystem`.
- `engine/Engine.cpp`
  - `LoadZoneProbeAssets()` : init du `WeatherSystem` depuis config JSON (`world.weather.*`).
  - `Update()` : `m_weatherSystem.Tick(dt, cam.x, cam.y, cam.z)` + `m_audioEngine.SetBusVolume("Weather", volume)`.
- `CMakeLists.txt` — `engine/render/WeatherSystem.cpp` ajouté à la liste des sources.

**Supprimés :** aucun.

---

### 2) Commandes Windows à exécuter

```bat
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-release --target engine_app
.\build\vs2022-x64\pkg\game\lcdlln.exe
```

---

### 3) Résultat

- **Compilation :** NON TESTÉ (pas de toolchain dans l'environnement)
- **Exécution :** NON TESTÉ

---

### 4) Validation DoD

| Critère | État |
|---|---|
| Build Windows OK | NON TESTÉ |
| Exécutable sans crash | NON TESTÉ |
| Aucun dossier racine non autorisé | ✅ |
| Rapport final complet | ✅ |
| Weather states enum (Clear/Rain/Snow/Fog/Storm) | ✅ |
| Transition system 30 s fade (intensity 0→1) | ✅ |
| Rain particles (1000/s, 2 s, vertical) | ✅ |
| Snow particles (500/s, 5 s, slow+drift) | ✅ |
| Fog density (exponential, distance-based) | ✅ |
| Audio volume (rain sound intensity) | ✅ |
| Chemins contenu relatifs via paths.content | ✅ |
