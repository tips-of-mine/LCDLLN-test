# M29.2 — Chat commands + moderation tools

**Status:** Closed

# M29.2_Chat_commands_+_moderation_tools

## Objectif
Commandes slash (/who, /ignore, /kick, etc.) et outils modération.

## Dépendances
- M29.1

## Livrables
- Command parser (/command args)
- Commands: /who, /ignore, /unignore, /report
- Admin commands: /kick, /ban, /mute, /announce
- Ignore list per player (persistent)

## Structure & chemins (verrouillé)
- Code moteur : uniquement sous `/engine/...`
- Contenu (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- Outils offline : uniquement sous `/tools/...`
- Tous les chemins de contenu sont relatifs à `paths.content` (config.json).
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.

## Spécification technique
- /who: liste joueurs online (zone ou global)
- /ignore <name>: block messages from player
- /report <name> <reason>: log pour modération
- /kick, /ban, /mute: admin only, log action

## Étapes d'implémentation
1. Parse input: detect "/" prefix
2. Map commands -> handlers (server-side validation)
3. /who: query online players, return list
4. /ignore: add to blacklist (server persist), filter incoming chat
5. /report: log to DB with timestamp/reporter/target/reason
6. Admin commands: check permissions, apply action, broadcast notification
7. Persist ignore list in player DB

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD

## Notes / pièges à éviter
- Permissions système pour admin (role-based)
- Log toutes actions modération (audit trail)

---

## Rapport final (agent)

### 1) FICHIERS

**Créés :**
- `engine/server/ChatCommandParser.h`
- `engine/server/ChatCommandParser.cpp`
- `tickets/issues/M29.2_Chat_commands_+_moderation_tools_Issue.md`

**Modifiés :**
- `engine/server/CMakeLists.txt`
- `engine/server/CharacterPersistence.h` / `CharacterPersistence.cpp`
- `engine/server/SecurityAuditLog.h` / `SecurityAuditLog.cpp`
- `engine/server/ServerApp.h` / `ServerApp.cpp`

**Supprimés :** *(aucun)*

### 2) COMMANDES WINDOWS

```bat
cmake --preset vs2022-x64-release
cmake --build --preset vs2022-x64-release
.\build\vs2022-x64\Release\pkg\server\lcdlln_server.exe -log -console
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (cmake non disponible sur l’agent)
- **Exécution :** NON TESTÉ

### 4) DoD

**NON** (build/run non exécutés) — périmètre code livré : parser `/command`, handlers serveur pour `/who`, `/ignore`, `/unignore`, `/report`, `/kick`, `/ban`, `/mute`, `/announce` ; ignore persisté fichier perso ; reports + actions admin dans `SecurityAuditLog` (chemin relatif `paths.content`) ; bans persistés `server.chat_ban_list_path` ; filtre ignore sur relais chat ; rôle admin `character.chat_moderator_role=1`.

### Commandes & config

- Cibles joueurs : tokens `P<clientId>` (ex. `P2`).
- `/who` ou `/who global` ; `/report P2 texte raison` ; `/mute P5 120` (secondes, défaut 60).
- Config : `server.moderation_audit_log`, `server.moderation_audit_rotation_mb`, `server.moderation_audit_retention_days`, `server.chat_ban_list_path`.
