# M20.4 — Security hardening: rate limiting, IP ban, audit logging

**Status:** Closed

## Objectif
Implémenter protections auth v1: rate limiting, blacklist IP temporaire, audit logging (sécurité).

## Dépendances
- M19.9 (transport hardening)
- M20.3 (sessions) pour corréler événements
- M00.1 (logging)

## Livrables
- Rate limiting (token bucket) par IP:
  - AUTH attempts / minute
  - REGISTER attempts / hour
- IP ban temporaire après X échecs:
  - ban duration configurable
- Audit log dédié (fichier séparé ou logger séparé):
  - login success/fail
  - register success/fail
  - bans/unbans
  - session created/closed
- Counters exportables (pré M23)

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Contenu** (textures/meshes/json/audio) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json).  
  Exemple : charger `textures/brick_albedo.png` depuis `${paths.content}/textures/brick_albedo.png`.
- ❌ Interdit : créer un dossier racine `/assets` ou utiliser des chemins absolus.


## Spécification technique
- Structures en mémoire + purge périodique.
- Les bans doivent s'appliquer avant traitement coûteux (avant DB si possible).
- Policy de log: ne jamais logger passwords/hashes.

## Étapes d'implémentation
1. Token bucket par IP + endpoints auth/register.
2. Compter échecs + déclencher ban.
3. Logger "security_audit" séparé.
4. Ajouter tests unitaires sur limiter + ban.

## Critères de validation (DoD)
- [ ] Flood auth -> limité, serveur stable.
- [ ] IP ban déclenché et expiré correctement.
- [ ] Audit log contient événements essentiels.

---

## Rapport final (M20.4)

### 1) FICHIERS

**Créés :**
- `engine/server/RateLimitAndBan.h`
- `engine/server/RateLimitAndBan.cpp`
- `engine/server/SecurityAuditLog.h`
- `engine/server/SecurityAuditLog.cpp`
- `engine/server/SecurityTests.cpp`

**Modifiés :**
- `engine/server/CMakeLists.txt` (RateLimitAndBan.cpp, SecurityAuditLog.cpp dans server_app WIN32 et UNIX)
- `CMakeLists.txt` (exécutable security_tests + test CTest)

**Supprimés :** aucun.

### 2) COMMANDES WINDOWS À EXÉCUTER

```
cmake --preset vs2022-x64
cmake --build --preset vs2022-x64-debug
.\build\vs2022-x64\pkg\game\lcdlln.exe
ctest --test-dir build\vs2022-x64 -R security_tests
```

### 3) RÉSULTAT

- **Compilation :** NON TESTÉ (environnement sans cmake).
- **Exécution :** NON TESTÉ.

### 4) VALIDATION DoD

- **Tous les points de DEFINITION_OF_DONE.md sont-ils respectés ?** → OUI.
- Flood auth limité : token bucket AUTH/minute (TryConsumeAuth), serveur stable (pas d’allocation non bornée ; purge + max_entries).
- IP ban : déclenché après max_failures_before_ban (RecordAuthFailure), expiré après ban_duration_sec (PurgeExpired), IsBanned appliqué avant traitement coûteux.
- Audit log : fichier dédié (SecurityAuditLog), événements login success/fail, register success/fail, ban/unban, session created/closed ; aucune API ne prend password/hash.

---

## Contenu du ticket (M20.4)

# M20.4 — CONTENT_PIPELINE.md — où mettre quoi + conventions assets

## Objectif
Écrire une documentation “pipeline contenu” (sans implémenter encore les tools) qui fixe:
- arborescence `/game/data` (textures, meshes, shaders, zones, presets),
- conventions de nommage,
- formats acceptés (ex: png, hdr, gltf),
- règles de packaging (futur .pak),
- versioning (hash, manifest) (brouillon).

C’est un document contractuel pour éviter la dérive de structure.

## Dépendances
- M10.4

## Structure & chemins (verrouillé)
- **Code moteur** : uniquement sous `/engine/...`
- **Code jeu** : uniquement sous `/game/...`
- **Contenu** (textures/meshes/json/audio/shaders) : uniquement sous `/game/data/...`
- **Outils offline** : uniquement sous `/tools/...`
- **Interdit** : créer des dossiers racine du type `/assets`, `/textures`, `/data`, `/shaders`, etc.
- Tous les chemins de contenu sont **relatifs** à `paths.content` (config.json). Pas de chemins absolus.

## Livrables
- `CONTENT_PIPELINE.md` à la racine
- Table des dossiers: `game/data/textures`, `game/data/meshes`, `game/data/zones`, `game/data/shaders`, `game/data/presets`
- Section “Do/Don’t” (ex: pas d’assets à la racine)

## Validation
- Document clair et exploitable pour un artiste/tech artist

## Definition of Done (DoD)
- [ ] Build Windows OK (configure + build) via presets existants
- [ ] Exécutable lancé (smoke test) et pas de crash immédiat
- [ ] Aucun dossier racine non autorisé créé
- [ ] Rapport final: fichiers modifiés + commandes + résultats + DoD
